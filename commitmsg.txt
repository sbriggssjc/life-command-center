Fix cap rate display (decimal→percentage) and Base: $NaN in rent roll

- Add _fmtCapRate() helper: DB stores decimals (0.07=7%), display multiplies
  by 100 when value < 1. Applied to all 12 cap rate display locations across
  ownership chain, deal history, sales timeline, export, and prior sale summary.
- Fix "Base: $NaN" in rent roll tenant header — add isNaN guard on baseRent
  before showing the base hint line
- Cache bump to v255

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
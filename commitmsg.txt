UI: Consolidated rent roll, ownership prospecting badges, deal history enrichment

- Rent Roll: Group leases by parent_lease_id chain into unified tenant view
  with term timeline, renewal options from lease_options table, active/expired badges
- Ownership: Add prospecting status badges (Active/Stale/Passive/Not Prospecting)
  with days-since-contact, show stated vs calculated cap rates, broker names
- Deal History: Add procuring_broker display across all sale renderers,
  enrich ownership events with buyer/seller/broker from v_ownership_chain
- Operations: Fix Lease Expiration KPI to prefer active lease (is_active=true)
  over expired terms when multiple leases exist
- Pipeline: lease_options now fetched in lease enrichment loader
- Cache bump to v254

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
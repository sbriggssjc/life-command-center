# Capital Markets Tab Packet Worklog

## Objective
Build the LCC Capital Markets tab so app charts, Excel export data, and report commentary read from one frozen figure packet per `(vertical, quarter)`.

## Instructions And Constraints
- Read `CLAUDE.md` and `.github/AI_INSTRUCTIONS.md` before editing `/api/`.
- Production runs on Railway through `server.js`.
- Prefer sub-routes/actions on existing handlers.
- No chart-logic forks: packet, app, and export should share the same chart payload wherever possible.
- Single writer per store: packet/commentary writes go through `/api/capital-markets`.

## Packet Layer Status
- Initial repo check found `docs/comps-rollout/capital-markets-update-PLAN.md` and skill text specifying `cm_report_snapshots`.
- No implemented `cm_report_snapshots` migration, `get_capmarkets_packet` MCP tool, or packet API action was found.
- Existing chart/export logic lives in `api/capital-markets.js` plus `api/_shared/cm-*`.

## Implementation Plan
1. Add `cm_report_snapshots` and `cm_report_commentary` schema in the dialysis migrations, government-ready by vertical/domain.
2. Add packet API actions to `api/capital-markets.js`: status, freeze-or-fetch, commentary CRUD/generation, and markdown emission.
3. Update `capital-markets.js` to route chart loads through frozen packets for selected quarters, show commentary, and export optional commentary.
4. Add `#/capmarkets` route alias and MCP `get_capmarkets_packet` wrapper.
5. Run targeted tests/checks.

## Progress
- 2026-08-09: Started implementation from existing CM export stack.
- 2026-08-09: Verified packet/tab code is present in `HEAD` for `api/capital-markets.js`, `capital-markets.js`, `app.js`, and packet/commentary migrations.
- 2026-08-09: Added MCP `get_capmarkets_packet` wrapper in `mcp/server.js`.
- 2026-08-09: Resolved existing conflict markers in `api/_shared/cm-excel-export.js` while preserving the methodology-note constants and the `commentary` workbook parameter.
- 2026-08-09: Verification passed: `node --check api/capital-markets.js`, `node --check api/_shared/cm-excel-export.js`, `node --check capital-markets.js`, `node --check app.js`, `node --check mcp/server.js`, and targeted CM tests.
- 2026-08-09: Production follow-up after live screenshot showed `/api/capital-markets?action=packet...` returning 501. Root cause: Railway redeploy shipped JS but the Dialysis_DB packet tables had not been applied. Applied `supabase/migrations/dialysis/20260809_cm_report_packets_and_commentary.sql` to linked Dialysis_DB (`zqzrriwuavgrquhisnoa`), verified `packet_status` returns `built: true`, and froze Q2-2026 dialysis packet snapshot `94437ab8-824e-4c42-ac3e-360709d36310` with 38 charts.
- 2026-08-09: Scott applied the same packet/commentary migration to Government DB (`scknotsqkcheojiaewwh`) and relinked the local Supabase project back to Dialysis_DB. Verified live Railway `packet_status&vertical=gov` returns `built: true`.
- 2026-08-09: New live screenshot showed the app still rendering old/incomplete chart output. Root cause: frozen packets were assembled through the lightweight dashboard `fetchQuarterly()` path, while the workbook uses the richer export assembly path with display_from crops, as-of clamps, reconstruction, monthly master_m overrides, synthetic chart composition, and modeled rent handling. Added an internal export payload path (`format=payload`) and changed packet freezing to call that same export assembly source. The app now requests packet-backed server PNGs through `action=packet_images` and displays those export-rendered images in chart cards instead of rebuilding browser Chart.js versions for normal charts. Fixed the quarter selector so rerendering preserves the selected as-of quarter.
- 2026-08-09: Follow-up screenshot still showed worksheet charts looking wrong in-app and one card stuck on `Loading export chart...`. Root causes: the app requested every PNG in one large batch, so one or more QuickChart failures left cards stuck, and the two-column grid compressed worksheet-sized chart images to half-width. Changed the app to render one chart per row at worksheet width and fetch chart images per-card with bounded concurrency, so each chart updates independently and failures show an explicit per-chart fallback message.
- 2026-08-09: Third screenshot showed the app still using the legacy QuickChart interpretation rather than the latest Excel native chart definitions (example: `bid_ask_spread`). Added a native-spec image adapter: migrated native Excel templates now build the same `buildInjectionSpec()` inputs as the workbook and translate those specs to Chart.js for app PNGs, with QuickChart legacy rendering only as fallback. Exported read-only chart column/tab-name accessors from `cm-excel-export.js` for this shared path. Fixed a shared native `bid_ask_spread` null-coercion bug where `Number(null)` forced a 0% axis floor; null Last Ask/Spread now stay null, so both Excel and app fit the cap axis to the actual plotted Last Ask/Achieved levels.
- 2026-08-10: Investigated government export screenshot showing blank x-axis lead-in on `bid_ask_spread`. Live `cm_gov_bid_ask_spread_m` coverage has rows back to 1997, but first non-null spread/last-ask/achieved values begin 2007-08-31 and full-year coverage begins 2008, so this is not a deeper data-pull gap. Root cause: native chart trim was year-granular; the density gate returned 2007 and chart ranges started Jan-2007, leaving Jan-Jul blank categories. Added exact-period trimming for bid-ask native specs plus a partial-year regression test.
- 2026-08-10: Investigated government `cap_rate_by_credit` screenshot. Live coverage: quarterly view has Federal 117/117 quarters (1997-03→2026-03), State 77 quarters (2004-12→2025-09), Municipal 29 quarters (2014-12→2023-03); monthly view has Federal 351 months, State 230 months, Municipal 84 months. Recent TTM counts show Municipal falls below the n>=2 gate after 2023-03 except a few isolated rows, and 2023+ unclassified cap-eligible rows mostly lack both `government_type` and `agency`, so this is primarily a source-data/enrichment gap, not a chart pull failure. Fixed chart brand styling: Municipal line/markers now use dark gray instead of green/peridot in native Excel and PNG renderer.
- 2026-08-10: Saved a future exploratory prompt at `docs/capital-markets/CLAUDE_CODE_PROMPT_gov_credit_tier_ingestion_propagation.md` to investigate gov deal ingestion/propagation for `government_type`, `agency`, classifier drift, State/Municipal coverage, and safe remediation options.
- 2026-08-10: Investigated government `case_for_renewal` chart. The current export reads annual `cm_gov_case_for_renewal_y` (`year`, `commencement_count`, `avg_rent_per_sf`) and native charts explicitly format the x-axis as integer years. Live gov DB findings: annual view has only 2013→2026 history, no `cm_gov_case_for_renewal_m` exists, and `gsa_lease_events.new_award` starts 2013-02-01 while curated `gsa_leases.latest_action = 'New'` starts 2005-06-01. The old report's 1991→2023 history is therefore not recoverable from the current live tables without importing the legacy source/master. The 2026 export spike is a data-source issue, not Excel rendering: `cm_gov_case_for_renewal_y` shows 2026 `commencement_count=4,291`, driven by bulk `new_award` event-date clusters on 2026-05-01 (1,191) and 2026-06-01 (2,798), plus earlier 2026 clusters; the old Round 6e view only hard-excludes 2019-03-01 and 2026-03-01 despite comments about a generic sentinel filter. Recommended improvement: replace the annual event-based chart source with a new monthly TTM view backed by `gsa_leases.latest_action = 'New'` for counts and rent/SF, with a robust sentinel/batch guard retained only for any event-derived fallback/audit columns. Display monthly TTM values with quarterly x-axis labels to match the rest of the capital-markets packet; document the history floor as 2005 for curated lease-action data unless/until the legacy 1991 annual master is imported as a separate source.
- 2026-08-10: Investigated government `lease_termination_rate` screenshot showing one missing/near-zero stacked bar in 2018/2019. Live `cm_gov_lease_termination_rate_m` rows around the gap show adjacent months at ~8,050 active leases, but `2019-02-28` has `total_leases_active=11`, `leases_outside_firm_term=5`, computed `in_firm=6`; the native and PNG renderers are therefore plotting a tiny bar, not dropping it. Root cause is the known partial per-lease snapshot: `gsa_snapshots` 2019-02 has only 11 lease keys. The current view already builds a `clean_snap` set for departure endpoints, but the active-count bar still carries forward from raw `snap_agg`, so it can select the corrupt snapshot. Required data-view fix: update `cm_gov_lease_termination_rate_m` and `_q` so the active denominator (`total_leases_active` and `leases_outside_firm_term`) carries forward the latest CLEAN snapshot on/before the period, using the same plausibility guard that excludes partial/restated snapshots. Expected result: February 2019 carries forward January 2019 (~8,054 active leases) and the single missing bar disappears. Separate source-data follow-up remains: re-ingest/repair corrupt snapshot months (`gsa_snapshots` 2019-02; `gsa_inventory_snapshot_lines` 2019-02, 2022-10, 2022-11) so other consumers do not hit the same partial-ingest records.
- 2026-08-10: Investigated government `seller_sentiment` screenshot showing missing series and low/light price-change bars. Missing series diagnosis: live `cm_gov_seller_sentiment_m` DOES emit the gov 6+ cohort fields (`pct_price_change_long_term`, `last_ask_cap_long_term`) with recent coverage (`pct_price_change_long_term` non-null in all 30 months from 2024-01→2026-06; `last_ask_cap_long_term` non-null in 28/30, null only 2026-05/06). The gov view does NOT emit `pct_price_change_long_term_8q` / `last_ask_cap_long_term_8q` (PostgREST 42703). Export `CHART_COLUMNS` nevertheless always adds those dialysis-only `_8q` columns, and the native chart spec asks for `_8q` first, so gov binds the charted core bar/line to blank export columns instead of falling back to the populated 6+ fields. This is a pull/export binding issue, not a propagation gap. Required code fix: for gov, either prune the `_8q` columns from `Data_Sentiment` or make `findCol`/seller-sentiment series binding skip an all-null preferred column and fall back to `pct_price_change_long_term` / `last_ask_cap_long_term`; update labels to read 6+ yr, not 10+ yr, on gov. Low price-change diagnosis: live source values are genuinely low (recent all-deal TTM roughly 0%→3%, long-term roughly 2%→4%) because the view computes `count(had_price_change) / count(all sales)` and gov listing-history capture remains sparse; `docs/capital-markets/CLAUDE_CODE_PROMPT_gov_listing_history_capture.md` records only ~7.6% of gov sales since 2014 carrying initial/last ask history and only 21 total detected price changes as of that audit. That is a propagation/capture coverage issue: improve CoStar/sidebar/gov ingestion of `initial_price`, `last_price`, `initial_cap_rate`, `last_cap_rate`, DOM, and `had_price_change` on `sales_transactions`; do not inflate the view denominator/series synthetically. Formatting note: the current native spec also sets price-change bars to 60% opacity (`alpha: '60000'`), which makes the visible bars intentionally lighter than the primary line series.
- 2026-08-10: Investigated government `core_cap_rate_dot_plot` screenshot and whether the dots are limited to core sales (6+ years firm lease term at close). Yes: the Excel catalog pulls from `cm_gov_core_cap_dot_q`, not the unfiltered `cm_gov_core_cap_rate_dots` superset, and the migrated wrapper is documented as gov core `firm_term_years >= 6` with a cap-rate guard of 4.0%→12.0%. The underlying superset computes `firm_term_years` from `sales_transactions.firm_term_years_at_sale` first, then falls back to the active lease's remaining firm term as of `sale_date`, so the displayed cohort is "at-close term" even when the sale row did not carry a direct stored value. Live paged verification of the complete chart view returned 1,143 rows from 2001-01-01 through 2026-06-23; `min_firm_term=6`, `below_6_count=0`, `cap_min=0.0401`, `cap_max=0.12`, and `out_of_4_12_cap_count=0`. Conclusion: this chart is not leaking non-core sales. Follow-up only if stricter provenance is required: audit qualifying rows where `firm_term_years_at_sale` is null and the view qualified the sale through lease-derived remaining firm term.
- 2026-08-11: Completed the stricter provenance audit for government `core_cap_rate_dot_plot`. Read-only Supabase REST reproduction of `cm_gov_core_cap_rate_dots` plus the `cm_gov_core_cap_dot_q` 2001+ wrapper returned the same report-visible count: 1,143 rows. The broader eligible sale population has 4,804 non-excluded sold rows; 1,679 have `firm_term_years_at_sale IS NULL`, and 386 of those have a valid 4.0%→12.0% cap. However, **zero** rows qualify as core through the lease-derived remaining firm-term fallback: `lease_fallback_would_core_before_cap_filter=0`, `lease_fallback_term_dots=0`, `fallback_share=0`. The 1,143 displayed dots all come from sale-frozen `sales_transactions.firm_term_years_at_sale` after the 2001+ chart window. Report-use verdict: acceptable for current report use because no plotted core row depends on lease-derived term provenance. Remaining structural caveat: the underlying view still contains the lease fallback, so future unbackfilled sales could enter the report unless the view exposes a term-source flag or requires `firm_term_years_at_sale IS NOT NULL` for the core-dot wrapper.
- 2026-08-10: Investigated government `rent_and_price_psf` / "Rent & Price Per Square Foot (TTM)" screenshot showing early-history gaps and jumps around the 1990-1996 x-axis. The chart source is `cm_gov_rent_price_psf_q`; bars plot `rent_psf`, line plots `price_psf`, and the native renderer has no special reliability trim. Repository audit notes already identify the source as TTM gross rent/SF (5-200) plus price/SF (50-2000), `sf_leased >= 500`, with no exclusion or n-gate. Live view check: 124 all-subtype rows from 1970-03-31 to 2025-09-30. Pre-1997 history is not reliable: isolated rows appear in 1970, 1979, 1988, and 1990-1996; 1990-1996 has `rent_psf = null` throughout (`n_with_rent_ttm=0`) while `price_psf` is based on only 3-4 TTM sale observations, producing the visible line jumps (e.g. 1994-12 around $638/SF, 1995-12 around $528/SF, 1996-09 around $506/SF). First sustained display period by a conservative continuity gate (8 consecutive quarters where both rent and price are populated and both TTM counts are >=4) is 1997-06-30. Sample density becomes materially stronger by 2003 (rent TTM avg n ~10) and especially 2005+ (rent TTM avg n ~25, price TTM avg n ~38). Current gov `cm_view_registry` has no `rent_and_price_psf` display row, so export/app rows are not cropped. Recommended fix: add a gov display policy for `chart_template_id='rent_and_price_psf'`, `view_name='cm_gov_rent_price_psf_q'`, curated `display_from='1997-06-30'` at minimum, or `2003-01-01` if the report standard should prioritize stronger sample density over maximum history. Longer-term source/data follow-up: decide whether to import/curate legacy pre-1997 rent-at-sale history; current live data does not support displaying 1990-1996 as a continuous TTM combo.
- 2026-08-10: Investigated government `sold_cap_by_term_dot_plot` / "Cap Rate Comparison — Closed Sales by Lease Term Remaining" screenshot. This is not a lack-of-term-data problem anymore: live cap-eligible government sales have `firm_term_years_at_sale` populated for 3,211/3,211 rows overall, 1,831/1,831 since 2015, 877/877 since 2020, and 162/162 since 2024. The problem is that `cm_gov_sold_cap_by_term_dot` still uses the old R66i term ladder and does NOT reference `firm_term_years_at_sale`; it prioritizes `(gsa_leases.termination_date - sale_date)`, then `leases.firm_term_years - elapsed`, then the older `sales_transactions.firm_term_years`, then `lease_expiration`. That is the opposite of the dialysis R68/R66x pattern, where the sale-frozen term field is the canonical input and view consumers do not fork the term definition. Live mismatch audit: 1,368 cap-eligible sales fall into a different bucket under the current gov view ladder than under sale-frozen `firm_term_years_at_sale`; examples include 2025-2026 sales with 10+ years at sale being plotted as `<5` or `outside` because a stale/short GSA termination date wins first. The view also still uses a `5-10` cohort (`cap_5to10`) while the chart/export label says `6-10`, so 5-6 year sales are assigned to the middle bucket behind a 6-10 label. Latest 24-month sale-frozen recompute at 2026-06-30 is robust and reasonable: 10+ n=26 avg 7.65%, 6-10 n=13 avg 7.73%, <6 n=39 avg 7.97%, outside n=35 avg 7.65%; the exported dot view's latest outside-firm label is 9.40%, matching the screenshot and indicating view-ladder contamination rather than actual market movement. Required fix: rebuild `cm_gov_sold_cap_by_term_dot` and any sibling `cm_gov_cap_by_term_m/_q` consumers to use `COALESCE(s.firm_term_years_at_sale, sale-date lease fallback only when sale term is null)` as the first term source, align the displayed bucket boundaries to 10+ / 6-10 / <6 / outside, keep the 2-year TTM + n>=5 gate, and expose per-cohort n columns for QA. Review whether sale rows with blank `firm_term_source` should be backfilled with provenance, but do not treat this chart as a data-ingestion failure; the data exists and the pull/view definition is stale.
- 2026-08-10: Updated `quarterly_volume_bars` / "Quarterly Volume Bars" for the government export feedback. Root cause: the synthetic composer already had the desired rolling-3-month monthly calculation, but the later `master_m` override path replaced chart rows with quarter-end `quarterly_volume` values from `cm_*_market_quarterly_master_m`; those values are repeated for every month in the quarter in the underlying monthly master, which produced the visual pattern of three straight months carrying a quarter's value. Code fix: added shared `buildRolling3MonthVolumeBars(masterMonthlyRows)` in `api/capital-markets.js` and used it from both the synthetic composer and the master-monthly mapper so `Data_Volume_Quarterly` now plots a true trailing 3-month rolling sum of `monthly_volume` / `monthly_count` at monthly cadence. Formatting fix: `api/_shared/cm-native-chart-injector.js` now selects millions vs billions for `quarterly_volume_bars` based on the plotted values; sub-$1B rotations keep `$XM` labels, while charts with billion-dollar bars use `$XB` on both the y-axis and peak/low/latest callouts. Verification: `node --check api/capital-markets.js`, `node --check api/_shared/cm-native-chart-injector.js`, and `node --test test/cm-native-chart-injector.test.mjs` all passed; added a regression proving billion-scale quarterly-volume callouts and y-axis formats switch to `$XB`.
- 2026-08-10: Investigated government `market_turnover` / "Market Turnover — TTM Sales / Active Universe" screenshot showing the chart axis starting in 2005 even though on-market inventory is not reliable until the 2012 era. Live `cm_gov_market_turnover_m` confirms this is source/view coverage, not Excel rendering: rows begin 2005-01-31, and early plotted inventory/mos values are tiny artifacts (e.g. 2005-01 active_count=1, 2006-06 active_count=3, 2008-12 active_count=2, 2011-12 active_count=6 against 79-180 TTM sales). By 2012 the series begins ramping into the sustained modern capture window (2012-12 active_count=55; latest 2026-06 active_count=107, months_of_supply=16.9). The government `cm_view_registry` in live DB does not yet expose `display_from` (`42703 display_from does not exist`), so the export cannot apply a DB-backed crop for this chart. Code fix: added an export-side curated `resolveEffectiveDisplayFrom()` fallback for `vertical='gov'` + `chart_template_id='market_turnover'` at `2012-01-01`, and routed direct-view, monthly-master, and synthetic crop paths through it. This keeps the chart from displaying unreliable pre-2012 on-market artifacts while preserving 2012+ history. Database follow-up: migrate the gov `cm_view_registry` display_from columns/functions or rebuild `cm_gov_market_turnover_m` to null/crop active inventory before the sustained coverage era so the data source and export policy converge.
- 2026-08-10: Investigated government `lease_structures` / `Data_Lease_Terms` table screenshot. The live view is not answering the stated question ("new lease commencements during current quarter / last 12 months / last 5 years"): `cm_gov_lease_structures` currently returns `current_quarter=3,989`, `ttm=4,484`, `last_5_years=6,805`, so the "current quarter" is effectively broad/bulk GSA lease-event stock rather than a quarterly commencement cohort. This is why many columns look the same or similar. Live `gsa_leases` has enough fields for a better source: `lease_effective`, `lease_expiration`, `termination_date`, `latest_action`, `lease_rsf`, and `pct_building`. Candidate recompute from `gsa_leases` using `latest_action IN ('New','New/Replacing')`, `lease_effective` inside each rolling window, and `pct_building >= 50` produced plausible majority-building/single-tenant-relevant sample sizes: current quarter 12, TTM 63, last 5 years 486. Stricter `latest_action='New'` only is much thinner (current quarter 8, TTM 26, last 5 years 123); strict `pct_building=100` is thinner again (current quarter 6, TTM 17, last 5 years 89). Chosen fix: added gov migration `supabase/migrations/government/20260810_cm_gov_lease_structures_majority_new_commencements.sql` replacing `cm_gov_lease_structures` with a long-format view preserving existing export columns (`period_label`, `term_bucket`, `bucket_count`, `pct_of_total`) but using the New/New-Replacing + `pct_building >= 50` cohort and true current-quarter/TTM/5-year `lease_effective` windows anchored on `cm_last_completed_quarter_end()`. Export formatting update: `Data_Lease_Terms` subtitle/footer now discloses the cohort. Verification: `node --check api/_shared/cm-excel-export.js`, `node --test test/cm-export-audit-fixes.test.mjs` (22 pass), and `git diff --check` passed. Deployment note: migration is prepared but not applied live from this repo session because the Supabase CLI link is not safely confirmed as the Government DB target.
- 2026-08-11: Investigated government `lease_renewal_rate` / `Data_Renewal_Rate` screenshot where rolling-12-month lifecycle bars looked too large for an ~8,800-lease inventory. Live monthly view confirms the commencement-side counts are true TTM lease-level action counts from `gsa_leases.lease_effective`: at 2026-06-30, first-gen 110, renewed 92, succeeding/superseding 139, total 341. The inflated/out-of-balance side is exits: the quarterly view still reports event-derived `expired_leases=1,222` at 2026-06-30 and lacks the appended true-departure fields the chart code expects; the monthly view includes `net_departures_ttm=449`, but still exposes `terminated_leases=524` from `gsa_leases.termination_date`, which is a firm-term/option date rather than an actual departure. Independent lease-level audit for 2025-07-01→2026-06-30 found `lease_expiration_ttm=305` and `termination_date_ttm=524`, confirming the raw event/option-date fields are not safe to plot as actual exits. Fix prepared: added gov migration `supabase/migrations/government/20260811_cm_gov_lease_renewal_rate_true_ttm_reconcile.sql` to rebuild both `cm_gov_lease_renewal_rate_m` and `_q`: expirations now come from lease-level `gsa_leases.lease_expiration`, `net_departures_ttm` remains sourced from snapshot-based `cm_gov_lease_termination_rate_m.terminated_ttm`, and the charted negative split (`terminated_leases` + `non_renewed_expirations`) is capped/reconciled so it always equals true departures instead of overstating movement. The quarterly view also gets the same true-departure appended-column contract as monthly. Deployment note: migration is prepared but not applied live from this repo session because the Supabase CLI link is not safely confirmed as the Government DB target.
- 2026-08-11: Investigated government `dom_and_pct_of_ask` / `Data_DOM_Ask` screenshot asking whether robust history can extend before 2018. Live ops catalog maps the chart to `cm_gov_dom_pct_ask_m` monthly. The view has 351 monthly rows from 1997-01→2026-03 and plotted values for both `avg_dom` and `pct_of_ask` from 2007-05 onward; complete full-year plotted values begin 2008. However, the live view does not expose `n_sales`, `median_dom`, or `median_pct_of_ask`, so the native chart density function could not confirm earlier robustness and fell back to the hard-coded 2018 floor. Independent source audit of 15,035 `sales_transactions` rows from 1997-01-01→2026-06-30 found the stronger density thresholds: first full year with at least 5 TTM samples for both DOM and % original/list ask is 2008, at least 10 is 2010, and at least 15 is 2011. At 2011 every monthly TTM window has at least 25 valid DOM observations and 15 valid % ask observations; 2010 is close but % ask bottoms at 11. Conclusion: yes, robust government DOM/% ask history can start in 2011, not 2018; pre-2008 is not robust, and 2008-2010 is usable but thinner. Code fix: updated `api/_shared/cm-native-chart-injector.js` so Government `dom_and_pct_of_ask` and monthly variant use the verified 2011 floor when `n_sales` is absent, while views that do expose `n_sales` continue to self-floor dynamically. Added regression coverage in `test/cm-native-chart-injector.test.mjs`.
- 2026-08-11: Applied live Government DB fix for `lease_termination_rate`: migration `supabase/migrations/government/20260811_cm_gov_lease_termination_rate_clean_active_snapshots.sql` rebuilds `cm_gov_lease_termination_rate_m/_q` so active denominators (`total_leases_active`, `leases_outside_firm_term`) select from the latest clean snapshot only. This removes the 2019-02 missing/near-zero stacked bar (`2019-02-28` now 8,054 active / 5,691 in-firm, not 11 active / 6 in-firm). Recoverable `gsa_inventory_snapshot_lines` partial months were backfilled from same-month `gsa_snapshots`: 2022-10 now 7,713 rows and 2022-11 now 7,708 rows. 2019-02 remains a source re-ingest item because both per-lease sources are partial; it is annotated in `gsa_inventory_snapshots.notes` and skipped by the clean-snapshot view logic.
- 2026-08-11: Set the government `rent_and_price_psf` display policy. Added gov migration `supabase/migrations/government/20260811163648_gov_rent_price_psf_display_policy.sql` to bring `cm_view_registry` display policy columns/functions to the Government DB if absent and upsert `cm_gov_rent_price_psf_q` with curated `display_from='1997-06-30'`. The policy uses the current robust minimum (first sustained period where both rent/SF and price/SF are populated with n>=4 for 8 consecutive quarters) and documents `2003+` as the stronger density standard. Added a temporary exporter fallback in `api/capital-markets.js` with the same date so current exports crop pre-1997 rows until the Government DB registry migration is applied; DB registry values still win when present. Legacy pre-1997 rent-at-sale history was not imported: verdict is defer/no-import unless a curated legacy master is approved as a separate provenance-tagged source and passes continuity/density QA. Live apply was intentionally skipped from this workspace because the local Supabase link currently points to Dialysis_DB (`zqzrriwuavgrquhisnoa`), not Government DB (`scknotsqkcheojiaewwh`).

## Follow-Up Prompt Bank
- Government credit-tier propagation: investigate missing State/Municipal cap-rate coverage and classifier/enrichment gaps for `government_type` / `agency`; start from `docs/capital-markets/CLAUDE_CODE_PROMPT_gov_credit_tier_ingestion_propagation.md`.
- Government Case for Renewal: decide between importing legacy 1991 annual history and replacing the event-based source with a monthly TTM `gsa_leases.latest_action='New'` view; audit and suppress bulk `gsa_lease_events.new_award` clusters.
- Government lease termination source re-ingest: reload exact per-lease rows for `gsa_snapshots` / `gsa_inventory_snapshot_lines` 2019-02, and close small header variances for 2022-10 (1 row) / 2022-11 (6 rows). The chart/view issue is fixed live by clean-snapshot carry-forward.
- Government seller sentiment: fix gov binding of `_8q` dialysis-only columns versus populated gov 6+ fields, then separately investigate listing-history capture for `initial_price`, `last_price`, ask caps, DOM, and `had_price_change`.
- Government core cap-rate dot plot: audit rows where `firm_term_years_at_sale` is null and the core qualification comes from lease-derived remaining firm term, then decide whether the provenance is acceptable for report use.
- Government rent and price PSF: apply `supabase/migrations/government/20260811163648_gov_rent_price_psf_display_policy.sql` to live Government DB, then verify `cm_view_registry` returns `display_from=1997-06-30` for `cm_gov_rent_price_psf_q`; exporter fallback is already in place until live DB policy is applied.
- Government cap rate by lease term: rebuild term-bucket views to use sale-frozen `sales_transactions.firm_term_years_at_sale` first, align 10+ / 6-10 / <6 / outside labels, and expose cohort n columns for QA.
- Government market turnover: migrate gov `cm_view_registry` display_from support or rebuild `cm_gov_market_turnover_m` to null/crop pre-2012 on-market artifacts at the source.
- Government lease structures: apply and verify the prepared majority-building new-commencement migration in the Government DB, then compare current quarter / TTM / 5-year term buckets against a spot-checked lease sample.
- Government lease renewal rate: apply and verify the prepared true-TTM reconciliation migration in the Government DB, then confirm charted exits reconcile to snapshot `net_departures_ttm`.
- Government DOM and % ask: update `cm_gov_dom_pct_ask_m/_q` to expose `n_sales`, `median_dom`, and `median_pct_of_ask`, then replace the current gov 2011 fallback with a DB-backed density/display policy.

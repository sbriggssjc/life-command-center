W6.5 Stage 4 Unit 2: extract sync health into ops-sync-health.js

Moves ops.js 6346-6538 VERBATIM (sha256 878c96f311af5913 before and after).
ops.js 6,803 -> 6,617. renderSyncHealthPage + triggerSync / retrySync /
reconnectConnector / removeConnector. Entry point: app.js:1135, pageSyncHealth.

TWO DIFFERENT window-BINDING MECHANISMS COEXIST IN THIS REGION, BOTH LOAD-BEARING:
  - reconnectConnector / removeConnector carry EXPLICIT window.x = x lines and are
    reached by onclick="reconnectConnector(...)".
  - triggerSync / retrySync have NO export. They are passed as BARE IDENTIFIERS
    inside an onclick string -- onclick="_opsBtnGuard(this, triggerSync, ...)" --
    resolved off window at CLICK time, which works only because a top-level
    function declaration in a CLASSIC script becomes a window property
    automatically.
Neither may be tidied into the other. The second pair has no export line to
assert, so the guard asserts the CALL SHAPE: the bare identifier must still
appear inside an inline onclick. Convert this file to type="module" and those two
buttons die silently while everything else keeps working -- so the
module-forbidding assertion carries that specific reason.

A GUARD THAT NAMES A FILE GOES STALE ON THE NEXT MOVE -- Unit 1's did, one unit
later. Unit 1 asserted "Sync Health IN OPS.JS still schedules
appendPerfToSyncHealth". True when written; false the instant this unit moved
Sync Health to a sibling, though the relationship it protects was never violated.
Re-based to assert the RELATIONSHIP: find whichever ops file defines
renderSyncHealthPage and require THAT file to schedule appendPerfToSyncHealth.
Move-proof and strictly more meaningful. Rule: assert the relationship, not the
address.

DEAD STATE FOUND: opsSyncData (ops.js:86, `let opsSyncData = null;`) is
referenced NOWHERE in the repo -- the declaration is its only occurrence. It sits
in the shared state header that Unit 1 established siblings must never own, which
makes that header partly dead weight. Not removed here (out of scope for a
byte-identical move); recorded in the map so the next reader does not assume
every line of it is live.

Mutation-tested, all six fail correctly:
  A drop the explicit window.removeConnector export
  B break the bare-identifier onclick for triggerSync
  C delete the perf-dashboard graft      (2 suites: the re-based Unit 1 guard
                                          AND the Unit 2 seam guard)
  D eval-time statement in the sibling   (Stage-4 rule)
  E app.js stops dispatching pageSyncHealth
  F load the sibling after ops.js

Cache busters moved as a set (16 files -> v=2026082014). Front-end suites 133/133.

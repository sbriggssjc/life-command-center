Fix CMS link buttons, HCRIS cost=revenue quirk, movers duplicates

1. Export _udCmsLinkCandidate, _udCmsClearLink, _udCmsTypeahead to
   window scope — onclick handlers in dynamic HTML couldn't call them.
   Fixes the non-functional "Link" and "Change" buttons in Operations tab.

2. When HCRIS total_costs equals total_patient_revenue (Form 265-11
   Medicare-allocated cost quirk), prefer derived costs (revenue - profit)
   for a meaningful profitability picture.

3. Fix v_facility_patient_counts_mom: use COALESCE(clinic_id, medicare_id)
   to eliminate null clinic_id rows, and filter to only the latest snapshot
   per clinic (row_number=1) so clinics can't appear in both Movers Up
   and Movers Down simultaneously.

Bump SW cache to v249.

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>

# STATUS.md Open-threads — "App feedback intake (SBN)" row, verbatim as of 2026-09-22 (before round 64)

The row's state cell had grown to a multi-round narrative (rounds 45–63). Cowork round 64 moved it here **verbatim** and replaced the live cell with a current-state summary. The dated `## ` entries in `STATUS.md` (and its archives) remain the full record.

```
| **App feedback intake (SBN)** | FLOWS1, FLOWS1-artifact, FLOWS-consolidate, FLOWS-consolidate-lcc, FLOWS1-path, HOME1, HOME2, PRI1, PRI2, PRI2-on, DIA1, DIA1b, DIA1c, ID3a-drift, RECON1, RECON1-b, RECON2, RECON2-b, RECON2-render, RECON3, RECON3-b, SIDEBAR3, SIDEBAR3-b, SIDEBAR3-c, SIDEBAR3-d, EXT-HOST-2, SIDEBAR4, LEASEJUNK1, PERF-SPQ2, HOME2-fix, HOME2-b, HOME2-c, HOME2-d, HOME2-e, PERF-SPQ1, PERF-SPQ1-b, PERF-SPQ1-c, PERF-SPQ2, RECON2-c, RECON2-d, RECON2-d-reconcile, RECON2-d-render, RESOLVER1, SIDEBAR-LEASE1, SIDEBAR2, SIDEBAR4, LEASEJUNK1, DIA-PROPAGATOR1, VERCEL-LIVE1 | 2026-09-22 | **Round 45:** `HOME2-e` live — the three-lane Home is done (one stacked column in the TODAY card); Q36 script ready for the Dialysis repo; Q37 recommendation = pause the propagation job, keep the scheduler, Dialysis prompt `DIA-PROPAGATOR1` written (writer went quiet 20:33 UTC on its own); PERF-SPQ2 20-s cold request measured; next: `SIDEBAR4` **👤 2026-09-21 (Round 48): `DIA-PROPAGATOR1` CLOSED** — PR #7421 confirmed merged/live, production purge confirmed complete and holding (zero new junk rows since 09-19). **👤 2026-09-22 (Round 53): SBN-18/19 intake** — a second flow-failure digest (SBN-18, `FLOWS1` updated, SF Listing Activity spiked 35→5368/week, cause unconfirmed) and a new reconciliation-gap case (SBN-19, `RECON3` filed) found live on a property our team just sold: raw Salesforce Account IDs stored as owner/buyer/seller names, a stale pre-sale valuation never yielding to the real closed sale price, and a sidebar-polluted lease tenant. Both source files moved to `SB notes/done/`. **👤 2026-09-22 (Round 54): SBN-20 intake** — the exported client "Asset Profile" report Scott added to the folder (the file SBN-19 flagged as referenced-but-missing) has zero sale-status awareness (`_udExportOperations()`, `detail.js:5436` never reads `pipeline_stage`/`sales_transactions`); folded into `RECON3` as Bug 4 / action item 7 rather than a separate prompt. **👤 2026-09-22 (Round 55): `RECON3` reconciled** — code shipped and verified real against the live diff (609+29 tests), but property 27266's own data is still bad (no fix is retroactive) and the new `current_value_estimate` guard has a live-verified logic flaw (uses `properties.updated_at` as a proxy for sale recency, which routine writers touch constantly) — `RECON3-b` filed. **👤 2026-09-22 (Round 56): `RECON3-b` reconciled** — the value-estimate guard flaw is genuinely fixed and property 27266's raw-SF-id names were cleared live (dry-run confirmed zero other rows fleet-wide, so this defect was isolated to this one property); `current_value_estimate` itself still needs a **Railway redeploy of `tranquil-delight`** before the fix takes effect on real traffic (Scott action item, not Cowork's) and `leases.tenant` still needs a retroactive touch. **👤 2026-09-22 (Round 57): cleanup + backlog review** — `leases.tenant` fixed live (now `DaVita Kidney Care`); government DB checked for the RECON3 defect, zero matches, no fix needed; `SIDEBAR3` re-confirmed still live (all three range-address twins unmerged) and prompted. `current_value_estimate` still awaits Scott's Railway redeploy (Q40). **👤 2026-09-22 (Round 58): `SIDEBAR3` reconciled** — two of the three range-address twins genuinely merged live (Orlando, Scranton), independently re-verified against Dialysis_DB (lease/sale counts match exactly); the third correctly refuted, not force-merged — it's a real distinct Goldsboro-NC facility, not a twin. Two small tangential findings filed as `SIDEBAR3-b` (a dangling `canonical_property_id`, a superseded-lease contamination). Scott's re-send verification for Orlando/Scranton still pending — `OPERATOR-CHECKLIST` Q41. **👤 2026-09-22 (Round 59): Q41 re-send surfaced a real guard defect.** Scott re-sent Orlando + Scranton. Scranton silently minted a fresh duplicate property (`51252`) instead of attaching to the merged `28547` — a directional-spelling gap in `sameStreetRest()` (abbreviated `S` vs spelled-out `South`); cleaned up live (merged `51252` back into `28547`, reversible, backup_id 598). Orlando correctly refused (the guard has no memory of `SIDEBAR3`'s own completed merge) — no path to ever land without a code fix. `SIDEBAR3-c` filed and prompted for both. **👤 2026-09-22 (Round 60): `SIDEBAR3-c` reconciled + a new extension defect triaged.** Verified the merged fix (PR `#2619`, commit `886e9757`) against the actual diff, not the response's own summary: both findings fixed, plus CC caught a second, real cause of the Scranton miss not in the original prompt (the candidate-fetch `ilike` hint couldn't match spelled-out streets either). 25/25 new tests + 442/442 related suite pass; live-confirmed the exact merge-ledger rows the fix depends on. Fleet sweep filed as `SIDEBAR3-d` (85 pairs for review, do not auto-merge). Separately, Scott's CREXi OM-upload screenshot showed the `EXT-HOST` failure mode recurring against a now fully-DELETED Vercel project (`J13-teardown` closed 09-18) -- filed as `EXT-HOST-2`, `OPERATOR-CHECKLIST` Q42 (reload extension to 1.0.54, re-save the Railway URL, retry). **👤 2026-09-22 (Round 61): all three operator actions verified closed.** Scott redeployed `tranquil-delight` and re-sent/re-tried all three: property 27266's `current_value_estimate` now `$2,587,220` (matches sale price exactly, Q40 closed); Orlando's re-send attached to `22887` instead of creating a twin, no third Kirkman/Washington Ave property exists (Q41 closed); the CREXi OM PDF re-upload landed `status=finalized` in `staged_intake_items` (Q42 closed). All live-verified by Cowork, not taken on Scott's word. **👤 2026-09-22 (Round 62): four next-round prompts drafted at Scott's request.** `SIDEBAR4` (duplicate contact entities from the still-unfixed double sidebar request), `LEASEJUNK1` (OM table headers landing in `leases.tenant`), `RECON2-render` (surface `expiration_state` in the four reader call sites found by grep), `PERF-SPQ2` (Home boot latency against `v_lcc_seller_prospect_queue`, measure-first). `SIDEBAR3-d`'s 85-pair review list also prompted (route into the existing `dia_property_twin_review` lane, no merges). All five backlog rows marked prompted. **👤 2026-09-22 (Round 63): all five round-62 prompts reconciled.** Each merge (#2623/#2625/#2626/#2627/#2628) was checked against its diff and live data, and `tranquil-delight` was verified live on `881dc2b4` (`verify:deploy`). `LEASEJUNK1`, `SIDEBAR3-d` and `RECON2-render` were verified as claimed. `PERF-SPQ2` summary is 946 ms, single-pass; a cold-load probe is owed (Q44). `SIDEBAR4` code is live, but its own run log shows a **second `process_sidebar_extraction` request 0.5–1 s after every Update**, source unknown → `SIDEBAR4-c`. The index migration and extension 1.0.55 reload are pending → Q43. |
```

# Operator checklist — closed rows (archive)

Rows moved **verbatim** out of `docs/claude-code/OPERATOR-CHECKLIST.md` once closed and verified. The live list keeps only open items. Q-numbers are never reused.

## Archived 2026-09-22 (Cowork round 64)

| # | tier | step | why / state | rows |
|---|---|---|---|---|
| Q3 | **A** | Execute `docs/history/RUNBOOK_vercel_teardown_2026-09-18_DONE.md` in order (repoint the daily-briefing caller, the mobile Shortcut, the extension/Copilot connector hosts, then tear down). The browser extension is **still writing through the retired Vercel build** from at least one profile. **Round 34 — do these in order:** ① Railway, both services, Variables: confirm every column-A name from `RUNBOOK_vercel_teardown.md` § Step 3b exists (names only) — say which are missing. ② Vercel env list: scroll the Jun 11 → Apr 20 block + open the two `RE…EY` rows; paste the names. ③ Step 3: rotate the LCC Opps `service_role` key and put the new one on both Railway services in one sitting. ④ Step 4: Delete Project on Vercel; proof = `DEPLOYMENT_NOT_FOUND`. ⑤ Tell Cowork → Step 5 is ours. **Round 35 — next step, precisely:** Railway → `tranquil-delight` → Variables → **Raw Editor** → copy → paste into Notepad → **delete everything from the first `=` to the end of each line** (values must not leave the dashboard) → save as `responses/Railway tranquil-delight variables (names).txt`. Your docx captured the 46 shared names but only the tail of the 79 service names. Then the same for the standalone MCP service. Cowork does the column-A comparison from those two files. Also: the 4 shared vars shown *ADD* (`OPENAI_API_KEY`, `REGRID_API_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_URL`) — are they service variables instead? The raw list will say. **Round 37 — lists received ✅, comparison done (`RUNBOOK_vercel_teardown.md` § Step 3b result). Four moves left, in order:** ① Railway → `tranquil-delight` → Variables → add `TEAMS_INTAKE_WEBHOOK_URL` (copy the value from the `life-command-center` service). ② Rotate the LCC Opps `service_role` key (Supabase → LCC Opps → Settings → API), then update `OPS_SUPABASE_SERVICE_KEY` / `LCC_SERVICE_ROLE_KEY` on all four Railway services — one sitting, the app is down in between. ③ Vercel → Settings → General → Delete Project; proof `DEPLOYMENT_NOT_FOUND`. ④ Delete `responses/*variable names.docx` (they contain values). Tell Cowork after ③. **✅ DONE 2026-09-18** — `DEPLOYMENT_NOT_FOUND` verified 16:50 UTC; Step 5 done by Cowork round 38. | ✅ done 2026-09-18 | J13-teardown, EXT-HOST, J13 |
| Q29 | **D** | ~~Your own name appears three times in `brokers`~~ **Answered 2026-09-17:** all three are Scott (1373 Northmarq, 2076 bare duplicate, 2437 Stan Johnson); keep firm attribution by date via `broker_company_history`, not a flat merge; firm 126 (`scott briggs`) → Northmarq. Recorded on `BR4-b`. | done | BR4-b |
| Q30 | **A** | **The LCC window you use is the old Vercel site, running old code.** ☐ Open `https://tranquil-delight-production-633f.up.railway.app` in Chrome, sign in, and look at Home — that is where the three-lane Home (and everything since early September) lives. Tell Cowork what Home looks like. ☐ If it works: uninstall the current LCC desktop app (⋯ menu → Uninstall) and install it again from the Railway URL. ☐ Do **not** delete the Vercel project yet — Cowork needs its env-var list compared with Railway's first (Vercel → Project → Settings → Environment Variables: names only, no values). **Round 31: steps 1–2 done ✅ (Railway Home seen in Chrome and the reinstalled desktop app; the three lanes did not show because the build has no markup for them — HOME2-fix, not you).** Still yours: ☐ Vercel → Settings → Environment Variables, send the **names**; ☐ stop opening the `vercel.app` address anywhere (bookmarks, phone). **Round 32:** the lanes are live but two show wrong data (HOME2-b) — no need to look again until HOME2-b ships. ☐ Vercel env-var names still needed. **Round 33:** env-var names audited → `docs/history/RUNBOOK_vercel_teardown_2026-09-18_DONE.md § Step 3b`. ☐ On Railway (both services) confirm every column-A name exists (names, not values). ☐ Scroll the Vercel list between `FOLDER_FEED_…` (Jun 11) and `INTAKE_EXTRACTION_ENABLED` (Apr 20) — the screenshots skipped it — and open the two `RE…EY` rows to read their full names. ☐ Then delete the Vercel project. **Round 33b:** this is the same job as **Q3 (J13-teardown)** — follow `RUNBOOK_vercel_teardown.md` Steps 3b → 3 → 4 → 5; Q30 closes when Q3 does. ✅ closed with Q3. | ✅ | VERCEL-LIVE1, HOME2 |
| Q31 | **A** | ☐ `responses/RECON2-b desktop response.docx` saved as **0 bytes** (Word lock file beside it) — re-save it so the round's own summary is on file. ☐ Read the **7 leases** RECON2-b now calls confirmed-expired (2 CMS closed/relocated with no operating clinic, 4 `status=Terminated`, 1 successor lease — listed by `select * from dia_recon2_classify_expired_leases(null) where proposed_state='expired_confirmed'`) and say confirm / hold; nothing is written until you do. **Round 33:** response re-saved ✅. The 7 leases are laid out in `docs/audits/RECON2-b-confirmed-expired-leases-review-2026-09-18.md` with Cowork's read per row — mark confirm / hold / wrong and say so in chat; a CC round runs the confirmations. **Round 34:** row 1 (Sierra Vista) = **Conflict, hold** — CoStar + DaVita locator say active; the CMS "closure" was a demoted-duplicate Fresenius CCN on a twin property row (`RECON2-c`). For rows 2–7 do the same two checks (CoStar lease status; operator locator for the address) and reply per row: *confirm / hold / conflict* + what each source said. **Log it in chat** — Cowork writes it into the audit file and the backlog; a CC round writes `expiration_evidence`. No app surface for this yet (that is RECON2-c (4)). **Round 35: rows 2–7 read ✅ and recorded** (audit file + `RECON2-b` row). Outcome: #2, #4 confirm; #3, #5, #6, #7 confirm-with-successor (the current CoStar lease has to land first — your sidebar sends did not write the expiration, `SIDEBAR-LEASE1`). Nothing more from you on Q31; a CC round (`RECON2-c`) writes the evidence and the confirmations. | ✅ read done — CC writes | RECON2-b |
| Q32 | **B** | **Where should the three-lane Home sit?** Today is ~1,800 px tall, so "right after Today" is still off-screen. (a) lanes above Today; (b) lanes replace Today's SIGNIFICANT block (same seller queue) so Today keeps IMPORTANT + URGENT; (c) leave it. One letter in chat → `HOME2-c` becomes a prompt. **Round 34:** still open (a / b / c). Note: with Today dark (PERF-SPQ1-b) the lanes appear near the top only because Today collapsed — not a placement fix. **Round 37:** still open — a / b / c. **Round 39 — how to decide, in plain terms.** Open Home on Railway. The TODAY panel has three blocks: SIGNIFICANT (seller prospects — the same list as the BD lane), IMPORTANT, URGENT; below it sits RESEARCH / BD / INBOX. **(a)** lanes move to the very top, TODAY unchanged below → one scroll saved, SIGNIFICANT and the BD lane still show the same five names twice. **(b) ← Cowork's recommendation:** the lanes take SIGNIFICANT's place at the top of TODAY; TODAY keeps IMPORTANT + URGENT under them → nothing duplicated, page ~600 px shorter, everything above the fold. **(c)** leave it: lanes stay below TODAY, one scroll. Reply with one letter; HOME2-c becomes the prompt. **Round 40:** your screenshots show the duplicate (SIGNIFICANT + BD lane, same five sellers). Cowork drafted `HOME2-c` as **(b)**. ☐ Send it — or say (a) / (c) first and Cowork redrafts. **Round 42 — ✅ built (b) as HOME2-c, live-verified on Railway with the flag on; the lanes overflow the card → `HOME2-d`.** | ✅ answered (b) 2026-09-18 | HOME2-c |
| Q33 | **A** | 🚨 **Today is dark.** PR #2581 (PERF-SPQ1) made `/api/seller-prospect-queue` return 500; Home shows *"Today unavailable — HTTP ?"*. Fastest fix: GitHub → PR #2581 → **Revert** → merge the revert PR (Railway redeploys itself). Or send `PERF-SPQ1-b` to Claude Code and let it revert. Either way, tell Cowork which. **Round 35:** #2583 did not fix it — 502 in 26 s, Today still dark. ☐ Send `PERF-SPQ1-c` (revert both) **or** revert #2581 and #2583 yourself on GitHub. Say which. **Round 37: ✅ Today renders again** (route 200/1.5 s after Cowork applied the merged-but-unapplied RPC). It can still paint "unavailable" on a cold load and needs one Retry — that residue is `PERF-SPQ2`, not yours. | ✅ closed | PERF-SPQ1-b |
| Q34 | **B** | **Wake the resolver's retrain loop (RESOLVER1).** Supabase → Dialysis_DB → Edge Functions → Secrets → add `RESOLVER_URL` = `https://gracious-radiance-production-eeaf.up.railway.app` (the same value `tranquil-delight` has). That is the one missing secret `feature_flags_registry` names for `RESOLVER_RETRAIN_LOOP`; the corpus has not grown since 2026-08-14. Then tell Cowork; the flag flips after one clean `/train`. **Round 39: ✅ done by Scott** — the secret existed; value refreshed to the live `tranquil-delight` URL. Why the corpus stopped growing on 08-14 is now the RESOLVER1 measurement (CC), not a Scott step. | ✅ done 2026-09-18 | RESOLVER1 |
| Q35 | **A** | **Three CoStar lease expiration dates** — Goldsboro `2609 Hospital Rd` (DaVita), Dixon `1131 N Galena Ave` (DaVita), Scranton `920 S Washington Ave` (DaVita, in the 83k SF centre). In CoStar open the property → **Tenants** (or Lease) tab → the DaVita row → *Lease Expiration* (month/year is enough; Cowork uses the last day of the month). Reply in chat as `Goldsboro: 2029-05; Dixon: …; Scranton: …` — the sidebar does not carry this field (`SIDEBAR-LEASE1`), chat does. Then `RECON2-d` inserts the three successor rows exactly as Orlando's (25432, 2028-06-30) and relabels the old rows. **Round 40: ✅ answered — CoStar shows no expiration for any of the three.** Nothing more from you; `RECON2-d` redefines the state to `occupied_term_unknown` instead of inventing a date. | ✅ answered 2026-09-18 | RECON2-d |
| Q37 | **A** | 🚨 **Your PC is re-writing Dialysis_DB around the clock.** The `DialysisProject` scheduler (`run_scheduler.ps1` → `src.run_scheduler`) is running on your machine and its full-data propagation PATCHes every property ~4×/hour and POSTs ~1M junk `learning_logs` rows a day (`ai_scrubber`, `invalid field`; 12.3M rows, 2.2 GB so far). ☐ Decide: stop the scheduler (Task Scheduler / the PowerShell window running `run_scheduler.ps1`) until the Dialysis repo fixes the loop, or tell me it is intended and I file it as accepted load. ☐ Then a Dialysis-repo prompt: stop logging schema misses as learnings, propagate changed inputs only, purge the `invalid field` rows. One word in chat: **stop** or **keep**. **Round 45 — Cowork recommends PAUSE the propagation job, not the whole scheduler** (email fetch, CMS catch-up, lease/listing expiration checks are worth keeping): in `DialysisProject/src/app_utilities.py` comment out the `schedule.every().day.at("02:45")` full-data-propagation line (or set the env/flag the repo uses) and restart `run_scheduler.ps1`; then send `prompts/DIA-PROPAGATOR1-…md` to Claude Code **in the DialysisProject repo**. The writer went quiet at 20:33 UTC on its own (pass finished) — it will start again at the next 02:45 CT unless paused. | ☐ pause + send the Dialysis prompt **Round 47/48 (Cowork) — closed: PR #7421 merged and confirmed live** (`compare_before_write.py` wired in, cadence is weekly Sunday 02:45, `safe_log_learning` refuses `invalid field`). Production purge confirmed complete and holding: `learning_logs` 12,269,613 → ~135k rows, and **zero new `invalid field` rows since 2026-09-19** (the day the fix landed) — the guard is working. Nothing further needed from you on Q37. | DIA-PROPAGATOR1 |
| Q40 | **A** | **✅ DONE by Scott 2026-09-22, verified live by Cowork.** `tranquil-delight` redeployed; property 27266's `current_value_estimate` now `$2,587,220` (matches the real sale price exactly, was `$10,257,374.40`). | ✅ done 2026-09-22 | RECON3-b |
| Q41 | **A** | **✅ DONE by Scott 2026-09-22, verified live by Cowork.** Orlando re-sent post-redeploy; live-confirmed it attached to `22887` instead of creating a twin (LCC Opps `entities` row: `pipeline_status=success`, `domain_property_id=22887`, `last_error=null`), `22887`'s address stayed `4578 S Kirkman Rd` (not overwritten to the range, as the fix requires), and no third property was minted on Kirkman or Washington Ave. | ✅ done 2026-09-22 | SIDEBAR3; SIDEBAR3-c |
| Q42 | **B** | **✅ DONE by Scott 2026-09-22, verified live by Cowork.** The OM PDF re-upload on the same CREXi listing succeeded -- `staged_intake_items` shows the PDF (`1632f966-...cfbd4a78.pdf`) landed `status=finalized` 18:26:46 UTC, seeded with the correct address. Whatever combination of extension reload / Railway URL resave fixed it, it's confirmed working now. | ✅ done 2026-09-22 | EXT-HOST-2 |

## Archived 2026-09-23 (Cowork round 65)

| # | tier | step | why / state | rows |
|---|---|---|---|---|
| Q43 | **A** | **SIDEBAR4 follow-through.** ~~Apply the unique-index migration~~ ✅ **applied by Cowork 2026-09-22 20:41 UTC** (LCC Opps, valid; rolled-back probe confirms a same-name + same-mailbox twin now raises 23505). ☐ **Still yours:** reload the Chrome extension (`chrome://extensions` → Life Command Center → ↻ reload; confirm it reads **1.0.55**), then open any CoStar property you've already saved and click **Update** once. Cowork reads that property's `_pipeline_run_log` to see whether the follow-up `process_sidebar_extraction` request carries the extension's new UUID request id (`SIDEBAR4-c`). | ✅ done 2026-09-23 — extension 1.0.55 reloaded, Updates sent; run log read (→ SIDEBAR4-c narrowed) | SIDEBAR4, SIDEBAR4-c |
| Q44 | **A** | ☐ **Standalone MCP service:** confirm in Railway that `life-command-center` (MCP, port 3100) deployed `881dc2b4` or later. Its `/health` shows no SHA, and the RECON2-render comps label runs there. ☐ **PERF-SPQ2 cold load:** open Home in a fresh tab (hard refresh) and check that **Today** paints without "unavailable" or **Retry**. Report yes/no. | ✅ done 2026-09-23 — MCP service on #2630 code (no mcp/ change since); cold-load Home paints Today (Market Briefs spinner → HOME-MB-BOOT) | RECON2-render, PERF-SPQ2 |

## Archived 2026-09-23 (Cowork round 66)

| # | tier | step | why / state | rows |
|---|---|---|---|---|
| Q46 | **C** | ☐ **Automation decisions from GOV-UX1 §D** (measured; `docs/audits/GOV_UX1_NAVIGATION_OWNER_VERIFICATION_2026-09-22.md`). Reply with the ones to build: **D1** fix the agency-drift detector (recommended first) · **D2** GSA → occupying agency (~272) · **D3** fill blank agency from lease (~46) · **D4** auto-cadence on open leads without one (35) · **D5** gated auto-lead: only seller-queue owners with a reason to sell + linked person (98). Everything else stays human. | ✅ answered 2026-09-23 — build all five in Cowork's order (D1 → D2+D3 in government-lease; D4 → D5 in LCC, in parallel). Prompts written round 66 | GOV-UX1-D1…D5 |

## Archived 2026-09-23 (Cowork round 68)

| # | tier | step | why / state | rows |
|---|---|---|---|---|
| Q45 | **B** | ~~Re-send the Findlay OM from Salesforce~~ **Not needed — Cowork re-ran it 2026-09-23** (the file was stored in the `salesforce-files` bucket after all; re-queued `sf_files` 1747; needed an inbox-key workaround → `INTAKE-RESTAGE1`). Result: extracted with **no address** this time (the broker-block guard held, nothing promoted), so it sits in the Inbox as `review_required` (intake `3605ee76…`, card "OM: USRenalMOB_Findlay_OH_OM_SB.pdf"). ☐ **Still yours:** tell Cowork the listing's street address — Dialysis_DB has four US Renal-related Findlay properties (1717 Medical Blvd ×2 records, 7527 Patriot Dr, 7519-7535 Patriot Dr) — and Cowork attaches it to the right one. **Round 67:** the Salesforce side has no address either. Opportunity `006Vs00000hhYfCIAU` "US Renal-Anchored MOB - Findlay - OH" is `listing_signed` with `property_address` null, and its LCC asset entity `084897cc…` is orphan-flagged with city only. So the address has to come from you, or be typed into the Salesforce listing/opportunity. The re-stage fix is now live, and the card's third extraction (17:30 UTC) re-ran cleanly on the same intake. The card itself shows `archived`. | ✅ 2026-09-23 — Scott: 1717 Medical Blvd, Findlay, OH 45840; Cowork linked asset `084897cc…` → dia 51194; the listing row itself → SF-BRIDGE1 | GOV-AVAIL1 |
| Q47 | **A** | ☐ **Two quick live checks once today's merges are deployed (already true: `tranquil-delight` is on `15de8524`).** (1) Reload the extension (`chrome://extensions` → Life Command Center → ↻; it should read **1.0.57** once `SIDEBAR4-d` is merged, else 1.0.56), then **Save** one new property and do nothing else. After it re-renders it should say **"Saved to LCC just now ✓"** with **"Up to date in LCC ✓"** greyed out and Re-run tucked behind **⋯**. Tell Cowork the address; Cowork reads its run log (should be one run, with `client: lcc-extension/1.0.57`). You don't need to click Update or Re-run after a Save: the Save already ran the pipeline. (2) Hard-refresh Home (Ctrl+Shift+R) and say whether **Market Briefs** now shows content or "Not live yet" instead of spinning. | ✅ 2026-09-23 — 1.0.57 Save = one run (Wichita Falls, `client: lcc-extension/1.0.57`); Home hard refresh shows Market Briefs | SIDEBAR4-c, HOME-MB-BOOT |
| Q48 | **C** | ☐ **Decide GOV-UX1-D5: how to auto-create leads, if at all.** CC re-measured 2026-09-23 before building and built nothing. The gate (seller queue + measured reason to sell + a linked person + no open lead) is **54 owners / 100 property rows**. **28 of the 54 qualify only through a `works_at` edge**, the bare Salesforce-account link P161 already refuses to count as reach. The first page includes `4238 Washington Street` (an address filed as the owner), Truist Bank, Homestead Community Pharmacy, REIT SPEs (`ARC GS…001, LLC`) and 6 repeat buyers the lead writer would refuse. Pick one: **(a)** tighten the gate (a real decision-maker role, not `works_at`/`parent_of`; an owner name that passes the junk and person-shape checks; repeat buyers excluded; lead owner = the point person), grade the named rows, dry-run, then automate: about 25 owners by today's roles (CC's recommendation); or **(b)** no automation: show the gated owners as a review list with a one-click "Create lead". Also FYI, no action: **D4 is closed.** The 35 opportunities "with no cadence" are our own Salesforce listing/escrow deals, not prospects (all 4 real prospects already have one). The Salesforce sync leaves their `type` blank, which is filed as `GOV-UX1-D4-sftype` and still to be prompted. | ✅ answered 2026-09-23 — "automate as much as possible; objective is accuracy and action" → hybrid prompted (GOV-UX1-D5-gate) | GOV-UX1-D5, GOV-UX1-D5-gate, GOV-UX1-D4-sftype; audit `GOV_UX1_NAVIGATION_OWNER_VERIFICATION_2026-09-22.md` §D "D4 / D5 re-measured" |
| Q49 | **B** | ☐ **Deploy the `intake-salesforce-files` edge function to Dialysis_DB.** INTAKE-RESTAGE1 added `?action=requeue`, the supported "re-process this Salesforce file" call, but the live function is still **v30** (checked 2026-09-23). From PowerShell in the repo: `supabase functions deploy intake-salesforce-files --project-ref zqzrriwuavgrquhisnoa`. CC confirmed the repo copy matched v30 before its edit, so nothing rolls back. Or say "deploy it" and Cowork will try through the Supabase connector. | ✅ 2026-09-23 — Scott deployed; v31 live, `requeue` listed, secret-gated (Cowork verified) | INTAKE-RESTAGE1 |

## Archived 2026-09-23 (Cowork round 69)

| # | tier | step | why / state | rows |
|---|---|---|---|---|
| Q50 | **B** | ☐ **Which Wichita Falls property did you mean?** You sent "2600 Central Fwy N", but the CoStar page captured was **4005 Call Field Rd, Suite 100** (CoStar #1014478): a multi-tenant retail center whose tenants include the Wichita Falls VA Clinic (7,031 SF), T-Mobile, GameStop and H&R Block. It was minted as new gov property **41083** (agency canonical VA). The VA clinic at **2600 Central E Fwy** already exists **twice** in gov (31048 and 31796, not merged). If 4005 Call Field Rd is the one you wanted, nothing to do. If you meant 2600 Central, open that CoStar page and Save it; Cowork routes the 31048/31796 pair into twin review either way. | ✅ answered 2026-09-23 — Scott meant 2600 Central Fwy N (the shopping center); the capture took the Contacts-tab leasing-company address → `SIDEBAR5` | GOV-AVAIL1-twins |

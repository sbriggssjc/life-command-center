# PLANNED-BACKLOG — shipped rows archive (2026-09-24, DOCMAP3)

> **What this is.** Every row below carried a closed `✅` State in `docs/os/PLANNED-BACKLOG.md` with no open
> marker (🟡 🔴 👤 ⏳ 🔍 ⏰ 🟠 🟢 ⛔ 🛑 ⚪, "verify", "pending", "partial", "open", "→", "owed", "blocked", "waits") and was
> moved here **verbatim** (byte-identical row text) by DOCMAP3, the first pass under the shipped-row rule in
> `docs/os/DOCUMENTATION-MAP.md` §3/§4. Nothing was reworded or summarized; rows are grouped by the `##` arc they
> lived under. Nine ✅ rows were deliberately LEFT in the backlog because their State or Item still names an owed
> step (HCRIS-TRACKER-BLIND, SIDEBAR5, GOV-CU1, GOV-AVAIL2, GOV-UX1-D5-gate-buyerspe, GOV-UX1-D5-gate-sponsor,
> C1C-UNAPPLIED, OWNERGAP2, OWNERGAP2-harris-c).
>
> **What is live** lives in `docs/os/CURRENT-STATE.md`; every id archived here is either already described there or
> has a one-line pointer in its §2b. `test/doc-clobber-guard.test.mjs` accepts a removed backlog id only when the row
> appears verbatim in a `docs/history/PLANNED-BACKLOG_shipped_*.md` file like this one.
> Earlier closed rows: [`PLANNED-BACKLOG_closed_2026-09.md`](PLANNED-BACKLOG_closed_2026-09.md) (CONSOLIDATE1) and
> CURRENT-STATE §2a (CONSOLIDATE2).

## P0 — Verify, don't build (open loops on work already shipped)

| # | Item | State | Source |
|---|---|---|---|
| EDGE-GATES1 | 🔴 **The other 18 Dialysis_DB edge functions with `verify_jwt:false` have no reviewed gate — `prompts/EDGE-GATES1-the-other-eight-ungated-edge-functions.md` (Cowork, 2026-09-17, from PL-1).** Measure each from the deployed body (writes? credential check? routes? callers in 24 h? repo/deploy drift?), gate every writer log-only with the shared helper (`<FN>_AUTH_MODE`, `DENY-WOULD` with UA/IP class), deploy log-mode builds, caller inventory per function. No `enforce`; `ai-copilot`/`salesforce-enrichment` untouched (Q1/Q2).   ✅ **DONE 2026-09-17 (PR #2564), verified by Cowork on Dialysis_DB `list_edge_functions`:** eight functions log-gated with the shared helper, `verify_jwt:false` unchanged, `<FN>_AUTH_MODE=log` default — `context-broker` v21, `template-service` v19, `intake-receiver` v20, `calendar-ics-sync` v21, `calendar-caldav-sync` v26, `calendar-caldav-push` v24, `calendar-capture` v15, `data-query` v44 (non-GET only); eight left alone because they already had enforced auth (`lead-ingest`, `intake-salesforce`, `intake-salesforce-files`, `sf-promotion-worker`, `npi-registry-sync`, `w41-corpus-export`, `w43-sf-link-export`, `w44-retrain-tick`). Caller inventory `docs/architecture/flows/edge-gates1-caller-inventory.md`; 70 tests. First live gate line: `DENY-WOULD` on `/calendar-ics-sync`, caller class unknown (PL-15). Residue → `EDGE-GATES1-b`. | ✅ 8 gated log-only | PARKING-LOT PL-1 2026-09-17 |
| VERCEL-LIVE1 | 🔴 **Vercel is not retired — it is serving a stale build, and it is the app Scott opens every day (found round 29, 2026-09-17, from Scott's Vercel log export + the Dialysis_DB edge log).** `CLAUDE.md` says *"PRODUCTION RUNS ON RAILWAY (Vercel retired 2026-07-20)"*; measured: `life-command-center-nine.vercel.app` answers in production (deployment `dpl_8CnznECT2siyQix7G3i4VwQkYDvR`, branch main, functions `/api/admin`, `/api/queue`, `/api/operations`, `/api/sync` in iad1) and its client code is older than `main` (issues `bd_worklist&limit=5`, added 2026-09-03 and since removed; lacks `home_three_lanes`; calls `ai-copilot` from the browser). Consequences: (1) everything shipped to Railway since that build — HOME2, PRI2-on, the DIA1 fixes, RECON1's effects on client-rendered views — may not be what Scott sees, so **SB notes written from that window may describe old code**; (2) a second, unowned backend with its own env vars and secrets is writing to the same databases (`/api/admin?_route=client-error` POSTs, flag writes); (3) it is the unnamed `/chat` caller (PL-14) and a browser caller the enforce flip would break; (4) `priority-queue` band-counts query aborts there (8 s). **Decision for Scott:** move to the Railway URL (`https://tranquil-delight-production-633f.up.railway.app`), reinstall the desktop app from it, then delete or pause the Vercel project. **Before deleting:** list Vercel's env vars against Railway's (anything only on Vercel is lost), check OAuth/redirect URLs and any Power Automate or extension URL still pointing at `*.vercel.app` (135 references to the `-nine` host remain in the repo — mostly docs; an `INVENTORY`-style sweep of live callers is the CC half). Options not chosen: redeploying Vercel to current `main` (keeps two productions). **Round 31:** Scott is on Railway — Home loads signed-in at `tranquil-delight-production-633f.up.railway.app` in a fresh Chrome and in the reinstalled desktop app (Edge); `/version` = `main`. Remaining: Vercel env-var names vs Railway, the live-caller sweep, then delete/pause the Vercel project; the `browser scott` `DENY-WOULD` lines should stop now — Cowork checks the log. **Round 32:** no `browser scott` `DENY-WOULD` since 21:08 UTC 09-17 — Scott's Railway sessions read through the server as expected. Still open: env-var names, live-caller sweep, teardown. **Round 33 — env-var names received (five screenshots) and audited against what `main` reads:** `docs/history/RUNBOOK_vercel_teardown_2026-09-18_DONE.md § Step 3b` — column A (must exist on Railway, ~45 names), column B (not read by current code, safe to lose), column C (unaudited: the Jun 11 → Apr 20 block the screenshots skipped, and two `RE…EY` names). Delete order: Scott confirms column A on both Railway services → scrolls the gap → deletes the project. **Correction (round 33):** this row duplicates a thread the repo already had — **J13 / J13-preflight ✅ / J13-teardown (checklist Q3) / J13a-guard** — the retired-host problem, its live-caller preflight (2026-09-09) and the teardown runbook `docs/history/RUNBOOK_vercel_teardown_2026-09-18_DONE.md`. Round 29's "Vercel was never retired" overstated the surprise: the repo knew the host still answered and had a runbook waiting on Scott. **What was new:** (a) the deployment is not the frozen July build — Vercel still builds from `main` (Git integration connected; client from early/mid September); (b) it was Scott's daily window; (c) it is the `/chat` and `browser scott` caller. VERCEL-LIVE1 therefore **folds into J13-teardown**: env audit = runbook Step 3b; the two rows close together. `test/retired-identifiers-guard.test.mjs` caught the separate audit file naming the host (a live doc outside the allowlist) — correct behaviour; the audit now lives in the allowlisted runbook. ✅ **Closed with J13-teardown 2026-09-18.** | ✅ closed 2026-09-18 | Vercel log export + edge log 2026-09-17 |
| ✅ **HCRIS-START-RUN-HEADER-BUG** | **Found 2026-09-16 during `HCRIS-TIMEOUT-4`'s triage — repo-wide, not CMS/HCRIS-specific, and the actual reason `HCRIS-TIMEOUT-3`'s fix produced zero output.** `ingestion_tracker.start_run()` calls `core_utils.safe_execute()` with a bare zero-arg lambda that already has `.execute()` baked in; `safe_execute()` only overrides the Supabase client's global `Prefer: return=minimal` default header to `return=representation` when it's handed a live query-builder object, which a pre-built closure never provides. Result: every insert genuinely succeeds (HTTP 201) but comes back with an empty body, `start_run()` reads `result.ok and result.data` as false, logs an error, and returns `None` — deterministically, on attempt 1, no retry budget can ever fix it. **This is why `ingestion_tracker.notes` has been blank on every `cms_medicare_clinics` row since at least 2026-08-31** (independently confirmed live by Cowork, 2+ weeks before this arc even started) **and why `HCRIS-TIMEOUT-3`'s heartbeat/retry fix changed nothing** — it hardened the wrong layer. **Same call site also used by `acquire_ingestion_lock()`**, which is why every ingestion-lock row has been orphaned every run. **Fixed 2026-09-16, `HCRIS-TIMEOUT-5`** (`start_run()` now passes a real query-builder object into `safe_execute()` with `return_representation=True`). `Dialysis` PR #7413 (commit `226f7e3`). **👤 2026-09-16 — `HCRIS-TIMEOUT-6`'s live evidence closes this out**: the fresh run made it past `aux_cms_tables` entirely into a much later step (`patient_counts`/`propagate_financials`) and acquired/held a lock row for 4h18m — behavior that's structurally impossible under the old header bug, since a discarded run id would have orphaned the lock immediately rather than let it run for hours. The one lingering symptom (`notes` still `'{}'`) is now attributed to a *different*, newly-found bug (`HCRIS-PROPAGATE-FINANCIALS-TIMEOUT-SWALLOWED`, below) rather than this one — moved to ✅. | ✅ fixed+deployed, live-confirmed working | `HCRIS-TIMEOUT-4`, Cowork 2026-09-16; fixed+deployed `HCRIS-TIMEOUT-5`, 2026-09-16; confirmed working by `HCRIS-TIMEOUT-6`'s live evidence same week **👤 2026-09-21 (Cowork): superseded by the compare-before-write finding on `HCRIS-TIMEOUT`'s row above** — the `StepTimeout` swallow this row tracked was real and is fixed, but `propagate_financials_to_properties()`'s unconditional per-row `.update()` (no compare-before-write at all) is the actual reason the step still runs 16-24h every day; see `HCRIS-TIMEOUT`'s 2026-09-21 update. |
| ✅ **HCRIS-AUX-CMS-TIMEOUT-SWALLOWED** | **Found 2026-09-16 during `HCRIS-TIMEOUT-4`'s triage — the actual reason the pipeline never reached HCRIS at all.** `ingest_cms_aux_tables()` (step 3 of ~15 in `run_cms_ingestion`, well before `hcris_cost_reports` at step ~8) wrapped each CSV row's sub-ingestors in their own per-row `except Exception:`, swallowing the step's own `SIGALRM`-based `StepTimeout` (900s) whenever it fired mid-row — `signal.alarm()` is one-shot, so nothing re-armed the guard for the rest of the step, and the pipeline never advanced past step 3. `facility_cost_reports` freezing at 2026-03-16 was a direct, mechanical consequence. **Fixed 2026-09-16, `HCRIS-TIMEOUT-5`** (`TimeoutError` re-raised before the per-row handler; 60s statement timeout + keepalives added to the direct `psycopg` calls). `Dialysis` PR #7413 (commit `226f7e3`). **👤 2026-09-16 — `HCRIS-TIMEOUT-6`'s live evidence confirms this fix genuinely works**: the fresh post-fix run advanced clean past `aux_cms_tables` and spent 4h18m deep in a *later* step (`patient_counts`/`propagate_financials`) — something no pre-fix run ever did. The loop-vs-hang question this row's fix round left open is answered too: it was neither a hang nor an oblivious loop specific to this step — `aux_cms_tables` itself completed; the *next* step (below) had the identical swallowed-timeout bug, independently. Moved to ✅. | ✅ fixed+deployed, live-confirmed working | `HCRIS-TIMEOUT-4`, Cowork 2026-09-16; fixed+deployed `HCRIS-TIMEOUT-5`, 2026-09-16; confirmed working by `HCRIS-TIMEOUT-6`'s live evidence same week |

## P1 — In flight / next up

| # | Item | State | Source |
|---|---|---|---|
| TIER0-flag-arity | ✅ **SHIPPED 2026-09-12 (Cowork) — DOC-CONTRA follow-up found a live production bug, not just a stale doc.** Re-verifying `tier0-owner-contact-system.md` against reality (CONSOLIDATE2 flagged it as not re-checked against its own corrections) found `TIER0_AUTO_ATTACH` has been silently non-functional for **16 straight days**: `tier0-auto-attach-tick.js:208` called the shared `flagEnabled(envName, flagRow)` helper with only ONE argument, so it always fell through to `false` regardless of `feature_flags_registry.state='on'` (set 2026-08-28). The doc had wrongly marked this **RESOLVED 2026-08-28** — the registry-vs-env deadlock diagnosis that day was correct, but the follow-up verification ("the next 06:55 run is the real test") was never actually done; the run log shows `flag_enabled=false, skipped_reason='flag_off'` on all 17 runs since. Fixed the one-line call-site bug (every other of 7 callers in the repo already used the correct two-arg form), added `test/tier0-auto-attach-flag-arity.test.mjs` as a source guard, and corrected the doc's own live-state table and history. | ✅ | tier0-owner-contact-system.md §5 trap 14; re-measured 2026-09-12 (Cowork) |
| N15 | ✅ **CLOSED 2026-09-15 — Scott decided and it shipped.** Scott's answer, verbatim: "These are members of a specific group? Usually means that there is some vested interest in the space mapped by the name. Some may be brokers, some may be a new fund exploring the space, but the vast majority will be owners or prior owners and the membership is evidence that some prior research has concluded that in our team's BD history and just because the LCC doesn't yet have that connection mapped, does not mean that its not out there undiscovered." Re-measured live 2026-09-15 (population moves; total email-orphan count is now 5,672, not 5,193): the SF-campaign gate is unchanged at **1,475** — `lcc_sf_list_membership` turns out to be a frozen 2026-07-16→07-21 snapshot, not a live-syncing producer (worth its own follow-up, not fixed here). Sampled before minting: side distribution seller 1,030 / unknown 416 / buyer 88, consistent with Scott's "vast majority will be owners" read; 15 random rows spot-checked, all real BD-relevant names/companies/campaigns (`VCA Animal Hospital Owners`, `DMR Urgent Care Owners`, `SAB GSA Prospects`, `GSA Buyer`). Checked mint-collision risk the way P197 did: 0 of 1,475 already resolve to a hub row under `sf_contact_id`. **Shipped `lcc_n15_mint_sf_campaign_hub_rows(dry_run, batch_tag)`** (dry-run-safe, reversible via `lcc_n15_unmint_sf_campaign_hub_rows`), reusing the exact `lcc_tier0_company_confirms_domain` gate P197 built so `company_name` is **never fabricated** — only written when the person's own email domain corroborates it. Ran live: **1,475 hub rows created, 0 failures**; only 228 (15%) got a domain-confirmed `company_name`, the other 1,247 correctly render "Not on file" rather than guess. Fully logged to `lcc_n15_sf_campaign_hub_mint_log`, batch `n15_sf_campaign_2026-09-15`. Migration: `supabase/migrations/20261102170000_lcc_n15_sf_campaign_hub_mint.sql`. This does NOT touch the remaining ~4,197 email orphans outside the SF-campaign gate, and does not by itself clear the Tier 0 blockage (P197 already fixed that with a read-time resolver) — it is Scott's stated connectivity-coverage goal, not a Tier 0 fix. | ✅ | Scott 2026-09-15; `docs/audits/P197_TIER0_EMPLOYER_RESOLVER_2026-08-27.md` §3-4 |
| C2e-T2b | ✅ **SHIPPED 2026-09-15 — Scott decided and it ran.** Scott's answer (decision #5 of six): "Yes, again, the objective is accurate coverage of all properties in our target submarket. We want to get there as fast and efficiently as possible." Re-measured live before running (population moves): 2,255 properties / 2,068 owners in `v_lcc_c2e_asset_mint_plan` (805 under $50k · 712 at $50-100k · 537 rent-unknown — essentially unchanged composition from the 2,241/2,054 sizing). Dry-ran `lcc_mint_gov_asset_entities` first (would_mint 2,255, skipped 0), matched the live run exactly: **2,255 minted, 0 skipped**, batch `t2b_gov_2026-09-15`. Drove the evidence ingest in the same pass as required: `lcc_ingest_domain_owner_evidence(false, 3000, 't2b_evidence_2026-09-15')` -> **evidence_written 2,262, assets_resolved 2,255, ambiguous_logged 1** -- the 7-row gap is the same guard as T2a: all 7 residual `eligible` rows are brokerages (`Stan Johnson Co` x4, `NAI Pfefferle`, `Bradford Allen Realty Services`, `SVN®`), correctly filtered by `lcc_reconcile_property_owner`'s scoring CTE, not a defect. `v_lcc_c2e_asset_mint_plan` now reads **0** -- both tranches fully applied. gov asset anchors now 10,255; `lcc_property_owner` 10,906 rows. `v_duplicate_candidates` and the Tier 0 open lane stayed in expected range (no blowup) -- full detail in `docs/architecture/ownership-truth-pipeline-state.md` decision #5 and `docs/claude-code/STATUS.md` 2026-09-15. | ✅ | Scott 2026-09-15; C2e_T2a_TRANCHE_TWO_STEP_ONE_MINT_2026-08-28.md §6 |
| C2k | 👤 **The domain-truth gate — same decision as `supersession-tie-lane-2026-08.md` §4, now sized at 1,036 assets (Cowork, 2026-09-15).** `v_lcc_domain_owner_candidates`'s `unresolved` CTE (`NOT EXISTS lcc_property_owner … owner_entity_id IS NOT NULL`) means the gov/dia `true_owner` — weight **5.0**, the feeder's highest — is entered as evidence **only on assets nobody has resolved yet**. On the 92 C2g assets: 0 `domain_true_owner` evidence rows. R6's "per-row domain truth OUTRANKS name patterns" is implemented as a gap-filler. 📊 Resolved assets whose domain `true_owner` maps to a different LCC entity AND would pass the view's own eligibility: **936 gov + 100 dia** (8,434 gov agree). Evidence quality is uneven: gov-wide, of 3,264 `true_owner`≠`recorded_owner` properties, 978 have an SOS manager on the SPE and **858 of those name the true_owner**; ~2,300 are unattested, not contradicted. **Options (Scott's call, none built):** (a) keep the SPE in `lcc_property_owner` and add the sponsor as a first-class edge — needs a relationship type LCC lacks; the 43 SOS-attested pairs are the seed; (b) lift the gate and let 5.0 win — 1,036 assets change meaning at once, brokers get the sponsor, unattested rows inherit gov quality wholesale; ⛔ (c) NOT a bulk write to `lcc_ownership_sponsor_family` — it is `(sponsor, token)`-keyed and cannot express a per-entity manager link. One-line view change either way; the cost is semantics. Two side-findings, both run down 2026-09-16 (Cowork): the `2023-10-01` sentinel is a real producer defect → **GOVDEED4** (gov-owned; 676 low-confidence dates, 493 properties). The 2 "seller" resolutions are **not a class**: `Scannell`'s evidence is a real 2017 `rel_purchase` and `Park De Ville Trio`'s a 2021 lease-diff transition; the later deeds (Nome 2021-09, SAM951 2021-01) simply **never became LCC evidence** — there is no deed-grantee feeder; deeds reach LCC only through gov's `true_owner`, which is behind this row's gate. Same decision, one more reason. ⭐ Also seen while reading `v_lcc_owner_supersession_candidates`: it carries the **same `unresolved`-only gate**, so **whichever feeder touches an asset first wins forever** — the domain feeder tiers `domain_true_owner` at 2 above `rel_purchase` at 3, but only ever competes on assets nothing else has claimed. Precondition: none.   ✅ **DECIDED 2026-09-16 (Scott): attested-only widening** → `prompts/C2k-attested-only-domain-truth-widening.md`. Gov step 1 (must land first): `v_property_owner_facts_portfolio` exposes `true_owner_attested` (SOS/SAM manager on the SPE names the true_owner, C2g §3 rule; expect ≈858). LCC: facts sync carries it; the view's `unresolved` CTE becomes `eligible = unresolved ∪ resolved-but-attested-and-below-tier` (never `manual`), `candidate_kind` fill / supersede; ingest ledgers prior owner + `lcc_c2k_unsupersede(batch)`; dry run with a 20-row hand read before any real run; the 43 A-class pairs must flip and the 16 no-evidence pairs must NOT move (positive control). Same shape offered to `v_lcc_owner_supersession_candidates`. Not chosen: full lift (1,036 at once), sponsor-as-edge (future work). (Note: the 493 in the side-finding sentence above was join rows; 147 distinct — see GOVDEED4.)  ✅ **BUILT + RUN LIVE 2026-09-16 — gov step 1 (`c2k_true_owner_attestation`, 856 attested of 13,696) then LCC PR #2506** (`supabase/migrations/20260916120000_lcc_c2k_attested_supersession.sql`). Facts mirror carries `true_owner_attested/_by` (856 mirrored; dia 17,355 rows mirrored, 0 attested — no registry data); `v_lcc_domain_owner_candidates` + `v_lcc_owner_supersession_candidates` admit `supersede` (resolved below-tier, attested, never `manual`); ingest ledgers prior owner; `lcc_c2k_unsupersede(batch)` re-runs the real reconcile. Batch `c2k_20260916`: dry **234 (13 fill / 221 supersede)**, 20-row hand read; real run 234 evidence, 224 resolved, **218 superseded**; **40/43** C2g A-class pairs → sponsor (the other 3 already repointed at the gov true_owner level since 09-15); **16/16** no-evidence pairs untouched; unsupersede round-trip **218/218 → 0 mismatches** both directions. ⚠️ CI caught a real hole: `lcc_c2k_unsupersede` was created with the default PUBLIC/anon grant — callable via unauthenticated RPC — fixed with the SEC1 revoke+assert stanza before merge (`1cf76d47`). Not done, filed: pytest/plpgsql tests for the gate; sponsor-as-edge stays future work. | ✅ live | Cowork 2026-09-16; decided 2026-09-16; built+run 2026-09-16 |

## P0b — ⭐ BD PIPELINE FUNNEL (re-audit 2026-08-28, Scott's framing: property → owner → chain → developer)

| # | Item | State | Notes |
|---|---|---|---|
| **B1b** | **`trace_ownership_to_developer` — graded 2026-09-11, floor NOT lifted.** Cron 145 (`developer-chain-resolve-tick`, gov-only, every 6h) is a mature, correctly-authenticated, already-fully-automated consumer — but `lcc_chain_lane_has_auto_consumer()` is hardcoded to `gov + establish_ownership_history` only, so it never recognized this lane and `lcc_b1_reopen_below_floor` is gated off for it. Replicated cron 145's exact classifier (`classifyDeveloperOrigin`) in SQL against the live 514 gov below-floor properties (983 total; dia's 469 have no automated consumer at all — `developer-chain-resolve.js` hard-returns for any domain ≠ gov): **only 7/514 (1.4%) resolve** (all 7 via Tier A `bts_origin`; 0 via Tier B `developer_keyword`) — vs 465 `ambiguous_generic_org`/`origin_is_person`, 36 `origin_equals_current`, 6 `no_chain`. This confirms B1a's own caution: lifting the floor here would not repeat A2's 89% win, it would just dump 507 unresolvable properties into the queue. **Recommendation: do not lift the floor for this lane.** The 7 bts_origin resolves are small enough to hand-verify and write directly if wanted, rather than reopening the whole 514. **The 'Do B5 first' note above was stale — B5 shipped 2026-08-28** (`docs/audits/B5_GOV_SELLER_EXIT_FEEDER_2026-08-28.md`); this grading was run against B5's already-updated `ownership_history`. | ✅ graded · declined | B1 writeup · graded 2026-09-11 |
| ✅ **BR3** | 🟡 **Confirmed live 2026-09-12 (Cowork) — the `&` vs `;` distinction still holds, folded into BR1's repair prompt rather than a separate build.** The `;` composite carries at least three different facts and is undocumented — 344 of 2,425 broker rows and **778 sales rows**. `Acre Advisors; Reid` is firm;agent · `Adrian Mendoza; Sean Sharko; Austin Weisenbeck` is a three-agent team · `Avison Young; Barnes` and `AY; Barnes` are the same firm spelled two ways. ⚠️ **49 rows carry `&` and no `;` and are mostly REAL firm names** (`Lee & Associates`) — the P158a hazard; **an `&` is part of a name, not a separator.** **A co-listing is a real fact: record it as a list, never collapse to one half.** ✅ **SHIPPED alongside BR1, 2026-09-16** — the `;` vs `&` distinction held exactly as this row predicted; the repair used it directly (agent tokens split only on whitespace-guarded `&`/`,` and `,` never a bare `&`, so `Lee & Associates`-shaped real firm names are never touched by the splitter).| ✅ | §3 |
| **BR4** | **143 duplicate-name groups in `brokers`** (2,280 distinct names across 2,425 rows), plus at least one non-broker entity in the broker table. Dedupe **after** BR1/BR2, not before — resolving the firm link is what makes a duplicate visible or explains it away. **UNBLOCKED 2026-09-16 — BR1 shipped.** `brokers.broker_company_id` now resolves for 366/2,549 rows via the repaired registry; BR4's dedupe can now run against a firm-linked population instead of raw free text.   🟢 **PROMPTED 2026-09-17 — `prompts/BR4-broker-dedupe-against-the-firm-linked-population.md`:** group table first (same name/same firm vs different firms vs one-linked), merge true duplicates only with every FK repointed and ledgered, resolve the 661 queued firm strings exact-or-alias and mint new firms only with evidence, route non-broker rows to review.   ✅ **APPLIED LIVE + MERGED 2026-09-17 (PR #2558), verified by Cowork on Dialysis_DB.** 146 duplicate-name groups: **3 true duplicates merged** (same name + same firm; 13 FK constraints on `broker_id` across 11 tables repointed; `dia_br4_broker_merge_log`, reversible); 120 one-linked-one-blank + 20 both-blank left (a sibling fill is an identity guess); 3 different-firm left. **52 firms minted** from the 661 queued strings (≥3 brokers sharing the token AND ≥2 sharing an email domain — Matthews, Sands, Northmarq, Newmark, CBRE, SRS…; `dia_br4_firm_mint_evidence`); `broker_company_id` **366 → 641 of 2,566 (25.0%)**, `broker_companies` 127. 123 firm/operator-shaped rows in `brokers` flagged, untouched; 468 review rows open. 19 tests; 6,543/0. Residue → `BR4-b`. | ✅ live (25.0% linked) | §4 |

## P0d — 🏛️ DATA COHERENCE: make the connections assertable, across every database

| # | Item | State | Notes |
|---|---|---|---|
| CONSOLIDATE3 | ✅ **SHIPPED 2026-09-12 (Cowork).** (1) Headroom fixed: budget raised to 3,000 with an 80%-mark soft warning and a documented 60%-of-budget archive target. (2) Entry-length convention adopted. (3) Table-placement guard shipped, and a real violation it would have caught (an entry landed above the table from a same-day merge) was found and fixed. (4) **DOC-CONTRA 3 of 3 done:** the FINAL STATE box fixed; `tier0-owner-contact-system.md` re-verified (surfaced a live bug, `TIER0-flag-arity`, not just a stale claim); and the FRED/CMS thread re-measured live — FRED is genuinely fine, but **CMS ingestion's "repaired" claim does not hold** (`cms_ingestion` failing 34/36 runs in 30d, `medicare_clinics` frozen at 2.9% for 12 days) — corrected in `DATA-PROCESS-AUDIT-HANDOFF.md` and appended to `B6d-cms-restart`, cross-referenced with `PRI6`. 🚨 **CORRECTION 2026-09-14 (Cowork): items 1 and 3 were NEVER MERGED — this row's ✅ has been false for two days.** `main` carried `LINE_BUDGET = 2500`, no 80% warning and no table-placement guard; all of it sat on the unmerged branch `docs/consolidate3-headroom-and-table-fix-2026-09-12`, found only because BRANCH1 rescued that branch. **Now genuinely shipped** (budget 3,000 + soft warning + placement guard, 10/10 green). ⭐ **And the guard earned itself on contact:** it failed against `main` because `## Open threads` had sunk to line **1,781**. Root cause was a **contradiction inside STATUS.md itself** — the convention block said "new entries go DIRECTLY BELOW this block" while the table's header said "below the `---`"; the header won and every prepend buried the table further, **Cowork's own ~8 entries on 2026-09-14 being most of the drift**. Table restored to line 25 and the convention rewritten so the two agree. Third "claimed shipped, actually isn't" of the day → another argument for **XB2**.| ✅ genuinely shipped 2026-09-14 (was falsely ✅ since 09-12) | Cowork 2026-09-12 |

| # | Item | State | Source |
|---|---|---|---|
| ID2b-caps-2 | ✅ **SHIPPED + VERIFIED LIVE 2026-09-12.** Root cause: `sf_comp_staging` (Team Briggs' own Salesforce-staged closed comps) has no `properties` join, so ID2b-caps' passthrough could only ever emit explicit NULLs for that arm — 196 `DaVita Dialysis` + 179 `Fresenius Medical Care` rows (exact matches of already-registered aliases → operator_id 4/5) were minting a second, text-keyed band under the identical canonical label the id-keyed band already carried. **Fix at the source of record (option b, not a query-time patch):** `sf_comp_staging` gained its own `operator_id` column, resolved through the SAME single ID2a resolver (`dia_resolve_operator`) every other caller uses — a fill-blanks `BEFORE INSERT/UPDATE OF tenant` trigger (deliberately lighter than the `properties` hard-block guard: it never raises, because `sf_comp_staging` is fed by the external Salesforce sync pipeline this repo does not control) plus a dry-run-default backfill function mirroring `dia_id2a_backfill_property_operator_ids` exactly. Unresolvable tenants route to the SAME shared `dia_operator_write_review` lane, never guessed. Migrations `20260912140000_dia_id2bcaps2_sf_comp_staging_operator_id.sql` (column + trigger + backfill) and `20260912150000_dia_id2bcaps2_rpc_query_comps_sf_operator_id.sql` (SF arm now resolves `operator_id`/`operator_canonical` from `sf_comp_staging.operator_id → dia_operator_survivor → operators.name`, identically to the sale/listing arms — every other key on every arm byte-identical), both applied live to `zqzrriwuavgrquhisnoa`. **Live backfill: 406 candidates, 400 auto-applied (matched), 6 routed to review** (`Reliant Renal Care`, `Dialysis Care Center`, `Georgia Nephrology`, `KidneySpa`, `Physicians Choice Dialysis` — needs_review; `Vanderbilt University` — non_dialysis). **Second guard shipped: a structural duplicate-display-label invariant** in `planOperatorCapRateBands()` — two DIFFERENT resolved `operator_id` groups may never render under one canonical label (scoped to id-keyed groups only; the documented ID2a coverage-gap fallback, where an unresolved property legitimately shares a raw-text label with a resolved sibling, is explicitly exempted and re-tested). On collision it logs loudly, keeps the larger-n band, and routes the loser through the existing `retireStaleFact()` supersede mechanism — never two live facts, never a silent drop. **Verified live on the tick's own rolling TTM window (2026-09-12, `zqzrriwuavgrquhisnoa`):** exactly THREE resolved bands — `id:4` DaVita n=58, `id:5` Fresenius Medical Care n=48, `id:73` US Renal Care n=7 — plus small residual text fragments (n≤1, all below `MIN_N_CAP_BAND=5`, one of them a genuine ID2a property-coverage-gap case on the `dialysis_db` sale arm, unrelated to this fix) that never clear the floor and never ship. Guard `test/id2bcaps2-sf-operator-resolution.test.mjs` (13 tests). Full suite 6,057 pass / 0 fail / 6 skipped. `MARKET_BRIEF_PSQL` NOT flipped. Writeup: addendum in `docs/audits/ID2b_caps_RPC_QUERY_COMPS_OPERATOR_ID_2026-09-12.md`. | ✅ shipped | Cowork 2026-09-12 | ✅ **SHIPPED (PR #2374), merged, deployed (`c91d5dabe932`) and GATE PASSED — Cowork live dry run 2026-09-12.** Chose the source-of-record path: a first-class `sf_comp_staging.operator_id`, resolved through `dia_operator_aliases` by a fill-blanks, non-raising trigger (deliberately lighter than the hard guard on `properties`/`leases`, since an external Salesforce sync feeds that table). Backfill: dry run predicted 406/400/6, applied exactly — **400 auto-resolved, 6 to review**. **Live bands now: `:5` Fresenius Medical Care n=72 · `:4` DaVita n=78 · `:73` US Renal Care n=8 — three clean id-keyed bands, no text-keyed fragments, no duplicate labels; whole-market n=169 unmoved; candidates 17 → 15.** The n differs slightly from the 75/78 predicted on 09-11 because the TTM window rolled and the SF-staged arm now contributes (SF TTM rows: DaVita Dialysis 13 · Fresenius Medical Care 10 · US Renal Care 2) — arithmetic, not drift. ⚠️ **Declared deviation:** the duplicate-label guard was scoped to *two different resolved operator ids sharing a label* rather than the blanket rule the prompt asked for, because the blanket version broke an intentionally-tested fallback — accepted, and recorded here so nobody re-derives it. | ✅ | Cowork 2026-09-12 |
| ID3a-dup | *(duplicate row, merge artifact — the pre-ship ID3a row plus Cowork's 2026-09-12 contamination-warning addendum both survived the ID3a build's merge alongside the real, shipped row above. Content fully superseded, nothing lost: the contamination finding is now §"THE ONE PREMISE THAT HAD TO BE CORRECTED" in the ✅ SHIPPED row above, which independently confirmed and extended it — NAVY, plus ICE/VA/USDA/HHS state-body folding this row never found. Kept as a one-line pointer rather than deleted, per this doc's own history-preservation convention.)* | ✅ superseded | ID3a |
| ID3b | ✅ **SHIPPED 2026-09-12 (Cowork).** Re-measured live immediately before building (numbers had NOT moved from this row's own prior-day measurement): gov `recorded_owners` fuzzy-variant population 1,380 groups / 2,870 rows, gov `true_owners` 227 groups / 461 rows (down from the prior day's 237/483 as other identity work continued to chip at it). Two new tick functions, `gov_owner_variant_merge_tick(p_dry_run)` and `gov_true_owner_variant_merge_tick(p_dry_run)`, group unmerged rows by `gov_owner_strict_core` (core length ≥4), pick the highest-property-count member as survivor, and guard every group through `gov_owner_name_is_brokerage`, `is_generic_gov_owner`, and a new bank/lender/lienholder regex (no reusable SQL guard existed for this — `lenderNamePasses` in `sidebar-pipeline.js` was checked and rejected as the wrong model, since it deliberately does NOT exclude banks). A flagged group is routed WHOLE to `entity_match_candidates`/`gov_owner_merge_review_log`, never auto-merged. Guard-clean groups merge via the EXISTING `apply_owner_merge`/`apply_true_owner_merge` primitives unmodified — both already accepted arbitrary caller-supplied survivor/loser pairs, no extension needed. Dry-run matched the live run exactly on both tables. **Shipped, live results:** `recorded_owners` 1,380 groups seen, **1,466 merged**, **24 routed to review** (22 groups — hand-spot-checked: correctly caught `CBRE`/`CBRE, Inc.`, four `U.S. Bank National Association` casings, `JPMorgan Chase Bank N.A.`, `TD Bank N.A.`, `Umpqua Bank`, `SunTrust`, `World Bank`, `Chicago Title Land Trust Co.`, three brokerage names — `Colliers`, `Northmarq`, `Marcus & Millichap` — riding inside a single JV description string, and a handful of exact-name city/agency duplicates the guard also happens to catch). `true_owners` 227 groups seen, **232 merged**, **2 routed to review** (the same TD Bank / U.S. Bank pair). **Parity confirmed bit-for-bit**: `total_properties` (20,509), `properties_with_recorded_owner` (9,327) and `properties_with_true_owner` (9,848) were UNCHANGED before/after both live runs — only unmerged-owner-row counts dropped, by exactly the merged-loser counts (17,143→15,677 recorded_owners; 15,061→14,829 true_owners). No property silently moved to a different real owner. Not built: no consumer was repointed (none needed — the merge primitives already repoint every FK consumer); the 26 review-lane rows are left for a human, per doctrine ("never blind-merge, route to review on any hit"); dia untouched (already at zero exact AND fuzzy dups, confirmed, no build needed there). Migration: committed in `government-lease` (the owning repo per ID3a-d — this file was initially committed to life-command-center's now-retired `supabase/migrations/government/` by mistake, caught by `test/gov-migrations-directory-retired.test.mjs`'s CI guard, and moved to `government-lease/sql/20261013_gov_id3b_owner_variant_merge.sql` where it belongs — the live database change itself was correct and unaffected). | ✅ | ID0; RO2a | Shipped by Cowork, 2026-09-12.
| ID3d | 🟡 **MEASURED live 2026-09-12 (Cowork), prompt written: `docs/claude-code/prompts/ID3d-guarantor-registry-wiring.md`.** dia `leases.guarantor`: 713/12,833 leases, **169 distinct strings** (matches ID0, not stale). `leases.guarantor_id` (integer FK, already exists): **1/12,833 populated.** A dedicated `guarantors` registry table already exists with exactly the right shape (`parent_company_id` for subsidiary→parent linking) — **it carries 1 row.** **Trap confirmed live, sharper than ID3a's: the obvious shortcut is measured to be actively WRONG, not just incomplete.** ID2a's `dia_operator_aliases` would resolve 17/169 strings (189/713 rows) if naively joined — but those 17 include `Total Renal Care, Inc.`, `DVA Healthcare Renal Care, Inc.`, `DVA Renal Healthcare, Inc.`, `Renal Treatment Centers-Southeast, L.P.`, all resolving to `operator_id=4` "DaVita" as pure aliases — correct for "who operates the clinic," wrong for "who legally signed the guaranty." Blindly joining would silently erase the parent/subsidiary legal distinction on exactly the credit-critical column this row protects — build a NEW `dia_guarantor_aliases` table, never extend or join the operator one. **Second finding: 114/713 rows (16%) are generic placeholders** (`Corporate` 74, `Corporate Guarantee` 33, etc.) — not unresolved names, route to their own sentinel, never force-matched to a company.   ✅ **APPLIED LIVE 2026-09-16 (Claude Code, via Supabase MCP, no PR first) — verified by Cowork on Dialysis_DB:** `leases.guarantor_id` **1 → 628 of 715** text-bearing leases (88%), **87** left to review (never force-matched); `guarantors` 18 rows — Total Renal Care, DVA Healthcare Renal Care, DVA Renal Healthcare, 3 Renal Treatment Centers rows → `parent_company_id` DaVita; National Medical Care, RCG Mississippi, 6 Bio-Medical Applications → Fresenius; a `generic_unspecified` sentinel takes "Corporate"/"Corporate Guarantee" (121 leases) and never matches a company; `dia_guarantor_aliases` (58) is a **separate** resolver, deliberately not joined to `dia_operator_aliases` (whose DaVita-subsidiary aliases would erase the legal-signer distinction); a real FK `fk_leases_guarantor_id` added (the column was described as an FK and was not); fill-blanks trigger `trg_dia_leases_guarantor_resolve_biu`; parity view `v_dia_guarantor_backfill_parity`. ⚠️ The migration + 9 tests went to the **`Dialysis` repo (PR #7415, merged)** — wrong repo under the doctrine re-stated the same day → **`ID3d-reconcile`**. | ✅ live; record in this repo (ID3d-reconcile) | ID0; built 2026-09-16 | **FOURTH by Scott 2026-09-12.**
| ID3d-reconcile | 🔴 **The ID3d migration lives in `Dialysis` (PR #7415); Dialysis_DB schema lands here — `prompts/ID3d-reconcile-the-guarantor-migration-is-in-the-wrong-repo.md` (Cowork, 2026-09-16).** Same incident class as DEED1-reconcile-2, one day later: port `20260916120000_dia_id3d_guarantor_registry_resolver.sql` byte-identical into `supabase/migrations/dialysis/` (pin the live function/trigger/view hashes first; emit-then-hash, never transcribe), port the 9 structural tests to `node --test`, open the removal PR on `Dialysis`, list the 87 unresolved guarantor strings by count (the ID3d-b review lane), and add one sentence to the doctrine table so a CC session with Supabase MCP applies from here or not at all. Nothing applied; hashes unchanged at the end.   ✅ **DONE 2026-09-16 (LCC PR #2539 merged; Dialysis PR #7416 opened — merge is checklist D3).** Three live hashes pinned and unchanged (`dia_resolve_guarantor`, `dia_normalize_guarantor_text`, the trigger); `20260916120000_dia_id3d_guarantor_registry_resolver.sql` ported byte-identical with an already-live header carrying the hashes; 9 tests → `test/id3d-guarantor-registry-resolver.test.mjs`; `CLAUDE.md` doctrine row extended to CC/Cowork sessions with Supabase MCP. Nothing applied.   D3 reported merged by Scott 2026-09-16 (Dialysis #7416; not verifiable from the VM — accepted as reported). | ✅ done | Cowork 2026-09-16; done 2026-09-16 |

## P14 — 🗄️ RECOVERED from the May-2026 ownership/sales remediation campaign (archived 2026-08-28)

| # | Item | State | Notes |
|---|---|---|---|
| **J13-teardown** | 👤 **Scott.** Execute `docs/history/RUNBOOK_vercel_teardown_2026-09-18_DONE.md` in order: (1) repoint the daily-briefing scheduled caller (Cowork desktop task "daily-briefing-cache") + resolve the mobile-share Shortcut blocker + confirm extension/Copilot Studio connector hosts + re-sweep the current PA export set; (2) hold an ≥8-day zero-ephemeral-writer observation window; (3) rotate the LCC Opps service key the frozen Vercel build holds (a separate trigger from the deferred SEC2–SEC4 rotation — Scott's call whether it changes that deferral); (4) delete the Vercel project, proof = `DEPLOYMENT_NOT_FOUND` at the platform level, explicitly distinct from today's frozen-build 200; (5) close the loop — flip J13-teardown to ✅ with dates, rewrite the ~10 `STALE (DOCMAP…` banners from "still answers" to "torn down `<date>`", date the `test/fixtures/retired-identifiers.json` note. ⚠️ *Cowork 2026-09-09: the caller to repoint is the one producing the 12:30 UTC `node` burst (no Vercel-side WRITE exists — the 10:00 upsert is the edge cron). Observation-window proof = no non-Railway `node` burst at 12:30 and no `v_my_work` 400 from the AWS pool for ≥ 8 days. The manual steps are being walked in the Cowork chat, one at a time.* ✅ **Step 1 DONE 2026-09-09 (Cowork walkthrough): the 12:30 UTC caller was the May-2026 PA flow "LCC Daily Briefing to Teams" (v1, no hyphen), still ON, pointed at Vercel, successful run history — turned OFF by Scott, not deleted.** Neither v2 flow nor the desktop task was the caller (both v2 flows already on Railway; the task's path is swallowed by `server.js:277`). Registry updated (PA5). **Observation window starts at the first clean weekday 12:30 UTC — expected 2026-09-10; proof = no non-Railway `node` burst, no `v_my_work` 400 from the AWS pool.** Remaining before step 3: desktop task disabled (👤 confirm), Copilot Studio connector host (👤), extension build ≥ 1.0.52 (👤), `Send webhook alerts to Daily Briefing` flow target (👤 check), mobile-share Shortcut (blocked: no route). **Day 1 closed 2026-09-09:** steps 1 + 5 + 6 of the operator list done (v1 Teams flow OFF · desktop task OFF · webhook flow clean · extension 1.0.52 · connector on Railway · Supabase side done). **Now in step 2 — the observation window: from the first clean weekday 12:30 UTC (expected 2026-09-10), ≥ 8 days with no non-Railway `node` burst and no `v_my_work` 400 from the AWS pool.** Then step 3 (rotate the LCC Opps service key the frozen build holds — Scott's call vs the SEC2–SEC4 deferral) → step 4 delete the Vercel project (proof `DEPLOYMENT_NOT_FOUND`) → step 5 close the loop in the repo. iPhone Shortcut: 👤 delete or file a route. **Day-1 observation (2026-09-09 12:30 UTC): NOT clean — the burst fired (`44.205.19.44`, 17 req, 3 × 400) because the v1 flow was turned off minutes AFTER its 12:30:00 trigger. Expected. First real test 2026-09-10; 👤 confirm the v1 flow's run history ends at today's 07:30 CT run.** **2026-09-10: a second live caller found by writer IP — the browser extension itself (EXT-HOST): 3 of 5 sidebar OMs on 09-09 were written by the frozen Vercel build. The runbook's step-1 extension line was wrong (1.0.52 is not sufficient); corrected. Teardown stays blocked until 1.0.53 is loaded in every browser profile and two captures per browser land from Railway.** **Round 33 (Cowork, 2026-09-18):** two facts change the runbook's premises and are written into it as Step 3b — the deployment is **not frozen** (Vercel still builds from `main`; the client Scott ran was from early/mid September) and it **was Scott's daily window** until 09-17. Env-var audit done from Scott's dashboard screenshots (Step 3b: A must exist on Railway / B dead / C unaudited gap). Remaining for Scott: confirm column A on both Railway services, scroll the Jun 11 → Apr 20 gap, then Steps 3 → 4 → 5. **Round 34 — what Scott does now, in order (the runbook's Steps 3b → 5, plainly):** ① Railway dashboard → each of the two services → Variables: tick every name in Step 3b column A (names only); note any missing name in chat. ② Vercel → Environment Variables → scroll the Jun 11 → Apr 20 block and open the two `RE…EY` rows; paste the names in chat. ③ Step 3: rotate the LCC Opps service-role key the Vercel build holds (Supabase → Project Settings → API → rotate `service_role`; then update it on Railway both services — the app breaks until you do, so do both in one sitting). ④ Step 4: Vercel → Settings → General → Delete Project; proof = `DEPLOYMENT_NOT_FOUND` at `life-command-center-nine.vercel.app`. ⑤ Tell Cowork; Step 5 (banners, fixture note, ✅) is a Cowork round. **Round 35 — Railway variables received (`responses/Railway tranquil-delight variables.docx`): 46 shared (42 attached to `tranquil-delight`; 4 shown *ADD* = not attached: `OPENAI_API_KEY`, `REGRID_API_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_URL`) + 79 service variables, of which the copy captured only the tail (`RESOLVER_URL` → `W75_ACTION_SUMMARY`).** Cannot finish column A until the A–R part of the service list is visible. Also noted: Railway has `LCC_DEFAULT_WORKSPACE_ID` where Vercel has `LCC_PRIMARY_WORKSPACE_ID` — check which name `main` reads before deleting. **Round 37 — column A finished from the raw lists (runbook Step 3b result):** one gap only — `TEAMS_INTAKE_WEBHOOK_URL` is read by the web app and missing on `tranquil-delight` (present on the MCP service). Everything else the app reads is already there; the rest of Vercel's 78 names are dead to current code or belong to BOV / resolver services where they exist. Fourth Railway service identified: `gracious-radiance` = owner resolver (added to CURRENT-STATE §1). **Scott's next four moves:** ① add `TEAMS_INTAKE_WEBHOOK_URL` to `tranquil-delight`; ② Step 3 rotate the LCC Opps key and update all four services; ③ Step 4 delete; ④ delete the `*variable names.docx` exports (they carry values). Then Cowork does Step 5. ✅ **DONE 2026-09-18 (Scott ①–④ + Cowork Step 5, round 38).** Proof: `curl https://life-command-center-nine.vercel.app/` → **404 `DEPLOYMENT_NOT_FOUND`** (16:50 UTC). Key rotated on all four Railway services; `TEAMS_INTAKE_WEBHOOK_URL` added to `tranquil-delight`. Step 5: runbook moved to `docs/history/RUNBOOK_vercel_teardown_2026-09-18_DONE.md`; the 11 `STALE (DOCMAP…)` banners that said *still answers* now say *torn down 2026-09-18*; fixture note dated; CLAUDE.md banner closed. ⚠️ `life-command-center.vercel.app` (no `-nine`) answers 200 with an **unrelated third-party app** — never ours; the 36 historical references to it are text, not a live caller. | ✅ done 2026-09-18 | J13-preflight · RUNBOOK_vercel_teardown.md · EXT-HOST |

## P16 — 🖥️ LCC APP OPERATOR REVIEW (Scott, 2026-09-02) — 48 comments, catalogued and tiered

| # | Item | State | Notes |
|---|---|---|---|
| **RO2a** | ✅ **SIZED 2026-09-11, merge lane NOT built.** `gov_owner_strict_core` fleet-wide on live (unmerged) `recorded_owners`, gated on core length ≥4 (< 4 is unreliable — e.g. `GLP` strict-cores to `g` because the suffix-stripper reads its trailing `lp` as the `Limited Partnership` suffix; 26 short-core groups / 60 rows excluded). Two distinct populations, not one: **311 exact-duplicate-name groups (628 rows, 589 properties touched)** — the identical literal name sitting on ≥2 separate `recorded_owner_id` rows, the safest, most mechanical merge class; and **1,069 true name-variant groups (2,242 rows, 800 property-referenced, 1,218 properties touched)** — genuine formatting/punctuation/abbreviation variants of one party (`Baker Properties Limited Partnership` / `Baker-Properties, Ltd.`, `Highwoods Properties` / `Highwoods Properties, Inc.`), spot-checked at both the largest groups and the short-core (4–6 char) end and mostly clean. ⚠️ **Not all of it is safe to blind-merge**: `CBRE` / `CBRE, Inc.` and 4-way `U.S. Bank National Association` variants are the SAME risk class RO2b just fixed (a brokerage or a lienholder captured as grantee is not a real owner, or if it genuinely is the owner needs a human's eyes, not an automatic merge) — before any merge sweep, run every group's names through `isCompetitorBroker` / `isFederalOwnerAntiPattern` / a bank-lender check (the same guards RO2b added to `granteePassesOwnerGuards`) and route any hit to human review instead of auto-merging. **Recommendation: this is big enough (1,380 groups / 2,870 rows / ~1,807 properties combined) to be its own build**, not a quick follow-on — the merge itself must move `properties.recorded_owner_id` and any deed/lease FK refs, log a reversible batch (the `lcc_merge_entity` / `lcc_b1_reopen_below_floor`-style pattern this repo already uses elsewhere), and dry-run before any live write. **EXECUTED as ID3b, 2026-09-12** — see that row for the live merge/review-routing split and parity check; this population is now largely collapsed (1,466 of 2,870 recorded_owners rows merged, 24 correctly routed to human review). Audit §10.7. | ✅ executed | RO2; ID3b |
| **RO2b** | ✅ **FIXED 2026-09-11.** `granteePassesOwnerGuards` (`api/_handlers/sidebar-pipeline.js`) now rejects the 9 named capture artifacts: `RMR` / `The RMR Group` / `RMR Group` (the property MANAGER of GPT/OPI assets, 7 rows) and `USPS` / `U.S. Postal Service` (the tenant, 1 row) via a small literal-name regex (too few for a generalized class, as the audit said); the hedge-phrase row (`CIM Group or affiliated investors`) via the OWN-T0i regex, reused here since it IS a validated class (57 live entities fleet-wide). All future deed captures of these names are blocked from ever becoming a recorded owner — not just the 9 existing proposals. 3 new unit tests added (`test/owner-deed-propagation.test.mjs`); full `owner-deed-propagation` (39/39) and `deed-parser` (58/58) suites pass. Audit §10.7. | ✅ fixed | RO2 |
| **OWN-T0h** | **DECIDED 2026-09-11: `v_lcc_property_ownership_reconciled` (`property_state='conflict'`, is_current) is canonical for "conflict properties," not `v_lcc_property_multi_current`.** The two answer different questions: `v_lcc_property_multi_current` only checks whether `lcc_entity_portfolio_facts` disagrees with ITSELF (>1 distinct current survivor in the raw fact ledger, or a tombstone duplicate) — a data-hygiene count, currently 740 (was 756 on 2026-09-02, small expected drift). `v_lcc_property_ownership_reconciled` is what the property panel and Decision Center actually READ (per OWN-T0's own "one door" doctrine): it also admits the resolver's `lcc_property_owner` proposal and the domain true_owner mirror as competing current-owner candidates, so it catches disagreement `multi_current` is structurally blind to (the fact ledger agreeing with itself while disagreeing with the resolver or the domain mirror). The audit's own §9.6 already said as much ("the conflict count over what the panel SEES is larger than 756") — this just finishes that thought and states it as the answer. ⚠️ **CORRECTED 2026-09-11: my first re-measurement ("doubled to 4,478") was WRONG — a counting bug, not a data finding.** `count(*)` on the reconciled view counts ROWS (one per current owner-candidate), not properties — a conflict property carries ≥2 rows by definition, so `count(*)` inflates the true property count. The correct measurement is `count(distinct (source_domain, source_property_id))`: **2,065 conflict properties today** (gov 1,752 / dia 313) — essentially FLAT vs the audit's 2,097 (gov 1,769 / dia 328), the small drop fully explained by OWN-T0e's confirm lane converting some `unclassified_rival` pairs (1,617→1,508) into `sponsor_family_confirmed` (64→142) and a handful of merges (`duplicate_entity` 417→415). No mystery growth, no follow-up needed on this point — retracting that flag. Lesson: always `count(distinct property)` on this view, never `count(*)`. Action taken: canonical page (`docs/architecture/ownership-history-lane.md` § OWN-T0) and `CURRENT-STATE.md` updated to state the reconciled-store number as canonical and carry today's live re-measurement instead of the stale 756/2,097 pair. OWN-T0e design §2. | ✅ decided · re-measured | OWN-T0e design §2 |
| **RO4** | ✅ **ROOT-CAUSED 2026-09-11: it's not a bug, and not the DISTINCT ON.** Re-measured live: **391 of 599 deed-arm properties (65.3%, matches the audit)** carry no `latest_deed_date`. Traced past `v_ownership_resolution`'s `DISTINCT ON ... ORDER BY latest_deed_date DESC NULLS LAST` (correct — it already prefers a dated row when one exists, so it isn't swallowing dates) to the source: `properties.latest_deed_date` is fed straight from `deed_records.recording_date` (`deed-parser.js`'s R51 write only sets the date when `parsed.recording_date` parses; it always writes the grantee regardless). Checked `deed_records` directly for all 391: **0 have a `recording_date` deed_records is holding that properties simply never picked up — every one of the 391 is null all the way down to the raw capture.** Read the raw payload on a sample: `{grantee: "Boyd Kearneysville GSA II, LLC", grantor: null, deed_type: null, consideration: 0, recording_date: null, document_number: null}` — a minimal, grantee-only capture stub across **229 distinct counties nationwide** (not one source/format issue), where only the grantee name was ever extractable and everything else in the deed schema (date, grantor, document number) came back null by construction of whatever produced these rows. Nothing to fix upstream — the date is genuinely unknown, not lost. **The actionable point stands**: `is_newer_than_recorded` (`latest_deed_date IS NOT NULL`) collapses "confirmed not newer" and "we don't even have a date" into the same `false` — whoever builds RO3's card should expose `latest_deed_date IS NULL` as its own explicit "date unknown" state rather than reading `false` as a claim. Not built here (RO3 hasn't been designed yet — Scott's call on whether this lane exists beside OWN-T0e or becomes OWN-T0's gov arm). Audit §10.1. | ✅ root-caused | RO3 |
| **RO5** | ✅ **SIZED 2026-09-11 — joined the 761 gov genuine disputes to the reconciled store.** `v_lcc_property_ownership_reconciled` (gov domain, `is_current`, `is_primary`) covers **742 of 761 (97.5%)** disputed properties; **19 absent** (mostly person-name-format mismatches the reconciled store never linked, e.g. `LIDDELL ANDY` / `Andy Liddell`, plus a few where the two sides propose entirely different parties). Of the 742 present, comparing the reconciled store's primary `owner_name` (normalized) against `v_ownership_resolution`'s three names: **169 (23%) match the lane's `proposed_owner_name`** (the deed/lessor signal the lane already surfaces), **198 (26%) match only `current_recorded_owner_name`** (reconciled store kept the recorded owner, rejecting the lane's proposal), **253 (33%) match only `true_owner_name`** (reconciled store already agrees with gov's own true-owner field, which the lane doesn't compare against directly), and **122 (16%) are hard disagreements** — the reconciled store's primary owner matches none of the lane's three names. Of those 122: 88 carry the reconciled store's own `conflict_class` (it hasn't fully resolved them either — mostly `unclassified_rival`, 34 clean); resolver_rung breakdown 69 `relationship_graph` (mostly the Boyd Watterson/Easterly/Gardner Tanenbaum sponsor-family SPE-flattening OWN-T0e already handles), 28 `supersession`, 16 `domain_true_owner`; 57 are `is_domain_true_owner=true` in the reconciled store (high-confidence) vs 65 not. **Answering RO5's original three questions: (1) done above — 82% agree in some form, 16% hard-disagree, 2.5% absent; (2) decided by Scott 2026-09-11 — yes, repoint the lane at the reconciled store (see RO3); (3) `recommended_action` is still `confirm`×1,597/1,597, no information — drop it or replace it with the reconciled-store's `conflict_class`/`is_resolved_owner` signal when RO3 repoints the lane.** No code changed this pass — sizing only, feeding directly into RO3's migration design. Audit §9.4, §9.6 rank 1; live re-measurement 2026-09-11. | ✅ sized | RO3 |

## P17 — 🏘️ PROPERTY DATA RECONCILIATION REVIEW (Scott, 2026-09-10) — DaVita Kidney Care, Donna TX walkthrough

| # | Item | State | Notes |
|---|---|---|---|
| **PDR2** | ❌ **Root cause found live 2026-09-11 (read-only investigation, no writes) — and it is systemic, not one-off.** `properties.property_id=37722.true_owner_id` already points at a `true_owners` row for "DaVita Kidney Care" that IS correctly flagged (`is_operator_not_owner=true`, `owner_role='operator'`) — the flag is right. The bug is a READ-path gap: `api/operations.js`'s `assemblePropertyPacket()` (~lines 8811–8829, the ownership block behind `get_property_context`) joins `true_owner_id → true_owners.name` **unconditionally**, ignoring the flag — unlike two other code paths in this same repo that already guard it correctly: `api/_handlers/entities-handler.js`'s `assemblePropertyDossier` §1.6 (~lines 742–751, nulls an operator-flagged owner and falls back to `recorded_owner_name`) and `api/_handlers/sf-link-reconcile.js`'s `isOperator()` (~line 270). **Blast radius measured: 1,182 properties nationwide point at this exact DaVita placeholder row; 4,026 properties fleet-wide have `recorded_owner_id IS NULL` and `true_owner_id` pointing at ANY `is_operator_not_owner=true` row** — this is a guard-gap bug affecting thousands of properties, not a DaVita-specific defect. Also layered under a deeper shared cause — see **PDR13**. Fix: close the guard gap in `assemblePropertyPacket` (and any other unguarded true-owner reader found while fixing it), sized against the real 4,026-property blast radius, not just this one property. ⚠️ **BLAST RADIUS RE-MEASURED 2026-09-14 (Cowork) — the 4,026 above is the NO-FALLBACK SUBSET, not the total.** Live in Dialysis_DB: **7,937** properties point `true_owner_id` at an `is_operator_not_owner=true` row; **4,022** of those also have `recorded_owner_id IS NULL`, so even the correctly-guarded readers have nothing to fall back to. Top offenders **Fresenius 3,077 · DaVita Inc. 2,625 · DaVita Kidney Care 1,182 · U.S. Renal Care 343 · Dialysis Clinic Inc 256 · American Renal 221** — **every major operator, not one bad placeholder row.** 🔴 **Recommended next build**: for a net-lease broker the entire job is finding the OWNER to call, and `get_property_context` currently answers "DaVita" when DaVita is the tenant. Prompt is written and its headline is now corrected: `prompts/PDR2-true-owner-operator-guard-gap.md`. ✅ **SHIPPED 2026-09-14** — `assemblePropertyPacket`'s ownership block now selects the operator signals (`is_operator_not_owner`/`owner_type`/`owner_role`, domain-scoped so gov never 400s on a column it lacks) via the shared predicate `api/_shared/true-owner-operator-guard.js::isTrueOwnerOperator`, and when the true owner is an operator it returns `ownership.true_owner_name: null` + `ownership.true_owner_is_operator: true` + `ownership.operator_name: '<tenant>'`, leaving `recorded_owner_name` as the owner of record (both-null stays "owner unknown" — never backfilled from the operator). `entities-handler.js` §1.6's own re-query was left in place (not repointed at the packet's verdict) because it also falls back to `prop.true_owner_name`/`prop.recorded_owner_name` (denormalized columns on `properties` that `sidebar-pipeline.js` populates unguarded) — repointing without auditing that write path first could silently regress the dossier. `admin.js::harvestResolveOwnersWithoutContacts` (the W9.4 create-contact target resolver) also now excludes operator-flagged true owners so the harvest can never propose a create-contact under a tenant's `true_owner_id`. Audited and left as-is with reasons: `operations.js:562` (Teams-alert display string from caller-supplied `entity_fields`, not a `true_owners` read), `operations.js` `bridgeCreateLead` (already an input contract — the caller passes `true_owner_is_operator`), `intake-promoter.js` `resolveOwnerLinksDia`/gov analogue (deal-derived seller-name fuzzy LINKING, not a display of "the owner"; `sf_sync_flags` is SF-match telemetry, a distinct root cause from PDR2). Guard `test/pdr2-operator-owner-guard.test.mjs` (18 tests, all green); `npm run check:boot` green. Live trace confirmed against 4 real dia rows (39874 Donna TX DaVita, 21924 Fresenius, 21893 U.S. Renal Care/no recorded owner, 21867 non-operator control) — matches the new code's logic exactly. PR: see STATUS.md 2026-09-14 PDR2 entry. ✅ **LIVE POSITIVE CONTROL RUN 2026-09-14 (Cowork) — against the DEPLOYED endpoint, not a code trace.** `get_property_context(property_id=39874, domain='dia')` on `tranquil-delight` (`/version` = `953b7a00` == `main`) returns `ownership: { recorded_owner_name: "Living Trust & Gina M Decarion Living Tr", true_owner_name: **null**, true_owner_is_operator: **true**, operator_name: "DaVita Kidney Care" }`. **Independently corroborated by the property's own transaction history in the same packet** — sale 2019-02-01, buyer *Phil Decarion*, seller *Davita* — so the Decarion trust genuinely is the owner and DaVita genuinely is not. The packet now answers the question a net-lease broker actually asks. | ✅ | shipped 2026-09-14 |

## P18 — 📰 EXECUTIVE BRIEFS: swimlane market briefs + CTO/CDO build brief + operator funnel (Scott, 2026-09-11)

| # | Item | State | Source |
|---|---|---|---|
| OC2 | ✅ **Shipped 2026-09-11, flag OFF (`OPERATOR_NOTE_TRIAGE`).** `GET/POST /api/operator-triage-tick`, deterministic rules first (error signatures/stuck-loading/missing-data/idea/ux/trailing-`?` patterns), on-box Ollama on a miss (fails closed, no cloud fallback), dedupe vs prior open notes + a generated PLANNED-BACKLOG index (`scripts/generate-operator-note-backlog-index.mjs`, 181 rows), routes via `docs/os/operator-note-routing.json` (7 threads). Logs to `producer_runs`. **Not yet live-verified** (needs a Railway deploy + a real note posted through each channel per spec §6) — flag stays off until then. ⚠️ **Shipped the handler but never registered the flag row** (`feature_flags_registry` had zero `OPERATOR_NOTE_TRIAGE` rows) and never scheduled a cron — both closed by OC-v2 (see OC-v) 2026-09-14, which also fixed a lane-detection gap and a GET-dry-run-never-calls-the-model gap in this same handler. ✅ **Flag registered and flipped ON 2026-09-14 (Cowork)** — it had never existed in `feature_flags_registry`, so it could not be turned on at all; `20261102140000` created it and a live dry-run grade (3/3 triaged, 0 unclassified) justified the flip. | ✅ live | OC-a 2026-09-11 |
| OC-v | ⏳ 👤 **Make OC-a live (reconcile 2026-09-11, measured):** PR #2298 merged, but (1) redeploy BOTH Railway services — **still half done after MB-a3 (app /version `78082f46`; LCC MCP still 21 tools, no `log_operator_note`/`get_operator_inbox`, 0 notes) — was: `tranquil-delight` /version = `e42dbcb7` (includes OC-a/MB-a/MB-a2; per the OWN-T0j thread); the standalone MCP still lacks the two tools after refresh → not redeployed** — the connected LCC MCP still lacks `log_operator_note`/`get_operator_inbox`; (2) run `OPERATOR-ACTIONS.md` OCa-live-verify (0 notes exist); (3) **insert** the `OPERATOR_NOTE_TRIAGE` registry row (absent — nothing to flip) + create the pg_cron tick job (none exists); (4) OCa-pa-note-category — confirm the PA category trigger fires; (5) OCa-teams-flow; (6) put `OPS_SUPABASE_*` in the Claude Code session env so the session-start inbox refresh stops no-opping. **Re-checked 2026-09-11 after MB-a: still 0 notes, `OPERATOR_NOTE_TRIAGE` row still absent, no crons.** Until (1)–(3), the funnel is code, not a loop. ✅ **(1) and (2) DONE, verified live by Cowork 2026-09-12.** Standalone MCP redeployed — `/health` now lists **`log_operator_note` and `get_operator_inbox`**, and `/api/operator-inbox` is a live read route. **End-to-end test passed:** a note POSTed to `/api/operator-notes` returned `{id, disposition:'open'}` and is row 1 in `operator_notes`. **Still open:** (3) the `OPERATOR_NOTE_TRIAGE` flag row still does **not exist** in `feature_flags_registry` (`registry_state: null`) and no pg_cron job posts the tick — so nothing triages on its own. A forced dry run (`?force=1`) scanned the note and returned **unclassified** with reason `no_deterministic_rule_matched_and_model_declined` — honest fail-closed behavior, but it means routing is unproven until a real bug/idea note runs through it. (4) Outlook PA category check and (5) the Teams flow remain. 🟢 **RE-MEASURED LIVE 2026-09-14 (Cowork) — blocker #1 is RESOLVED, the rest is not.** The standalone MCP redeploy happened: `log_operator_note` and `get_operator_inbox` are live and answer, so intake works end to end. **But nothing consumes the queue.** Measured: `operator_notes` holds **1** note (filed 2026-09-12 via Cowork), still `disposition=open`, `note_type=null`, `routed_to=null` two days later; the `OPERATOR_NOTE_TRIAGE` row **does not exist in `feature_flags_registry` at all** (0 rows — OC2 shipped the handler and never registered the flag, so it cannot be switched on); and there is **no triage cron**. ⚠️ This is worse than not having the funnel: Scott was told it is live, so a note filed there looks captured — it is captured and then silently ignored, the same class as a dead feed reporting healthy. Prompt: `prompts/OC-v2-notes-go-in-and-nothing-happens.md`. 🔬 **Triage measured live 2026-09-14 (Cowork) — the handler IS deployed (`cf04ae04ae52`) and reports its own flag state honestly, but classification is mostly not working.** Forced dry run over 3 notes (the meta test note plus two realistic ones filed on purpose): **scanned 3, triaged 1, routed 1, unclassified 2.** An unambiguous **dialysis bug report** naming a route, a lane and a mechanism came back `unclassified` (`no_deterministic_rule_matched_and_model_declined`); the one success was a comps idea routed `deterministic`ally — with **`lane: null`** despite "government deals" and "GSA lease". So only keyword rules fire, they lack market-brief/dialysis vocabulary, lane is never set, and the model arm declines (`triage_source: null`). ⚠️ **Flipping the flag as-is routes ~1 note in 3** and loses the rest silently. MB-a measured Ollama reachable from Railway in the RSS path, so check whether TRIAGE reaches the model at all — unreachable is a wiring bug, conservative is a prompt question. ✅ **OC-v2 SHIPPED 2026-09-14 (code+migration, not yet applied live):** (a) the `lane: null` bug was the deterministic path never computing a lane at all — `classifyDeterministic` only ever set `note_type`/`severity`; added a coarse domain keyword map (`detectLane`, `api/_shared/operator-notes.js`) covering dialysis/government/comps/market-brief/buyer-engagement/automation/data-coherence/canon, attached to every deterministic verdict; (b) the "model declined" reading was itself wrong — a plain `GET` dry run (no `?generate=1`) never called Ollama at all, so the bug note's `no_deterministic_rule_matched_and_model_declined` verdict is misleading: the model was never invoked, not consulted-and-refused. `operator-triage-tick.js` now attempts the on-box model on every miss (GET or POST alike, since a GET never writes); `?skip_model=1` opts out for a cheap deterministic-only probe; (c) migration `20261102140000_lcc_ocv2_operator_note_triage_wiring.sql` registers the `OPERATOR_NOTE_TRIAGE` flag (still **off**), schedules `lcc-operator-triage` (07:25 UTC, NOT flag-gated per the P138 pattern), and adds a **distinct** visibility monitor (`v_operator_notes_stale_open` + `lcc_check_operator_notes_stale`, cron `lcc-operator-notes-stale-check` 07:30) opening `alert_kind='operator_note_stale_open'` for any note open ≥3 days — the MB2e "name the fault, don't fold it into a generic health check" pattern; closes part of OC4. **NOT YET APPLIED LIVE** (no DB access from this session) and the flag stays **off** until a live dry run is re-graded against the fix — do not flip it on the strength of this entry alone. ✅ **LIVE 2026-09-14 (Cowork) — the funnel now works end to end.** Migration `20261102140000` **applied live** (flag row registered, `v_operator_notes_stale_open`, `lcc_check_operator_notes_stale`, crons `lcc-operator-triage` 07:25 UTC + `lcc-operator-notes-stale-check` 07:30 UTC — **both slots verified free against live `cron.job` first**, as the migration's own comment asked). Deployed code confirmed to contain the fix (`87042eb63878`, `merge-base` proves it includes PR #2438). **Re-graded with the model actually invoked: scanned 3, triaged 3, routed 2, unclassified 0** (was 1/1/2). The dialysis bug note that previously failed now grades `bug` / `lane: dialysis` / `medium` → `app/briefing` via `onprem_ollama`; the comps idea now carries `lane: government`. `OPERATOR_NOTE_TRIAGE` **flipped ON** against that evidence — the exact gate the migration documented. POST run wrote the classifications; all three fixtures then closed through the normal disposition path. Stale monitor: 0 open notes, 0 alerts. ⚠️ **Correction to Cowork's own earlier diagnosis:** the `model_declined` verdict meant **never asked** — a plain GET never called Ollama — not "asked and refused". CC found that; it is why the fix was a code path, not a prompt. 👤 One residual: the meta note graded `bug` (over-classification) and `routed_to: null` — triaged-but-unrouted correctly stays `open` and would have aged into an alert, which is the monitor behaving as designed. | ✅ live | Cowork 2026-09-14 |
| MB2a | ✅ **BUILT 2026-09-12 (this change) — the two feeds Cowork verified live (Federal Register ESRD + Google News dialysis-operator query) replace the three dead MB-b URLs (403/404/404) in `RSS_FEEDS.dialysis`** (`supabase/functions/briefing-intel-snapshot/index.ts`). A third publisher-specific feed was deliberately NOT added — this session's sandbox has zero egress to any candidate host (policy-denied CONNECT, confirmed via the agent-proxy status endpoint), and per this task's own instruction a feed that cannot be verified is skipped rather than shipped with a spoofed User-Agent. **Google News redirect caveat handled, not ignored:** `parseRss()` now takes a `redirect` flag; for a redirect feed it splits the item title on the LAST `" - "`/`" – "` into `{headline, publisher}` (never mid-headline, so a dash inside a real headline survives), and `market_brief_facts` gained two additive columns — `source_publisher` (the real outlet) and `source_url_is_redirect` (true only for Google News) — so a renderer can label the citation honestly instead of presenting a `news.google.com/rss/articles/...` link as a direct publisher link. **A dead feed cannot ship silently again, two ways:** (1) `scripts/verify-rss-feeds.mjs` parses `RSS_FEEDS` straight out of the edge-function source (never hand-duplicated, so it cannot drift) and fails non-zero on any non-200 or zero-item feed — run with `node scripts/verify-rss-feeds.mjs [--stream=dialysis]`; deliberately NOT wired into `npm test` (the suite is hermetic by guard, TEST-NET-LEAK) and confirmed to actually attempt real egress in this session (blocked 403 by the sandbox's own policy proxy, not a code defect). (2) new `market_brief_feed_health` table (migration `20260912150000`) — `fetchSectorNews()` writes one row per (stream, source, day) with `item_count`/`error`, and `lcc_check_market_brief_feed_health(p_stale_days=3)` opens a deduped `lcc_health_alerts(alert_kind='market_brief_feed_stale')` row when a feed has read zero items for 3+ consecutive checked days, auto-resolving on the positive statement of a fresh item (never on absence, per the B6d doctrine) — cron `lcc-market-brief-feed-health` (11:15 UTC, after the RSS tick). Guard: `test/sql-definer-privilege-stanza.test.mjs` passes over the new function (revoke public/anon/authenticated + `has_function_privilege` assertion in the same file). **PRSS ("MARKET_BRIEF_PRSS") flag LEFT OFF — NOT flipped.** This session has no live egress to actually fetch either replacement feed nor any Railway/Supabase write access, so there is no fresh evidence of "good facts flowing" from THIS session; the only live fetch evidence on record is Cowork's 2026-09-12 run (Federal Register 200/3 items, Google News 200/~100 items), which predates this change's code and cannot itself be re-verified here. **Operator next step:** `node scripts/verify-rss-feeds.mjs --stream=dialysis` from a host with real egress, then `GET /api/market-brief-rss-tick?lane=dialysis&stream=dialysis` dry run (check the relevance-filter survival rate on the broad Google News query before trusting volume), then `POST` once with the flag forced on, then flip `MARKET_BRIEF_PRSS`. Full repo suite unaffected: 6,130 pass / 0 fail / 6 skipped. 🟡 **RECONCILED LIVE 2026-09-12 (Cowork) — the feeds are fine; the code is not deployed.** Both replacement URLs re-fetched independently via pg_net: Federal Register ESRD **200, 3 items**; Google News operator query **200, 100 items** — CC could not do this (its sandbox had zero egress), so the URLs are now verified against the committed code rather than against a prior session's note. **Migration APPLIED live to LCC Opps**: `market_brief_feed_health`, `v_market_brief_feed_health_stale`, `lcc_check_market_brief_feed_health` (executes clean, 0/0), `market_brief_facts.source_publisher` + `.source_url_is_redirect`, cron `lcc-market-brief-feed-health` 11:15 UTC. Also verified CC handled the bucket hazard correctly — `fetchSectorNews()` builds its result keys from `RSS_FEEDS` in BOTH the initializer and the catch fallback, so adding a stream cannot throw. **`MARKET_BRIEF_PRSS` stays OFF, and the reason is now a deploy, not the feeds** → **MB2a-deploy**. Relevance survival (the measurement the flip depends on) is unmeasurable until the edge function actually carries the dialysis stream. ✅ **UNBLOCKED AND CLOSED 2026-09-14 (Cowork).** The edge-function deploy this row was waiting on happened and has since gone through three more (v21 → v25); `MB2a-deploy` carries the deploy story and its still-open I16 half. Verified live 2026-09-14: the dialysis stream returns items, ESRD contributes 1 of 1 under its `maxAgeHours`, and **all 13 feeds contribute ≥1 with zero open alerts**. The state cell on this row read `⛔ blocked on edge-function deploy` for two days after the block cleared — the exact stale-blocker class **XB2** is meant to detect automatically. | ✅ | this change 2026-09-12; Cowork feed discovery 2026-09-12 |
| FEED1 | 🚨 **Three RSS feeds outside the dialysis lane are dead and have been failing silently — found by MB2a's new `market_brief_feed_health` monitor on its FIRST run, 2026-09-12, each then confirmed independently via pg_net: `gsa.gov/about-us/newsroom/news-releases/rss` **404**, `healthaffairs.org/rss/site` **410 Gone** (publisher retired it outright), `globest.com/feed/` **403**.** Live effect: the **government** stream runs on a SINGLE feed (Government Executive), `net_lease` on two of three, `healthcare` on two of three — and §8 Sector Watch in the daily email has been built on that reduced set without ever saying so. This is the same defect class as MB2a, found in three more places the moment something finally looked: **nobody was checking, so nothing was wrong.** Replace each with a live-verified URL (fetch and parse BEFORE committing, per MB2a), and note 410 means gone for good — find a different source rather than a different path. 🟢 **Replacements verified live 2026-09-12 (Cowork, fetched and parsed via pg_net) — prompt written: `prompts/FEED1-three-dead-feeds-outside-dialysis.md`.** `government`: Federal Register GSA-agency feed **200, 14 items**, newest 2026-09-11. `healthcare`: STAT News **200, 20**, newest 2026-09-12; Healthcare Dive **200, 10**, newest 2026-09-11. `net_lease`: Connect CRE **200, 10** and REBusinessOnline **200, 20**, both newest 2026-09-11. Government Executive re-verified **200, 23** — the only reason the government lane is not at zero. **Measured and rejected:** Modern Healthcare 403, The Real Deal 403. Newest-pubDate is recorded for each because MB2a proved a feed can return 200 with items and still contribute nothing; **all five publish daily, so all five clear the 72h cutoff as-is** and MB2b is NOT a prerequisite. ✅ **SHIPPED + DEPLOYED + LIVE-VERIFIED 2026-09-12 (this change).** This session's own sandbox has zero egress (policy-denied CONNECT, same as MB2a) — re-verified all six URLs via `net.http_get` from LCC Opps instead: all 200, item counts matching Cowork's figures exactly. `RSS_FEEDS` updated in `briefing-intel-snapshot/index.ts`; deployed to LCC Opps (`v22 → v23`, `supabase functions deploy briefing-intel-snapshot --project-ref xengecqvemvfknjvbvrq`) and the deployed body re-read to confirm the new URLs shipped — MB2a-deploy's exact lesson applied ("merged is not running" for edge functions has a THIRD instance now; check every time, not just once). Triggered a live `?dry_run=1` POST against the deployed function via pg_net (200, real market data) and read `market_brief_feed_health` for `checked_date=current_date` immediately after: all six replacements wrote `ok=true` with real item counts (Federal Register-GSA 14, STAT 15, Healthcare Dive 10, Connect CRE 10, REBusinessOnline 15, Government Executive 15); the three retired sources (GSA News, Health Affairs, GlobeSt) still carry `ok=false` rows dated today from the LAST cron run under the old deployed code (pre-fix) — they will not recur, since the new code never calls those URLs. No `market_brief_feed_stale` alert had opened for any of the three (checked `lcc_health_alerts`, 0 rows), so nothing needed resolving. ✅ **SHIPPED + VERIFIED LIVE 2026-09-12.** CC re-fetched every URL itself rather than trusting Cowork's table, swapped all three, deployed **v22 → v23** and re-read the deployed body. Cowork confirmed independently: the three dead URLs are gone from `RSS_FEEDS` (they survive only as comments explaining why), and today's health rows show `government` 2 feeds (Federal Register GSA 14 + GovExec 15), `healthcare` 4, `net_lease` 4. The three dead feeds' `ok=false` rows for 2026-09-12 are leftovers from Cowork's pre-deploy run, not evidence they are still configured. | ✅ | this change 2026-09-12 |
| FEED2 | ✅ **FIXED + APPLIED LIVE 2026-09-12 (Cowork) — the feed monitor FEED1 relied on was itself broken, caught before its cron had ever fired.** Every feed read `zero_item_streak_days = 9999`, including feeds that had just returned 15 items. **Two bugs in one expression:** (1) the streak was `checked_date - (last date with items)` — **calendar days** — while the producer `lcc-briefing-intel-snapshot` runs `0 10 * * 1-5` (**weekdays**) and this check runs `15 11 * * *` (**daily**), so **Monday − Friday = 3** and tripped `p_stale_days=3` on perfectly healthy feeds; (2) with no prior row the COALESCE fell back to a **9999 sentinel** standing in for "unknown", so the FIRST run alerted on all 16 feeds. Proven by simulation over synthetic weekday-only rows: a feed returning 15 items on EVERY check alerts on run 1 and again every Monday. **This is an I11 inversion** — rather than staying silent about its own blindness, the monitor attributed that blindness to the feeds. Also fixed: a **retired** feed (FEED1 removed three) kept its last rows at 9999 forever and could never auto-resolve, because resolution needs a new row and no new rows ever arrive for a feed nobody fetches. Migration `20260912190000` renames the measure to `zero_item_streak_checks` so the name states its unit. **Verified live after the fix:** 13 healthy feeds **0**, the 3 retired feeds **1**, `lcc_check_market_brief_feed_health(3)` returns **0 opened / 0 resolved**; positive controls pass (a feed with 3 zero-item checks alerts; a healthy feed spanning a weekend stays silent). Tomorrow's 11:15 UTC run would have opened **16 false alerts**. 🟢 **Holding live: cron has now run twice (2026-09-13 Sunday and 2026-09-14 Monday) with ZERO alerts and max streak 1** — Sunday being exactly the run the old `9999` sentinel would have turned into 16 false alerts. Today's rows: 13 feeds, all with items. ✅ **THE OPEN TEST GAP IS CLOSED 2026-09-14** — `test/feed2-streak-checks-not-days.test.mjs`: the four named cases (weekend-boundary healthy feed, no-history feed, 3-zero-check positive control, retired feed) plus structural guards on the migration's own SQL text (no `9999` sentinel, `count(*)` not date subtraction, both branches of the "no prior good check" case present) — a future edit that reintroduces either root cause fails this test even without a live database, matching this repo's `mba2-market-brief-psql-source-fixes.test.mjs` pattern for pure/no-DB SQL-behaviour coverage. | ✅ | Cowork 2026-09-12 (FEED1 reconcile), test 2026-09-14 |
| MB2b | 🔴 **The Google News dialysis query returns noise, measured not assumed: 0 of 6 items on 2026-09-12 were market signal** — local EMS coverage, a Canadian wildfire item, a $4,100 clinic-refund story, a PFAS lawsuit, a supplier award, and a DaVita one-day stock move already read straight off the DVA ticker. Tighten the query (`cap rate`, `clinic`, `acquisition`, `when:7d`) and re-measure survival before `MARKET_BRIEF_PRSS` is reconsidered — an empty news section beats a brief full of ambulance stories. **Two concrete defects to fix in the same pass:** (1) **Federal Register is healthy and contributes nothing** — 3 items parsed, 0 survive the shared **72h cutoff** in `fetchSectorNews()`, because its documents are weeks old BY DESIGN and that is precisely what the policy section wants; give a policy-class feed its own window instead of the news window. (2) **`item_count` measures PARSING, not CONTRIBUTION**, so a feed can sit green in `market_brief_feed_health` while adding zero facts — record items-after-cutoff too, or the I11 monitor is blind to exactly the failure it was built for. 🟢 **Prompted 2026-09-14 (Cowork) with a sharper diagnosis: the ESRD feed's problem is its narrow QUERY's low volume, not Federal Register.** The FR GSA-agency feed on the same service publishes daily and behaves normally, so the fix is a per-feed freshness window (optional `maxAgeHours`, default 72) plus possibly broadening the ESRD query — **not** a blanket exemption for "policy" feeds, and **not** widening the 72h window globally, which is what keeps the daily brief daily. Second half unchanged: record items-after-cutoff as a NEW column beside `item_count` (don't repurpose it — consumers read it), so a feed contributing zero is a named gap instead of a green row. Bundled with MB2c + the FEED2 test in `prompts/MB2bc-fix-the-instrumentation-then-judge-PRSS.md`. ✅ **SHIPPED + DEPLOYED + LIVE-VERIFIED 2026-09-14.** `RSS_FEEDS['dialysis']['Federal Register (ESRD)']` carries `maxAgeHours: 24*30`; `fetchSectorNews()` computes per-feed cutoff and records the new `market_brief_feed_health.items_after_cutoff` column (migration `20260914120000`, additive, NULL for pre-migration rows — never conflated with 0). Deployed to LCC Opps (v23 → v24), deployed body re-read and confirmed byte-identical to source. Forced live via `net.http_get`/`lcc_cron_post`: Federal Register (ESRD) went from `item_count:3, ok:true, [invisible zero-contribution]` to **`item_count:1, items_after_cutoff:1`** on the SAME day's real feed — the exact defect closed, not simulated. | ✅ | this change 2026-09-14 |
| MB2e | 🔴 **MB2b's new `items_after_cutoff` column found its next two customers on day one (Cowork, live 2026-09-14).** Two feeds return HTTP 200 with real items and contribute **zero**: **Federal Register (GSA)** `government` — 6 items, **0 after cutoff**; **Tax Foundation** `tax_policy` — 15 items, **0 after cutoff**. MB2b set `maxAgeHours` on the ESRD feed ONLY. Fetched both directly to size the window rather than guess: FR GSA newest item **82h** old (0 within 72h, **5 within 7d**, 7 within 30d); Tax Foundation newest **92h** (0 within 72h, **5 within 7d**, 20 within 30d). **Live consequence: the `government` lane contributes from ONE feed (GovExec, 5) — FEED1 only half-fixed it — and `tax_policy` contributes NOTHING, so that stream is empty in the daily email while every row about it reads green.** ⭐ **This is FEED2's twin, and worth naming:** FEED2 was a monitor measuring calendar days against a weekday producer; this is a cutoff measuring calendar hours against feeds that publish a few times a week. A fixed calendar window pointed at a source slower than that window is **empty by construction on some days — and Monday is worst, since 72h on a Monday excludes everything before Friday morning** (today is a Monday; both feeds' newest items are Thu/Fri). The 72h default is a NEWS window and we keep aiming it at non-news sources. Fix: `maxAgeHours = 24*7` on both (measured, captures 5 each, far tighter than ESRD's 30 days), then extend `lcc_check_market_brief_feed_health` with a distinct `market_brief_feed_no_contribution` arm so the monitor catches this class itself — a different fault from a dead feed, with a different fix (widen the window vs. replace the URL). Prompt: `prompts/MB2e-two-more-feeds-green-and-contributing-nothing.md`. ✅ **SHIPPED + DEPLOYED + LIVE-VERIFIED 2026-09-14.** `maxAgeHours: 24*7` set on both `government/Federal Register (GSA)` and `tax_policy/Tax Foundation`; deployed to LCC Opps (v24 → v25), deployed body re-read and confirmed byte-identical to source. Forced live via `net.http_post`: same-day feeds went from `items_after_cutoff: 0` on both to **Federal Register (GSA) 6 items / 4 after cutoff**, **Tax Foundation 15 items / 5 after cutoff** — the exact defect closed on the real feed, not simulated. **Monitor half also shipped**: migration `20260914130000` adds `v_market_brief_feed_health_no_contribution` + `lcc_check_market_brief_feed_no_contribution(p_stale_checks default 3)` — a DISTINCT `alert_kind` from `market_brief_feed_stale`, counting consecutive CHECKS (FEED2's fix, applied here from the start) that parsed items but contributed 0, with rows carrying `items_after_cutoff IS NULL` (pre-MB2b) never counting toward the streak either way (P180). Rides the SAME cron as `lcc_check_market_brief_feed_health` (`lcc-market-brief-feed-health`, 11:15 UTC). Ran both functions live post-deploy: both feeds' `no_contribution_streak_checks` read 0 (contributed on the check that mattered), confirming the monitor resets correctly. Guard: `test/mb2e-feed-cutoff-window.test.mjs` (structural assertions the fix touched only the two named feeds and not `DEFAULT_MAX_AGE_HOURS`, plus a pure streak-predicate fixture + SQL-migration structural guards). | ✅ | this change 2026-09-14 |
| MB2c | 🟡 **Google News publisher parser drops any hyphenated outlet.** `splitGoogleNewsTitle()` (`briefing-intel-snapshot/index.ts`) matches the suffix with `([^-–—]+)$`, which forbids a hyphen INSIDE the publisher name — so "… - Honolulu Star-Advertiser" fails to match, `publisher` comes back **null**, and the raw " - Publisher" suffix is left in the headline. Measured live 2026-09-12: 1 of 6 items hit this. Fix by splitting on the LAST separator rather than forbidding hyphens in the tail, and cover Star-Advertiser / Al-Monitor / U-T San Diego style names in the test. Matters because MB2a added `source_publisher` precisely so a redirect-URL citation can still name who published it — a null here silently returns the citation to the state MB2a set out to fix. 🟢 **Fix measured on the live feed 2026-09-14 (Cowork) — it is ONE character.** Widening the tail from `([^-–—]+)$` to `(.+)$` is enough; the greedy `(.*)` already anchors to the LAST separator, so no "split on last occurrence" rewrite is needed. Across **101 real titles** carrying a separator: old regex parsed 98, new parses **101**, and **0 previously-correct parses changed** — the regression check, done empirically rather than argued. Real failures fixed: `Honolulu Star-Advertiser` and two `ad-hoc-news.de` items. Regression corpus named in the prompt (`low- and middle-income … - nature.com`, `Ready4 Ci-Ca: …`, `NYC Health + Hospitals`) because hyphens BEFORE the separator are what make a naive rewrite dangerous. Prompt: `prompts/MB2bc-fix-the-instrumentation-then-judge-PRSS.md`. ✅ **SHIPPED + DEPLOYED + LIVE-VERIFIED 2026-09-14.** `splitGoogleNewsTitle()`'s publisher half widened `[^-–—]+` → `.+`; test `test/mb2c-google-news-publisher-split.test.mjs` extracts the LIVE regex literal out of the shipped source (rather than a hand-copy that could drift) and covers the hyphenated-publisher regression plus every dash-before-separator regression case named above. Deployed to LCC Opps (v23 → v24), body re-read to confirm. Forced live: today's `sector_news.dialysis` carries `"publisher": "Honolulu Star-Advertiser"` on a real Google News item captured on 2026-09-14 — the exact pre-fix failure (`publisher: null`, suffix stuck on the headline) does not reproduce. | ✅ | this change 2026-09-14 |
| MB3 | ✅ **BUILT 2026-09-12 (MB-b), flag `MARKET_BRIEF_RENDER` OFF, NOT live-verified.** Daily "Lane Briefs" block in the morning email (`renderMarketBriefLanes`, `api/_handlers/briefing-email-handler.js`), sits above §8 Sector Watch (unchanged, kept below it). Per lane with live facts: fact-diff "what changed since yesterday" (`diffFactSets`, against the prior frozen `market_brief_issues` daily row), the 2–3 most material live facts (`selectTopFacts`, section-weighted), named gaps rendered plainly (`selectGapFacts`), "Read the full brief →". A lane with no live facts is omitted, not shown empty. Freezes one `market_brief_issues` row per lane per day, idempotent on the EB1 `(lane, issue_type, issue_date)` unique index. Every number traces to a fact object — pinned by a dedicated tripwire test over the rendered HTML. Shared fetch/diff/select logic: `api/_shared/market-brief-render.js`. Guards: `test/market-brief-render.test.mjs` (14), `test/market-brief-lane-briefs-email.test.mjs` (11). Full suite 6,114/0/6-skipped. **NOT deployed/live-verified this session** (no Railway/Supabase write access) — operator must apply the new migrations, redeploy, run the P-SQL tick once, preview the block, THEN flip the flag. 🚨 **PARTIALLY UNBLOCKED, VERIFIED LIVE 2026-09-12 (Cowork): the redeploy already happened.** `net.http_get` against `tranquil-delight-production-633f`'s `/version` returns `54ca77699efe`, confirmed (`git merge-base --is-ancestor`) to be 10 commits past the MB-b merge (`94a08eca`) — the code is live. **What is still actually missing: the two MB-b migrations** (`20260912120000_lcc_mbb_rss_dialysis_stream_cron.sql`, `20260912121500_lcc_mbb_market_brief_render_flag.sql`) — `MARKET_BRIEF_RENDER` does not exist yet in `feature_flags_registry`, confirmed live. Both are additive, idempotent (`ON CONFLICT DO UPDATE` / unschedule-then-reschedule), default OFF, and carry their own reversal runbooks — low-risk to apply. **The remaining live-verify calls (`GET`/`POST /api/market-brief-psql-tick`) return `401 Authentication required` from this session — needs an operator's `X-LCC-Key`.** ✅ **LIVE 2026-09-12 (MB-b, PR #2391; Cowork applied the migrations and flipped the flags, verified on deployed `6b28835f28ac`).** The daily email renders the Lane Briefs block above Sector Watch — yesterday-diff, top facts, named gaps, link to the tab — and freezes one `market_brief_issues` row per day. ✅ **LIVE 2026-09-12 (Scott, in parallel with the above) — the whole sequence actually ran.** `MARKET_BRIEF_PSQL` flipped on 14:48 UTC, the tick wrote **31 live facts** (14:48:51), the render froze a `market_brief_issues` row for `dialysis`/`daily`/2026-09-12 (14:49:15), and `MARKET_BRIEF_RENDER` is now `state='on'` (confirmed live). **Cowork applied both migrations 2026-09-12** (`...mbb_rss_dialysis_stream_cron.sql`, `...mbb_market_brief_render_flag.sql`) — the second's `INSERT` found the flag row already existed (`state='on'`, `owner='scott'`), so it went through `ON CONFLICT DO UPDATE` and left the live `on` state untouched, exactly as designed. `MARKET_BRIEF_PRSS` stays off (no dialysis RSS content yet) — unrelated to this surface being live. | ✅ live | spec §4 · MB-b 2026-09-12; redeploy confirmed 2026-09-12 (Cowork); migrations applied + live 2026-09-12 (Cowork/Scott) |
| MB4 | ✅ **BUILT 2026-09-12 (MB-b), flag `MARKET_BRIEF_RENDER` OFF, NOT live-verified.** Homepage "Market Briefs" tab `#/briefs/<lane>` (default `dialysis`) — new `app.js` route (`ROUTE_SLUG_TO_PAGE.briefs`, sub-path parsed like `#/inbox/<id>`), new `pageMarketBriefs` page in `index.html`, rendered by `renderMarketBriefsPage()` from `GET /api/market-brief-tab` (new handler `api/_handlers/market-brief-tab.js`, registered in `server.js`). Live facts by section (operators/policy/capital_markets/trades/implications), citation + staleness per fact, issue archive, changed-since diff (the same `diffFactSets` MB3 uses). Read-only; returns `{enabled:false}` while the flag is off, never a 404/500. A small teaser sits beside `#dailyBriefingWidget`. Guard: `test/market-brief-tick-handlers.test.mjs` additions. **NOT deployed/live-verified this session.** 🚨 **Same redeploy-confirmed status as `MB3` (Cowork 2026-09-12) — code is live on `tranquil-delight` (`54ca77699efe`), only the `MARKET_BRIEF_RENDER` flag migration and an authenticated live-verify call remain.** ✅ **LIVE 2026-09-12 — same sequence as `MB3`** (migrations applied by Cowork, flag confirmed `on`, 31 live facts + a frozen issue row for `dialysis`/2026-09-12). ✅ **LIVE 2026-09-12.** `#/briefs/<lane>` + `GET /api/market-brief-tab` return `enabled:true, has_facts:true` with live sourced facts and an archive; a teaser widget sits beside the daily-briefing panel. | ✅ live | spec §4 · MB-b 2026-09-12; redeploy confirmed 2026-09-12 (Cowork); migrations applied + live 2026-09-12 (Cowork/Scott) |
| MB9 | ✅ **BUILT 2026-09-12 (Cowork) — collapsed `net_lease`/`broad_net_lease` into one `net_lease` lane, per Scott's explicit call (redundant for his purposes) + homepage widget redesign.** Verified via Supabase MCP before changing anything: **zero rows existed under either lane** in `market_brief_facts` or `market_brief_issues` (both are dialysis-only today — no producer for gov/NL exists yet, see below), so this is a pure constraint/view narrowing, no data migration. Changed: `KNOWN_LANES`/`LANE_LABELS` (`api/_shared/market-brief-render.js`), `MARKET_BRIEF_LANES`/`MARKET_BRIEF_LANE_LABELS` (`app.js`, replacing the old inline `laneTabs`/`laneLabels` literals so frontend/backend can't drift independently again), `chk_mbf_lane`/`chk_mbi_lane`/`v_market_brief_staleness` (migration `20261101200000_lcc_mbb2_lane_collapse_net_lease.sql`, **applied live**), `EXEC-BRIEFS-SPEC.md` §0 swimlane row. **Homepage widget redesign (same change, Scott's other ask):** `renderMarketBriefsWidget()` no longer dumps the top-2 raw fact bullets from dialysis only (this read as "two dialysis briefs" with no lane label) — it now shows one short snapshot line per *enabled* lane (`<Lane> — N live facts`, the single freshest claim, "Open full brief →"), and a lane with the flag on but zero facts (government, net_lease today) renders one muted "no live facts yet (producer not built)" line instead of being silently omitted — so the missing government/net-lease content is visible and explained in the product itself, not just in this doc. Guards: `test/mbb2-lane-collapse.test.mjs` (4, new — structural guard mirroring `eb1-market-brief-foundation.test.mjs`'s comment-stripped-SQL pattern), `test/market-brief-render.test.mjs` + `test/market-brief-tick-handlers.test.mjs` (2 assertions updated to 3 lanes). Targeted suite: 53/53 pass, 0 fail (`market-brief-render`, `market-brief-tick-handlers`, `market-brief-lane-briefs-email`, `mbb2-lane-collapse`); full 423-file suite not run to completion in this session (device shell's per-call timeout can't cover it) — every `KNOWN_LANES`/`broad_net_lease` call site was grepped and confirmed covered by the targeted tests instead. **NOT built here, stated plainly (this is the real reason gov/NL show no content — not a bug):** no government or net-lease producer exists (`buildAllLaneBriefContexts` only ever had dialysis data to iterate) — MB1a/MB2's P-SQL/P-RSS producers are dialysis-only by original scope (spec §3 "no new gov/NL lanes here"). Building a government producer (GSA lease-event source, per MB1a's own note) and a net-lease producer (needs the general-NL on-market store MB8/BUY0/UX-T4 gap closed first) are separate, unscoped decisions for Scott to sequence — not silently started in this pass. | ✅ live | Scott 2026-09-12; Cowork 2026-09-12 |
| XB1 | **Collector** — `scripts/build-brief-collector.mjs` (repo-side: branch debt, orphaned prompts, doc sizes, GENERATED-file changes) + `public.lcc_build_brief_db_audit()` (DB-side: flag-long-dark, producer-stall-not-flag-gated, market-brief lane staleness), merged into one `build_brief_snapshots` row. ✅ **SHIPPED 2026-09-14** — migration `20260915120000_lcc_xb1xb2_build_brief_db_audit.sql` applied live to LCC Opps; `.github/workflows/build-brief-collector.yml` runs on push to `main` + nightly (11:30 UTC) + `workflow_dispatch`; read endpoint `GET /api/admin?_route=build-brief-latest`. **NO dashboard** — `#/exec` (XB3) and the Ollama narrative (XB4) are separate, unbuilt items; this ships the deterministic half only. Guard: `test/xb1-xb2-build-brief-collector.test.mjs` (19 tests, positive+negative control per rule). First live snapshot **id=1, 32 findings** — see XB2 for the table. ✅ **SHIPPED + VERIFIED LIVE 2026-09-15 (Cowork), and it is already running unprompted.** PR #2456 merged; `build_brief_snapshots` holds **2 rows** — snapshot 2 carries `commit_sha 3f391fb1` and was generated **11:42 UTC by the GitHub Action on the merge itself**, not by hand. 32 findings, reproducing the hand-run. | ✅ live, collector self-running | spec §5 |
| XB2 | **Audit rules**, scoped to what is deterministically answerable in this unit (doc contradictions, dated-blocker re-measure age and "flags ON with no consumer" are explicitly OUT — too fuzzy to detect without false positives per the rule below; filed as XB2-followup). ✅ **SHIPPED 2026-09-14 — reproduced the hand-run's numbers exactly, live:** `flag_long_dark` **15 rows** (11 critical ≥60d, 4 warn ≥21d; oldest `SF_LIST_SEED_INSTITUTION` 107d, was 3.5mo when hand-measured — grew, as expected) · `producer_stall_not_flag_gated` **1 row**, `sidebar_contact_guard` (now 69 runs / 69 skipped / 0 completions, up from 31 when hand-measured — `p_rss` correctly excluded, its skips are 100% `flag MARKET_BRIEF_PRSS is off`) · `market_brief_lane_stale_or_missing` **11 rows** (reused EB1's `v_market_brief_staleness`, never re-derived) · `doc_size_approaching_budget` **1 row** (STATUS.md 2,411/3,000) · `prompt_without_matching_response` **4 rows** (BR1, ID3b, ID3d, XB1-XB2 itself — current top-level `prompts/` population, smaller than the hand-run's "4 of 8" because most of that day's prompts have since moved to `prompts/done/`) · `remote_branch_debt` **0 in this sandbox's shallow-fetch checkout** (a real CI run with `fetch-depth: 0` will report it; **local branch clutter (Scott's 618) is structurally invisible to a fresh CI clone — the workflow reports a different, real `origin/*` population, never a proxy for the local one**, stated in the collector's own file header). **32 findings total**, snapshot id=1. ⭐ **The rule refinement the hand run produced is preserved exactly**: `producer_stall_not_flag_gated` is defined as "skips NOT matching `^flag \S+ is off$`, zero completions ever" — a flag-gated skip streak of any length can never appear in this finding, it surfaces instead via `flag_long_dark` once the flag itself is old enough. ⚠️ **The orphan-prompt false positive is fixed, not papered over**: matching is now on the LEADING ID TOKEN(S) as a case-insensitive substring, checked **recursively** across `responses/` (including `responses/done/`), so a response filed as `MB2b desktop response.docx` for prompt `MB2b-...md` matches; test file proves both the fix and its own honest miss case. ✅ **VERIFIED LIVE 2026-09-15 (Cowork) — the rule refinement holds STRUCTURALLY, not just in prose.** `producer_stall_not_flag_gated` fires on exactly one subject (`sidebar_contact_guard`) and stays silent on `p_rss`, whose skips are flag-gated — the SQL excludes `^flag \S+ is off$` rather than relying on a human to remember the distinction. Orphan-prompt false positive is also fixed (matches the leading ID token across `responses/`, not a filename prefix): 4 open, and they are the genuinely open 4. | ✅ live | spec §5 |
| XB2-precision | 🔴 **Two precision gaps in the first live snapshot, measured 2026-09-15 (Cowork) — both are the failure mode XB2 was scoped to avoid.** **(a) The biggest number is measured and silent.** The collector records `branches.total_remote = **1,722**` in the payload (up from 1,718 the day before) but **no rule fires on it**, so it never becomes a finding — the same "recorded but not surfaced" shape as `item_count` before MB2b added `items_after_cutoff`. Related: `branches.unmerged` reads **0** because a CI clone has no local branches, so **local branch debt is unmeasurable from CI by construction** — only the REMOTE count works there, which makes `total_remote` the metric XB can actually carry (see **BRANCH2**). **(b) One known gap is inflated into 10 critical findings.** `market_brief_lane_stale_or_missing` emits per lane×section, so `government`×5 sections + `net_lease`×5 = **10 of the 32 findings (31%)** all restating a single fact: no producer has ever written a government or net-lease fact. ⚠️ **CORRECTION 2026-09-15 (Cowork): the second half of this finding was WRONG and is retracted.** I wrote that `v_market_brief_staleness` "reports staleness on a retired lane" because MB9 collapsed `net_lease`. **MB9 collapsed `broad_net_lease` INTO `net_lease` — `net_lease` is the SURVIVOR**, and the view's three lanes (`dialysis`, `government`, `net_lease`) match `KNOWN_LANES` in `api/_shared/market-brief-render.js` exactly. The view is correct; I misread a collapse as a retirement and asserted it without checking the lane constant. **The real fix is aggregation only:** emit one finding per lane with the sections as detail, instead of one per lane×section. A brief where a third of the findings are one known gap is on its way to being a brief nobody reads. 🟢 **Prompted 2026-09-15 with the trend measured, not just the level: remote branches went **1,718 (09-14) → 1,722 (09-15)**, ~4/day — growth is the actionable half, and the rule should report it. Prompt: `prompts/XB2-precision-the-brief-is-31pct-one-known-gap.md`, which also carries the retraction above so the lane list is left alone. Expected outcome stated as an acceptance target: findings drop **32 → ~23** with `branch_debt` newly present. ✅ **SHIPPED 2026-09-15** — (a) new `branch_debt` rule (`branchDebtFinding`, `scripts/build-brief-collector.mjs`) fires on `total_remote` alone (warn, warnAt=200, stated reasoning: never critical, a count is a debt LEVEL not a blocker), carries a growth-rate trend when a prior snapshot's total is fetchable (`fetchPriorBranchTotal`, best-effort, silent on any failure), independent of the CI-unmeasurable `unmerged` count. (b) `lcc_build_brief_db_audit()`'s `market_brief_lane_stale_or_missing` rule now emits ONE finding per LANE (not per lane×section) with `missing_sections`/`stale_sections` arrays in `measured` and both counts in `detail` — severity critical iff any section is missing. Lane list (`dialysis`/`government`/`net_lease`/`broad_net_lease`) left untouched per the retraction. 6 new/updated unit tests, all pass (22/22 in `test/xb1-xb2-build-brief-collector.test.mjs`). Not yet re-measured live post-deploy (the before/after 32→~23 table is the next step, on the next collector run against the redeployed migration). ⚠️ **RECONCILED 2026-09-15 (Cowork) — the code shipped, the MIGRATION WAS NEVER APPLIED, so the deliverable never happened.** PR #2460 merged and `main` carries both halves, and the JS half is live (`branch_debt` fires in every snapshot from 12:52 onward). But the **live `lcc_build_brief_db_audit()` still had no `GROUP BY`** — it was emitting one finding per `(lane, section)`, so snapshots 7–11 all read **32 findings with 11 lane rows**, unchanged. The prompt's stated deliverable was a before/after findings table after applying and redeploying; the "after" never existed. ✅ **Applied live by Cowork**: DB-side findings **27 → 19**, lane rule **11 → 3** (`government`, `net_lease`, `dialysis`), flags 15 and stall 1 unchanged — so the next collector run should read **≈24** total against the prompt's predicted ~23. → the class is filed as **DEPLOY2-unapplied**. ✅ **SHIPPED + VERIFIED LIVE 2026-09-15 (Cowork), acceptance target met.** Snapshot 13: findings **32 → 24** (predicted ~23), `market_brief_lane_stale_or_missing` aggregated **11 → 3** (one per lane), and `branch_debt` present (subject `origin`, warn). The collector has now produced **13 snapshots in one day**, each tied to a merge commit — it is genuinely self-running. ⚠️ One finding in that snapshot is still wrong and it is the only `producer_*` one → **XB2-counter**. ✅ **XB2-counter SHIPPED 2026-09-15** — `v_build_brief_producer_stall` now excludes any producer that has never run with `trigger_source='cron'`; findings **24 → 23**. 🔭 Note the new shape of the brief: `flag_long_dark` is now **15 of 24 findings (62%)** — that is not a monitor defect, it is a real backlog awaiting a decision from Scott (11 flags dark >60 days, oldest since 2026-05-30). | ✅ live | Cowork 2026-09-15 |
| DOC-TABLE3 | ✅ **SHIPPED 2026-09-15 (Cowork) — 27 backlog rows were invisible to every guard in this file AND rendered as literal text on GitHub.** Found while measuring DOC-TABLE2: the unescaped-pipe count came back **21 from parsed tables but 25 from a raw scan of the file**, and chasing the 4-row gap showed the rows were not in any table at all. **Three spans, two causes:** a stray BLANK LINE inside a table (13 rows from `C13h`, 12 rows from `XB2-counter` through `MISPARSE-BACKLOG1`) and two rows (`CONSOLIDATE3`, `DOC-TABLE1`) prepended **above their own header**. ⭐ **This is I11 applied to a doc guard — a monitor must alert on its own blindness.** Every detector in `backlog-table-shape` iterates `parseTables()`, so an orphaned row is silently exempt from the width, duplicate-ID and pipe checks; a row could be doubly broken and pass. Proven, not asserted: a fixture with a duplicate ID hidden behind a blank line reports `duplicateIds 0` and `orphanRows 1`. **Fixed mechanically** (deleted 2 blank lines, moved 2 rows below their separator) and the baseline is **0**, not a ratchet — there is no judgment call here. 🔍 **Re-measuring with the 27 rows newly visible moved two other baselines: wrong-width 45 → **39**, duplicate IDs 14 → **0**.** The duplicate ratchet is now fully paid off and locked at zero, which is what actually stops the merge-keeps-both-sides regression DOC-TABLE1 was written about. | ✅ | Cowork 2026-09-15 |
| MISPARSE-BACKLOG1 | 🟡 **44 `contact_misparse_review` items sit `new` — oldest 2026-08-10 (~36 days), average age 4.9 days (Cowork, measured 2026-09-15).** Found while disproving SIDEBARGUARD1. A human HAS been working the queue (105 dismissed of 149 raised since 2026-08-10), so this is a backlog rather than a dead surface — but it matters more than a normal queue because **HP1-P2misparse is the thread about this guard rejecting REAL people**, and `person_junk_name` is the dominant rejection reason across the 166 blocks recorded. Every unreviewed item is a contact the system decided was junk and nobody confirmed. 👤 Needs a pass through the 44, and a decision on whether an ageing-review alert is warranted (the OC-v2 `operator_note_stale_open` monitor is the pattern to copy if so). 🔍 **TRIAGED 2026-09-15 (Cowork) — the 44 were measured by reason, and the queue splits cleanly in two.** `person_junk_name` (71 rejections) is **~99% correct**: "Marcus & Millichap" x16, "Demographics" x7, "Cushman & Wakefield" x5, "View Less" x4, "Equity Funds"/"Vice Chairman"/"Public REIT"/"CoStar Property Contact" x2-3 — firms, page furniture and scraped labels, exactly what the rule is for. **One miss: "Brian Lane"** (a real person's name whose surname is a street word). ⚠️ **`email_fanout` (26 rejections) is the problem, and it confirms HP1-P2misparse's worry while localizing it to ONE rule.** Roughly half are **real, named brokers**: Edward C. Mann (x2), Bradley Lagomarsino, Clifford L. Lamar, Conrad Buhler, Dail Longaker, Debbie Gallimore CCIM CIPS, Drew A. Flood, Jacob Fahner, James D. Collins, Nancy J. Bouton, Paul J. Collins, William M. Collins — mixed in with genuine junk ("Gross Income", "Vacancy", "PO Box 61381") and firm names (NAI Columbia, NAI DESCO, Southpace Properties). ⭐ **The FOURTH instance of "one signal, two meanings"** (after XB2-counter, `flag_long_dark`, and DOC-TABLE2): one email address on several contacts means either *a shared/generic inbox behind scraped junk* or *a listing that legitimately names several brokers at one firm*, and `email_fanout` cannot tell those apart. 🟢 **Prompted 2026-09-15: `prompts/MISPARSE1-email-fanout-is-blocking-real-brokers.md`** — scoped to `email_fanout` only, with an explicit instruction **not to weaken `person_junk_name`** (it is working) and an explicit prohibition on bulk auto-accepting the blocked contacts. `misparse_name` (3 items) also carries "Brian Lane", under a second reason. ✅ **SHIPPED 2026-09-16 (MISPARSE1) — the split is the mailbox's LOCAL PART, and it was measured on the exact 15-row `email_fanout` fixture from this row before committing.** `isGenericMailboxLocalPart()` (`api/_shared/misparse-disposition.js`) names role inboxes (`info@`/`leasing@`/`admin@`/`pm@`/…); a personal-shaped address that fans out to several DIFFERENT person-shaped, non-org names is now `recoverTeamRosterBatch()`'s to admit — strictly additive to the existing single-owner `recoverFanoutOwner`, never applied to a generic mailbox. **On the fixture: strict-only recovered 4 of 12 named real brokers; strict + roster recovers 9 of 12** (Clifford L. Lamar, Conrad Buhler, Edward C. Mann, Drew A. Flood, Paul J. Collins newly admitted). The junk in the same batches (`NAI Columbia`, `NAI DESCO`, `Southpace Properties, Inc.`, `Special Projects & Consulting`, `View Less`, the marketing-headline row) stays blocked — the widening never touches page chrome, organizations, or a name that fails the 2–5-token person shape. Separately strengthened `person_junk_name` so the genuine junk that had been leaking into `email_fanout` (`Gross Income`/`Other Income`/`Net Income`/`Revenue`/`Vacancy`/`Occupancy`/bare `Trust`, `PO Box #####`, the `NNN leased`/`corporate guaranteed` marketing-headline shape, the `NAI <City>` franchise-brand prefix with no suffix word) is now caught **before** it ever reaches the fan-out check, on both `hasFirmSuffix()` (`entity-link.js`, shared with `contactEntityType`) and `tmMisparseReason()` (`tm-misparse.js`) — never a second copy. `person_junk_name` itself is UNCHANGED (per the prompt's instruction). Guard: `test/hp1-p2misparse-guard-disposition.test.mjs` (+9 tests, 23/23 pass). Full suite: 6458/6458 pass, 0 regressions. **Residual, not fixed here:** Bradley Lagomarsino, Nancy J. Bouton, Debbie Gallimore CCIM CIPS (named in this row but not in the measured 15-row fixture — likely on generic mailboxes or a different batch; re-run the live queue post-deploy to confirm they clear). | ✅ shipped | MISPARSE1 2026-09-16 |

## HP1 — 🏠 HOMEPAGE ATTENTION SURFACE (Scott, 2026-09-12) — the Today 500, stale My Work, mailbox Inbox

| # | Item | State | Source |
|---|---|---|---|
| BACKLOG-ids | 🚨 **ORIGINAL FINDING (2026-09-12) — 28 backlog IDs are used more than once, and at least one pair is a genuine COLLISION between two different issues.** `SEC2` is **`wave0-config-values.txt` is tracked in git** under **§P0s line 263**, *and* **rotate the Supabase `service_role` key** under **§P9 line 624**. A reader told "see SEC2" gets a coin flip — and **it already misfired: I folded `HP1-P1a-sec` into "SEC2" without knowing there were two.** Same shape on `SEC1`/`SEC3`/`SEC4` (162-165 vs 623-626), `A5d`/`A5e` (74/75 vs 444/445), `D1` (288 vs 544). Others are restatements rather than collisions: `MB3`×4, `MB4`×4, `MB2a`×3, `B6d-cms-restart`×3. **The two classes need opposite fixes** — a collision must be RENAMED (and every citation updated), a restatement must be COLLAPSED — so this needs a measured pass, not a bulk edit, plus a CI guard that fails on a duplicate ID the way `status-header-integrity` now guards the STATUS H1. ✅ Already fixed in place: three byte-identical `HP1-P1a-sec` rows → one, two `HP1-P1a-fix` rows → the richer one, and `HP1-P1b`'s missing pipe space. ✅ **DONE 2026-09-12 (Claude Code) — the full pass this row asked for.** Re-measured from scratch (26 by the follow-up prompt's count, **27 by a fresh scan** — it had missed `COPILOT-OPEN`, whose ID carries a trailing `(was COPILOT-CHAT-OPEN)` annotation that broke a naive parser, and `ID3e`/`ID3e (original measurement)`). **13 Class-A collisions renamed** to a free ID in their own section's series (`SEC1-4`→`SEC9-12`, `A5d/A5e`→`A5i/A5j`, `D1/D3/D4/D5`→`D1-monitor/D3-digest/D4-recall/D5-dealname`, `R1/R2/R3`→`R1b/R2b/R3b`), every real external citation checked and none needed updating (`OPERATOR-ACTIONS.md`'s `SEC2` cite already pointed at the kept §P0s row). **14 Class-B restatements merged** into one row apiece keeping every distinct fact — including `MB3`×4→1, `MB4`×4→1, `B6d-cms-restart`×3→1 as this row anticipated, plus `AC2`, `AC3`, `B6d-cms-escalation`, `B6d-cms-step`, `B6e-fred`, `B6e-fred-cm-exposure`, `B6e-fred-verify`, `COPILOT-OPEN`, `ID3a-d`, `PR1d` this row did not enumerate. **One genuine disagreement found and reported, not silently picked**: the two `ID3a-d` copies disagree on whether `ID3a-d-dia` is closed or still open — both readings kept in the merged row with the conflict stated explicitly. `docs/os/PLANNED-BACKLOG.md` **1172 → 1154 lines**; no distinct fact lost (verified per merged group). **Guard shipped:** `test/backlog-id-uniqueness.test.mjs` — exactly the CI guard this row called for, modeled on `test/status-header-integrity.test.mjs`, positive-controlled both ways (a seeded prose cross-reference is not counted as a duplicate row; a seeded real duplicate row fails with the repair procedure in the message — verified live against this file, not assumed). Full record: `docs/claude-code/STATUS.md` 2026-09-12. | ✅ | backlog-id audit 2026-09-12; fixed by Claude Code 2026-09-12 |
| HP1-P2f-urgent | ✅ **SHIPPED + RUNNING 2026-09-14** (PR #2419; deployed SHA `ba22b8abac78` == `main`, `/version` confirms — CC's own "takes effect after the next redeploy" caveat is resolved). `contact_writeback` is out of Urgent's ranked union **and** its `total_open`, and surfaces instead as a persistent pointer carrying the true uncapped count, linking to the BD worklist's pre-existing **"Push to CRM"** chip — that destination was not touched. **Urgent ≈ 1,664 → 59** rows of real deal correspondence; **1,603** moved. Guards: `uxt1a-today` + `hp1-badge-today-total-open` assert `contact_writeback` never reaches Urgent's items or `total_open` at any `rank_value`, and that the pointer is `null` (= unknown) when unmeasured but a genuine `0` when that is the answer. Significant/Important byte-identical. ⚠️ Population moves: **1,664 → 1,730 → 1,669 → 1,603** across four reads in a day — quote it at read time. 📘 Map: `docs/architecture/HOMEPAGE-ATTENTION-SURFACE.md`. | ✅ | HP1-badge prompt 2026-09-12; shipped 2026-09-12 |
| HP1-P1a-fix | ✅ **CLOSED 2026-09-12 — verified on `UPDATED_not_inserted = 608`, `brand_new_rows = 0`.** PR #2371 + both services redeployed; **two silent defects found on verification and fixed live**: (a) the migration had **never been applied** (`pg_proc` had no such function — the redeploy would have 404'd all 608 deals per run, i.e. made the feed worse), (b) the function as written raises **42702** on its first call (OUT params `sf_opp_id`/`entity_id` collide with columns; a plpgsql body is not parsed at CREATE time). Corrected by `20261101170100_lcc_hp1p1a_upsert_rpc_fix_out_param_ambiguity.sql` (OUT params `out_`-prefixed, INSERT aliased `AS t`; no JS change). **The 12:47 UTC PA run wrote 608 UPDATEs** — the first this path has ever produced. Six weeks of drift landed as **10 stage changes / 6 close-date changes / 4 newly closed / 0 amount changes / 0 new rows**, all recognisable live deals. ✅ **Unit 4 held** — `closed_at` moved on exactly the 4 genuine transitions; the 569 already-closed rows kept their original timestamps. ⚠️ **One backward stage move** (*Essentia Health — Hinckley MN* `loi_executed` → `listing_signed`) — 👤 Scott's eye: real re-trade or SF hygiene. LCC now mirrors Salesforce faithfully, errors included. ⚠️ **The mass-auto-retire warning was misdirected** — there is no `deal_next_step` table; `lcc_generate_deal_next_steps()` writes into **`action_items`**. ℹ️ A parallel session filed this row 🟡 "live run not yet confirmed" at 12:59 UTC on the reading *zero rows synced since 2026-09-09* — correct when taken, superseded 12 minutes later by the 12:47 UTC run landing. Duplicate row collapsed into this one; the earlier reading is preserved in `STATUS.md`. | ✅ | HP1-P1a-fix verified 2026-09-12 |
| HP1-P1a-rpc | ✅ **DONE 2026-09-12 — recorded so the class is not repeated.** A merged migration is not an applied migration, and an applied plpgsql function is not a working one. Both halves failed here silently. **Standing check for any RPC-shipping prompt from now on:** after deploy, (1) assert the function exists in `pg_proc`, and (2) execute it once inside a rolled-back transaction against a real row — `CREATE` success proves neither. A `RETURNS TABLE` OUT parameter that shares a name with a column of the table the body writes is a 42702 waiting for its first call; prefix them. | ✅ | HP1-P1a-fix verify 2026-09-12 |
| REPO1-root-clutter | ✅ **DONE 2026-09-12 — swept by the filed method, not in bulk.** Grepped every candidate first. **Moved (12, all verified unreferenced outside `STATUS.md`/`docs/history`):** `err.txt`, `draft1/draft2/draftsave.json`, `harvest.json`, `twin.json`, `seed-apply/seed-dryrun.json`, `acq-dryrun.json`, `fix-allother-pagination.patch`, `_commit.bat`, `_deploy_hardening.bat` → `_superseded/scratch-2026-09-12/`, with a manifest row in `_superseded/README.md`. **Deliberately NOT moved, and documented instead** (the graveyard README's own "left in place" convention): the **15 `flow-*.json`** — referenced from `CLAUDE.md`, `.env.example`, code comments and ~10 docs, so a move makes a dozen accurate references stale; indexed by topic at **`docs/flows/README.md`** instead. The **~10 loose `.docx`/`.xlsx`** — cited from `audit/ROUND_2_FINDINGS_2026-05-19.md` and several `audit/patches/*/COMMIT_MSG.txt`; historical, nothing live reads them. **`wave0-config-values.txt` and `ACTIVATE_unit4.sql`** — the former is allowlisted **BY PATH** in `test/retired-identifiers-guard.test.mjs` (a move breaks the guard) and is a secrets item (**SEC2**), the latter is cited in `docs/architecture/document-capture-ocr-and-deeds.md`. | ✅ | root-clutter sweep 2026-09-12 |
| HP1-P1d | ✅ **SHIPPED 2026-09-12.** Extended `producer_runs` (already producer-keyed, already built for this shape, holding only 2 EB1 rows before this) rather than adding a table-keyed `feed_freshness_registry` row — that shape cannot express the `sf_opp_id IS NOT NULL` filter and would have gone green on the priority_queue producer's writes over a dead SF pipe (the exact B6a trap). `mcp/opportunity-sync.js::ingestBatch` now writes ONE `producer_runs` row per batch (`producer='sf_opportunity_sync'`, `facts_written` = the RPC's own inserted+updated tally, never `succeeded`), on a 3-arg `opsQuery` call only — no 4th-arg options object, so the standalone-MCP Prefer-header-mangling class of bug (P1a-fix's own root cause) cannot recur here. Separately, migration `20261101180000_lcc_hp1p1d_sf_opportunity_feed_freshness.sql` ships `lcc_check_sf_opportunity_freshness(p_stale_hours numeric DEFAULT 3)` — the actual freshness ASSERTION, on `max(bd_opportunities.last_synced_at) FILTER (WHERE sf_opp_id IS NOT NULL)`, never bare `last_synced_at` or `updated_at` — opening/auto-resolving an `lcc_health_alerts(alert_kind='sf_opportunity_feed_stale')` row on the EXISTING generic `v_lcc_health_alerts_open` + Teams-push surface (no new dashboard), cron `lcc-sf-opportunity-freshness-check` hourly. **Confirmed live, investigation claims 1–4 all held exactly as filed**, including the `salesforce_sync`-watches-`sf_sync_log` finding (untouched, correct). ⚠️ **A KNOWN, STATED LIMITATION, measured not theoretical:** a MAX-based predicate over a mixed population does not stay frozen during a real outage — a genuinely NEW `sf_opp_id` still INSERTs cleanly (no ON CONFLICT collision) even while every UPDATE of an existing row silently 502s, so the check flickered green for a few hours around each of six sporadic new-deal inserts (2026-08-04, 08-20, 09-03, 09-07×2, 09-09) and red the rest of the time — **13 of 16 historical checkpoints across the outage window would have fired**, replayed both live (read-only SQL against `created_at`, since zero UPDATEs ever landed pre-fix) and in an offline JS shadow model (`test/hp1-p1d-sf-feed-freshness.test.mjs`). All four required positive controls run live in rolled-back transactions AND pinned as automated tests (green-now / simulated-stale-fires / historical-replay / non-SF-write-does-not-clear). PR: see `docs/claude-code/STATUS.md` 2026-09-12 entry for the branch/PR link. | ✅ | HP1-P1d 2026-09-12 |
| HP1-P2a | ✅ **SHIPPED 2026-09-12** — migration `20261101190000_lcc_hp1p2a_inbox_exclude_contact_qualify_hygiene.sql`, applied live to LCC Opps. **Re-measured before quoting the stale prompt figures**: `inbox_items WHERE status='new'` reads **1,061**, not 1,052 — `new_contact_qualify` **879**, `contact_misparse_review` **117**, `email_alert` **20**, `email_om`/`sidebar_om`/`folder_feed_om` **33**, `flagged_email` **12**. Only `new_contact_qualify` is excluded (at `v_inbox_triage`, the read every consumer — v1 `case 'inbox'`, `handleInbox`/`/api/inbox`, `v2GetInbox`/`/api/queue-v2?view=inbox` — shares); **`contact_misparse_review` has NO resolution surface anywhere in the repo** (zero readers in `api/` or the front end) and stays ON the Inbox — routing it off would have deleted the only place it is visible (P131). **The Inbox correctly reads 182, not 65** — the prompt's 65 assumed both hygiene lanes would leave; they don't, and `email_alert` (personal, HP1-P2b's scope) stays too. `mv_work_counts.inbox_new`/`inbox_triaged` carry the same exclusion so the header total agrees with the list (the QA-18 shape, avoided). Every `v_inbox_triage` consumer's response now also carries `hygiene_pointer {source_type, count, label}` — an EXACT, uncapped count read straight off `inbox_items` (never the view, never a page), so the excluded population is never silently dropped (HP1-badge's P159a trap, avoided in the same change). Rendered on the full Inbox page (`ops.js renderInboxTriage`) as a persistent row linking to the existing `renderContactQualifyWorklist()` surface (868 live rows there, confirming reachability); the small Today-page widget already benefits from the filter via its existing "View all N" total and was left without a duplicate pointer (scoped decision). Guard: `test/hp1-p2a-inbox-hygiene-pointer.test.mjs` (2 tests). Full suite: 6,083 pass / 0 fail. | ✅ | HP1-P2a shipped 2026-09-12 |
| HP1-P2misparse | ✅ **SHIPPED 2026-09-12** — migrations `20261102120000_lcc_hp1p2misparse_dispose_guard_notifications.sql` + `20261102130000_lcc_hp1p2misparse_contact_guard_block_surface.sql`, both applied live to LCC Opps; JS in `api/_shared/misparse-disposition.js` + `api/_handlers/sidebar-pipeline.js`; guard `test/hp1-p2misparse-guard-disposition.test.mjs` (17 tests). **The framing in my own earlier version of this row was wrong and the prompt superseded it** — these were never work awaiting a decision; they are the contact guard announcing, one Inbox row at a time, that it successfully blocked something. **⚠️ RE-MEASURED BEFORE BUILDING: 130 rows, not 117** (it was still growing), carrying **307 rejections across 42 distinct (name, reason) pairs / ~26 properties**. **Live result: `contact_misparse_review` `status='new'` 130 → 25 (−105); the whole Inbox 196 → 91; `mv_work_counts.inbox_new` 91 — the list and the header still agree (HP1-P2a's own invariant, held); `entities` 69,764 → 69,764, so not one guard decision moved.** Disposition split: `all_chrome` 15 · `chrome_and_duplicate` 39 · `all_duplicate` 51. **Two notification-only rules, neither of which touches mint-vs-block:** (1) class-A CoStar page furniture (`View Less`, `Demographics`, `Public REIT`, `Equity Funds`, `CoStar Property Contact`) is counted, never notified — **EXACT match, never a substring** (P158a: `Demographics Research Group LLC` must survive); (2) one notification per **(property, name, reason)**, not one per capture — that rule alone removed 90 of the 105 without changing a single decision. **B6a correctly applied:** every suppressed block emits to `producer_runs` (producer `sidebar_contact_guard`, lane `misparse_block`), read via the new **`v_lcc_contact_guard_blocks`** — ⚠️ it reads **0 runs today**, which is the Railway deploy gap (*merged is not running*), not a quiet guard; P180 — 0 runs means NOT MEASURED. **Class D recovered 4 real people** — James D. Collins ← `jcollins@southpace.com` (initial_last), William M. Collins ← `william.collins@cushwake.com` (first_last), Dail Longaker ← `dlongaker@trinity-partners.com`, Jacob Fahner ← `jfahner@hanleyinvestment.com` — with **0 false positives on the live batches** (`Paul J. Collins` and `Drew A. Flood` correctly refused on the same mailbox). ⚠️ **3 of the 4 already exist in `entities`; only James D. Collins is genuinely absent** → `HP1-P2misparse-jcollins`. Classes B and C deliberately left notified (deduped) → `BR1-misparse-handoff`, `HP1-P2misparse-titleparse`. ✅ **The class-D single-owner cap (only ONE recovery per fanned-out email) was widened 2026-09-16 — see `MISPARSE-BACKLOG1`** (`recoverTeamRosterBatch`, personal-shaped shared mailboxes only). | ✅ | HP1-P2misparse shipped 2026-09-12 |
| MISPARSE1 | ✅ **SHIPPED 2026-09-16 (LCC PR #2533), running on Railway `affc5d84` — `email_fanout` split by mailbox genericness.** `isGenericMailboxLocalPart()` + `recoverTeamRosterBatch()` in `api/_shared/misparse-disposition.js`: a personal-shaped shared mailbox that fans out to several distinct person-shaped, non-org names is admitted whole; a role inbox (`info@`, `leasing@`, `admin@`…) stays as strict as before. Live 15-row fixture: **4 → 9 of 12 real brokers recovered, 0 new junk**; `person_junk_name` strengthened (`hasFirmSuffix`/`tmMisparseReason`) for the junk that had been leaking into `email_fanout` (financial line items, `PO Box ####`, `NAI <City>`, marketing headlines) without touching the working rule. `test/hp1-p2misparse-guard-disposition.test.mjs` +9 (23/23); suite 6,458/0. The round wrote its own STATUS entry; reconciled by Cowork 2026-09-16 (prompt `prompts/done/MISPARSE1-…md`). Still open from the same family: BR1-misparse-handoff (firms parsed as persons — BR1's registry now exists to resolve against), HP1-P2misparse-titleparse, HP1-P2misparse-fp. | ✅ live | prompt 2026-09-15; shipped 2026-09-16 |

## P10a — Cross-lane property identity and address resolution (designed, not built)

| # | Item | State | Source |
|---|---|---|---|
| RECON3 | 🔴 **A second, independently-found reconciliation-gap case — 175 Righter Rd, Succasunna NJ (DaVita, `property_id=27266`), from Scott's own "Dialysis Property - Sept 22" SB note (SBN-19) plus his follow-up exported client report (SBN-20).** Different failure mode than RECON1's Banning clinic: `sales_transactions.buyer_name`/`seller_name` and `recorded_owners.name`/`canonical_name` hold **raw Salesforce Account IDs** (`0018W00002X08eTQAR`/`0018W00002XDlmDQAT`) instead of resolved names, on a real closed sale ($2,587,220, 2026-09-09) with `sf_deal_id` null. `properties.current_value_estimate` still reads the stale pre-sale $10.3M estimate — `reconcilePropertyOwnership()`'s value back-fill (`sidebar-pipeline.js:10347`) only fires when the field is empty, never when a more-authoritative closed sale arrives. The active lease's `tenant` is the property's own display-name string (`costar_sidebar` source, `lease_id 16621`) instead of `DaVita`. **New (SBN-20): the client-facing "Asset Profile" export (`_udExportOperations()`, `detail.js:5436`) has zero sale-status awareness** — Scott's exported PDF, generated two weeks after close, shows live Risk Assessment/Lease Expiration scoring with no SOLD indicator; the export never reads `pipeline_stage` or checks `sales_transactions`, so this same button could send a client an analysis of a property no longer under contract. Blast radius across the fleet not yet measured. Prompt: `prompts/RECON3-succasunna-sf-account-id-owner-and-value-backfill.md` (7 action items). Second concrete test case for RECON2's general `reconcile_property()` once that lands — no one-off reconciler should be built just for this property.  ✅ **SHIPPED 2026-09-22, PR `sbriggssjc/life-command-center#2608` (commits `c386de22`, `07473131`, `720aadcf`), merged and verified: 609 existing tests + 29 new pass, diff read line-by-line, not just the response summary trusted.** Real fixes: raw-SF-id name guard (`isJunkSalesParty`/`ensureEntityLink`), the single-tenant-fallback gap that let the listing-description sentence into `leases.tenant` (root cause more precise than the original guess), cohort Rent/SF+Census columns (3 render paths, not just 2), export SOLD-banner gate. ⚠️ **Two things need a follow-up round, `RECON3-b`:** (1) none of this is retroactive -- property 27266's own bad rows (buyer/seller/owner names, lease tenant, `current_value_estimate`) are still exactly as bad as SBN-19 found them, live-reconfirmed post-merge; (2) the new `current_value_estimate` overwrite guard has a **live-verified logic flaw** -- it uses `properties.updated_at` (a general last-modified stamp touched by routine background writers) as a proxy for "is the sale newer than the estimate", so on property 27266 itself (`updated_at` touched today by an unrelated process, sale 13 days old) the guard reads the sale as "older" and never overwrites -- reproducing the exact original bug on the exact property the fix targeted. Prompt: `prompts/RECON3-b-live-data-still-bad-plus-value-estimate-guard-flaw.md`. Parts (a)/(b)/(d) of the original 7-item prompt also still open (write-path not pinpointed, blast radius not measured, Documents links not touched). | ✅ closed 2026-09-22 (code + data + follow-ups verified live) | SBN-19/SBN-20 2026-09-22; shipped 2026-09-22; Cowork round 64 2026-09-22: state cell corrected. Every follow-up had closed in rounds 56–61 (RECON3-b shipped; lease 16621 tenant fixed round 57; gov checked round 57; `current_value_estimate` = sale price after the round-61 redeploy, Q40) |
| RECON3-b | 🔴 **PROMPTED 2026-09-22 (Cowork) -- reconciling RECON3's response against live Dialysis_DB.** RECON3's code shipped and is real (see that row), but two things surfaced only by checking the live property after the merge, not by reading the response alone: (1) property 27266's own bad rows are untouched -- none of RECON3's guards are retroactive, and the SF-id backfill migration it wrote was never applied (no DB egress in that sandbox); (2) the new `current_value_estimate` overwrite guard (`sidebar-pipeline.js`, ~line 10381) uses `properties.updated_at` as a proxy for "is the sale newer than the estimate", and that proxy is unsound -- `updated_at` is touched by every routine writer on the row (propagation, enrichment, sidebar sends), not specifically by valuation logic, so almost any past sale reads as "older" than the row's last touch. **Live-verified on property 27266 itself 2026-09-22 post-merge: `current_value_estimate` is still $10,257,374.40, reproducing the exact bug the fix targeted, on the exact property it was written for.** Prompt: `prompts/RECON3-b-live-data-still-bad-plus-value-estimate-guard-flaw.md`.  ✅ **SHIPPED 2026-09-22, PR `sbriggssjc/life-command-center#2610` (commits `1c579a04`, `7bbb1c3d`), merged and independently verified against live Dialysis_DB, not just the response's own claims.** (2) is fixed: the `saleIsOlder`/`properties.updated_at` heuristic is removed entirely -- a closed sale now unconditionally overwrites a disagreeing estimate (option (a) from the RECON3-b writeup). (1) is partly done: the previously-written backfill migration was actually applied live via Supabase MCP -- dry-run first (`v_dia_recon3_sf_id_as_name`), confirmed the fleet-wide blast radius was exactly the 5 rows already named on property 27266 (nothing else in Dialysis_DB carries this defect), cleared under a reversible batch tag (`recon3b_20260922`). Live-reconfirmed by Cowork: `sales_transactions.buyer_name`/`seller_name` (`sale_id 15170`), `recorded_owners.name`/`canonical_name`, and `true_owners.name` are all `null` now (not repaired -- the raw id was removed, not replaced with the real buyer/DaVita name, which still needs a manual lookup). 29/29 + 609/609 tests re-verified green. ⚠️ **Still open:** `current_value_estimate` is still live-stale ($10,257,374.40) -- the JS fix is merged to `main` but needs a Railway redeploy of `tranquil-delight` to take effect for real traffic (per this repo's own deploy doctrine), then a `reconcilePropertyOwnership()` run on this property to actually apply it; `leases.tenant` (`lease_id 16621`) was NOT retroactively fixed (needs a re-save or a targeted one-row correction to trigger the now-guarded write path); government DB was never checked for the same SF-id-as-name defect (migration is dia-only by design, a parallel gov migration would need to live in the `government-lease` repo); parts (a)/(d) of the original RECON3 prompt (exact write-path trace, Documents-tab links) remain untouched. | ✅ closed 2026-09-22 (redeploy done + verified, Q40; residual cleanup done round 57) | Cowork 2026-09-22; shipped 2026-09-22; Cowork round 64 2026-09-22: state cell corrected (was still "Railway redeploy + residual cleanup open") |
| SIDEBAR3-c | 🔴 **Scott's Q41 re-send (Orlando + Scranton, 2026-09-22) exposed two live defects in the `SIDEBAR2-b` range-address guard (`detectRangeAddressCollision`/`sameStreetRest`, `api/_handlers/sidebar-pipeline.js`).** (1) **Scranton silently minted a fresh duplicate property (`51252`, `920-1000 S Washington Ave`)** instead of attaching to the merged canonical `28547` (`920 South Washington Ave`) -- root cause live-confirmed in `sameStreetRest()`'s normalizer: it only strips ABBREVIATED leading directionals (`n`/`s`/`e`/`w`/`ne`/`nw`/`se`/`sw`), not spelled-out ones (`south`), so `"S Washington Ave"` (captured) and `"South Washington Ave"` (DB) normalize to different strings, the guard never fires, and a brand-new twin is created -- exactly what `SIDEBAR3` just merged away. **Already cleaned up live (Cowork, 2026-09-22):** `51252` held 1 sale/0 leases; merged back into `28547` via `dia_merge_property_reversible(28547, 51252, 'sidebar3_scranton_resend_accidental_dup_20260922')` (backup_id 598, reversible); live-reconfirmed `51252` gone, `28547` now 2 leases (unchanged) + 3 sales (was 2, +1 folded in). (2) **Orlando correctly triggered the guard and was refused** (`"4550-4666 S Kirkman Rd" is a near-miss (range_containment) of existing property_id=22887"`) -- but the guard has no memory of `SIDEBAR3`'s own completed merge (`37640`→`22887`, backup batch `sidebar3_kirkman_20260922`, `37640`'s address WAS this exact range), so this exact re-send will refuse forever with no path to ever land. Prompt written: `prompts/SIDEBAR3-c-range-guard-directional-spelling-and-post-merge-attach.md` -- asks for (1) completing `sameStreetRest()`'s directional token list (spelled-out N/S/E/W/NE/NW/SE/SW) + a live fleet scan for other silent twins this gap may have already minted since `SIDEBAR2-b` shipped 2026-09-18, and (2) teaching `detectRangeAddressCollision`'s refusal path to check `dia_property_merge_backup` for an already-completed, already-reviewed merge matching the near-miss candidate before refusing -- attach instead of refuse only when a human-reviewed merge decision is already on the ledger, never a new identity guess.  **✅ SHIPPED (CC, branch `claude/loving-noether-91ejk3`):** directional token list completed **and** the candidate-fetch hint fixed (the ilike hint `*S Washington*` could not fetch `South Washington` — the second half of the Scranton miss); merge-ledger attach via `findMergeLedgerConfirmation()` (exactly-one confirmed candidate, un-reversed, state agrees; PATCH keeps the kept property's address). Fleet sweep: 85 historical spelled-vs-abbreviated pairs, 0 minted since SIDEBAR2-b besides 51252 → `SIDEBAR3-d`. Test `test/sidebar3c-directional-and-merge-ledger.test.mjs`. **✅ INDEPENDENTLY VERIFIED (Cowork round 60, 2026-09-22):** diffed the actual merged commit (`886e9757`, PR `#2619`) rather than trusting the response -- confirms both fixes plus the extra bug CC found (the candidate-fetch `ilike` hint couldn't match spelled-out streets either, a second, real cause of the Scranton miss not in the original prompt). Ran `test/sidebar3c-directional-and-merge-ledger.test.mjs` (25/25) + full related suite (442/442), zero regressions. Live-confirmed the exact merge-ledger rows the fix depends on: backup_id 596 (`sidebar3_kirkman_20260922`, dropped address `4550-4666 S Kirkman Rd`, un-reversed) and 597/598 (Scranton). **✅ FULLY CLOSED (Cowork, 2026-09-22): Scott redeployed `tranquil-delight` and re-sent all three properties (Succasunna, Orlando, Scranton).** Live-reconfirmed: Orlando's re-send attached to `22887` instead of creating a twin (LCC Opps `entities`: `pipeline_status=success`, `domain_property_id=22887`, `22887`'s address stayed `4578 S Kirkman Rd`, not overwritten to the range); exactly one property each still exists on Kirkman Rd (`22887`) and Washington Ave (`28547`), no new duplicates minted. | ✅ shipped, verified, and live-confirmed closed | Cowork 2026-09-22, surfaced during Q41 re-send |
| EXT-HOST-2 | 🟡 **The `EXT-HOST` failure mode is back, now against a fully-deleted Vercel project.** CREXi "Upload OM PDF" (`2508 Airport Rd, Colorado Springs` / Liberty Dialysis, screenshot 2026-09-22) hit `DEPLOYMENT_NOT_FOUND` from **both** upload paths (Storage-first via `prepare-upload`, then its own inline-POST fallback) -- both resolve `host` through the same `pickIntakeHost()`, so both hitting the identical Vercel 404 means `host` itself resolved to a dead Vercel origin, not a one-path bug. `EXT-HOST` (2026-09-10, extension 1.0.53) already shipped a guard that refuses any stored `*.vercel.app` origin and falls back to the Railway default; the repo is at 1.0.54 now. Since `J13-teardown` closed 2026-09-18 the Vercel project is not just frozen but **deleted outright** (`life-command-center-nine.vercel.app` → 404 `DEPLOYMENT_NOT_FOUND` at the platform level, confirmed then) -- so any config this browser profile still holds from before that guard, or before the teardown, now fails hard instead of silently succeeding on a frozen build the way it used to. Most likely cause: this Chrome profile is running an extension build older than 1.0.53, or `chrome.storage.sync` holds a stored URL the guard's `.vercel.app`-suffix check doesn't happen to catch (e.g. a custom domain proxied through Vercel rather than the literal `.vercel.app` host). 👤 **Operator, `OPERATOR-CHECKLIST` Q42:** confirm the LCC Assistant extension is v1.0.54 in that Chrome profile (reload/update if not), re-save the Railway URL in Settings (`https://tranquil-delight-production-633f.up.railway.app`) to overwrite any stale value either way, then retry the OM upload on the same CREXi listing to confirm it stages. **✅ CLOSED (Cowork, 2026-09-22): Scott re-tried the upload post-redeploy; live-confirmed in `staged_intake_items` -- the PDF (`1632f966-...cfbd4a78.pdf`) landed `status=finalized` 18:26:46 UTC, correctly seeded with the Liberty Dialysis / Colorado Springs address.** | ✅ verified closed | Cowork round 60, 2026-09-22, from Scott's screenshot |
| SIDEBAR4-c | 🔴 **Something sends a second `action=process_sidebar_extraction` request 0.5–1.0 s after every sidebar Update (PATCH).** Found round 63 from SIDEBAR4's own `_pipeline_run_log` (5/5 entities with a PATCH since the redeploy: Ellijay GA, 2101 NW Hawthorne, Springfield OH, Hoffman Estates IL, Minden LA). The repo's extension calls that action only from the Re-run button, and nobody clicks that within a second. The logged request ids are Railway edge ids, not the extension's new UUID `X-LCC-Request-Id`, so the installed extension is **not yet 1.0.55**. SIDEBAR4's coalescer now serializes the pair, so no twins are minted, but every Update runs the pipeline twice. Next: after the 1.0.55 reload (Q43), read the run log again. If the second request carries no UUID it is not from this extension build; then check older installed builds, the web app, and any Power Automate / scheduled caller. **Round 65 (Cowork) — measured after the 1.0.55 reload:** both requests now carry the extension's own UUID `X-LCC-Request-Id` (e.g. Lewistown MT: PATCH `8a6e001c…` 21:10:27.830, then `process_sidebar_extraction` `79acf84d…` 21:10:28.546, coalesced). So the second request **comes from the side panel itself**, and the only call site is the Re-run button's click handler (`extension/sidepanel.js:1689`). Leading hypothesis: a layout shift — clicking **Update** swaps its label to "Updating…", the button shrinks, the adjacent Re-run button slides under the cursor, and the follow-up click of a double-click lands on it. A deliberate Re-run shows a different signature (Yucca Valley CA: 10 min after the Update, not coalesced). Separately, one capture (`2727 Washington Ave, St. Louis`, `entities.post`) carried a non-UUID id → a second writer outside `apiCall` (likely a content-script/background save path) — include in the prompt. **✅ BUILT 2026-09-23 (CC, extension 1.0.56): layout-shift hypothesis confirmed from the code** — Update's label shrinks 27→11 chars on click and the inline Re-run slides under the cursor; toasts were also prepended ABOVE the buttons. The run-log ordering (PATCH first, process 0.5–1.0 s later) fits a deliberate confirm click better than a sub-200 ms double-click, and the fix closes both. New `extension/shared/action-guard.js`: one action group per render; Update/Save/Re-run/Verify/Mark-off-market all go through `actionGroup.wrap` (siblings disabled while one runs, stray clicks swallowed, prior disabled state restored); widths frozen before the label changes; toasts and poll lines go into a slot below the buttons; Update/Save hold the group until their pipeline poll reports. `lccRequestHeaders()` now stamps every LCC call (`apiCall`, the pipeline poll, background `callLCCApi`) with a UUID `X-LCC-Request-Id` and a new `X-LCC-Client: lcc-extension/<version>`, and the server records `client` in `_pipeline_run_log`. Server single-flight and the SIDEBAR4 index are untouched. **2727 Washington (non-UUID `entities.post`): no second writer exists in the repo.** Its `metadata.source='crexi'` is set only by the side panel's Save (which uses `apiCall`), and a census of every extension `fetch` finds no other `/api/entities` writer. The likely cause is a client not on 1.0.55. From 1.0.56 the `client` field makes that provable. Guard `test/sidebar4c-action-guard.test.mjs` (16 tests, 5 on-disk mutations red). **Round 66 (Cowork):** 16-test guard passes on `main`. **Scott supplied the real behaviour (2026-09-23):** after Save, the panel re-renders into the matched state with "Update LCC with CoStar Data" + "Re-run Pipeline" and looks unsaved, so he deliberately clicks both. The layout-shift fix is still right, but the root cause is the post-save UX → `SIDEBAR4-d`. The latest run logs (11:34, 11:38, 13:53 UTC) still show PATCH → coalesced process pairs with no `client` field, i.e. pre-1.0.56. | ✅ closed 2026-09-23 (live proof via SIDEBAR4-d's 1.0.57 Save) | Cowork round 63 2026-09-22 (SIDEBAR4 run-log evidence); Cowork round 64 2026-09-22: waits on Scott reloading the extension to 1.0.55 + one Update (checklist Q43); Cowork round 65 2026-09-23; prompt `prompts/SIDEBAR4-c-rerun-button-layout-shift-second-click.md`; built CC 2026-09-23; Cowork round 66 2026-09-23; Cowork round 68 2026-09-23 |
| SIDEBAR4-d | 🔴 **After Save, the side panel re-renders into a state that looks unsaved, so the user clicks Update *and* Re-run (Scott, 2026-09-23).** Save success → `pollPipelineStatus` → `setTimeout(loadPropertyTab({prefetchEntityId}), 1500)` (`extension/sidepanel.js` ~2639–2653) lands in the matched render with the same "Update LCC with CoStar Data" + "Re-run Pipeline" buttons a months-old capture shows. There is no saved-at / pipeline-✓ / nothing-changed indicator, and the toasts vanish. `pageContext` storage changes re-render it too. Update already runs the pipeline server-side, so Re-run duplicates it. Fix: an honest "in LCC — up to date" state, Update enabled only when the page differs from the stored capture (hash), Re-run secondary unless the last run failed. **✅ BUILT 2026-09-23 (CC, extension 1.0.57, extension-only — no server change).** New `extension/shared/capture-state.js` decides the matched render from data: (1) Save and Update now stamp `metadata._capture_field_hashes` (FNV-1a per field over exactly what `extractSourceFields` + the FRESH `buildMetadata` send; `extracted_at`, `source_url` and every `_` key excluded) plus `_capture_saved_at`. The server keeps them: PATCH/POST/dedup/pipeline all spread incoming metadata. (2) The render compares the live page to that fingerprint. Nothing changed → **"Up to date in LCC ✓", disabled**. Changed → **"Update LCC (N fields changed)"**, with the names in the tooltip; a field in both the column and metadata counts once. A value the page no longer shows is not counted (a partly-loaded page must not read as changed, and Update never clears it anyway). A record saved before 1.0.57 has no fingerprint, so Update stays enabled and the tooltip says the next update records one. (3) A status block above the buttons: "Saved to LCC just now ✓" / "In LCC — last saved …", the pipeline status + time (`_pipeline_status` / `_pipeline_processed_at` / last `_pipeline_run_log.finished_at`, error on failure), and "Nothing new on this page" / "N fields differ". (4) Re-run is primary only when the last run **failed** or **none has ever run** (and nothing was just saved in this panel); otherwise it sits hidden behind a "⋯" overflow. A just-saved record whose pipeline is still running shows "Pipeline running for the latest save…" rather than a prominent "Run Pipeline". (5) A 10-minute in-memory memo of the last Save/Update/Re-run keeps "Saved ✓ / Pipeline ✓" through the 1.5 s re-render and any `pageContext` re-render. Update now re-renders into that state too. `pollPipelineStatus` returns its outcome for the memo. SIDEBAR4-c's action guard and the server coalescer are untouched. Guard `test/sidebar4d-capture-state.test.mjs` (19 tests; runs the real `sidepanel.js` functions extracted by AST, with a fake clock so `extracted_at` really differs; 4 in-memory mutations red). `costar-record-identity-isolation` pin moved 1.0.56 → 1.0.57. Full suite 6,924 pass / 0 fail. | ✅ closed 2026-09-23 (live-verified: a 1.0.57 Save = one run) | Scott 2026-09-23; Cowork round 66 2026-09-23; prompt `prompts/done/SIDEBAR4-d-post-save-panel-state-looks-unsaved.md`; built CC 2026-09-23; Cowork round 67 2026-09-23: 19-test file passes on `main`; no server change, so live = Scott's reload; Cowork round 68 2026-09-23: Scott reloaded 1.0.57 and saved a Wichita Falls VA property → entity `2f90e232…` run log = exactly ONE `entities.post` run, `client: lcc-extension/1.0.57`, success in 55 s, no follow-up PATCH/process; Scott reports the panel shows the property sent/processing and blocks re-clicks |
| HOME-MB-BOOT | 🔴 **The Home "Market Briefs" widget spins forever on a cold load (Scott, 2026-09-23 screenshots).** Cause, read from code: `renderMarketBriefsWidget()` is called only from `handlePageLoad('pageHome')` (`app.js` ~1138). On a cold load `pageHome` is already the active page, so the router bootstrap (`applyRoute` → `navTo` only when the page is *not* active, `app.js` ~2297) never calls `handlePageLoad`, and `bootApp()` (~9610) renders Today / three lanes / briefing but not this widget. Pre-existing since MB-b (`71fccd05`); a Today-tab click would load it. The endpoint itself answers (401 unauthenticated in 0.26 s). Fix: render the widget from `bootApp`, or run `handlePageLoad` for the initially active page once. Add a test.  **BUILT 2026-09-23 (CC):** `bootApp()` now calls `renderMarketBriefsWidget()` after `autoConnectCredentials`, gated on `_routeIsPageActive('pageHome')`. Chosen over running `handlePageLoad` from the router bootstrap because that bootstrap fires on DOMContentLoaded *before* auth resolves (a pre-auth fetch 401s → "Unavailable right now." with nothing to re-render it after sign-in), and running the whole `pageHome` case there would double the briefing/NBA fetches `bootApp` already makes. Audit of the other `handlePageLoad('pageHome')` calls: `renderDailyBriefingPanel` and `renderNextBestActionPanel` are covered, because `bootApp` calls `loadDailyBriefingData`/`loadNextBestActionData` and both render on entry and on settle. `renderTodaySections` is covered and called directly after `Promise.all`. The Market Briefs widget was the only gap. Guard `test/home-mb-boot-cold-load.test.mjs` runs the real `bootApp`/`applyRoute` bodies (AST-sliced). It passes 5/5, and 3 go RED with the fix reverted. Cache busters 2026092205 → 2026092301. Owed: redeploy both Railway services, then Scott hard-refreshes Home. | ✅ closed 2026-09-23 (Scott's hard refresh shows Market Briefs: Dialysis 23 live facts; gov + net lease "producer not built") | Scott Home screenshots 2026-09-23; Cowork round 65 2026-09-23; prompt `prompts/HOME-MB-BOOT-market-briefs-widget-cold-load.md`; Cowork round 66 2026-09-23: 5-test file passes on `main`; CC audited the other Home renderers and found them covered by `bootApp`; Cowork round 68 2026-09-23 |
| LEASEJUNK1 | ✅ **Junk lease rows — table-header text as `leases.tenant` (SHIPPED 2026-09-22, CC).** Source was NOT an OM parse: the extension's CoStar Tenants-panel parse puts panel headers/summary rows into `metadata.tenants[]`, and an OM promote merged into the same entity, so `upsertDomainLeases` wrote them stamped `email_intake`. Quarantined **56 rows / 25 properties / 1 active** (flag + inactive, logged, reversible via `dia_leasejunk1_restore_quarantine`). DB guard trigger + `isJunkTenant` header list (lock-step SQL/JS) + OM promote `firstOfWhere` + dia lease readers filtered. Migration `supabase/migrations/dialysis/20261013090000_dia_leasejunk1_header_tenant_quarantine.sql`; guard `test/leasejunk1-header-tenant-guard.test.mjs`. **Residue:** 29671 now has no active lease (DaVita 16828 was already inactive; untouched); ~~extension `COSTAR_UI_REJECT` lacks these strings (defence-in-depth)~~ ✅ closed by SIDEBAR5 2026-09-23 (the extension filters the same list, lock-step, drift-tested); CoStar industry-category values ("Pizza", "Fitness") as tenants are unaddressed. Prompt: `prompts/LEASEJUNK1-om-table-headers-parsed-as-lease-tenants.md`. | ✅ shipped + live (DB since 09-22; JS live on `tranquil-delight` `881dc2b4`) | Cowork round 44; prompted + shipped 2026-09-22; reconciled Cowork round 63 2026-09-22: 56 flagged / 0 active / 56 logged / 0 unflagged detector matches / 0 flagged outside detector; trigger `dia_leasejunk1_header_tenant_guard_biu` present; 29671 still 0 active leases; drift mutation turns 4/8 red (PR #2628) |
| DIA-PROPAGATOR1 | ✅ **A runaway writer on Dialysis_DB: the `DialysisProject` scheduler's full-data propagation is re-PATCHing the whole fleet around the clock and logging ~1M junk `learning_logs` rows a day.** Measured round 44 from Supabase edge logs (UA `python-httpx/0.28.1`, Scott's egress IPs 152.55.178.28 00:00–11:04 UTC then 162.220.232.102 13:06→still running at 20:33): today alone 998,911 POST `learning_logs` (all `source = ai_scrubber`, `notes = 'invalid field'`, table `facility_patient_counts`, no property_id — payer-mix keys 0.11 / 0.2 / 0.65, `base_payer_mix_state=national` not in the scrubber's schema_map, so every row is dropped AND logged), ~200k PATCH `facility_patient_counts`, ~200k PATCH+POST `clinic_financial_estimates`, **126k PATCH `properties`** (7,099 of 11,837 rows `updated_at` moved today; ~4 PATCHes per property per hour). `learning_logs` = 12.27M rows / 2.2 GB (DB 8.8 GB); every day since at least 09-02 is 100% `invalid field`. Source: `DialysisProject/src/ai_scrubber.py` ~4500 (`safe_log_learning(... notes="invalid field")`) under `src/app_utilities.py` `schedule_tasks()` (`run_full_data_propagation` 02:45 daily + the propagation loop); `runlogs/pending_updates_fallback.log` on Scott's PC was written at 20:56 UTC — the scheduler is live there. **Consequences here:** `properties.updated_at` is useless as sidebar-send evidence (SIDEBAR-LEASE1/SIDEBAR2 measured against it — `last_ingested_at` is the real ledger), and the twin-address and RECON measurements that read `updated_at` need re-reading. **Decision for Scott (Q37):** stop the scheduler's propagation (or the scheduler) until the Dialysis repo (1) stops logging schema misses as learning rows, (2) propagates only changed inputs, (3) purges `learning_logs` `invalid field` rows (12M). Not this repo's code; this repo records it because it writes to a DB this repo owns. **Round 45 — shape of the writer, and the recommendation.** Edge logs show the writer went quiet at **20:33 UTC** (nothing 20:45–22:40): it is a **fleet pass that takes ~8–11 h** (00:00→11:04, then 13:06→20:33 — the second start coincides with the PC coming back), re-triggered by the daily `02:45` full-data-propagation job / scheduler restarts — not a tight loop, but each pass re-writes every derived field on every property and logs every schema miss once per row. Cumulative `pg_stat_statements` shapes for `python-httpx` PATCHes on `properties`: `public_record_count + tax_*` (972k calls), `estimated_annual_revenue` (934k), `building_size` (837k), **`true_owner_id` (≥ 700k across variants)** — the last competes with the owner resolver / OWNERGAP guards and must be checked. **Recommendation (Cowork, guiding principle = an accurate source of truth): PAUSE the propagation job, keep the rest of the scheduler.** A source of truth needs writes that (a) happen when an input changed, (b) say which input, (c) never rewrite a field another writer owns. This pass fails all three and destroys `updated_at` as provenance for every other reader. Fix in the Dialysis repo (`prompts/DIA-PROPAGATOR1-…md`, written round 45): compare-before-write, schema misses logged once per (table, field) per run to `ingestion_run_errors` not `learning_logs`, purge the 12.3M `invalid field` rows with a ledger count, an owner-of-record list for every column the propagator writes (drop `true_owner_id`), weekly or event-driven cadence. Resume after. | ✅ closed 2026-09-21 (Dialysis PR #7421 merged; purge complete and holding; Q37 closed) | Cowork round 44 edge-log measurement **👤 2026-09-21 (Cowork): confirmed NOT the same writer as `HCRIS-TIMEOUT`'s `cms-ingestion` pipeline.** Traced both codebases: `DIA-PROPAGATOR1`'s target (`full_data_propagator.py`/`database_updater.py`/`financial_estimate_tracker.py`, driven by the local Windows-scheduled `run_scheduler.ps1`) never calls into `ingest_medicare_clinics.py` or `propagate_property_financials.py`, and vice versa — two independent, uncoordinated writers on the same `properties`/`facility_patient_counts` tables, which is why their symptoms and timestamps looked related. `DIA-PROPAGATOR1`'s own fix (PR #7421, weekly-Sunday cadence + `compare_before_write.py`) stands as correctly scoped and does not need to be walked back; see `HCRIS-TIMEOUT`'s row for the separate, still-open fix that pipeline needs. **✅ 2026-09-21 (Round 48, Cowork) — CLOSED.** PR #7421 confirmed merged and live in the `DialysisProject` mount (`compare_before_write.py` wired into the propagators, weekly-Sunday cadence, `safe_log_learning` refuses `invalid field`). Production purge confirmed complete and holding: `learning_logs` 12,269,613 → ~135k rows, zero new `invalid field` rows since 2026-09-19 (the day the fix landed and the old process stopped writing). Tail items (VACUUM FULL for disk reclaim, confirming the DIA-PROPAGATOR1-c burst is actually `cms-ingestion`'s own startup, not a second Task Scheduler job) filed as OPERATOR-CHECKLIST Q38, optional/low-priority.; Cowork round 64 2026-09-22: state cell corrected (was still "paused? (Q37)" after the round-48 close) |
| LOG5 | ✅ **Gov `intake-promoter`'s materialized-view refresh has been silently failing on every promote — `"v_available_listings" is not a table or materialized view` (code `42809`).** `api/_handlers/intake-promoter.js` calls a `REFRESH MATERIALIZED VIEW` step (embedded in its per-promote response as `mat_view_refresh: {ok:false,status:400,...}`) against `v_available_listings`, which is a **plain view**, not a materialized one — matches a bug an earlier audit already found and archived (`audit/data-flow-2026-05-30/archive/build_report.js:198`: "`REFRESH MATERIALIZED VIEW` is called on `v_sales_comps` and `v_available_listings`, which are plain views. Two steps fail every night.") — this was flagged 2026-05-30 and never fixed. The promote itself still succeeds (this sub-step is caught, not fatal), so it has stayed invisible. Fix: delete the dead REFRESH call, or point it at whatever the intent actually was (a real materialized view, if one exists, or drop the step entirely since plain views recompute on read). | ✅ closed 2026-09-22 (fixed live) | LOG-round51 2026-09-22 **👤 2026-09-22 (Cowork): CLOSED — fixed live and committed.** Confirmed live (`pg_class.relkind='v'`, `pg_get_functiondef` showing the unguarded `REFRESH MATERIALIZED VIEW`), then shipped the same relkind-guard pattern already proven on `v_sales_comps`'s sibling fix. Applied via Supabase MCP directly (verified with `select public.lcc_refresh_available_listings()` — clean, no error) and committed as the first source-of-truth copy in `government-lease` (branch `fix/log5-refresh-available-listings-matview-guard`, commit `eb31836`) since neither the original RPC nor `v_sales_comps`'s 2026-06-01 sibling fix was ever committed there — both were untracked drift, applied live only from this repo's now-historical migrations directory. Needs Scott's push+PR in `government-lease` to land the source record (the live DB fix is already in effect).; Cowork round 64 2026-09-22: state cell corrected to match its own CLOSED note |
| RECON2-render | 🔴 **Honest rendering of `leases.expiration_state` in the rent-roll/comps/property-context readers (filed round 30, 2026-09-17, out of scope of RECON2 unit 1's time budget).** `expiration_state='expired_unconfirmed'` leaves `is_active=true` unchanged by design (RECON2), so a lease past its own `lease_expiration` with no confirming evidence still renders as a flat-schedule "Active" lease in every reader that reads `is_active` alone. Readers found by grep, none yet changed: `mcp/comps-tools.js::hydrateSubjectFromRecord` (computes `remaining_term` from `lease_expiration` with no `expiration_state` read, ~line 690-698); `api/_handlers/entities-handler.js::buildPropertyPacket` (`leases?…&order=is_active.desc.nullslast,lease_start.desc&limit=1`, ~line 653, returns the raw row — `expiration_state` passes through automatically since it's `select *`, but nothing labels it for the client); `api/_shared/asset-entity.js` (`lease_expiration` field builder, ~line 110); `api/_shared/provenance-row-context.js` (renders a provenance sentence off `lease_expiration`, ~line 54-56). Add a one-line label (`"Expired <date> — renewal not on file (unconfirmed)"`) wherever a lease is surfaced to a human, keyed on `expiration_state`, not a broader refactor. **Prompted 2026-09-22 (Cowork round 62): `prompts/RECON2-render-honest-expiration-state-rendering.md`.** ✅ **BUILT 2026-09-22 (CC, branch `claude/busy-hawking-91u6hp`):** one helper `mcp/lease-expiration-state.js::leaseExpirationStateLabel` (in `mcp/` because the MCP image copies only `mcp/`; `api/` already imports `../../mcp/`), wired into all four readers, each adding a key ONLY for `expired_unconfirmed` (comps subject `lease_expiration_note` + `fields.lease_expiration_note`; packet `tenancy_lease.lease_expiration_state`; asset-entity tenant row `expiration_state` + `lease_expiration_note`; provenance label replaces `expires <date>`). `is_active` untouched. ⚠️ **`expiration_state` is dia-only — gov `leases` has no such column (measured live)**, so the two explicit `select=` additions are dia-only paths. Live scale: **2,447 active dia leases** read `expired_unconfirmed`. ⚠️ For a past expiration the comps `remaining_term` was already null (by design) — the label is now the only term signal on that subject. Guard `test/recon2-render-expiration-state.test.mjs` (13 tests; byte-identical check for all 5 other states + NULL + absent; stubbing the helper to null turns 6 RED). Suite 6,806/0. **Two further human-facing sites found, filed not touched:** `RECON2-render-spa`, `RECON2-render-dossier`. **Round 63 (Cowork) verified:** PR #2625 diff read (one helper `mcp/lease-expiration-state.js`, four call sites, label only for `expired_unconfirmed`); 13/13 tests pass and a helper-stub mutation turns 6 red; live dia count re-queried = **2,447** active `expired_unconfirmed` leases (matches); gov `leases` confirmed to have no `expiration_state` column. | ✅ closed 2026-09-23 (both Railway services live) | RECON2 unit 1, round 30; prompted 2026-09-22; reconciled Cowork round 63 2026-09-22 (PR #2625); Cowork round 65 2026-09-23: Q44 MCP half confirmed by Scott (MCP deployed #2630 code, which contains #2625) |
| RECON2-render-dossier | 🔴 **`dossier-generator.js` does not render the new `tenancy_lease.lease_expiration_state` tag** (Tenancy & Lease section / `renderTermTag`, ~lines 444/485/662). The packet carries it since RECON2-render; the HTML dossier is a human surface and still shows the bare expiration. One `kvRow` + test. ✅ **BUILT 2026-09-22 (CC, with RECON2-render-spa):** an `Expiration status` `kvRow` in both `renderPropertySections` and `renderDealSections`, present only when `tenancy_lease.lease_expiration_state` is set. Output is byte-identical for every other state, NULL, absent and gov (behavioural test over both renderers). | ✅ shipped + live 2026-09-23 (PR #2631, folded into RECON2-render-spa) | RECON2-render sweep 2026-09-22; built 2026-09-22; Cowork round 65 2026-09-23 |
| GOV-AVAIL1 | 🔴 **Gov › Deals › Sales › Available is carrying ingestion errors: our own Tulsa office is an active gov listing, 111 addresses embed city/state/ZIP, agency shows in 270 spellings, 60 rows have no government type (SBN-21/22/23).** Traced 2026-09-22: a **dialysis** OM (`USRenalMOB_Findlay_OH_OM_SB.pdf`, SF Listing seed, `source_vertical: dia`; intake `9c2dc902…`) was extracted by local `qwen2.5:14b`, which returned the listing broker's office block (`6120 South Yale Avenue, Suite 300`, Tulsa); it matched 0.97 and was promoted onto gov property 11255 (`5110 South Yale Ave`, different civic number) as listing `c04dc749…`. Second live case: `2800 Post Oak Blvd, Suite 500, Houston` stored on a Brownsville TX listing. GSA alone appears as 18 strings although the ID3a registry exists. Duplicate Malta MT property pair (16193 / 40643) created 5 min apart today. Fix every link in the chain (extractor broker-block guard + office-address registry, civic-number match guard, no cross-vertical promote), quarantine the residue reversibly, normalize address display, show the canonical agency, report the 60 untyped rows. ✅ **SHIPPED 2026-09-22.** JS chain (LCC): `api/_shared/intake-address-guard.js` is the ONE own-/brokerage-office list (`own-firm-addresses.js` now re-exports it; the old substring list missed the spelled-out `6120 South Yale Avenue`) + a contact-block detector (address within ±250 chars of an email or phone+cue, unit or repeated) applied in `callAiExtraction` and again with the DB registry `lcc_brokerage_office_address` (LCC Opps, 2 rows) before the matcher; `checkPromotionIdentityGuards` in the promoter refuses `vertical_domain_mismatch` (seed `source_vertical` vs target domain, both directions) and `civic_number_mismatch` (reads the domain property's address; unreadable ⇒ `civic_check='unverified'`, proceeds); `pickDomainForTenant` honours the stated vertical. Source fix: LCC asset entity `658c4713…` renamed `6120 South Yale Ave` → `5110 South Yale Ave` (ledgered, reversible) — it had absorbed 124 match rows from 5 intakes. Class detector `v_lcc_asset_entity_civic_drift` (dia 51 / gov 64 bridged entities whose civic number disagrees with their property). gov (`government-lease sql/20260922_gov_avail1_available_listings_hygiene.sql`, applied live): both office listings quarantined (`gov_avail1_listing_quarantine_log`, restore by batch; property 11255 untouched — its last update predates the listing); `v_available_listings` **496 → 494** rows, appended `address_display` (**106** stripped), `address_locality_conflict` (**0** after quarantine), `agency_code`/`agency_canonical_full`/`agency_resolution` via `gov_resolve_agency` (**273 rows / 83 of 268 strings resolve; 208 rows / 185 strings unresolved; 13 blank**); report-only `v_gov_available_listing_twin_candidates` (**18 pairs, 2 in the existing lane**). gov.js Available agency/address cells read them (raw on hover). Findlay re-run NOT possible here: artifact bytes were not retained and dia holds 3 US Renal Findlay OH candidates — re-send from SF ContentVersion `068Vs000019XB2IIAW`. Tests `test/gov-avail1-intake-guards.test.mjs` (19; 11/11 mutations RED). Follow-ups: GOV-AVAIL1-postoak, -agency-tail, -twins, -govtype, -civic-drift. **Round 65 (Cowork) verified live:** both office listings `off_market` and absent from `v_available_listings` (`c04dc749…` Tulsa, `6cdda883…` Post Oak); `gov_avail1_listing_quarantine_log` present; view carries `address_display`, `address_locality_conflict`, `agency_code`, `agency_canonical_full`, `agency_resolution`; LCC entity `658c4713…` renamed to `5110 South Yale Ave` (log `lcc_gov_avail1_entity_name_log`); registry `lcc_brokerage_office_address` present. 19-test guard file passes; my own mutation (`civicNumbersAgree` → always true) turns 2 red. The Findlay OM itself was not recovered — needs a re-send from Salesforce (checklist Q45). | ✅ shipped + live 2026-09-23 (LCC PR #2633 + government-lease PR #408; DB migrations applied by CC 2026-09-22; JS live on `06faa4f3`) | SBN-21/22/23 (`SB notes/done/gov availables view.docx`); Cowork round 64 2026-09-22; prompt `prompts/GOV-AVAIL1-gov-available-listings-ingestion-accuracy.md`; Cowork round 65 2026-09-23; Cowork round 66 2026-09-23: Findlay OM re-run from the stored `sf_files` 1747 (requeue #1 hit `INTAKE-RESTAGE1`; requeue #2 → intake `3605ee76…` `review_required`, address null, nothing promoted — the guard holds); placement waits on the address from Scott (Q45). Dialysis_DB has Findlay twin pairs 51194/28037 (1717 Medical Blvd) and 22621/39404 (Patriot Dr) — not in any twin lane yet, noted here |
| GOV-AVAIL1-agency-tail | ✅ **Measurable part closed in `GOV-AVAIL2` (2026-09-23).** The resolver was never case-sensitive: `gov_agency_alias_key` uppercases. 26 reviewed single-agency spellings promoted through the ID3a path (`gov_agency_aliases`, source `gov_avail2_promote`, only where `canonicalize_agency` agrees), and MSHA + NARA registered. Available **271 → 294 matched / 206 → 168 unresolved rows**. The residue is split into `GOV-AVAIL2-state-registry` (state agencies), `GOV-AVAIL2-multi-agency` and `GOV-CU1` (non-government tenants). Generic "United States of America" / "US Government" (13 rows) stays unresolved on purpose: it names no agency. | ✅ closed 2026-09-23 (`GOV-AVAIL2`) | GOV-AVAIL1 2026-09-22; Cowork round 70 2026-09-23; GOV-AVAIL2 2026-09-23 |
| INTAKE-RESTAGE1 | 🔴 **Re-staging an already-stored Salesforce file fails with `inbox_item_insert_failed`.** The OM inbox card is keyed `om_sha256:<sha>` under the partial unique index `inbox_items_workspace_external_id_unique`, and the insert uses `resolution=merge-duplicates` without `on_conflict`, so any second stage of the same file raises instead of reusing the card (`api/_shared/intake-om-pipeline.js` ~379–425). Found re-running the Findlay OM (sf_files 1747) on 2026-09-23; worked around by suffixing the old card's `external_id`. Also: no supported "re-extract this file" path exists except SQL (`extraction_status='queued'`, picked up by cron `sf-files-stage-queued-15m`). ✅ **BUILT 2026-09-23 (CC).** Measured first: the only unique index is the partial one, and PostgREST cannot target it (`on_conflict` sends no predicate, so a partial arbiter cannot be inferred; 42P10). So the fix is **lookup → insert → on 23505 look up again and attach** (`resolveOmInboxCard`); a 409 with **23503** is an error, not a conflict. `planOmRestage` classifies a reused card: `restage` (staged row settled, or `queued` and stale > 2 min) → an atomic conditional PATCH (`or=(status.neq.queued,updated_at.lt.<now-2m>)`) claims it back to `queued`, the artifact is reused on the same sha256, and extraction + matching re-run with `forceReextract` (prior extraction rows kept); `in_flight` (freshly queued, a card < 2 min old with no staged row, or reached via 23505) → returns the same card, `deduplicated:true`, writes nothing; `resume` (an old card with no staged row) → stages onto it. **Why reuse the existing `staged_intake_items` row rather than mint a new one:** its PK *is* the card id (1:1), so a second row cannot exist without a second card, and every downstream consumer keys on that id. A reused card is never rolled back or re-statused. Supported re-run path: `intake-salesforce-files?action=requeue` (webhook-secret gated; `content_version_id` or `file_id`+`vertical`; `stage_now:true` drains that one row), documented in `docs/architecture/om_intake_pipeline.md`. Tests `test/intake-restage1.test.mjs` (12; **12/12 mutations RED**), full suite 6,923 / 0 fail. **Deploy:** Railway `tranquil-delight` (the stage-om handler) + edge function `intake-salesforce-files` on **Dialysis_DB** (repo copy matched deployed v30 before the change). No migration. **Verify (Cowork):** requeue one already-extracted file via `?action=requeue` and confirm `sf_files` → `extracted` with the SAME `intake:` id as before, one card, and a new `staged_intake_extractions` row. The Findlay card's `:superseded-20260923-gov-avail1` suffix can stay; it is harmless. | ✅ closed 2026-09-23 (stage-om fix live-verified round 67; `intake-salesforce-files` v31 deployed by Scott, Q49) | Cowork round 66 2026-09-23 (GOV-AVAIL1 Q45 follow-through); prompt `prompts/INTAKE-RESTAGE1-re-staging-a-stored-sf-file-fails-on-inbox-dedup.md`; Cowork round 67 2026-09-23: live verify — requeued sf_files 1747 by SQL; the 17:30 UTC cron drained it to `extracted` on the SAME intake `3605ee76…` (1 staged row, 2 extraction rows, no new card, no `inbox_item_insert_failed`). 12-test file passes on `main`; Cowork round 68 2026-09-23: GET info reports `sf-files-2026-09-v8` with `requeue` in its action list; an unauthenticated POST `?action=requeue` returns 401 (the webhook-secret gate holds) |
| SF-BRIDGE1 | Our own Salesforce deals reach LCC without a type or address, and their OMs can't follow the Salesforce seed to the property. **BUILT 2026-09-23 (CC).** (1) **Type:** the only writer, `mcp/opportunity-sync.js` → `lcc_upsert_bd_opportunities`, never sent `type`, and the RPC had no `type` column; all **610** SF deals read NULL (35 open). Migration `20261102270000` (applied live; renumbered from `…260000`, which collided with GOV-UX1-D5-gate's file) adds `listing`/`bov`/`buy_side`/`sf_deal` to the CHECK. The RPC writes the type and never overwrites an LCC-owned type (`prospect`/`buyer`/`other`/`government_buyer`). The backfill set 610 rows from stage, logged in `lcc_sf_bridge1_type_backfill_log` (reverse with `lcc_sf_bridge1_unbackfill_type`). Open deals now: listing 19 / bov 7 / sf_deal 9. `deriveDealType` refines on the next sync: a contractual stage with a staged `Listing__c` becomes `listing`. The record type (`IS CM`/`D&E`) does not state a side, so `sf_deal` is the honest default. (2) **Address:** the PA payload has never carried `Property2__r`. Salesforce **does** hold the address, in dia/gov `sf_deal_staging` (written by `intake-salesforce`). The sync now reads it (payload > staging) and stamps `metadata.address_source`, and the RPC is fill-forward, so a 30-min resync cannot erase it. Coverage: **12 of the 35 open deals** have a staged address. **21 are not staged anywhere**, and for those Salesforce's address is unreachable until the PA flow sends `Property2__r` (`SF-BRIDGE1-flow`). `vertical` falls back to the staging domain. (3) **Seeded OM matching:** `api/_shared/sf-seed-match.js` resolves `Listing__c`/`Opportunity` → `sf_listing_staging.sf_deal_id` → `bd_opportunities` → `external_identities(asset)` by ID. Rules: no address → seed matches; address agrees (civic number AND street name) → match; conflict → `review_required` with both candidates; the other vertical → refused; two linked properties → unresolved. `is_northmarq=true` is set on the listing row only when the seed resolves to one of our listings. (4) **Panel:** `/api/priority-band` returns `open_deal` (the property's asset entity, by ID), and the property and entity banners show "Our listing is live" instead of "Create the lead". Guard `test/sf-bridge1.test.mjs` (30 tests, 15/15 mutations RED). | ✅ closed 2026-09-23 (Findlay listing live; MCP redeployed by Scott, Q52 — Findlay deal address filled from `dia.sf_deal_staging` at the 20:00 UTC sync) | Cowork round 68 2026-09-23; prompt `prompts/SF-BRIDGE1-salesforce-deals-type-address-and-seeded-om-matching.md`; CC 2026-09-23; Cowork round 69 2026-09-23: 30-test file passes on `main`; live: open deals typed (listing 19 / bov 7 / sf_deal 9 / prospect 4 / government_buyer 4); Findlay opp `type=listing` but `property_address` still null after the 19:00 UTC sync, consistent with the MCP service not yet redeployed; Cowork round 69 LIVE: requeued sf_files 1747 → 19:15 UTC drain → intake `3605ee76…` `finalized`, `match_status=matched`, domain dialysis, property **51194**, created dia `available_listings` **15286** (`status=active`, `is_northmarq=true`, `listing_date=2026-09-23`, price null — nothing fabricated). Minor: `sf_files.process_notes` still reads "extract:processing match:pending" (written before the stage finished); Cowork round 70 2026-09-23: live — 12 of 43 open deals now carry an address; the rest wait on the flow edit (SF-BRIDGE1-flow) |
| GOV-UX1-D1 | ✅ **SHIPPED + LIVE 2026-09-23: the agency-drift detector reads the current lease only and compares occupying agency to occupying agency.** `v_gap_agency_drift` now filters to unsuperseded, unexpired leases (`gov_ux1_lease_is_current`; `termination_date` is deliberately not used because on a GSA lease it is the early-termination right). Each side is `coalesce(using_agency_canonical, agency_canonical)`, the stored `canonicalize_agency()` output. Where either side is unrecognised it falls back to the old raw test, so that residue stays visible. Columns, order and `drift_kind` values are unchanged; `v_next_best_action`, `app.js` and `api/admin.js` need no change. **Denominator, live:** `agency_disagreement` **1,481 rows / 1,239 props → 646 / 555**; `lease_agency_but_property_agency_null` **46 / 45 → 6 / 6**. 1,481 is rows and 1,239 is properties of the same view; CC's 1,644 is the same pairs before DISTINCT. Where the old pairs went (1,478 distinct): 481 on superseded leases only, 207 on expired only, 145 agree once canonicalized, 645 still drift. Of the 646: 75 rows are two recognised, different codes; 571 have a side `canonicalize_agency()` does not recognise (mostly GSA field-office names such as "PITTSBURGH FIELD OFFICE (PA)" in `leases.tenant_agency`) → **GOV-UX1-D1-registry**. ⚠️ **The first applied version called `canonicalize_agency()` per row. It reads the registry (~3.5 ms a call), so `v_next_best_action` went from 2.3 s to 45 s for about 5 minutes before the stored-column version replaced it.** A guard now forbids the call inside the view. | ✅ live 2026-09-23 | GOV-UX1 §D; Q46; government-lease `sql/20260923_gov_ux1_d1_agency_drift_detector.sql` + `sql/20260923_gov_ux1_d2_d3_agency_autoresolve.sql` (branch `claude/vigilant-faraday-uqm82a`, commit `3bcd627`), guard `tests/unit/test_gov_ux1_agency_drift_autoresolve.py`; prompt `prompts/GOV-UX1-D1-D3-agency-drift-detector-and-gsa-occupant-writer.md`; Cowork round 67 2026-09-23: live-verified: `v_gap_agency_drift` 625 rows / 534 props disagreement + 2 null (matches CC); `v_next_best_action` back to 2.29 s (EXPLAIN ANALYZE) after CC's 5-min 45 s regression |
| GOV-UX1-D2 | ✅ **SHIPPED + LIVE 2026-09-23: 21 properties, not ~272.** Column written: `properties.using_agency_canonical` (+ `_full`), ID3a's occupying-agency column. `agency` / `agency_canonical` are untouched because GSA really is the lessee of record. Rule: the property canonicalizes to GSA, its using agency is blank, and every current lease resolves to the same one non-GSA code that is an active `government_agencies` row with `government_type = Federal`. Plan view `v_gov_ux1_agency_autoresolve_plan` names each exclusion: 51 have an unrecognised current-lease string, 23 have a current lease naming GSA itself, 1 is multi-tenant, 1 has a code missing from the registry. Writer `gov_ux1_apply_agency_autoresolve(dry_run default true, batch)` ledgers every field to `gov_ux1_agency_write_log`. Restore is `gov_ux1_restore_agency_writes(batch)`, which never restores over a later human edit. Both are service_role only (asserted with `has_function_privilege`). Batch `gov_ux1_20260923` wrote 42 ledger rows. The detector dropped by exactly 21 (646 → 625). A re-run plans 0. A restore inside a rolled-back transaction put all 46 fields back. **Kept true:** cron **52** `gov-ux1-agency-autoresolve` runs daily at 04:40 UTC. It is needed because the canonical trigger re-derives the using agency from `agency` on every write of `agency`, and new leases arrive monthly. ⚠️ **The ~272 counted superseded and expired leases.** The screenshot case (13923 Gold Cir Omaha, property 9272) is one of them: its DHS lease was superseded 2026-04-28, and the current lease reads "NEBRASKA OFFICE", a GSA field office. Writing DHS would assert an occupant from an ended lease, so it stays in the human lane. | ✅ live 2026-09-23 | GOV-UX1 §D; Q46; government-lease `sql/20260923_gov_ux1_d1_agency_drift_detector.sql` + `sql/20260923_gov_ux1_d2_d3_agency_autoresolve.sql` (branch `claude/vigilant-faraday-uqm82a`, commit `3bcd627`), guard `tests/unit/test_gov_ux1_agency_drift_autoresolve.py`; Cowork round 67 2026-09-23: live-verified: `gov_ux1_agency_write_log` batch `gov_ux1_20260923` = 46 field writes (21 D2 + 4 D3 properties); cron job 52 `gov-ux1-agency-autoresolve` 04:40 UTC daily, active; gov PR #409 merged; Omaha 9272 intentionally left human (its DHS lease is superseded) |
| GOV-UX1-D3 | ✅ **SHIPPED + LIVE 2026-09-23: 4 properties, not 45–46.** Same writer, ledger and restore as D2. Writes `properties.agency` (blank only) from the most recent current lease when all current leases agree on one code; the trigger derives canonical, full, using and government_type. It wrote FAA ×2, USDA and DOI. The 45 counted leases of every vintage; only 6 blank-agency properties have a current lease. Two stay human because their string is unrecognised: "PD", and "NASA" (a registry gap, → GOV-UX1-D1-registry). `agency_id` is not written; `gov_id3a_backfill_agency_ids()` owns it. Null-kind drift went 6 → 2. | ✅ live 2026-09-23 | GOV-UX1 §D; Q46; government-lease `sql/20260923_gov_ux1_d1_agency_drift_detector.sql` + `sql/20260923_gov_ux1_d2_d3_agency_autoresolve.sql` (branch `claude/vigilant-faraday-uqm82a`, commit `3bcd627`), guard `tests/unit/test_gov_ux1_agency_drift_autoresolve.py`; Cowork round 67 2026-09-23: live-verified with D2 (same ledger batch) |
| GOV-UX1-D4 | ~~Auto-add to cadence when an open BD opportunity has none (35 of 43 open leads)~~ — **CLOSED, NOT BUILT (re-measured 2026-09-23).** The 35 are `type IS NULL` Salesforce-synced **listing/escrow deals** (stages `listing_signed`/`bov`/`in_escrow`/`loi_executed`/`non_refundable`/`off_market_listing`/`qualified_lead`), not prospects; seeding them would put signed clients on a seller-prospecting cadence. All **4 of 4** open `type=prospect` opps already carry a cadence via trigger `bd_opportunity_auto_seed_cadence`, which is the forward path. The panel counts only `type=prospect`, so these 35 never produce "Add to cadence". Follow-up: `GOV-UX1-D4-sftype`. | ✅ closed — no population (2026-09-23) | GOV-UX1 §D; audit §D "D4 / D5 re-measured"; Cowork round 65/66 2026-09-23; prompt `prompts/GOV-UX1-D4-D5-auto-cadence-and-gated-auto-lead.md` |
| SF-BRIDGE1-flow | The SF Deal → LCC Opportunity Sync PA flow should also select `Property2__r.Street__c/City__c/State_Province__c/Zip_Code__c` and `CreatedDate` (and `RecordType.Name` if FLS allows). `normalizeDeal`/`dealAddress` already read them. This is the only way to reach the **21 open deals** not in any `sf_deal_staging`. ⚠️ **The flow's definition is not in the repo:** `docs/flows/README.md` names `flow-definition.json`, which is a Microsoft To-Do sync (corrected there 2026-09-23). Export the live flow and commit it before editing. | ✅ closed 2026-09-23 (Scott retest #3 succeeded; Q54) | SF-BRIDGE1 2026-09-23; Cowork round 70 2026-09-23: read Scott's export (flow `eb1181ba…`): Recurrence 30 min → Get records (Opportunity, 6 RecordTypeIds, $select without Property/CreatedDate) → HTTP POST tranquil-delight `/api/pipeline/ingest-opportunities`. Edit = swap to "Execute a SOQL query" selecting `Property2__r.*`, `CreatedDate`, `RecordType.Name`; body from `body/records`. Export moved to gitignored `private/power-automate/exports-2026-09-23/` (it carries the ingest bearer token). `normalizeDeal` reads no `CreatedDate` yet; Cowork round 71 2026-09-23: Scott saved the edit; test run failed in `Get_deals_soql` because `queryParameters/query` began "sql SELECT…". Not a field or permission error. Guide updated; `RUNBOOK_sf_opportunity_inbound_flow.md` got a clarification banner (the Opportunity object exists for deals; BD opportunities are Tasks); Cowork round 72 2026-09-23: test #2 — `Get_deals_soql` succeeded (5 s), HTTP 400 because `deals` = `{totalSize: 608, done, records}` (object) and `ingestBatch` (`mcp/opportunity-sync.js` ~407) requires an array. Optional LCC hardening (not done): accept `deals.records` as well; Cowork round 73 2026-09-23: retest #3 green; live LCC Opps — open deals with an address **12 → 29 of 43** (`metadata.address_source=sf_payload`; 521 closed deals also filled). Remaining: 8 open SF deals have no `Property2__c` in Salesforce (data, not flow), 6 open deals are not SF. `opened_at` still NULL on all 610 SF deals → `SF-BRIDGE1-opened-at` (prior state: 🟡 SOQL step working (608 deals, 2026-09-23); HTTP body still sends the SOQL result object, not `body/records` → Scott retest (Q54)) |
| GUARD-CLOBBER1-buffer | `test/doc-clobber-guard.test.mjs` read the base/HEAD copies with `execFileSync` at Node's default 1 MiB `maxBuffer`. `PLANNED-BACKLOG.md` crossed it (main 1,047,603 B; round 73 1,050,382 B), `git show` threw ENOBUFS, and the setup check reported "PLANNED-BACKLOG.md not found at HEAD". So the guard went red on **any** backlog growth. Fixed: `maxBuffer: 64 MiB`. The long-term cure is DOCMAP3's shipped-row archive (the backlog is over 1 MB). | ✅ fixed 2026-09-23 (Cowork round 73) | Cowork round 73 2026-09-23: reproduced red at HEAD `a08a114a`, green after the fix |
| GOV-UX1-D5-gate-bank | ✅ **CLOSED 2026-09-23 (GOV-UX1-D5-gate-2).** New `lcc_owner_name_is_plain_bank` OR-ed into `lcc_owner_name_is_bank_or_trustee` (the one choke point, 7 consuming views). Matches a name ending in Bank / `Bank of X` / Bancorp / Bancshares / Banking Corp / Bank & Trust; never a name with an SPE/real-estate suffix (LLC, Limited, Building, Plaza…) or a charity (`Food Bank`). Graded 32/42 bank-ish seller-universe names, all banks (misses `Ameriserv Financial Bank, Pensylvania`, stated). **Seller queue 502 → 498: exactly 4 rows, all Truist Bank ($27.6M).** Universe 8,282 → 8,234, top-seller 4,055 → 4,029, Tier 0 candidates 740 → 724; decidability / loan-maturity / user-owner / entity-roles unchanged. | ✅ live 2026-09-23 | GOV-UX1-D5-gate grading; Cowork round 69; migration `20261102280000`; response `docs/claude-code/responses/GOV-UX1-D5-gate-2.response.md`; guard `test/gov-ux1-d5-gate-2.test.mjs` (11 tests, 17/17 mutations RED) |

## P19 — 🏛️ OWNERSHIP EVIDENCE: deeds, owner-source conflicts, research lanes, the owner gap (2026-09-13 → 2026-09-16)

| # | Item | State | Source |
|---|---|---|---|
| DEED1-reconcile-2 | 🟢 **PROMPTED 2026-09-15 — port the reconciliation migration into this repo and close Dialysis PR #7412.** `20260915200000_dia_deed1_reconcile_live_drift.sql` was applied live and verified behavior-neutral (all seven checks identical), but committed to `sbriggssjc/Dialysis` and absent from `life-command-center`, so the drift **moved rather than closed** — rebuilding Dialysis_DB from this repo still restores the old comparator and drops the alias table. ⚠️ **Leaving #7412 open is the dangerous outcome**: a dia migration in a repo that may not own those objects can be re-applied later and overwrite a running object — the exact hazard the `government/` retirement (I16) exists to prevent. Prompt forbids re-applying anything to the database (this is a file-location fix, not a schema change), forbids resolving the canon contradiction by editing the doctrine table (👤 Scott's call → **CANON-OWNERSHIP1**), and forbids touching `supabase/migrations/dialysis/README.md`, which is the page that got this right. 🔍 **One check Cowork could not run:** the `Dialysis` repo is not connected to this session, so whether it ALSO carries an older copy of `v_owner_source_conflict` is unverified — a second copy of a live object would matter and the prompt asks for it to be checked before porting. Prompt: `prompts/DEED1-reconcile-2-the-migration-is-in-the-wrong-repo.md`.   ✅ **DONE 2026-09-16 (LCC PR #2535 + Dialysis PR #7414, both merged).** Hashes matched the pin before and after (view `9fc5aa3f…`/4747, function `72b48cd9…`/2183); migration ported byte-identical to `supabase/migrations/dialysis/20260915200000_dia_deed1_reconcile_live_drift.sql`; the Dialysis copy removed (#7412 had already merged, so #7414 removes it); `CLAUDE.md`'s Dialysis_DB inventory row rewritten so it reads as a file-location census, not an ownership verdict, citing this incident. Nothing applied. | ✅ done | Cowork 2026-09-15; done 2026-09-16 |
| GOVDEED2 | 🟢 **HANDOFF WRITTEN 2026-09-15 to `government-lease`** — add `AND d.recording_date IS NOT NULL` to step 1's `bridged` CTE (matching step 2 exactly), commit the function to source control for the first time, and guard `consideration` in `save_deed_record` with `_positive_or_none` so a model's `0` sentinel stops satisfying `has_value`. ⛔ No backfill: the 3,930 stay NULL ("Not on file") — no source exists to recover a date from. ⚠️ **But the 478 manufactured conflicts need their own decision** — they are not stale data, they are assertions that were never evidence; sizing what happens to them is a separate round. ⛔ Prompt forbids applying anything from `life-command-center`. Prompt: `prompts/GOVDEED2-handoff-to-government-lease.md`.  ✅ **SHIPPED AND VERIFIED LIVE 2026-09-15 — the manufacturing has stopped.** Read from the live `pg_get_functiondef`: step 1's `bridged` CTE now carries `AND d.recording_date IS NOT NULL` and `LIMIT p_limit`, matching step 2 exactly. Committed to `government-lease` as `sql/20260915_gov_govdeed2_propagate_deed_step1_date_guard.sql` (PR #400) — **the first time this function has existed in any repository**. dia untouched (correct, per DEED-DIA-LATENT). ⛔ **Recommendation #3 was REFUSED by the round, and the refusal was RIGHT — Cowork retracts it.** I recommended guarding `consideration` with `_positive_or_none`; `government-lease`'s CLAUDE.md §13d documents that a **$0 deed price is a real recorded fact** (quitclaims, intra-sponsor transfers), deliberately unguarded, with a test that fails if someone "fixes" it. Declining a handoff instruction because it contradicts documented doctrine in the owning repo is exactly right. ⚠️ Cowork could not read §13d directly (`government-lease` is not connected to this session) and takes the round's report of it at face value. ⭐ **But the underlying defect is real and the target was simply wrong** — measured across the 4,908 dateless rows: **4,854 have `consideration = 0`, ZERO have `consideration > 0`**, and **4,881 have a placeholder grantor**. The $0 quitclaim §13d protects **does not appear in this population at all**, so doctrine and defect were never in conflict. → **GOVDEED3** retargets it at the accept gate. ⚠️ **Two consequences verified and worth not forgetting:** (a) **"no backfill" did not leave the 3,930 as NULL — it FROZE them as the manufactured value** (still exactly 3,930), because the guard removes those properties from `bridged` entirely, so this producer can **never revisit them**; the **478** manufactured conflicts remain and gov `auto_fixable` is still **0**. (b) `LIMIT p_limit` in step 1 sits after a window function with no ordering, so it truncates arbitrarily **after** `row_number()` and could drop `rn=1` rows — latent only: the dated bridged population is **729 rows across 178 distinct properties**, far under 5,000, and it matches step 2's existing shape. | ✅ live | Cowork 2026-09-15 |
| GOVDEED4 | 👤 **80% of the DATED gov deeds carry a date the model was TOLD to invent, and no consumer can tell (Cowork, 2026-09-16) — `prompts/GOVDEED4-low-confidence-dates-are-written-as-recorded.md`.** The C2g read noticed `latest_deed_date='2023-10-01'` on six pairs; one query up it is on **130 properties**, and it is the single most common `recording_date` in `deed_records` (**486** rows). Payloads explain it: `src/public_record_ingest.py`'s extraction prompt rule 5 says *"if exact day is unknown but month/year is known, use YYYY-MM-01 and set date_confidence='low'"* — and the recall path emits a day-01 date when it knows nothing at all. `save_deed_record` writes it straight to `recording_date` (l.1164), stores `date_confidence` **only in `raw_payload`** (l.1228), and lets it satisfy `has_chronology_key` (l.1179), so a `"Prior Owner"` placeholder + a guessed date passes the gate. 📊 Of **844** dated rows, **676 (80%) are `date_confidence='low'`, 676/676 day-01, 667 no document number, 451 placeholder grantor**; only **2** rows are `high` with a document number; top values `2023-10-01`×486, `2023-01-01`×117, `2025-01-01`×43; created 2026-04-03 → **2026-09-14, still producing**. ⚠️ **GOVDEED2 made these the WINNING deed**: the NULL guard removed the dateless competitor, so **493** properties now source `latest_deed_date` from a `low` row, and 25 `ownership_history` rows carry one. dia is clean (230 dated, 0 low, no clustering) — do not fix in sympathy. Non-NULL half of the GOVDEED sentinel class; one more flag a producer emits and nothing reads. 👤 `government-lease`: honour the flag at write time (approx column or drop), stop rule 5 inviting the shape, test with positive control; the 676 join GOVDEED-478's disposition round. ⛔ No backfill, no dia, nothing applied from LCC.   🟡 **BUILT 2026-09-16 — gov PR #401 (`claude/modest-wozniak-kyexxl`, `ce1d5bc`), migration `sql/20260916_gov_govdeed4_low_confidence_date_guard.sql` NOT applied** (live: columns absent, 844/676 unchanged). Shape chosen: `recording_date_approx` + real `date_confidence` column; `has_chronology_key` ignores approx; rule 5 → `null`; 5 tests w/ positive controls. Deviations: ① guard is `low` **AND day-01** only (a `low` non-01 date still writes as recorded — follow-up); ② migration **demotes the 676 in place** with a reversal snapshot `_gov_govdeed4_demoted_dates_20260916` — the disposition the handoff deferred to GOVDEED-478; ledgered + reversible, accepted, but it makes the apply a data change. ⚠️ Unstated: whether the `properties.latest_deed_date` values (**147** distinct properties; first written as 493 = join rows) / **25** `ownership_history` rows sourced from those deeds are re-propagated — read the SQL for that before applying.   ✅ **APPLIED LIVE 2026-09-16 (Cowork, Scott's call: apply + clear).** Migration read first: it never touches `properties`, and `propagate_deed_to_property` only SETs. Applied → **168 dated / 676 approx / 676 snapshot**, 0 low still dated (∅ 145 · medium 21 · high 2). Then ledgered in `_gov_govdeed4_cleared_properties_20260916` (147 rows w/ prior date+grantee, `outcome`): propagate re-run → **12 repointed** to a real dated deed; **135 cleared** (`latest_deed_date`+`latest_deed_grantee` → NULL, no surviving dated source). `latest_deed_date='2023-10-01'` 130 → 5 (those 5 are `sales_transactions`-sourced → GOVDEED5). 17 `ownership_history` rows still carry a demoted date — GOVDEED-478 disposition. Reversal: the two `_gov_govdeed4_*` tables. Follow-ups owned by gov: the day-01-only guard (deviation ①); the migration header's "493" is the join-row figure. | ✅ live | Cowork 2026-09-16; built 2026-09-16; applied 2026-09-16 |
| GOVDEED5b | 👤 **Three sale propagators the split missed rewrote `latest_deed_date` on 3,310 properties twenty minutes after GOVDEED5 applied (Cowork, 2026-09-16) — `prompts/GOVDEED5b-three-more-writers-undid-the-split-in-20-minutes.md`.** `gov-propagate-recompute-tick` (03:30 UTC) → `propagate_sales_recompute(48)`, whose candidate predicate `COALESCE(latest_deed_date,'1900-01-01') < sale_date` admitted every property GOVDEED5 had just NULLed, and whose write is `latest_deed_date = sale_date`. Two triggers (`propagate_sale_to_property`, `pse_propagate_sale_to_property`) carry the same shape. Fix: re-point all three to `latest_transfer_*` (source `sale`, newer-wins), re-run the deed-only recompute (snapshot first), prove by hand-running the cron and firing the trigger in a rolled-back txn that 43 stays 43, and add a live `pg_proc` test that only `propagate_deed_to_property` writes `latest_deed_date` (positive control against today). Class: the writer inventory was taken from the `sql/` tree, not from `pg_proc` — the same gap as C1B-GOV-GATE's live-only view. ⛔ Do not disable the cron/triggers as the fix.   ✅ **FIXED + APPLIED LIVE 2026-09-16 — gov PR #405** (`sql/20260916_gov_govdeed5b_recompute_writers_fix.sql`, migration `20260916115400`). All three re-pointed to `latest_transfer_*` (`source='sale'`); `latest_deed_date` writers in `pg_proc` **4 → 1** (`propagate_deed_to_property` only); `latest_deed_date` **3,340 → 43**, grantee 43; cron hand-run and a trigger fire in rolled-back txns proved no touch; 15 tests, 2 go red on revert. Re-measured here: 43 / 43 / 1 writer / transfer 26 deed + 5,347 sale. `v_owner_source_conflict` 1,295 (1,290 sale-party vs recorded owner — the review population GOVDEED-478 protected, now the whole sale set; not a defect, a queue). Out of scope and not re-verified: the Python writer's deployed state. | ✅ live | Cowork 2026-09-16; fixed 2026-09-16 |
| GOVDEED-478 | 🔴 **The 478 manufactured owner-source conflicts need a disposition decision, and nothing will correct them on its own — 👤 Scott / `government-lease`.** GOVDEED2 stopped the producer but deliberately did not backfill, which is correct — **but the consequence is that these rows are now FROZEN, not pending**: the date guard removes those properties from the producer's `bridged` CTE entirely, so no future run will ever revisit them. **478 of 938 government owner-source conflicts (51%)** assert that a real property changed hands, sourced from an LLM's recall of a grantee it was never shown, with no recording date and no document number — and **84.1%** of the underlying deed rows have a grantee byte-identical to the property's own `recorded_owners.name`, i.e. an echo of the prompt's own context. ⚠️ These are **not stale data to age out; they are assertions that were never evidence**, and this repo's standing rule is "Not on file" over a plausible value. Options to size, none chosen: null the `latest_deed_grantee` where it traces to a dateless placeholder row; mark the underlying `deed_records` rows rejected and let the conflict view drop them; or leave them and teach the view to exclude placeholder-sourced deeds. ⛔ No option may invent a date or a grantee.   ✅ **DECIDED 2026-09-16 (Scott): exclude placeholder deeds in the view + clear the planted grantee** → `prompts/GOVDEED-478-disposition-exclude-placeholder-deeds.md` (👤 gov). Re-measured after GOVDEED4: `v_owner_source_conflict` **899** = **478 dateless** (all from deed rows with no date, no approx, no document number — unchanged) + **405 backed by a `sales_transactions` date** (real review candidates, must not be hidden) + 9 real-deed + 7 other. Fully undated no-doc deed rows: **4,995**. Plan: mark (not delete) `evidence_status='rejected_placeholder'` with a snapshot; view + propagators ignore them; `latest_deed_grantee` NULLed only where it traces solely to a rejected row (ledgered like GOVDEED4). Not chosen: delete the 4,995 (GOVDEED3 still wants to read them); view-only (leaves the grantee lie on 478 property rows).  ✅ **APPLIED LIVE 2026-09-16 — gov PR #402** (`sql/20260916_gov_govdeed478_placeholder_deed_conflict_cleanup.sql`, dry-run in a rolled-back txn matched the apply exactly). `deed_records.evidence_status='rejected_placeholder'` on **4,995** (snapshot `_gov_govdeed478_rejected_deeds_20260916`); `propagate_deed_to_property` + `v_property_earliest_deed_grantee` exclude them (the only two decision-surface readers; two coverage counters left alone). Properties: **290** grantee cleared, **91** grantee+price cleared, **97 held** (sale-corroborated → GOVDEED5), ledger `_gov_govdeed478_cleared_properties_20260916` 478 rows. `v_owner_source_conflict` dateless 478 → 97; total 899 → **518** at the time (now 1,295 — see GOVDEED5b, not this round's doing). No deed row deleted. | ✅ live | Cowork 2026-09-15; decided 2026-09-16; applied 2026-09-16 |
| C1B-GOV-GATE | 🚨 **C1b's gov gate is LIVE and gates the WRONG LANE — a guard that has never once guarded (Class 11), found 2026-09-16 (Cowork).** C1b was supposed to set `gate_pass=false` / `gate_reason='lane_no_consumer'` on `owner_needs_salesforce` so the generator would stop minting into it. In the live government project (`scknotsqkcheojiaewwh`) `v_ownership_gaps` **does** contain exactly one `lane_no_consumer` marker — but it sits on the **`owner_needs_sos`** arm. The actual `owner_needs_salesforce` arm's `gate_pass` is still the ordinary value/placeholder predicate (`NOT gov_research_gate_is_placeholder_owner(...) AND NOT is_generic_gov_owner(...) AND n_props > 0 AND owner_rent >= gov_research_gate_value_floor()`). ⚠️ **This is why an existence check was never going to be enough** — the view exists, the marker string exists, and a grep for `lane_no_consumer` returns a hit. Only reading WHICH ARM carries it shows the gate missing. Same shape as XB2-precision (function present, old body) and the reason **DEPLOY2-stale** is still open. 💥 Measured effect: **175 `owner_needs_salesforce` tasks minted in 8 days** (2 on 09-08, 1 on 09-09, 10 on 09-12, **156 on 09-13**, 1 on 09-14, 5 on 09-15) against a lane everyone believed was sealed on 2026-09-08. The mint path itself is correct — `fetchNbaFeed` applies `NBA_MINT_GATE_FILTER = 'gate_pass=is.true'` server-side (`api/_shared/nba-feed-sweep.js`) — so the defect is entirely in the view arm, not the generator. 👤 **Owner is `government-lease`, not this repo** (ID3a-d): the gov copy of C1b lives in this repo's retired `supabase/migrations/government/` directory, so whatever is live over there either never took this change or superseded it. ⛔ **Do not fix it by re-applying this repo's copy** — that directory's README exists precisely because re-applying its files restores known-bad state. ⚠️ **Blocks the gov arm of C1c / C2**; the dia arm is unaffected and can proceed.   ✅ **DECIDED 2026-09-16 (Scott): seal SF, unseal SOS** → `prompts/C1B-GOV-GATE-fix-seal-sf-unseal-sos.md` (👤 gov). Re-measured live 2026-09-16: `owner_needs_salesforce` **1,838 passing** of 13,727; `owner_needs_sos` 0 of 16,861, the only `lane_no_consumer` arm — still wrong. Fix: move the marker to the SF arm, restore the ordinary predicate on SOS (OWNERGAP2 will consume it); the test must read WHICH ARM carries the marker, with a positive control that the current definition fails it. After it lands: C1C's gov arm (`p_research_types='{owner_needs_salesforce}'`, dry run, ≈1,851).  ✅ **FIXED LIVE 2026-09-16 — gov PR #403** (`sql/20260916_gov_c1b_gate_fix_seal_sf_unseal_sos.sql`; `v_ownership_gaps` had **no committed source anywhere** before this — live-only, now in the repo). After: `owner_needs_salesforce` **0** passing (`lane_no_consumer`), `owner_needs_sos` **2,019** passing with the ordinary reasons; the other two arms unchanged. Test parses the view by UNION-ALL branch keyed on each branch's `gap_type` literal, with positive controls reconstructing the buggy text (8/8). ⚠️ Consequence: LCC now holds **1,346 open `owner_needs_sos`** tasks and no consumer — OWNERGAP2 is the consumer and is now the next build, not a someday. | ✅ live | Cowork 2026-09-16; fixed 2026-09-16 |
| C1C-SPLIT | 🟢 **PROMPTED 2026-09-16 — `prompts/C1C-SPLIT-retire-the-dia-lane-only.md`.** C1c cannot be applied as written because `lcc_c1c_retire_sf_lanes(p_dry_run, p_batch_tag, p_limit)` has **no lane scoping**: its plan selects `research_type = any(_lcc_c1c_lane_types())`, a hardcoded two-element array, so it is all-or-nothing and "all" now includes the ungated gov lane (**C1B-GOV-GATE**). ✅ **Measured, and it makes the fix small: lane and domain are 1:1** — `owner_needs_salesforce` is **1,851 rows, all `domain='government'`**; `true_owner_needs_salesforce` is **838 rows, all `domain='dialysis'`**. So a `p_research_types text[] default null` parameter is sufficient and the prompt explicitly **forbids adding a domain parameter** (two selectors that must agree is a defect waiting to happen). ⚠️ Prompt carries the **42725 overload trap** by name: adding a defaulted parameter with `create or replace` leaves BOTH signatures live, so it requires an explicit `drop function` first **and an assertion that exactly one `lcc_c1c_retire_sf_lanes` remains in `pg_proc`** — do not trust the DROP, assert it. Also required: an out-of-range lane name must **raise, not no-op** (a silent zero-row success is this arc's signature failure), and `null` must still mean both lanes so the reversal runbook and a future gov retirement keep working. ⛔ Forbidden: touching the gov lane, fixing the gov gate, re-applying the retired `government/` C1b copy, narrowing `_lcc_c1c_lane_types()`, or editing `20260908130300` in place. 🔍 CC's sandbox has no Supabase egress (measured on DEPLOY2), so the prompt tells it to ship and state the live run as pending rather than fake it; **Cowork applies and runs**: DDL → scoping migration → dry run (expect **838 dia / 0 gov** — anything else is a STOP) → real run → re-read the lane summary with gov unchanged at ~1,851. ✅ **SHIPPED 2026-09-16 (Claude Code) — `supabase/migrations/20260916120000_lcc_c1csplit_scope_retire_by_lane.sql`.** `lcc_c1c_retire_sf_lanes` gains `p_research_types text[] default null` as its 4th, trailing parameter; the pre-split 3-arg signature is `drop function if exists`-ed **before** the `create or replace`, and the migration asserts (via a `do $$ ... raise exception` block reading `pg_proc`) that exactly **1** signature survives — the overload trap, self-checked rather than trusted. `NULL` still resolves to the full `_lcc_c1c_lane_types()` set (both lanes); a non-null array is validated against that same set and **raises** on an empty array or any unrecognised lane name — no silent zero-row success. `_lcc_c1c_lane_types()` itself, `lcc_c1c_unretire`, `lcc_c1c_reopen_tasks` and `lcc_c1c_reopen_relinked` are untouched. Guard: `test/c1c-split-scope-retire-by-lane.test.mjs` (12 structural assertions over both migration files, comment-stripped; **all pass**, including a positive control that the unknown-lane RAISE interpolates the actual bad values rather than a static string). ⚠️ **THE LIVE APPLY AND RUN ARE PENDING — this sandbox has no Supabase egress (the same DEPLOY2 constraint), so nothing here was executed against LCC Opps.** Cowork applies in order: (1) `20260908130300` (DDL only, defines functions, runs nothing inline), (2) this scoping migration, (3) `select lcc_c1c_retire_sf_lanes(true, 'c1c-dia-<date>', null, array['true_owner_needs_salesforce'])` — **expect `tasks_to_retire = 838`, `research_types = ["true_owner_needs_salesforce"]`, and 0 gov rows touched; anything else is a STOP, re-measure before writing** — (4) the same call with `p_dry_run => false`, (5) re-read `v_lcc_research_lane_summary`: dia lane → 0 open, gov `owner_needs_salesforce` unchanged at ~1,851. C2 stays open until the dia lane is confirmed retired live; C1B-GOV-GATE stays open and out of scope for this change.   ✅ **APPLIED + RUN LIVE 2026-09-16 (Cowork, LCC Opps):** `20260908130300` (9 objects, previously absent) then `20260916120000` applied; both validation guards positive-controlled live (empty array and `owner_needs_sos` each RAISE). ⚠️ **The first dry run said `tasks_to_retire = 1` with `by_type = {dia: 839}`** — the dry-run `count(*)` ran over the per-lane GROUP BY, so it counted LANES (present in 20260908130300 too; never seen because never applied). The A2 shape, inside the function whose dry run is its safety property. Fixed as **`20260916130000_lcc_c1csplit_b_dry_run_counts_tasks.sql`** (`sum(n)`), applied live BEFORE writing, guard test added. Corrected dry run: **839 dia / 0 gov** (838 + one row minted 2026-09-15 — the dia trickle is 2 in 9 days, not 0; a trickle, not a refill). Real run `c1c-dia-20260916`: **839 retired**, 839 ledger rows, 839 stamped `terminal=true`/`c1c_lane_no_consumer`, `v_lcc_c1c_retired_watch` 839 (0 gov). `v_lcc_research_lane_summary`: dia lane gone (0 open), gov `owner_needs_salesforce` **unchanged at 1,851**. Reverse with `lcc_c1c_unretire('c1c-dia-20260916')`.   ✅ **GOV ARM RUN 2026-09-16 (Cowork)** once C1B-GOV-GATE was verified live: dry run `tasks_to_retire = 1851` / `by_type {owner_needs_salesforce: 1851}`, all `government`; real run `c1c-gov-20260916` → **1,851 retired**. `v_lcc_c1c_retired_watch`: `{c1c-dia: 839, c1c-gov: 1851}`; `v_lcc_research_lane_summary` no longer lists `owner_needs_salesforce` at all. C1C is closed on both arms. Reverse: `lcc_c1c_unretire('c1c-gov-20260916')`. | ✅ closed both arms | Cowork 2026-09-16; dia 2026-09-16; gov 2026-09-16 |
| OWNERGAP1 | 🚨→✅ **Unit 1 (containment) SHIPPED 2026-09-14 (Cowork) — and the originally-named producer was WRONG.** `tax_records.raw_payload->>'mailing_owner'` contained **12 distinct `ABC`/`XYZ` names** across **228 rows / 119 counties** (first seen 2026-05-20, last 2026-08-24) plus **142 rows** holding the literal `"Unknown"` as if it were a fact (**P180**). None were linked to a property, so nothing rendered them. **The finding named `api/_handlers/sidebar-pipeline.js` as the writer — verified false (zero grep hits for `mailing_owner` in this repo).** The real producer is **`Dialysis/src/public_record_ingest.py::write_tax_record`** (sibling repo), calling `gpt-4o` with the property's own recorded/true owner fed into the prompt and no county HTTP call anywhere in the module — the **fourth instance of the exact mechanism** already recorded in this repo for `assessed_value`/`tax_amount`/`tax_delinquent` (see `docs/architecture/public-records-source-lane.md` §2a, PR1/PR1a/PR1b), recurring on a field those rounds never touched. **Contamination was also wider than named** — `entity_registry_records.entity_name`, `recorded_owners.name` and `true_owners.name` all carry the same pattern. **Shipped (Dialysis_DB `zqzrriwuavgrquhisnoa`, live, migration `20260914150000_dia_ownergap1_fabricated_owner_quarantine.sql`):** one detector (`dia_is_fabricated_placeholder_owner`, never a `contains` rule per P158a), a reversible append-only quarantine log, `tax_records.mailing_owner` NULLED + flagged (not an identity column), the three identity tables flagged-only with names PRESERVED (they carry FKs), write-time guard triggers on all four tables so the write cannot recur, and a properties-FK loophole closer (`trg_dia_ownergap1_property_owner_link_guard`) scoped to `fabrication_quarantine_reason='fabricated_placeholder'` **only** — ⚠️ corrected live before shipping after discovering a `recorded_owners` row literally named `"Unknown"` is referenced by **23 real properties**; a generic `quarantined_at IS NOT NULL` check would have silently severed those links. Before/after: fabricated 228→0, `"Unknown"` 142→0, **0 properties reference any `fabricated_placeholder`-flagged row**, **no property owner field written by any of this**. Guard `test/ownergap1-fabricated-owner-quarantine.test.mjs` (40 tests, positive+negative controls incl. `"AZ Business Trust LLC"` / `"X Y Z Dialysis Consulting LLC"` adversarial negatives). Full suite 6,231/0/6-skipped. **Not shippable from this session:** the Python producer fix itself (this session has read-only access to `Dialysis`) — filed **`OWNERGAP1-producer`**. No Railway redeploy needed — the whole fix is a live Supabase migration + test + docs. Units 2–3 (re-measurement, costed decision doc) — see `PDR2-noowner` row above and `docs/audits/OWNERGAP1_FABRICATED_OWNER_AND_UNRECOVERABLE_GAP_2026-09-14.md`. ✅ **VERIFIED INDEPENDENTLY 2026-09-14 (Cowork) — containment is live and positive-controlled BOTH WAYS.** `trg_dia_ownergap1_*_guard` exists and is **enabled** on **four** tables (`tax_records`, `entity_registry_records`, `recorded_owners`, `true_owners`); **370 rows quarantined** (228 `fabricated_placeholder` + 142 `unstated_placeholder`), rows intact, reasons distinct. Re-ran the control from this side in a rolled-back transaction: `XYZ Dialysis Centers LLC` → nulled + `fabricated_placeholder`; `Unknown` → nulled + `unstated_placeholder`; **a real owner (`Decarion Family Trust`) passes through untouched** — no false positive. | ✅ | OWNERGAP1 unit 1 shipped 2026-09-14 |
| OWNERGAP1-pilot | ✅ **PILOT RUN 2026-09-14 (Cowork, live in the browser) — the answer is "it depends on the county," and the three failed for THREE DIFFERENT REASONS.** OWNERGAP1's decision doc proposed a bounded manual check of Cook IL / Los Angeles CA / Harris TX (177 properties) to learn whether Option A's yield is nearer 40% or 5%. Done, in minutes rather than an hour. ⚠️ **A constraint the doc did not weight: these properties have essentially NO APNs** (Cook **0/73**, Harris **0/50**, LA **1/54**), so every lookup must work from a **street address alone**. **(1) Harris TX ✅ WORKS** — `search.hcad.org`, free, address search, one-time Cloudflare check then clear, **and it returns the owner**: `5040 Crenshaw` → **`CRENSHAW MOB LLC`** (16,915 SF, $1.90M, *Commercial*), cleanly separated from the tenant's two *Personal* property accounts (`FRESENIUS MEDICAL CARE GREATER SOUTHEAST HOUSTON LLC`, `FUSA MARKETING`) — **the operator-vs-owner distinction PDR2 is about, drawn correctly by the county**. CSV/XLS/PDF export buttons present, so bulk extraction looks feasible. **(2) Cook IL ⚠️ MANUAL ONLY** — free address search exists (house number / direction / street / city) but **every search is CAPTCHA-gated**; usable by a human, **not automatable**, and not something this session may bypass. **(3) LA CA ⛔ YIELDS NOTHING** — free, no CAPTCHA, but the portal **does not publish owner names at all**: read the full parcel-detail page for AIN 2350012065 — situs address, use code, building characteristics, complete assessment history, and ownership *events* (recording date, doc #, sale price) — **and no owner name anywhere**. No amount of effort recovers an owner LA does not publish. | ✅ | OWNERGAP1 pilot 2026-09-14 |
| OWNERGAP1-decision | ✅ **REFRAMED AGAIN BY A SECOND SWEEP 2026-09-14 (Cowork, live) — and Scott's no-paid-vendor constraint looks satisfiable.** Six more jurisdictions checked. **The useful question is not "can we search this county's portal" but "does this jurisdiction publish a FREE BULK FILE containing the owner"** — and several of the largest do. ✅ **Harris TX** (portal + CSV/XLS export) · **Dallas TX** (owner-name search + COMMERCIAL filter) · **Miami-Dade FL** (dedicated OWNER NAME tab) · **Philadelphia PA** (address→owner, full grantee/grantor sales history, **and a documented free bulk dataset download**) · **NYC/PLUTO** (free, tax-lot level, **monthly**, 26v2 Aug 2026, covers all 5 boroughs). ⚠️ **Cook IL** CAPTCHA → manual only; **LA CA** publishes no owner → unreachable from the county at any price. **Measured coverage of the obvious free-bulk targets: TX 432 + FL 302 + Philadelphia 25 + NYC 56 = 815 of 4,020 (20.3%)** with no scraping, no CAPTCHAs and no vendor. ⚠️ **That is COVERAGE, not a hit rate** — matching is by address and these rows carry almost no APNs, so **measure the match rate on one downloaded file (Philadelphia, 25 properties) before building anything.** 👤 Next step is a download and a match count, not a build and not a quote. ✅ **ANSWERED BY OWNERGAP2, 2026-09-16 — the real resolved count is 20 of 26 Philadelphia properties (76.9%), measured against the live API rather than sampled by hand.** The 'download a file and count matches' step this row asked for was superseded by a better one: the free open API was queried directly for the whole population and the production matcher scored against it. ⚠️ **The 20.3% free-bulk coverage figure above is COVERAGE and still is** — 76.9% is the match rate *within* Philadelphia's 25, so the recoverable population is ~0.77 × coverage, not 4,020. ⚠️ **And re-measure before quoting this row's denominators:** owner-unknown is **4,014** (not 4,021), `recorded_owners` **7,585** (not 7,487), owned properties **5,473** (not 5,467). | ✅ superseded by OWNERGAP2 (41 owners live) | OWNERGAP2 build 2026-09-16 |
| OWNERGAP1-match | ✅ **MATCH RATE MEASURED 2026-09-14 (Cowork, live) — Philadelphia returns ~68%, and the misses are a formatting bug, not missing data.** Queried Philadelphia's free public open-data endpoint (`phl.carto.com/api/v2/sql`, `opa_properties_public`) — no scraping, no login, no vendor. **Pass 1 (exact address): 9 of 22 distinct addresses. Pass 2 (house-number prefix + street): 6 more → 15/22 ≈ 68%.** 🔑 **Every pass-1 miss has one cause: Philadelphia stores address RANGES and LCC stores the lead number** — `4126 Walnut St` vs `4126-38 WALNUT ST`, `1300 W. Lehigh Ave` vs `1300-24 W LEHIGH AVE`, `1172 S Broad` vs `1172-74 S BROAD ST`. That is an address-normalisation problem (**OWNERGAP1-ollama's legitimate lane**), not a data-availability one. 🚨 **First prospecting signal fell out immediately: `FILIPPONE EDWARD J TR` owns 109 Dickinson St and `FILIPPONE-NEWMAN LLC` owns 1172-74 S Broad St — the same family behind TWO Team Briggs dialysis properties**, invisible yesterday because both read "owner unknown". ⚠️ **One jurisdiction is not a rate** — Philadelphia is a well-run open-data city; re-measure on a TX CAD and a FL county before projecting 68% onto 4,021. ⚠️ **The real hard case is multi-parcel sites**: `3300 Henry Ave` returns **six** owning entities (Falls Center LPs) for one street address — needs a unit/parcel discriminator, and no address matching resolves it. Full detail: audit doc §8. | ✅ | OWNERGAP1 match test 2026-09-14 |
| OWNERGAP1-match2 | ✅ **HARRIS TX MEASURED 2026-09-15 (Cowork, live): 6 of 7 = 86%** — second jurisdiction, and it beats Philadelphia's 68%. Owners recovered: **CRENSHAW MOB LLC** (FKC Pasadena-Crenshaw) · **BEAMER SCARSDALE LP** (DaVita Sagemeadow) · **ROY AND VEVA MORRISON RANCH CORPORATION** (DaVita Garden Oaks) · **US INVESTMENTS** (DaVita Inwood) · **AALS PROPERTIES LLC** (FKC Atascocita) · **FULTON SHOPPING CENTER INC** (FMC Moody Park). 🔑 **Harris types every account `Personal` vs `Commercial`, so the county draws PDR2's operator-vs-owner distinction FOR US, for free** — the tenant is always the Personal account, the owner always the Commercial one. **A matcher should key on that account type rather than re-deriving operator-vs-owner from name text.** ⚠️ The single miss (`4427 Cypress Creek Pkwy`) is a **street alias** — Cypress Creek Pkwy is Houston's renamed FM 1960 — a **third** miss cause distinct from Philadelphia's address ranges and its multi-parcel sites, and the one a string normaliser cannot fix alone (needs an alias list or a geocode). ⚠️ **Miami-Dade was NOT sampled** — two rates in this range would not have changed the decision; named as not-done rather than implied. Detail: audit doc §9. | ✅ | OWNERGAP1 match test 2 2026-09-15 |
| OWNERGAP2-harris-b | 🔴 **Fetch the stage by HCAD's street shape, compare with the suffix optional, stream the 889 MB file, take `owner_name` from `owners.txt` — `prompts/OWNERGAP2-harris-b-…md` (Cowork, 2026-09-16, from the first live dry run).** Test against the 37 real staged rows (expected resolves and refusals listed in the prompt: two-account/two-owner streets refuse, three-account/one-owner resolves). Then Cowork re-runs the dry run → Scott's read → apply.   ✅ **BUILT 2026-09-16 (PR #2531, `6c97c86a`), reconciled by Cowork.** `harrisPdataStreetKeys()` now queries HCAD's bare `str` with `str_num` and alias expansion (SH 249 / State Hwy 249, N Loop E, FM 1960…); suffix optional when LCC carries none; two extra real bugs found and fixed on the way — `stageRowToLocation` was concatenating `site_addr_2/3` (city/zip) into the street text, and `normalizeAddress` collapsed `Northwest Fwy` to `FWY`. Against the 37 real staged rows: **24 of 25 named targets resolve, Little York 2711 correctly refuses** (`needs_parcel_discriminator`); Crawford and Kirby resolved rather than refused because their non-F1/F2 accounts are excluded as untyped (Cowork flagged Kirby 9001 for a spot-check on the harris row). 19 new tests, 89 OWNERGAP2 tests, suite 6,452/0. Loader streams (`JSZip.nodeStream()` + `readline`), `owner_name` from `owners.txt`, `mailto` → `owner_name_2`, `--dsn` accepted; verified through the real decompression path on a synthetic 50k-row zip — **the production zip has not been loaded yet** (H6). Live outcome is on the OWNERGAP2-harris row (19 applied). | ✅ merged; live 19 applied | Cowork 2026-09-16 |
| OWNERGAP2-ledger-order | 🔴 **An owner was written without a ledger row — `prompts/OWNERGAP2-ledger-order-…md` (Cowork, 2026-09-16, from the H7 apply).** The tick writes the owner first and the ledger second; when the ledger insert collides on `uq_dia_ownergap2_open_attempt (batch_tag, property_id)` — a property refused in an earlier run under the same tag, resolved now — the owner write stands and `wrote` over-reports. Fix: ledger first in the same transaction (or roll the owner write back), the tick derives its own batch tag or refuses one with open attempts, a test where the ledger insert fails and `recorded_owner_id` stays unchanged, and an explanation of the 48 consumed ledger ids (sequence at 125, 77 rows). Ledger row 125 is Cowork's hand repair and stays.   ✅ **BUILT AND MERGED 2026-09-16 (PR #2540), running.** `applyOwnerResolution()` reads the ledger result and on failure PATCHes the property back to NULL (re-checked against the owner id it wrote) reporting `ledger_write_failed:<status>` — `wrote` ≤ ledger rows, always. Default `batch_tag` minute-granular; a supplied tag is checked against open attempts for the population and refused `409 batch_tag_collision` on the write path (dry-run GET with the old tag still answers — correct, dry runs ledger nothing; verified live). Sequence gap: `bigserial` `nextval()` fires before CHECK/unique evaluation → the 48 ids are failed inserts, nothing deleted. Row 125 untouched. 6,486/0. | ✅ merged + running | Cowork 2026-09-16; built 2026-09-16 |
| OWNERGAP2-harris-d | 🔴 **`include_classes=C2` on the resolve tick, exact-arm only for admitted classes, `state_class` in the citation — `prompts/OWNERGAP2-harris-d-include-classes-on-the-tick.md` (Cowork, 2026-09-16, from S5).** harris-c put `includeClasses` on the matcher and loader; nothing passes it from the route, so the two C2 targets (`380 E Little York`, `10311 S Post Oak`) cannot resolve live. Range/containment arms stay F1/F2-only (a vacant-lot account next door must never be taken for the clinic's parcel). Cowork runs the dry run after merge; expect 2 of 29.   ✅ **MERGED 2026-09-17 (PR #2548), dry run done by Cowork.** `include_classes=C2` (closed allow-list), exact-arm-only for admitted classes (`class_admitted_requires_exact_situs`), `state_class` in the citation, 45 tests, suite 6,510/0. Live: population 30 → **1 resolved / 29 refused**. `10311 South Post Oak` → `LUEL PARTNERSHIP LTD 2-03` (acct 0440360000028, C2, exact) — **H8, apply on Scott's go**. `380 W Little York` refused: HCAD's C2 account is at 380 **E** Little York Rd, a different address — the exact-situs rule did exactly what S5 asked. 27 remain the situs gap (§P10a).   ✅ **H8 APPLIED 2026-09-17 (Cowork, Scott's go):** `10311 S Post Oak` → LUEL PARTNERSHIP LTD 2-03, batch `ownergap2_harris_tx_202609171319` (tick-derived), ledger row written with `state_class=C2` — the ledger-order fix seen working. Harris: **21 of 50 applied.** | ✅ live; H8 applied | S5 2026-09-16; built 2026-09-17 |

## P20 — 🖥️ APP OBSERVATIONS & FLOW HEALTH (SB-notes intake, 2026-09-16 →)

| # | Item | State | Source |
|---|---|---|---|
| FLOWS1-artifact | ✅ **LCC side SHIPPED 2026-09-16** — `fetchSharepointBytes()` accepts BOTH response shapes now: the byte shape (unchanged) and a metadata-only reply (pre-F1c live export, `{name,size,link,path}`, no `ok`/`content_base64`) OR the explicit F1c `{ok:false,reason:'too_large'}`, both normalized to a NAMED terminal `reason:'too_large'` (never the generic `pa_fetch_failed`). Propagated through `fetchDocBytes`/`extractDocumentText`. The CRE longdoc lane (`lcc-cre-doc-text-backfill`) already had a dated-marker mechanism (`writeDeferredMarker`/`CRE_CEILING_REASONS`) — `too_large` is now registered there as a CEILING reason (30-day expiry) instead of falling into the 24h transient bucket, so a doomed request drops from ~48 retries/30-day-window to 1. The plain doc-text lane (`lcc-document-text`, `processOneUrlRefetch`) gets its own terminal (`ARTIFACT_TOO_LARGE_TERMINAL`, distinct from the misleading `url_expired` — the file is real and reachable, just oversized), excluded from the eligible-queue query going forward. Read-only visibility: `GET /api/document-text-tick?mode=dead-letter` lists both terminal populations per domain (no new view/table — reuses `property_documents.ingestion_status`). Tests: `test/flows1-artifact-too-large.test.mjs` (11 tests). Not done: size-aware skip at discovery time (item 3, needs the sf-file-discovery flow to capture size first) and a true per-attempt backoff schedule for *non*-terminal failures (5xx/timeout) — those still retry every tick as before, which the ticket allowed as lower priority than stopping the terminal-reason storm. Scott's side: flow addendum F1c, already documented in `OPERATOR-CHECKLIST.md`.   ✅ **LIVE (PR #2528, Railway `c6fda4e7`) and F1c applied by Scott, verified from the export 2026-09-16** (metadata first from `triggerBody()?['server_relative_url']` with the site prefix stripped, `Size < 20000000`, content fetch inside True, bytes Response as specified; False = bare metadata, which LCC reads as `too_large`). Not done: size-aware skip at discovery; backoff for non-terminal failures → `FLOWS1-artifact-b` (low). | ✅ live both sides | Cowork 2026-09-16; built + F1c verified 2026-09-16 |
| HOME1-deploy | 🚨 **`daily-briefing` edge function on LCC Opps is still v25 (2026-05-12); HOME1 §C's `inferDomain` fix is merged but not running — the DaVita-under-Government bug is still live in every briefing until `supabase functions deploy daily-briefing` runs.** Same class as DIA1 §B (data-query v41) and "merged is not running": edge functions do not deploy with Railway. Add the edge-function deploy step to the PR checklist for any change under `supabase/functions/`.   ✅ **DEPLOYED 2026-09-16 by Scott** — `daily-briefing` **v26** (`functions list` verified); `_shared/domain-routing.ts` shipped with it. Behavioural check: the next briefing should list DaVita – The Villages under Dialysis. Process change stands: any PR touching `supabase/functions/` gets an edge-function deploy row in `OPERATOR-CHECKLIST.md`. | ✅ deployed | Cowork 2026-09-16; deployed 2026-09-16 |
| HOME2-c | 🟡 **Where do the three lanes go? "After Today" is below the fold because Today is 1,800 px tall.** Options: (a) lanes above Today (the lanes *are* Home; Today becomes the second panel); (b) lanes replace Today's SIGNIFICANT block (the BD lane is the same seller queue, so Today would keep IMPORTANT + URGENT only); (c) leave as is and accept a scroll. HOME1's spec decides if it says; if it does not, Scott does. No build until then. **Round 40 — PROMPTED as option (b)** after Scott's screenshots showed SIGNIFICANT and the BD lane carrying the same five sellers on one screen ("two different sections for this important, significant and urgent area"): `prompts/HOME2-c-lanes-replace-todays-significant-block.md` — lanes replace SIGNIFICANT inside the TODAY card, IMPORTANT + URGENT stay, equal-width lanes, flag-off = today's layout. Scott can still say (a) or (c) before sending. **Round 42 (Cowork) — ✅ LIVE, verified in Chrome on Railway `95137d03` with the flag ON:** the RESEARCH / BD / INBOX lanes sit at the top of the TODAY card, `#todaySignificantSection` hidden (display none, its BD content still rendered underneath), IMPORTANT + URGENT unchanged below, three See-all links present. One layout defect measured and filed as **`HOME2-d`**: the TODAY card is 498 px wide, the lanes' `1fr 1fr 1fr` grid resolves to 342 / 208 / 139 px and the INBOX lane runs to x=1006 past the card edge (809) — hidden behind MY WORK. CC's STATUS entry for this round is titled "Round 41 (CC)" — Cowork's own round is therefore 42. | ✅ live (HOME2-d filed) | Cowork round 42 browser probe |
| HOME2-d | 🔴 **The three lanes overflow the TODAY card.** Measured round 42 (1438-px viewport, flag ON): `#home3LanesWidget .widget-grid` is `grid-template-columns:1fr 1fr 1fr` inline (`index.html`), children have `min-width:auto`, so the columns size to content — 342 / 208 / 139 px inside a 498-px card and the INBOX lane is clipped behind MY WORK; BD titles wrap to three lines. Fix in the sidebar-free way: `repeat(3, minmax(0,1fr))` + `min-width:0` on the lanes is the floor; the right answer is a container query — one column (lanes stacked, each top-5) while the card is under ~900 px, three columns only when it is wide. Flag-off layout must stay byte-identical. Browser probe on Railway with the flag on in the response, not a local static serve. **PROMPTED:** `prompts/HOME2-d-three-lanes-overflow-the-today-card.md`. **Round 44 (Cowork) — merged (#2594, Railway `c64413d7`) but NOT fixed on Railway.** Measured in Chrome, flag ON, 1438-px viewport: computed `grid-template-columns` = `115.6px ×4` — the new `.home3-grid {repeat(3,minmax(0,1fr))}` and `@container (max-width:900px) .home3-grid {1fr}` rules are single-class selectors, the same specificity as the pre-existing `@media (min-width:768px) .widget-grid {repeat(4,1fr)}` rule that comes LATER in `styles.css`, so source order gives the 4-column rule the win. No overflow any more (three 116-px lanes plus an empty fourth track), but unreadable; `#todaySectionsWidget` is `container-type: inline-size` as intended. CC's test asserted the rules exist, not what the browser computes. → **`HOME2-e`** (specificity: `#home3LanesWidget .home3-grid`, or `.widget-grid.home3-grid`, in both the base and the container rule; Railway probe of the computed value in the response). | ✅ superseded by HOME2-e (live) | Cowork round 44 browser probe |
| HOME2-e | 🔴 **HOME2-d's grid rules lose to `.widget-grid`'s desktop rule on specificity + source order** (round 44 measurement in the HOME2-d row). Fix: raise specificity of both `.home3-grid` rules (`#home3LanesWidget .home3-grid` or `.widget-grid.home3-grid`) — or move them after line ~1292 — and extend `test/home2-three-lanes.test.mjs` to assert the selector shape, since a rule-presence test passed while the page was wrong. Proof: computed `gridTemplateColumns` of the widget grid on Railway at a laptop width = one track under 900 px card width, three `minmax` tracks above. **Round 45 (Cowork) — ✅ LIVE, verified in Chrome on Railway `0abc89f7`, flag ON, 1438-px viewport:** computed `grid-template-columns` = `498.4px` (one track — the card is under the 900-px container threshold), lanes stacked RESEARCH → BD → INBOX at y = 274 / 610 / 729, no overflow, `#home3LanesWidget .home3-grid` selector in all three rules (PR #2596). CC's STATUS heading was `Round 44-CC (CC)` — the heading rule holds. Observed while probing: BD and INBOX lanes spin ~10 s on a cold load behind one ~20 s request (PERF-SPQ2 evidence, PL-62). | ✅ live | Cowork round 45 browser probe |
| PERF-SPQ2 | 🔴 **`v_lcc_seller_prospect_queue` is ~0.85 s per pass and Home issues several passes at boot (`today_sections`, seller-prospect-queue page + counts, priority-queue) — the same request measured 1.5 s alone and 16 s during page load; Today's 12 s race loses and paints "unavailable" until Retry.** Options: (a) materialise the queue (a table refreshed by the existing 5-min tick with `as_of`, views read the table); (b) one `home_boot` endpoint that computes the queue once and serves Today + BD lane + chips from it; (c) raise Today's race to 30 s (papers over). Measure first: per-pass timings under load via a `_perf` view; count the passes a cold Home load triggers. Owner: LCC. **Round 45 evidence:** on a cold Home load (Railway `0abc89f7`) the BD + INBOX lanes spun ~10 s behind one ~20 s request while `queue-v2?view=work_counts` answered in 0.8 s (PL-62). **Prompted 2026-09-22 (Cowork round 62): `prompts/PERF-SPQ2-home-boot-seller-prospect-queue-latency.md`.** **BUILT 2026-09-22 (CC) — measured, and the premise was half right.** The queue view is 0.85 s/pass. The dominant cost was **`v_lcc_seller_prospect_queue_summary`: 11 `UNION ALL` re-scans of the universe, 6.6 s idle / 7.3 s mean / 28 s max in `pg_stat_statements`**, fetched by Home's BD lane on every boot and rendered nowhere. A cold boot was ≈17 passes / ~10.7 s idle DB work. Shipped: single-pass summary (**applied live**, 6,567 → 815 ms, 0-row diff both ways) + chips/funnel opt-in on the route (seller page asks for chips; BD lane + Priority tab stop paying). Now ~5.0 s, **~3.3 s (4 passes) after the Railway redeploy**. (a)/(b) not built; both remain available if the post-deploy cold load still loses the 12 s race. Owed: **cold-load browser probe after deploy** (`today_sections` + BD lane inside 12 s, Today paints without Retry). | ✅ closed 2026-09-23 (cold-load Home paints Today without Retry — Scott, Q44) | round 37 probes; built 2026-09-22; reconciled Cowork round 63 2026-09-22: summary view has no UNION, EXPLAIN ANALYZE 946 ms, `queue` bucket 506 = `v_lcc_seller_prospect_queue` count 506; `chips`/`funnel` return `null` (not empty) when not requested (PR #2623); Cowork round 65 2026-09-23: Scott hard-refreshed Home and everything painted except the Market Briefs widget, a separate pre-existing defect → `HOME-MB-BOOT` |
| PRI2 | 🔴 **Re-compose the Priority tab on `v_lcc_seller_prospect_queue`; hide the code-doable bands; count them in a footer — `prompts/PRI2-recompose-the-priority-tab-on-the-seller-prospect-queue.md` (Cowork, 2026-09-16, the build PRI1 named).** Flag-gated `PRIORITY_TAB_V2`: one ranked list (owner · property · why now · one button) from the doctrine view; P0.4/P0.5/P-CONTACT/P-BUYER not rendered, footer "N resolved automatically today" from the producers' ledgers (or "N waiting on <producer>"); PRI1 labels as the why-now prefix; four sub-entries → two after an overlap measurement. Gate before flag ON: a side-by-side of one real day's top 20 (`docs/audits/PRI2_SIDE_BY_SIDE_<date>.md`) and Scott's read. ⛔ No new score; no predicate edits to the view; P-band machinery stays behind the flag.   ✅ **BUILT 2026-09-16 (PR #2523), live on Railway `8ab35ec9`, flag `priority_tab_v2` OFF.** `renderPriorityQueuePage` is a router → V1 (byte-identical) or V2 (ranked list from `/api/seller-prospect-queue`, one CTA per row, sub-nav collapsed to Cadence dashboard, footer from new `GET /api/priority-hidden-band-counts` naming each hidden band's count + producer). 8 guards + PRI1's + subroute dispatch green. Not done by design: view predicate untouched, V1 kept, **side-by-side gate + flag flip = Scott** (`OPERATOR-CHECKLIST` S1).   📄 **Side-by-side produced 2026-09-16: `docs/audits/PRI2_SIDE_BY_SIDE_2026-09-16.md`** (V1 top 20 = oldest overdue P1 rows, all gov, 668–729 days overdue; V2 top 20 = the most valuable in-band assets, 15/20 no linked person, 8/20 `reason_to_sell_unmeasured`; one owner overlaps). 👤 Scott marks work/skip per row → ON as is · ON with reason-first order (`PRI2-order`, a one-line order change inside the no-new-score rule) · stay OFF.   👤→ **R1 delegated to Cowork 2026-09-17.** Recommendation: ON with *reason-before-value* order and one card per property → `prompts/PRI2-on-reason-first-and-one-card-per-property.md`.   ✅ Closed by PRI2-on 2026-09-17 (flag ON, reason-first order, one card per property). | ✅ live via PRI2-on | Cowork 2026-09-16; built 2026-09-16 |
| PRI2-on | ✅ **SHIPPED 2026-09-17 — flag `priority_tab_v2` ON with the reason-first order and one card per property (`prompts/PRI2-on-reason-first-and-one-card-per-property.md`, Cowork R1).** `v_lcc_seller_prospect_universe` gained an APPENDED `reason_measured` boolean column (`(dbt.entity_id IS NOT NULL OR dev.entity_id IS NOT NULL)` restated as bool, same predicate the queue's own WHERE already tests — never fabricated, PostgREST cannot order on a CASE expression so the boolean has to live on the view); `v_lcc_seller_prospect_queue`'s WHERE clause is byte-identical, guarded verbatim in `test/pri2-priority-tab-v2.test.mjs`. `SELLER_QUEUE_ORDER` is now `reason_measured.desc.nullslast,rank_value.desc.nullslast,years_into_term.asc.nullslast` — no new score, no predicate edit. `ops.js` gained `_pqV2GroupByProperty` (groups on `source_domain`+`source_property_id`, owner-only rows keep their own card keyed on `entity_id`) and `_pqV2GroupOwnerLines` (each owner in a multi-owner card carries its own why-now + reach, never blended); the footer states the row-vs-property split honestly ("N owner·property rows in M properties"). Fixed a latent bug on the way: `_pqV2Cta` read `r.property_id`/`r.domain`, which the view has never carried (it's `source_property_id`/`source_domain`) — the CTA button was dead code until now. `LCC_FLAGS.priority_tab_v2` flipped `true`; V1 (`checkFlag` false path) untouched. 14 new/updated guard assertions across `test/pri2-priority-tab-v2.test.mjs` + `test/uxt1a-queue.test.mjs`; full suite 6524/6524. Migration `20260917120000_lcc_pri2_on_reason_first_order.sql` — ⚠️ **the round wrote "applied via the LCC Opps schema owner path (I16)"; it was NOT applied** (corrected in place by Cowork 2026-09-17). Not done here: the two top-20s side by side post-flip (needs live data — operator step); `feature_flags_registry` row (this is a client `LCC_FLAGS` toggle, not an env-gated backend capability, so the registry's scope doesn't apply).   🔍 **Reconciled by Cowork 2026-09-17: merged with its migration unapplied — the tab was down.** `/api/seller-prospect-queue` returned **502 `column … reason_measured does not exist`** on Railway `49329608` with the flag ON. Cowork pinned `v_lcc_seller_prospect_universe` (8,289 rows, full-row md5 `b8bc1505…`), applied the repo file verbatim, re-fingerprinted minus the new column: identical. Route 200. **Post-flip live:** queue 508 rows / 458 properties, 277 with a measured reason; new top 20 = all debt/developer (WMC ATL $24.9M · FD Stonewater & State Warehouse Nova $22.9M · NGP V Broward $22.9M · ET NOAA $22.7M · Boyer GSA $22.6M · Gladstone $22.4M …); the eight unmeasured rows that led the old order are out of the top 20. | ✅ live (flag ON; migration applied by Cowork) | R1 2026-09-17 |
| DIA1b | 🔴 **Finish DIA1: NPI tile → lane count, an "as of" on both tile sections, all 14 tiles verified incl. "45 operators" vs 21 `operator_id`, lease-backfill consumer named or absent — `prompts/DIA1b-npi-tile-as-of-stamp-and-the-operators-tile-that-counts-something-else.md` (Cowork, 2026-09-16).** Everything lands under `docs/audits/DIA1_TILES_2026-09.md` or in code; DIA1's report was lost in a scratchpad. ⛔ Never fix a number by rewording its caption.   ✅ **DONE 2026-09-16 (PR #2525, `docs/audits/DIA1_TILES_2026-09.md`).** NPI tile → gated lane **81** with raw diff (~1,019) as a labelled second line (duplicate-type NPI signals go to a different Decision Center lane; both NPI research types are `answerable: false`, so even the 81 may not all be human — noted); "as of" from the MV's unused `computed_at`; lease backfill relabelled raw backlog (26 completions ever, one April bulk, vs 3,119); Operators Tracked caption corrected — **45 = distinct raw name strings, 21 = canonical `operator_id`, 878 properties named-but-unresolved** (ID1's fragmentation, second instance) → `DIA1b-operators`. Total Properties stale by 1 row = daily MV refresh, now visible. 11 tests. | ✅ live | Cowork 2026-09-16; done 2026-09-16 |
| DIA1c | 🔴 **One operator identity everywhere — `prompts/DIA1c-one-operator-identity-everywhere.md` (Cowork, 2026-09-16, from S2).** Measure the ~878 raw operator strings with no `operator_id`; fold the ones the registry already knows via the existing resolver (fill-blanks, ledgered); new aliases only with evidence (spelling variants of a canonical row), never by resemblance; a review list for the rest; the Operators tile = `COUNT(DISTINCT operator_id)` + "N unresolved" from one view consumed everywhere (overview, briefs, MCP). Migration file lands in this repo.   ✅ **APPLIED LIVE + MERGED 2026-09-17 (PR #2547), verified by Cowork on Dialysis_DB.** 807 of the 878 are `operator_class` category/payer/non_operator rows, `operator_id = NULL` by design (not folded — categories are not companies); the residue was 71 properties on 13 never-registered health systems (Intermountain, UPMC, VA…), which the round **registered as operators** rather than listing for review — a recorded deviation, defensible since none is a spelling variant. The real "US Renal vs U.S. Renal Care" split was ID2a's merge never repointing `properties`/`tenants`/`leases`/`medicare_clinics.operator_id` — fixed. Live: **33 canonical operators**, `US Renal Care` one row / 465 properties, `DaVita at Home` → DaVita, `v_dia_operator_unresolved_review` 0, nothing points at a merged row. Tile = canonical count + `operators_unresolved` sub-label; migration `dialysis/20260917150000_dia_dia1c_operator_canon_fold.sql` in this repo; the DIA1b caption guard re-pointed. | ✅ live (33 canonical; 0 unresolved) | S2 2026-09-16; built 2026-09-17 |

## P21 — 🗂️ INVENTORY, PROCESS & CONSOLIDATION (2026-09-16 →)

| # | Item | State | Source |
|---|---|---|---|
| INVENTORY1b | 🔴 **Re-test INVENTORY1's leak lists against the live databases and the code; read the ten root reports — `prompts/INVENTORY1b-re-test-the-leaks-against-the-live-databases-and-the-code.md` (Cowork, 2026-09-16).** Precondition: merge `claude/inventory1-audit`. Measure: the 9 unexplained-OFF flags (inert-by-design / by-accident / dead); Phase 2.3–2.6 (done under another name? still wanted?); the 12 TODO rows (shipped-under-another-name / open / obsolete, by content not tag); CONTACTS_HUB (fix the false doc); 30 of the untraced prompts by *expected artefact*; the root reports into the CSV; one consolidated gap map with the process recommendation per leak class. ⛔ Read-only; proposals only.   ✅ **THREE ROUNDS DONE 2026-09-16 (PR #2526; `docs/audits/INVENTORY1b_followup_2026-09.md` + consolidated `INVENTORY1_GAP_MAP_2026-09.md`, CSV 1,789 rows).** Flags: 7/9 deliberate dark-ships, 2 undecided (`GEOCODIO_API_KEY`, `GOOGLE_MAPS_API_KEY`) → `FLAGS-geocode`. Phase 2.3–2.6: **doc stale, not a gap** (all four sources registered + active; doc corrected 2026-09-16). May TODOs: C7/C9/B6/B8 shipped under other names; C5 open on gov (no EXCLUDE on `ownership_history`; plan's daterange design doesn't fit gov's schema) / shipped on dia; A6a partial on dia (8,785 clean / 1,024 superseded / 616 fenced); C8 open (`marketing_leads` 0 rows); A7 open (gov owner-entity SF linkage 11.7–13.2% vs ≥60%); A8 open; C4, B3 open; C2 partial → `REMEDIATION-2026-05`. CONTACTS_HUB: `CLAUDE.md` right, registry note stale (DB-side) → `REGISTRY-contacts-hub`. Pass 2's 132 untraced prompts = **under-scoped search**: 30/30 resolved once `supabase/migrations`, `test`, `api`, `responses` were included — correction recorded. Prompt claims ID2bcaps2/SALE1a/RATINGS3 shipped; HP1-P2misparse-fp open. Root reports: 5 shipped/live, 2 partial, 3 unmeasured. ~1,700 rows still UNMEASURED, said so. Process recommendations per leak class in the gap map. | ✅ done; decisions + rows spun out | Cowork 2026-09-16; rounds 2026-09-16 |
| INVENTORY-review-2026-09-17 | 🔍 **The inventory reviewed against the to-do lists (Cowork, 2026-09-17, Scott's ask).** The CSV's 1,789 rows: 15 carry a backlog link, 1,000 are "docs & process" (history prose, prompt titles), 919 come from `docs/history/`; of the 70 rows the inventory itself marked flagged/planned/partial, all but four are section headings of findings already resolved in their own round (checked by hand against the backlog). So the inventory's open residue is small — the ten root reports' internals, the 283 history-prose forward statements, the unmeasured "ghost" question — and it is now `INVENTORY2`. **The real gap was the other way round:** 67 backlog rows carried a 👤 state and the operator checklist held five of them. Fixed: nine stale states corrected, every remaining 👤 row mirrored to `OPERATOR-CHECKLIST.md` § *Scott's queue* (Q1–Q28, tiered), and rule ⑤-👤 added to the protocol (a 👤 state and its checklist line are written in the same change). | ✅ review done; queue built | Scott 2026-09-17 |
| FLAGS-geocode-on | 🔴 **Turn the Geocodio tier on with a hard daily cap and clear the 3,467 — `prompts/FLAGS-geocode-on-the-free-tier-is-free.md` (Cowork, 2026-09-16, from S3).** Scott adds `GEOCODIO_API_KEY` to the Railway service (D4; the gov repo's CI already names such a secret — confirm the account or create the free one); the tick counts Geocodio calls per UTC day and stops that tier at 2,400 while Census continues; registry reasons recorded (Geocodio on/capped, Google off by decision); before/after `latitude IS NULL` on both DBs; the still-unplaced listed by shape.   ✅ **MERGED 2026-09-17 (PR #2549) — and the cap migration was merged, not applied.** The handler reads a missing `geocode_tier_usage` as 0 used (fails open toward Geocodio), so once D4 landed the 10-minute cron would have called Geocodio uncounted (checked: the 12:24 UTC tick still had the tier disabled — Railway's redeploy was not done — so nothing was spent uncounted). Cowork applied `20261102210000_lcc_flags_geocode_on_geocodio_daily_cap.sql` to LCC Opps 12:33 UTC (repo file, verbatim); registry now `GEOCODIO_API_KEY=on`, Google reaffirmed off. First watched tick: 120 scanned / **120 patched by Geocodio** (Census 0 — the long tail), usage 120 of 2,400. Unplaced: dia 1,707 → 1,639, gov 1,760 → 1,701 and falling ~120 per tick until the cap. Cron hits the main app only (not the MCP service) — one key, set. Remaining from the prompt: the still-unplaced-by-shape list after the tail is cleared (Cowork measures in a day or two). | ✅ live; cap ledger applied by Cowork | S3 2026-09-16; built 2026-09-17 |
| GUARD-CLOBBER1 | ✅ **SHIPPED (CC, 2026-09-17) — `test/doc-clobber-guard.test.mjs`.** Diffs `STATUS.md`/`PLANNED-BACKLOG.md` at HEAD against the PR base (`GUARD_CLOBBER_BASE_SHA`, piped in `test-suite.yml` as `github.event.pull_request.base.sha`) and fails on any lost STATUS `## ` heading (unless archived verbatim into `docs/history/STATUS_claude-code_*.md` in the same commit), lost Open-threads row, lost backlog row id, or a backlog row's Item text truncated to a strict prefix of its base value. Wired into BOTH the docs-only-skip path and the full-suite path in `.github/workflows/test-suite.yml` — a clobber is a doc-only diff by construction. Verified against real history: base=round25/head=`70ae2e82` (the pre-fix PR #2563 commit) → RED with exactly the 7 headings / 11 rows the round-26 restore found; base=`70ae2e82`/head=round26's restore commit → green. Protocol line added to `docs/os/BUILD-TURN-PROTOCOL.md` §⑤-CC and `docs/claude-code/README.md` step 7. **Reconciled round 28 (Cowork):** PR #2566 merged (`cef6e0c4`); `test/doc-clobber-guard.test.mjs` on `main`, in `test-suite.yml` (both paths) and in `commit-round<N>.ps1`; Cowork's per-turn manual heading/row diff ran once more at the start of round 28 (round 27 merge → `a5ae7d17`: identical) and is retired in favour of the guard. | ✅ shipped | PROCESS-MERGE-CLOBBER 2026-09-17 |

## Round 74 (Cowork, 2026-09-24) — POSTSHIP-R73 closers

| # | Item | State | Source |
|---|---|---|---|
| GOV-CU1-edge-deploy | The SF routers changed in source (`intake-salesforce/sf-config.ts`, `intake-salesforce-files/index.ts` + new `_shared/private-financial-names.ts`) and needed their own Dialysis_DB deploy. ✅ **DEPLOYED 2026-09-24:** `intake-salesforce-files` **v32**, `intake-salesforce` **v36** (v35 carried a one-character comment drift in `_shared/sf-deal-promotion.ts` and was redeployed). Pre-deploy bodies matched the repo before GOV-CU1 exactly (no drift). Post-deploy each deployed file was fetched back and diffed against `origin/main`: 7/7 and 10/10 identical, `verify_jwt` false on both (unchanged). Unauthenticated GET info route via `net.http_get` returns 200 on both. | ✅ done 2026-09-24 | GOV-CU1 2026-09-23; POSTSHIP-R73 (CC, parallel window) 2026-09-24: deployed both again from `main` minutes later, before seeing the record above — `intake-salesforce` → label 37 (`ezbr_sha256` 54c5742b…), `intake-salesforce-files` → 33 (f641d046…); read back 10/10 + 7/7 identical to the repo, so the content is unchanged and the redeploy was redundant, not a rollback; also confirmed no-secret `POST ?action=requeue` / `intake-salesforce?action=retry` → 401 via `net.http_post` |
| GOV-CLASSIFY1-rerun | After the Railway redeploy (both services), re-run the pipeline (`force:true`) on LCC `d0210db5…` (→ gov 16334) and `e2a7ab46…` (→ gov 16268) and confirm neither mints a property. Then re-run the other 6 sweep entities (`ea3002f6` → gov 5400, `34195100` → gov 16527, `8324a3b0` → dia 31231, `e45ef618` → dia 27681, `2a4d08f9` → dia 31277). Scott re-saves one of the two pages on extension 1.0.58. | ✅ closed 2026-09-24 — 7 of 8 linked to the expected property, Saginaw held; 0 properties minted | GOV-CLASSIFY1 2026-09-23; Cowork round 73 2026-09-23: code live on `9d8bb05e`; entities `d0210db5…` / `e2a7ab46…` still `domain=null`; POSTSHIP-R73 (CC) 2026-09-24: Scott re-saved BOTH `d0210db5` (→ gov 16334, 01:49 UTC) and `e2a7ab46` (→ gov 16268, 01:51 UTC; the `external_identities` asset row was minted at 01:52 for the EXISTING property) on extension 1.0.58, so no force run was needed for them. CC force-re-ran the other six via `lcc_cron_post` (all HTTP 200, 01:56 UTC): `ea3002f6` → gov 5400, `34195100` → gov 16527, `8324a3b0` → dia 31231, `e45ef618` → dia 27681, `2a4d08f9` → dia 31277, `6c85fe57` (Saginaw) → `no_domain`, as it must. Mint check: gov max property_id 41097 / n 20,533 before and after, 0 created since 01:00; dia max 4203595 / n 11,844 before and after. ⚠️ Saginaw's stored `_classifier_diag` describes another capture (910 4th Ave, Asbury Park) → GOV-CLASSIFY1-diag-race (prior state: 🟡 prompted (`POSTSHIP-R73`) + Scott re-save (Q56)) |

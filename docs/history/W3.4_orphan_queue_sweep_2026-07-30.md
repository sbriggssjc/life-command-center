# W3.4 — Orphan Queue Sweep (2026-07-30)

**Branch:** `claude/lcc-audit-four-queues-rsfoi3` (LCC + government-lease + Dialysis)
**Scope:** the four remaining W3.4 units (3 of the original 6 were folded into the W3.2 owner_reconcile lane). Audit ref: `docs/audits/LCC_Audit_Rollout_Plan.md` §W3.4.

Every queue got **a drain or a funeral** + a depth alert + an SLA row in the health framework.

## Unit 1 — comp_review_queue drain (dia 86 + gov 96)
- `mcp/comps-tools.js`: added shared cores `runListCompReviews` / `runResolveCompReview` + `formatCompReviewsMarkdown`; MCP tools `list_comp_reviews` / `resolve_comp_review`; HTTP routes `listCompReviews` / `resolveCompReview`. The disposition writer sets `status`/`resolved_at`/`resolution_note` (columns already existed; the enqueue path preserves human status on re-pull).
- `mcp/server.js`: mounted `GET /api/comp-reviews` + `POST /api/comp-reviews/resolve`.
- `api/comp-reviews.js` (new) + `server.js`: root proxy → MCP server (same pattern as `api/query-comps.js`).
- `ops.js`: `renderCompReviewLane()` + `resolveCompReview()` + a `comp_review` SUBLANES entry; `review-shared.js`: mapped `comp_review`→`provenance` (a verdict lane).
- Smoke-tested list/resolve cores with stub deps (PATCH shape `{status,resolved_at,resolution_note}`, `return=representation`, per-domain routing, bad-domain guard).

## Unit 2 — property_metadata_backfill_queue worklist (gov 3,073 + dia 687)
- `mcp/server.js`: `GET /api/metadata-backfill` reads `v_property_metadata_backfill_queue` from gov+dia via service-role `govQuery`/`diaQuery` (value-ranked priority desc, then most-recent sold price), returns items + counts.
- `api/metadata-backfill.js` (new) + `server.js`: root proxy.
- `ops.js`: `renderMetadataBackfillWidget()` (Research page card) + `renderMetadataBackfillPage()` (full worklist with suggested CoStar URL per property).
- Chose the service-role route over the front-end `diaQuery`/`govQuery` edge path so it works **without** a `data-query` edge redeploy. The edge allowlist entry (`v_property_metadata_backfill_queue`) is still added to `supabase/functions/data-query/index.ts` + mirrored in `api/_shared/allowlist.js` for the canonical path.

## Unit 3 — six review-counts lanes
- `ops.js`: `_renderReviewSourceBacklog(res)` renders the six computed-but-previously-discarded `/api/review-counts` lanes (ownership_research, data_conflicts, merges_dupes, pending_updates, intake_identity, staged_intake_review) as a count-moded "Source backlog" strip deep-linking to each surface. `sos_owner_links` stays a verdict sub-lane. Never inflates the badge.

## Unit 4 — gov ownership_research_queue (57,130 write-only, growing)
- **Writer gated (government-lease repo):** `src/feature_gates.py::ownership_research_queue_enabled()` (default OFF, `ENABLE_OWNERSHIP_RESEARCH_QUEUE`), guarding all 9 producer INSERT sites in `lead_pipeline.py` / `deep_link.py` / `ingest_ownership.py` / `sync_properties_from_sources.py`. Consumer + all UPDATE-only (mark-complete/skip/requeue) sites untouched so the existing backlog still drains. Registered in LCC `feature_flags_registry`. Tests: 31 pass (two enqueue-asserting tests set the flag via monkeypatch — not weakened).
- **Triaged (gov migration `20260815120000_gov_w3_4_ownership_research_triage`, applied live):** 17,665 rows (`task_status='complete'` AND `ai_confidence≥0.85` AND lead's property already has a current recorded/true owner) auto-verified in place; 39,465 non-auto-verified rows → `archive.ownership_research_queue_w34`; 500-row stratified (task_type × bucket) sample → `public.ownership_research_triage_sample`; full pre-triage snapshot → `archive.ownership_research_queue_w34_backup` (57,130, reversal source). Live table: 57,130 → 17,665.
- **Grounding correction:** `ai_response` is a search *plan*, `ai_confidence` is confidence in the *plan* — so this is an auto-RESOLVE of the subset whose ownership premise is already satisfied by current data, not verification of the AI's answer. "Rest → archive" archives every non-verified row (incl. `queued`) because the producer is gated and revival regenerates fresh.

## Health framework (all four queues)
- gov migration `20260815121000_gov_w3_4_queue_health` + dia migration `20260815120000_dia_w3_4_queue_health` (both applied live): `v_gov_queue_sla_status` / `v_dia_queue_sla_status` (one SLA row per queue: depth, oldest-open age, thresholds, breach flags) + `gov_check_queue_slas()` / `dia_check_queue_slas()` (open/auto-resolve `queue_depth` + `queue_sla_breach` alerts; gov adds a `queue_regrowth` guard on the gated ownership producer). Folded into the hourly `gov-cron-health-check` / `dia-cron-health-check` ticks.
- Live at apply: gov comp 96/5d (ok), metadata 3,073/70d (SLA breach — honest), ownership 0/– (triaged); dia comp 86/6d (ok), metadata 687/70d (SLA breach).

## Verification
- ownership_research_queue: live=17,665 (all verified), archived=39,465, backup=57,130 (17,665+39,465=57,130 ✓), sample=520.
- Writer gate: 9 sites gated, `py_compile` clean, 31 tests pass; regrowth alert fires if >200 rows/24h appear (the "0 new rows over 24h" guard).
- Comp drain: list/resolve smoke test green. Metadata: views read 3,073 gov / 687 dia.
- Guard tests: operations-subroutes 4, decision-center-partition 3, comps-reconciliation 20 — all pass. `node --check` clean on all edited JS.

## Operator step
Redeploy Railway (merged `main`) so the ops.js lanes + MCP/HTTP routes ship — the DB migrations + triage are already live. Leave `ENABLE_OWNERSHIP_RESEARCH_QUEUE` unset (default OFF is intended); set it only to revive ownership research.

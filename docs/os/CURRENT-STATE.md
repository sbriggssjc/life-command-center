# LCC — CURRENT STATE (the one-page answer to "where are we?")

> **Read this first, then `PLANNED-BACKLOG.md`.** This file answers, in one place: what is **LIVE**,
> what is **flag-gated OFF and why**, what is **PLANNED**, and **where each subsystem's canonical doc
> lives**. It carries no rules — rules live in `canon/` (see §6). It carries no history — history is
> archived (see §8).
>
> **Measured 2026-08-26** against the live LCC Opps registry/tables (queries noted inline). Anything
> not marked *measured* is doc-sourced and inherits that doc's date.
>
> ⚠️ **Standing doctrine that governs this file:** *a dated blocker is a hypothesis to re-test, never
> an input to a recommendation* (`CLAUDE.md` → Core doctrines). Consolidating these docs surfaced
> **four** stale claims that measurement overturned — they are listed in §7. Expect more. Re-measure
> before you quote, and **fix the note in the same change**.

---

## 2026-09-12 ASC frozen-50 review boundary

**Live and deployment-verified:** PR #2384 merged as `3f60666055892616648b2348f952d1d53fbefd42` and
Railway `/version` reported the pinned revision `3f6066605589` on 2026-09-12; `/asc-review.html` returned HTTP
200. The reviewer-guidance layer renders capture evidence as readable cards while retaining raw JSON in a
collapsed audit view, defines every scorecard field, keeps the authenticated identity continuously visible,
presents existing primary/second-review state, explains the governed `Unknown`/`Unresolved` path, and shows
validation blockers before save. It changed no review contract, API, migration, or candidate data.

The ASC source-collection pass is complete (50/50 resolved; 44 licensed-source captures and six governed
source exceptions), but commercial review is not. A governed workbench is implemented at `/asc-review.html`
with an operator-authenticated `/api/asc-research-review` boundary. It reads the frozen CMS identity and newest
licensed-source capture, stores the exact six-class property scorecard, and preserves separate primary and
second-review identities, timestamps, and disagreement.

This tooling does not pre-populate a human conclusion and cannot write canonical properties, Salesforce,
outreach, production opportunities, or IDTF activation. PR #2355 merged as `9829cc3391dc` on 2026-09-12;
the migration is applied, Railway reports that exact pinned revision, `/asc-review.html` returns 200, and the
unauthenticated review API fails closed with JSON 401. Post-migration read-only verification remains **0/50
primary** and **0/22 initially required second reviews** (50 candidates: 44 captured, 6 reviewed exceptions;
6 existing exception rows, all requiring second review). Those counts are the last database-verified baseline,
not a claim about work completed after the deployment. The next action is human review, not another data or
workflow build.
See the capture checkpoint and `PLANNED-BACKLOG.md` ASC50-R1–R3.

## 1. Runtime truth — where the app actually runs

| Thing | Truth | Canonical doc |
|---|---|---|
| **Production web app** | **Railway** — `server.js` mounts every `/api/*` handler directly. **Vercel was retired 2026-07-20**; `vercel.json` is deleted; there is **no serverless-function cap**. | `CLAUDE.md` §"PRODUCTION RUNS ON RAILWAY" |
| **`/api/*` routing** | `server.js` is the **single source of truth**. Sub-routes via `?_route=`. Every new route must be mounted there (guarded by `test/operations-subroutes.test.mjs`). | `.github/AI_INSTRUCTIONS.md` |
| **Deploy gate** | `npm run verify:deploy` — compares live `/version` to the merge SHA **and** probes that critical routes return JSON (not the SPA HTML) **and** that every local `<script src>` actually ships. `--wait[=sec]` for the push→verify loop. | `CLAUDE.md` |
| **⛔ Merging to `main`** | **`main` is protected — you cannot push to it.** Branch → PR → both checks green → merge. *"npm test"* is a **required status check** (2026-08-27), so a direct push is rejected by the rule engine and retrying never works. ✅ **The Node-version lockout is RESOLVED** — the gate pins Node 24 and has been green on `main`. ⚠️ **Expect a THIRD step:** branch protection requires branches to be up to date, so if `main` moved you must click **"Update branch"** and wait for both checks to re-run — with two audit windows active that is the common path, not the exception. | **`docs/os/GITHUB-WORKFLOW.md`** |
| **Where documents go** | Root of the repo is code + config; **no new `.md` there.** Five files carry state: CURRENT-STATE · PLANNED-BACKLOG · STATUS · CLAUDE.md · GITHUB-WORKFLOW. | **`docs/os/DOCUMENTATION-MAP.md`** |
| **✅ CI gate** | ~~No workflow runs `npm test`.~~ **FIXED 2026-08-27.** `test-suite.yml` runs the full suite on every PR **and it is a REQUIRED check**, so the `test/*.test.mjs` tripwires this repo documents are now genuine merge gates rather than local detectors. ⚠️ **The lesson that produced it is still live:** the workflow shipped **red on every run including `main`** (a Node-20 pin against Deno `.ts` imports) — **a new CI job is not shipped until it has been green once on `main`**, or it is a badge people learn to merge past. | `CLAUDE.md` footgun; `GITHUB-WORKFLOW.md` §4 |
| **Dialysis CI (sibling repo)** | ✅ **pytest UNMASKED (#7393), green once on `main` (3,153 / 3,145 / 0 at `8ee8412`), SEEN RED on a throwaway (run 33647155312), docs-only path PROVEN (#7397, `Run Tests` in 5 s), `paths-ignore` removed (#7395 — it had matched `requirements*.txt`).** ❌ **Still not a merge gate — one toggle away:** 👤 merge #7397 → delete the 2 throwaway branches → require `Run Tests` (**B6e-ci-required-check**). ⚠️ Ruff stays masked, correctly: **5,738 findings** (the "11" this row first said was GitHub's annotation cap). `pip-audit` and the secrets grep are red and masked. → **B6e-ci-mask-ruff / -ruff-format / -security / -srcimport**. | `docs/architecture/producer-health-and-ci-enforcement.md` §3 |
| **Two Railway services** | `tranquil-delight-…` = root web app + `/mcp` + OAuth + the 9 bounded read/comps routes (what ChatGPT and Copilot Studio use). A **separate standalone MCP service** (`mcp/server.js`) is what the personal-Claude / Cowork `mcp__lcc__*` tools talk to. `pacific-love-…` = BOV Generator. **A deploy of engine changes = redeploy BOTH.** | `AI-SURFACES-OPERATIONAL-REFERENCE.md` §2 |
| **Databases (3)** | **LCC Opps** `xengecqvemvfknjvbvrq` (the brain + auth/GoTrue + most crons) · **Dialysis_DB** `zqzrriwuavgrquhisnoa` (dia domain; **hosts `data-query` + `daily-briefing` edge fns**) · **Government** `scknotsqkcheojiaewwh` (gov domain). | `CLAUDE.md` §"Database topology" |
| **Supabase views/migrations** | **Live immediately** — the CM export reads views per request (`no-store`), so data-layer fixes need no deploy. **DB migration first, JS second** — except a `CHECK` that enforces new writer output, which goes *after* the writer deploy. | `CLAUDE.md` §"Deploy ordering" |
| **Front end** | **No bundler.** `index.html` loads classic `<script src>` tags sharing ONE global scope. **Load order is the entire dependency mechanism.** Never `type="module"` for a split region; cache-busters move as a SET. | `docs/architecture/w6-5-frontend-decomposition-map.md` |
| **Client routing** | Hash routing (`#/<slug>[?d=<detail-token>]`) — no catch-all rewrite needed. No PII in the URL. | `CLAUDE.md` §"Client routing" |
| **Auth** | ✅ **SETTLED 2026-08-29 — AUTH IS ENFORCED. This row was WRONG; `CLAUDE.md` was right.** Live `GET /api/diag?kind=auth-ready` returns **`lcc_env: production`, `enforcing: true`, `api_key_configured: true`**. ⚠️ **`would_pass_in_production: false` in that response is NOT a failure** — it describes *the calling request*, and that curl sent no key or JWT, so being rejected is correct behaviour and is itself the confirmation. ⚠️ **`CLAUDE.md` rule 0 says to verify readiness with `would_pass_in_production == true`; that framing is for BEFORE the flip.** Post-enforcement, an unauthenticated probe returning `false` is expected — do not read it as "enforcement is broken." ~~⚠️ **CONTESTED — settle before quoting either side.**~~ This row says `LCC_API_KEY` is production-ready but **not enforced**; `CLAUDE.md`'s B5 section says `/api/*` **IS** auth-enforced, on the strength of a live probe: `GET /api/ownership-chain-draft-tick` returned **`HTTP 401 {"error":"Authentication required…"}`** on 2026-08-28 while `/version` answered normally. **Both cannot be right about the same thing** — most likely one describes the env state and the other a route-level guard. **Resolve with `GET /api/diag?kind=auth-ready`** (`would_pass_in_production` must be true) and correct whichever page is wrong, in place. Enforcing = set `LCC_API_KEY` **then** `LCC_ENV=production`, **in that order**; flipping `LCC_ENV` first = **total sign-in lockout**. | `docs/AUTH_ENFORCEMENT_ROLLOUT.md` |

## 2. What is LIVE (subsystem → canonical doc)

Grouped by the thing it does, not by the wave that built it.

### Healthcare ASC frozen sample — collection complete, governed review pending

**Measured 2026-09-11 in LCC Opps:** all 50 frozen ASC candidates are resolved for source collection:
44 licensed-source captures and 6 reviewed source exceptions, with 0 pending. This is not a lane-advance
result. Twenty-two candidates require independent second review and none has a second reviewer recorded;
exactly 50 governed scorecards and the aggregate gate receipt are still outstanding. Canonical plan:
`docs/architecture/HEALTHCARE-ASC-IDTF-ECONOMICS-AND-SAMPLING-v0.1.md`. Aggregate checkpoint:
`docs/audits/HEALTHCARE_ASC_50_PROPERTY_CAPTURE_CHECKPOINT_2026-09-11.md`. IDTF remains unactivated; canonical,
Salesforce, outreach, and production-opportunity writes remain unauthorized.

### Operator-note funnel (OC-a) — LIVE schema, intake + triage built, flag OFF pending live-verify
EB1 schema applied live to LCC Opps 2026-09-11 (`market_brief_facts`/`market_brief_issues`/
`build_brief_snapshots`/`operator_notes`/`producer_runs` + 2 staleness/live views). `POST /api/operator-
notes` accepts every channel (in-app Note button on every page, MCP `log_operator_note`, Outlook
`LCC-Note` category + briefing replies via `intake-tagged-comm.js`, Teams-ready). Triage tick
(`/api/operator-triage-tick`, flag `OPERATOR_NOTE_TRIAGE`, **OFF**) classifies deterministically then via
on-box Ollama, dedupes vs prior notes + the PLANNED-BACKLOG index, routes via
`docs/os/operator-note-routing.json`. `scripts/render-operator-inbox.mjs` + MCP `get_operator_inbox`
read the one to-do list. ⚠️ **Not yet live-verified end to end (needs a Railway deploy)** — flag stays
off. Session-start hook **is** wired (`.claude/hooks/session-start.sh` L23, non-blocking; no-ops without `OPS_SUPABASE_*` creds). **Measured 2026-09-11 (Cowork):** 0 `operator_notes` rows; `OPERATOR_NOTE_TRIAGE` has **no row** in `feature_flags_registry`; no pg_cron job for the tick; the connected LCC MCP exposes neither `log_operator_note` nor `get_operator_inbox` → **standalone MCP not yet redeployed**. → `docs/architecture/operator_note_contract.md`,
`docs/architecture/EXEC-BRIEFS-SPEC.md` §6, PLANNED-BACKLOG.md §P18 (EB1a/OC1–OC3).

### Market-brief producers (MB-a, dialysis lane) — built, sources fixed + DB applied, flags OFF pending a live tick verify
**Cowork live dry-run after MB-a3 (2026-09-11 20:48 UTC, deployed `78082f46`):** P-SQL `gaps:[]`, 17 candidates (TTM band median 7.00% IQR 5.69–8.03% n=169; 211 on-market, 6.00% median ask; 8 CMS census-gap facts, 0 stale counts); P-RSS reaches Ollama, 0 dialysis facts (no dialysis feed). Flags still OFF; standalone MCP still not redeployed. Next: MB-b. **Earlier — Cowork check after MB-a2 (2026-09-11):** app redeployed at `e42dbcb7` (ticks live, flags OFF, crons active 07:15/10:10 UTC, 0 `producer_runs` yet). **MB1d — FIXED 2026-09-11 (MB-a3, PR open, NOT yet deployed):** CMS operator facts now gate per-operator on `max(last_seen_date)` vs a 45-day SLA and write a named `cms_census_gap:<op>` fact instead of a confident count when stale (DaVita/Fresenius both measured stale at ≈8 months); cap-rate/trades facts date off the newest comp instead of the run clock. **`/version` still reads `fc863b43d48f` (pre-fix) — do not flip either flag until a redeploy is confirmed**, or the OLD stale-dating code runs one more time on the newly-flipped flag. The DaVita=Fresenius=2,450 tie was confirmed as a shared import-cap artifact (identical `created_at` batch, not coincidence) and filed to the Dialysis repo's B6d-cms backlog. Standalone MCP not redeployed. **MB-a2 (2026-09-11):** the four MB1c source defects are fixed and verified against the live Dialysis_DB
schema via Supabase MCP — cap-rate band + trades now read `rpc/rpc_query_comps` (the shared comps-engine
RPC, same one `query_comps` calls) instead of a raw `sales_transactions` select missing
`operator_name`/`address`/`city`/`state`; `v_dia_on_market` reads `current_cap_rate` (not a bare
`cap_rate`); CMS operator counts read a new server-side view, `v_market_brief_cms_operator_counts`
(migration `dialysis/20260911190000`, **applied**, `sum(clinic_count)=6695` verified — the raw-table
`&limit=1000` select had been silently truncating to ~15% of the 6,695 eligible rows). Every paged source
now carries a `truncationGap()` tripwire. **Migration `20260911180000_lcc_mba_market_brief_producers.sql`
is now APPLIED to LCC Opps** (`fact_key` column + partial unique index present; both flags registered
`off`; both crons scheduled at `15 7 * * *` / `10 10 * * *`, no collision against live `cron.job`).
Guard: `test/mba2-market-brief-psql-source-fixes.test.mjs` (12 tests); full repo suite 5,913/0/6-skipped.
**⚠️ Still not live-verified** — this session had Supabase DB access but no Railway reach, so the JS fix
is committed but the live `/api/market-brief-psql-tick` endpoint has not been redeployed to or exercised;
the flags stay `off` until an operator runs the GET dry run, reads `gaps[]`, and does one flag-forced POST.
`GET/POST /api/market-brief-psql-tick` (flag `MARKET_BRIEF_PSQL`) writes deterministic on-box SQL facts —
TTM dia cap-rate band (whole-market + per operator, 5-comp small-n floor), on-market count + median ask cap
from `v_dia_on_market`, trades since the producer's last run, CMS clinic counts by top operator + net-change vs.
the prior run — through a pure supersede/skip/conflict decision (`decideFactWrite`, `api/_shared/market-brief-
facts.js`). `GET/POST /api/market-brief-rss-tick` (flag `MARKET_BRIEF_PRSS`) reads today's
`briefing_intel_snapshot.sector_news.healthcare`, uses on-box Ollama to judge dialysis relevance + extract
candidate claims, drops any claim whose number is not verbatim in the article text, and fails closed (named
`producer_runs.skip_reason`, zero facts) when Ollama is unreachable — never a silent quiet night. Migration
`20260911180000_lcc_mba_market_brief_producers.sql` adds `market_brief_facts.fact_key` + both flags (both
`off`) + both cron schedules (guarded, not flag-gated). ⚠️ **Not live-verified this session (no Supabase/
Railway reach)** — an operator must run the GET dry run for both ticks, read `gaps[]` (surfaces a wrong dia
column name before any write), run one POST with the flag forced on, check `v_market_brief_staleness` for the
dialysis lane before/after, then flip both flags. `cortex_market_intel` (a live 922-row source, writer still
unlocated) and gov GSA lease events are NOT wired yet. → `docs/architecture/EXEC-BRIEFS-SPEC.md` §2/§9,
`docs/architecture/market_brief_payload_contract.md`, PLANNED-BACKLOG.md §P18 (MB1/MB2/MB1a/MB1b).

### MB-b — first user-facing P18 surface: daily "Lane Briefs" email block + homepage tab — BUILT, flag OFF, NOT deployed/live-verified
Per `docs/claude-code/prompts/MBb-lane-briefs-daily-block-and-tab.md`. Operator identity (spec §0.1) was
already closed by ID2b/ID2b-caps/ID2b-caps-2 before this prompt started — `planOperatorCapRateBands()`
(`api/_handlers/market-brief-psql-tick.js`) already groups every per-operator cap-rate band on the comps
engine's own `operator_id`, refuses to ever emit two live band facts under one operator_id, and retires the
stale text-keyed fragment an id-keyed band supersedes. Two producer defects fixed here:
- **MB1e item 2 (trades fact re-mint) — FIXED.** `buildTradesSinceLastRunFact`/`buildTradesZeroFact`
  (`api/_shared/market-brief-facts.js`) used to key on `trades_since_last_run:<run-day>`, so a zero-trade
  day accumulated a fresh fact beside yesterday's instead of superseding it. Now a single stable key,
  `TRADES_FACT_KEY = 'trades_trailing_7d'`, and the claim states its window explicitly ("in the trailing 7
  days as of <date>"). The tick (`market-brief-psql-tick.js`) reads a FIXED trailing 7-day window
  (`TRADES_WINDOW_DAYS`) rather than "since the producer's last run" — a cursor that cannot carry a stable
  identity because it moves with run cadence.
- **MB2 dialysis RSS feed — ADDED, NOT egress-verified.** `supabase/functions/briefing-intel-snapshot/index.ts`
  gained a new `dialysis` RSS stream (Renal & Urology News, Nephrology News & Issues, CMS Newsroom) separate
  from the generic `healthcare` stream that MB-a3-reconcile measured carries 0 dialysis content most days.
  Migration `20260912120000_lcc_mbb_rss_dialysis_stream_cron.sql` repoints the `lcc-market-brief-rss` cron at
  `stream=dialysis`. ⚠️ **The sandbox has no outbound reach to verify these URLs actually parse as
  RSS/Atom** — same limitation MB-a3-reconcile already recorded for this exact task. Operator step: hit each
  feed, confirm `parseRss()` handles it, check the tick's `gaps[]`/`results` on a dry run.

New surfaces (both flag-gated `MARKET_BRIEF_RENDER`, migration `20260912121500_lcc_mbb_market_brief_render_flag.sql`,
registered `off`):
- **Daily "Lane Briefs" email block** (`renderMarketBriefLanes`, `api/_handlers/briefing-email-handler.js`) —
  sits above Sector Watch (kept, unchanged, below it). Per lane with live facts: "changed since yesterday"
  (a fact-set diff against the prior frozen daily issue, `diffFactSets`), the 2–3 most material live facts
  (section-weighted, `selectTopFacts`), named gaps rendered plainly (`selectGapFacts` — e.g. a
  `cms_census_gap:*` fact), and a "Read the full brief →" link to `#/briefs/<lane>`. A lane with no live
  facts is omitted entirely. Every number traces to a fact object — no client-side computation (tripwire
  test asserts this over the rendered HTML). Freezes one `market_brief_issues` row per lane per day
  (`issue_type='daily'`), idempotent via the EB1 unique index on `(lane, issue_type, issue_date)` — a
  same-day re-render upserts, never duplicates.
- **Homepage Market Briefs tab** (`#/briefs/<lane>`, default `dialysis`) — new route in `app.js`
  (`ROUTE_SLUG_TO_PAGE.briefs`, sub-path parsed like `#/inbox/<id>`), new `pageMarketBriefs` page in
  `index.html`, rendered by `renderMarketBriefsPage()` from `GET /api/market-brief-tab` (new handler,
  `api/_handlers/market-brief-tab.js`, registered in `server.js`). Shows live facts by section
  (operators/policy/capital_markets/trades/implications), citation + staleness per fact, the issue archive,
  and the same changed-since diff the email uses. Read-only; returns `{enabled:false}` while the flag is
  off, never a 404/500. A small teaser (`renderMarketBriefsWidget()`, `#marketBriefsWidgetContent`) sits
  beside `#dailyBriefingWidget` on the homepage.
- Shared fetch/diff/select logic lives in `api/_shared/market-brief-render.js` — one implementation for
  both surfaces, so the email and the tab can never disagree about "live" or "changed".

Guards added: `test/market-brief-render.test.mjs` (14), `test/market-brief-lane-briefs-email.test.mjs` (11,
incl. the number-tripwire and the omitted-empty-lane case), `test/market-brief-operator-canonicalization.test.mjs`
(3, "no two live band facts share an operator_id"), plus additions to `test/market-brief-facts.test.mjs` and
`test/market-brief-tick-handlers.test.mjs`. Full repo suite: **6,114 pass / 0 fail / 6 skipped** (962
suites) — no pre-existing failures were masked; the only failures seen before the fix were a missing
`node_modules` in this sandbox, not real regressions.

⚠️ **NOT deployed, NOT live-verified, flag NOT flipped.** This session had no Railway/Supabase write
access. Two migrations are committed but unapplied (`20260912120000`, `20260912121500`). Operator sequence
per the prompt's §5: apply both migrations, redeploy Railway, run the P-SQL tick once via POST with the
flag forced on and confirm the supersede chain clears the old date-suffixed trades fragments, render the
email with a preview and load `#/briefs/dialysis`, THEN flip `MARKET_BRIEF_RENDER`. → `docs/claude-code/
prompts/MBb-lane-briefs-daily-block-and-tab.md`, `docs/architecture/EXEC-BRIEFS-SPEC.md` §9 "MB-b" addendum,
PLANNED-BACKLOG.md §P18 (MB1e, MB2, MB3, MB4).

### Deal-intelligence spine — LIVE end to end
SF Opportunity sync → `bd_opportunities` (592 deals) → Team-Briggs scope (roster edges) → deal-email
matcher → cadence-scan → weekly pipeline email; deal dossier + link-only Salesforce write-back.
→ `docs/os/BUILD-STATUS.md` (BUILD 01–05), `docs/architecture/SF-WRITEBACK-AND-DOSSIER-BUILD-STATE.md`

### Ingestion & intake
Three OM channels converge on `intake-om-pipeline.js::stageOmIntake` (email PA flow · CoStar sidebar ·
Copilot Studio). ⚠️ **They converge on the pipeline but NOT on the hardened prompt — measured
2026-08-26, the sidebar channel has produced 0 hardened-schema extractions out of 350 in 30 days
while being the largest producer (56% of rows). `staged_intake_extractions` is therefore not one
population: split by channel before grading it, or you are measuring the channel mix.** →
`docs/audits/W53_INTAKE_CHANNEL_PROVENANCE_2026-08-26.md`, backlog N8/L8/V6.
Multi-model AI fallback on extraction. Tiered OCR — digital `pdf-parse` → free OSS →
**Google Document AI** (`docai-ocr`, LIVE + verified 2026-08-12) → gpt-4o vision last resort. Office
docs (docx/xlsx) never go to OCR (byte-sniffed, extracted in-process).
→ `docs/architecture/om_intake_pipeline.md`, `docs/architecture/document-capture-and-ocr-status.md`

### BD spine, ownership & provenance
`entities` + `external_identities` + `lcc_property_owner*` + the priority queue / Decision Center;
field-level provenance (`field_provenance`, `field_source_priority`, `lcc_merge_field`); the
Ownership Resolution Engine; supersession tiers; the gov ownership-transition feeder.
→ `CLAUDE.md` §"BD spine", `docs/architecture/property-owner-subsystem.md`,
`government-lease/docs/OWNERSHIP_RESOLUTION_ENGINE.md`

### Government agency identity — the registry is WIRED (ID3a, 2026-09-12)

gov `government_agencies` (65 rows) existed and nothing pointed at it. Now:
`properties.agency_id` **0 → 7,369** / 20,509 · `property_agencies.agency_id` **160 → 119,361** /
132,243 (**0.12% → 90.3%**). VA collapses **37 raw strings → 1 code** (2,063 properties), SSA
**13 → 1** (1,248). One resolver (`gov_resolve_agency`, exact alias match, fails closed,
service_role only), a hard write guard on BOTH columns (proven live: an unknown `agency_id` RAISEs),
a reason-classified review lane (1,483 rows over 23,025 source rows, raw text intact) and the first
identity detector for the ID4 program, **run once and deliberately unscheduled**.
⚠️ **The alias table is NOT seeded from `agency_canonical`** — measured, that column conflates
federal agencies with same-named STATE bodies and COMMERCIAL lookalikes (`NAVY` is 145× *Navy
Federal Credit Union*; `ICE` contains *Handel's Homemade Ice Cream*; `VA` folds in the Arkansas and
Virginia state veterans departments). Every auto-applied row is a string a human enumerated;
`(VA)` suffixes, `GSA - <occupant>` compounds and 14 codes with no registry row are routed, never
guessed. Batch `id3a_20260912`, fully reversible.
→ **`docs/audits/ID3a_GOV_AGENCY_IDENTITY_WIRING_2026-09-12.md`**; open follow-ups `ID3a-regdup`
(the `CIS`/`USCIS` registry duplicate), `ID3a-registry-gaps`, `ID3a-detector-schedule`,
`ID3a-consumer-switch`.

✅ **ID3a-b (same day) fixed `agency_canonical` itself — closing `ID3a-gsa-compound` and most of
`ID3a-canonical-repair`.** `canonicalize_agency()` no longer conflates: NAVY **150 → 3** properties
(the 145 *Navy Federal Credit Union* rows now resolve NULL, routed to review as
`private_company_name_collision`), STATE **213 → 9** (bare `\mstate\M` — which matched "State of
Texas", "Washington State Dept of Social and Health Services" and even "DEPARTMENT OF STATE HEALTH
SERVICES" — replaced with a closed allowlist of the actual federal Dept-of-State spellings), bare
DOC **16 → 1** (routed to review as `ambiguous_doc_commerce_or_corrections` — `DOC&PS` reads state
Corrections, not Commerce), and — found while shipping, not in the original brief — ICE **44 → 43**
(*"Handel's Homemade Ice Cream & Yogurt"*). `RICHMOND FIELD OFFICE (VA)` and every trailing
`"(XX)"` state-code suffix is now stripped before any match, generally. `GSA - <occupant>` compounds
(624 properties / 106 raw strings, not the ~168/944 first estimated) keep `agency_canonical='GSA'`
as the lease counterparty and gain a second `using_agency_canonical`/`using_agency_full` pair for
the occupant. ⚠️ **DOJ/EPA/DOL/ED/DOT carry the identical state-body-conflation shape and are
confirmed still live** (`TEXAS JUVENILE JUSTICE DEPARTMENT` reads DOJ, etc.) — filed **ID3a-c**,
not fixed here. Migration
`supabase/migrations/government/20260912030000_gov_id3ab_agency_canonicalizer_contamination_fix.sql`,
reversible (`_gov_id3ab_agency_backup_20260912`); guard `test/gov-id3ab-agency-canonicalizer.test.mjs`.
The separate `government_agencies`/`gov_agency_aliases` FK registry from ID3a is untouched by this
change — display column and FK registry are two different systems, on purpose.

⚠️ **The migration file cited above (`supabase/migrations/government/20260912030000_...sql`) is
now HISTORICAL, not live (ID3a-d, same day).** `government-lease` — not this repo — owns the
government database's objects; that repo's own PR #398 shipped the real, currently-deployed
canonicalizer fix (with a state-qualifier guard and the corrected ICE/CBP branch order this repo's
copy lacks). This repo's `supabase/migrations/government/` directory is retired
(`README.md` + a per-file header on all 213 files); never re-apply anything from it. See
`CLAUDE.md` → "ONE REPO OWNS EACH DATABASE'S OBJECTS" for the full ownership table covering
government, Dialysis_DB and LCC Opps.

### County/city vocabulary fold — I14, never merges across state (ID3e, 2026-09-12)

gov `properties.{county,city}` and dia `medicare_clinics.city` each gained a STORED generated
`*_norm` column keyed on `(normalized_name, lower(trim(state)))` as a **pair** — county/city alone
is never the key. Punctuation normalizes to a space (never deleted), so a Virginia independent
city's `(city)`/`city` token survives and `RICHMOND (CITY)` / `Richmond city` fold together while
never colliding with a same-named county; cross-state same-name counties (`St Louis` MN vs MO,
`LaSalle` IL vs TX) verified to carry distinct keys. Two corrupted-state rows route to a review
view, untouched. Fold sizes (live, 2026-09-12): gov county/state 2,445→1,611 (834 collapse), gov
city/state 3,454→3,225, dia city/state 4,367→3,635. This is I14 (controlled-vocabulary
normalization) — a different mechanism than ID3a's FK/registry wiring; no consumer has been
repointed to the new columns yet. dia `property_type` was measured and scoped OUT (a taxonomy
question, not a case fold) — filed `ID3e-property-type-taxonomy`.
→ `docs/os/PLANNED-BACKLOG.md` §ID3e; migrations
`supabase/migrations/{government,dialysis}/20260912120000_*_id3e_*_vocab_fold.sql`; guard
`test/id3e-migration-shape.test.mjs`.

### Entity identity — one key, one writer, two tiers (P189 → PR5c-entities-c, 2026-09-03)
`entities.canonical_name` is trigger-owned (N15c, drift 0 at 4,618 mints). `ensureEntityLink`'s
canonical_name tier no longer scopes identity by `entities.domain` — a provenance tag, not a scope —
which minted 9 of 11 recent duplicates (`d5b0ac8`); its email tier keeps the filter ON PURPOSE (27%
precision without it). All merge review views carry no `auto_mergeable`; `lcc_merge_entity` is
reversible. 👤 N15e: 6,608 groups violate a `(workspace_id, canonical_name)` unique key.
→ **`docs/architecture/entity-identity-and-dedup.md`** (canonical).

### dia operator source-of-record — registry hygiene done, no consumer switch yet (ID2a + ID2a-cleanup, 2026-09-12)
`dia.operators` is the canonical registry (`kind` company/category/payer/non_operator/junk,
`parent_operator_id` for brand children, `merged_into_operator_id` for retired dupes — retire, never
delete); `dia_operator_aliases` (207 rows, exact-match only, seeded from every live company's own
name + `dba_names`); `dia_resolve_operator(text)` is the one resolver, fails closed, never mints;
`properties.operator_id` FK covers **9,449/11,804 (80.1%)**; a hard-block write guard
(`dia_operator_write_guard`) refuses an unresolved write and routes classifications to the new
`properties.operator_class` column instead of the human review queue. `dia_operator_write_review`:
**71 open** (genuinely unregistered operator names), 142 resolved, 807 dismissed as classifications.
Orphan-registry-gap detector reads 0. **ID2b + ID2b-caps (2026-09-12) started the consumer switch —
two surfaces shipped, most still read raw text.** Shipped: `v_market_brief_cms_operator_counts` (the
market brief's CMS-operator-count source) now groups on `properties.operator_id` (survivor-resolved),
measured row-count-parity (6,695→6,695), Satellite Healthcare(54)+Satellite Dialysis(14)→69;
`market-brief-facts.js` unblocked with zero code change. Also shipped: `rpc_query_comps` (the comps
engine's own RPC, read by the market brief's cap-rate band, BOV workbooks and every other comps
export) now returns `operator_id`/`operator_canonical` additively, and the market brief's
per-operator cap-rate band groups on it — the `Fresenius`/`DaVita` text fragmentation is fixed. **ID2b-caps (2026-09-12) closed the market brief's second, larger surface:** `rpc_query_comps`
now ADDITIVELY returns `operator_id`/`operator_canonical` on its output (comp SELECTION/scoring —
`mcp/comps-tools.js`'s `p_tenant` text filter and `operatorTier()`/`compTenantText()` — is
UNCHANGED; only new output fields were added, never an existing one, so which comps are chosen
could not have moved). The market brief's per-operator TTM cap-rate band now groups on the id.
⚠️ **ID2b-caps's own live-verification was incomplete — it read only the `properties`-joined
sale/listing arms and missed a THIRD comp source.** `sf_comp_staging` (Team Briggs' own
Salesforce-staged closed comps) has no `properties` join, so it could only ever emit
`operator_id: null`, and 375 rows spelled exactly as already-registered aliases (`DaVita
Dialysis`, `Fresenius Medical Care`) kept minting a second, text-keyed band under the identical
canonical label. **ID2b-caps-2 (2026-09-12, same day) fixed it for real:** `sf_comp_staging`
gained its own `operator_id` (fill-blanks, resolved via the same `dia_resolve_operator`), the SF
arm now resolves through it identically to the other two arms, and a structural
duplicate-display-label invariant in `planOperatorCapRateBands()` refuses to ever ship two
id-keyed bands under one label. Live-verified on the tick's own TTM window: exactly three bands
(DaVita, Fresenius Medical Care, US Renal Care), no duplicate label. **The
`Fresenius`/`Fresenius Medical Care` and `DaVita`/`DaVita Dialysis` fragmentation is gone as of
ID2b-caps-2, not ID2b-caps.** **Switching comp SELECTION itself** (the `p_tenant` filter, and
whether `mcp/comps-tools.js` should filter/score on `operator_id` rather than text) is unchanged
and remains **ID2b-c**, still needing Scott's call on a live 5-subject diff before any filter
logic moves. **Still reading raw operator text, measured and deferred, not started:** the `cm_dialysis_*`
CM exhibits (`cm_dialysis_operator_unit_economics` already ILIKE-bucket-normalizes via
`dia_operator_bucket()`, a heuristic not the registry; `cm_dialysis_available_by_tenant[_q]`/
`cm_dialysis_industry_participants` still raw — ID2b-cm), the dossier, and MCP tools generally
beyond the additive `rpc_query_comps` fields (ID2b-mods). The true population measured this round
is **96 views** referencing operator text (not the 45 first estimated), most correctly excluded as
review/audit queues where raw text is the deliverable.
→ `CLAUDE.md` (ID2a's Dialysis-repo mirror doc, if any), `docs/os/PLANNED-BACKLOG.md` §P0d
(ID2a / ID2a-cleanup / ID2b / ID2b-caps / ID2b-caps-2 / ID2c / ID2c-payer), `docs/audits/ID1_OPERATOR_IDENTITY_AUDIT_2026-09.md`,
`docs/audits/ID2b_OPERATOR_ID_CONSUMER_SWITCH_2026-09-12.md`,
`docs/audits/ID2b_caps_RPC_QUERY_COMPS_OPERATOR_ID_2026-09-12.md`.

### CoStar sidebar capture — the largest producer, and five defect arcs in one place
The extension → `sidebar-pipeline.js` path writes `properties`, `sales_transactions`,
`parcel_records`/`tax_records`, `entities`/`contacts`, `loans` and `property_documents` DIRECTLY to
the domain DBs (it does not go through `stageOmIntake`). ⚠️ **Its defects share a shape: the right
page, the wrong region of it** — the Contacts tab's broker office winning the property street
(ADDR1), a traffic table read as contacts (Prompt 89), the parcel stats dropped between capture and
row (PR2) — plus one overwrite class (SALE1: a re-match PATCH replacing an already-recorded
`sold_price`). All are guarded now; the address class is closed by a **role-agnostic server-side
belt**, not by the header regex.
→ **`docs/architecture/costar-sidebar-capture-pipeline.md`** (canonical).

### Public records — one real source, and it now carries its stats (PR1 → PR2, 2026-09-02)
`county_records` @5 is registered on 93 rungs and has **never written a field, by decision** — both
domains' `public_record_ingest.py` are gpt-4o recall, not a county fetch (PR1; §2a of the lane page).
The only genuine acquisition is the **CoStar sidebar** (dia 931 APNs / 883 properties), and since
PR2 its writer carries `building_sf` / `lot_sf` / `year_built` / `zoning` (767 / 734 / 714 / 232 on
dia, backfilled; gov writer fixed, backfill pending Scott). ⚠️ The **producer proof is still
open** — assert on a new post-redeploy sidebar row, not the totals. Ceilings of ZERO are stated for
`tax_amount` / `land_use` / `owner_name` (never captured). Provenance is the allowlist (PR8):
`field_source_priority` row ⇒ merges under its own name, else `domain_trigger`.
The ladder itself is TRIAGED (PR5, 2026-09-02): 2,141 rungs / 68 sources; **426 rungs carry a verdict on
`v_field_source_priority_triage`**, 49 sit on columns that do not exist (PR7; one live). Of 39
never-written sources only 14 are dead — 7 are live on the property-owner ledger, which writes no
`field_provenance`, and **33 rungs on LCC-internal tables have never been exercised** (PR5c).
→ `docs/architecture/public-records-source-lane.md`, `docs/audits/PR5_LADDER_SOURCE_TRIAGE_2026-09-02.md`;
`field_provenance` can hold ANY value since PR12 (2026-09-02): `value_text_hash` is a plain column
owned by a BEFORE trigger (`convert_to`), no rewrite, 1025 MB unchanged; before it every
backslash-rendering value (quotes, **newlines** — ~1,101 exposed, mostly `sales_transactions`
narrative) aborted `lcc_merge_field` and the JS failed open silently. The failure is now counted
(`provenance_failed`) and alerted (`provenance_write_failed`) — that half awaits the Railway redeploy.
⚠️ `target_database` is a CLOSED vocabulary (`lcc_opps`/`dia_db`/`gov_db`, CHECK-enforced) and it
is NOT part of the rung lookup — five callers sent other strings and failed 23514 on every call
into a bare catch (PR5c, fixed via `provenanceTargetDatabase()`; the edge-function copy awaits its
own deploy). **`lcc_merge_field` always inserts a row, so zero rows = the call never completed.**
The two `entities` contact writers consult the ladder since PR5c-entities — **recording only**: all ten
`email`/`phone` rungs are `record_only`, and a ladder over an empty ledger cannot protect a value it has
never seen. Nothing is scheduled to exercise the two propagate/writeback writers. ⚠️ **The "Salesforce
bridge CREATE path records since #2072" claim above was WRONG — CONTACT1 (2026-09-03) found that
handler is dead code (its `job_type`s are never enqueued anywhere).** The real writer, found by an
AST census of `ensureEntityLink()`'s 30+ callers, is `ensureEntityLink`'s own CREATE payload — the
ONE place it ever writes `email`/`phone` (it never PATCHes them onto an existing entity). **CONTACT1a
(2026-09-04) wired that site instead** — covers all 9 of 48 call sites that ever pass email/phone
(CoStar sidebar contact mint, `sf-list-import.js`, and 7 others), audit-only, no `enforce_mode`
change. CONTACT1a merged to `main` 2026-09-04 (PR #2114); Railway `/version` at last check
(`886cdf86`, 22:08 UTC 09-02) predates it — confirm the redeploy landed before quoting live traffic
off the new writer.
→ **`docs/architecture/field-provenance-ladder.md`** (canonical); backlog PR1d (`REGRID_API_KEY`), PR5a–e, PR5c-entities/-signal/-deploy, PR7a–b, PR9 (Scott), PR11, PR12a–b.

### Comms → context
Outlook/Teams intake → `activity_events` / `email_bodies` → deal attribution → correspondence
summaries, role evolution, next-step derivation, mailbox mirror + move-queue executor.
→ `docs/architecture/correspondence-ingestion-design.md`, `docs/setup/OUTLOOK_MAILBOX_MIRROR_FLOW.md`

### Voice & drafting
Voice profile distilled on-box from Scott's sent corpus; `/api/draft-assist` RAG drafting with the
real branded signature, threading, and deal context; save-to-Outlook-Drafts (save, never send).
→ `BRIGGS-WRITING-VOICE.md`, `docs/audits/W10_VOICE_AND_DRAFTING_KICKOFF.md`

### Capital Markets exports
`cm_*` views → Excel/PDF book export with native charts + brand tokens; KPI tiles read the same view
their data tab renders (`checkKpiSeriesConsistency` is the tripwire).
→ `CLAUDE.md` §"CM export", `public/reports/cm-brand.json`

### ⚠️ Salesforce research lanes — RETIRE, do not build a consumer (C1, 2026-08-27)
`true_owner_needs_salesforce` (dia) and `owner_needs_salesforce` (gov) are a **capture-less second
copy** of a consumer that has worked since June: the Decision Center lane **`sf_link_candidate`**
(3,369 candidates with resolved `001…` Account ids; verdict path `api/admin.js:10764` PATCHes the
exact column whose NULL-ness defines both lanes; **102 decisions, last 2026-08-14**). The research
lanes have **no capture path** — `completeResearch()` writes nothing and the seeder re-mints.
⚠️ **Live defect: the gov lane reads `unified_contacts.sf_account_id` while its only writer writes
`recorded_owners.sf_account_id`** — 1,961 gov owners already linked, 1,292 still read as a gap,
**29 agree**; working the DC lane does not clear the task, and **96 admitted rows ($314.7M) are
phantom work.**
⚠️ **"Link *or create* Salesforce account" was never buildable** — LCC's SF surface is a **read-only
Power Automate proxy** (no Connected App; no `sobjects` call anywhere).
**Recommendation: automate 27 · retire 945 · gate 1,702 · repair 1,292 — build no consumer.**
→ backlog **C1a–C1e**; `docs/audits/C1_SALESFORCE_LANES_CONSUMER_OR_RETIRE_2026-08-27.md`

### Research-task producer — correct and value-gated (A5a + A5c, 2026-08-27)
The generator read a 29,643-row feed through a call **PostgREST caps at 1,000** and auto-closed
everything outside the window as `gap_resolved` — its guard compared the **requested** limit against
a **capped** response. **100% of the "completions" in the two biggest lanes were false** (4,781 and
596). **A5a fixed it** (returned-count guard + paging + stable tiebreak; verified in production —
zero `gap_resolved` closures since deploy). **A5c value-gated it**: pool **71,448 → 2,530 admitted
(3.5%)**, floor reused at **$500k**, operators excluded by **recorded fact** (never a name test —
P113), unknown rent gated (P161). First live run minted **343**, `closed: 0`.
⚠️ **Every lane it feeds still has ZERO real completions** — the constraint has moved downstream to
consumption. → backlog **P1a / C1–C3**
→ `docs/audits/A5_TRUE_OWNER_SALESFORCE_STALL_2026-08-27.md`, `A5c_RESEARCH_TASK_VALUE_GATE_2026-08-27.md`

### Ownership-history lane — ✅ WORKING, and now CLOSED as a source of chain DEPTH (2026-08-28)

> **LATEST — read this before the 2026-08-27 block below, which it supersedes on the numbers.**
> **B1** split the $500k floor by consumer (automated path vs human surface) and **B1a** merged the
> duplicate entities blocking A2. Live: **completed 314 → 1,302 · open 156 → 579 · gov
> `any_history` 1,272 → 2,238 · `lcc_entity_portfolio_facts` 12,724 → 14,076** — with
> **`human_actionable` flat at 55 throughout** (89% of the newly-drafted population routes to
> automation, which is the design).
>
> ⚠️ **B1a REFUTED its own premise and that closes the lane as a DEPTH source.** `chain_2plus`
> moved **177 → 178** because **64 of B1a's 65 completions carried exactly ONE link**. Duplicates
> constrained chain **EXISTENCE**, never depth. The entire remaining blocked residue is worth **12**
> `chain_2plus` properties (8 permanently blocked by design — the placeholder is the GRANTOR), and
> **99 of 132 remaining open tasks carry one link.** **Stop looking for the next blocker here.**
>
> ⚠️ **AND THE FOLLOW-UP CONCLUSION ("we must acquire deeds") WAS WRONG.** gov had **never consumed
> its own `sales_transactions`** as ownership history. **✅ B5 SHIPPED 2026-08-28** and the premise
> held: gov `ownership_history` **16,177 → 18,953** (+2,776 / 2,000 properties, **677 with no prior
> history at all**), transitions view 9,595 → **12,371** rows / 4,698 → **5,555** properties.
> Deed acquisition is **deferred, not refuted**.
>
> 🚨 **THE LCC SIDE HAS NOT MOVED AND WILL NOT UNTIL THE RAILWAY REDEPLOY.** Verified post-B5:
> facts **14,076**, completed **1,302**, open **579**, `chain_2plus` **178**, `any_history` **2,238**
> — all identical to pre-B5. **527 of 579 open tasks carry a pre-B5 draft** and the drafter prepares
> only `fresh = open ∧ undrafted`. `runB5RedraftPass` is **JS**: without the deploy B5 converts on
> **52** tasks, not 579.
>
> ⚠️ **B5 also found and fixed a DESTRUCTIVE trigger** — `trg_propagate_ownership_to_property` nulled
> `properties.recorded_owner_id` for any row naming parties as text; **7,567 rows already in that
> shape**, **1,446 of 9,312 would have been destroyed**. Guard verified live; `recorded_owner_id`
> held at **9,312**. Other propagation triggers are **unaudited** → backlog **D3**.
>
> ⛔ **`B6_…md` §6 is SUPERSEDED — do not act on its `~270–370` resizing of B5 or revert it.**
> Two windows measured one population and disagreed 10×; **2 of 2,776 rows (0.07%)** are the
> circular class it objected to.
> → `docs/architecture/ownership-history-lane.md` §3a/§3c · `BD_PIPELINE_FUNNEL_AUDIT_2026-08-28.md`
> §3b/§3c · `connectivity-and-open-threads.md` §4j · **`data-coherence-invariants.md`** (the new
> standing contract) · playbook **Class 20 / Class 21**

### Ownership-history lane — the first dead research lane with a working consumer (2026-08-27)
`establish_ownership_history` produced **545 items and consumed none for 69 days**. A1 split it into
four real actions; A2 applies `agrees`; A4 retires the unanswerable; A3 turns the mismatches into
sponsor decisions. **Completed ever: 0 → 288. Open: 545 → 182.** **+304 historical ownership facts**
(12,724 → 13,028), 280 owners, **$579.9M**. Nightly on **crons 244/245**, reversible by batch tag.

**Measured 2026-08-27 14:00 UTC — completed 314 / open 156.** Split: `agrees` 64 · `mismatch` 49 ·
**`sponsor_spe` 25** · `all_guarded` 18 · **`no_records` 0** (all 74 retired).

**Six sponsor families confirmed** (`boyd`, `highwoods`, `rxr`, `arc`, `east`, `sunflower`) —
mismatch 74 → 49 with `agrees`/`all_guarded` unmoved. **Held deliberately:** `fgf` (90 SPEs;
Scott's note says they are Boyd subsidiaries — confirming to FGF Management could misattribute at
scale), `commonwealth` (15 unrelated parties incl. government bodies), `madison` ×2 (duplicate
entities), `carrington` / `sequoia` (name-derived evidence only).
→ `docs/audits/V8_SPONSOR_FAMILY_REVIEW_2026-08-27.md`

**Remaining residue is named and routed, not pooled:** blocked `ambiguous_entity` 18 ·
`no_entity` 18 · `placeholder` 15 · ~~`repeat_transfer_unrepresentable` 14~~ (**A2b ✅ DONE** —
collapsed in the drafter, 32 links → 15, all 14 unblocked; it was NOT the P138 flicker, see the
audit); plus **A4b**, a
P138 guard misfiring on street-numbered SPEs (**10 of 18 recoverable, defect wider than this
lane**), and **A3-residue** ~31 chains with no sponsor family.

⚠️ **New model gap this surfaced: JV / fund ownership is MULTI-PARTY and the chain is
single-valued today** — `Boyd Watterson JV UBP` resolves to Boyd alone and its partner is
invisible. Also **leasehold is a real ownership interest**, not a weaker one (ground lease splits
fee from leasehold). → backlog **P1c / J1–J4**
→ `docs/audits/A1_OWNERSHIP_LANE_SPLIT_2026-08-27.md`, `DATA_PROCESS_AUTOMATION_AUDIT_2026-08-26.md`

### The shared entity-merge path is REVERSIBLE (2026-08-27, P196 / N11)
`lcc_merge_entity` snapshots the whole loser side, **folds `owner_contact_pivot` fill-blanks before
the dedup DELETE**, calls the reconcile with `p_snapshot => true`, and action-labels every P160
dedup/repoint. Reverse with **`lcc_unmerge_entity(loser)`**; ledger `lcc_entity_merge_log`;
instrument `v_lcc_entity_merge_reversibility`. Round-trip proven on live data (16 rows before, 16
after, 0 lost, 0 new; `auto_mergeable` 3,053 → 3,053). ⚠️ **The "dormant" verdict described the
auto-merge LOOP, not the function** — `lcc_merge_entity` has 9 human-verdict call sites and ran
**285 merges in 30 days**. ⚠️ **2,411 pre-P196 tombstones are `reversible=false` and always will
be.** `lcc_apply_fuzzy_merges` is still unwired — that is a separate decision. **A2a is unblocked.**
→ `docs/audits/P196_MERGE_REVERSIBILITY_AND_PARK_REASONS_2026-08-27.md` §1–6

### Parked Tier 0 cards now say WHY (2026-08-27, P196 / N3e)
**146 parked / 105 owners / $180.3M**, each carrying `park_reason` + both compared strings:
`employer_on_file_differs` 76 (the gate working), `no_employer_on_file` 68,
`employer_not_comparable` 2. Sponsor-shaped parks surface as
`v_lcc_tier0_sponsor_map_proposals` → a curated `lcc_owner_sponsor_domain` INSERT (one decision per
SPE family). ⚠️ The prescribed company-string normalisation was **measured and refuted** (0 of 146),
and a naive sponsor detector reads **~25% precision** — three guards take it to 4 of 6, top-4 by
rent. **The un-park was NOT widened** (ask 77 / auto 9 / parked 146, before and after).
→ same audit, §7–13

### Conflict-marker guard (2026-08-27)
`test/no-conflict-markers.test.mjs` — committed conflict markers had been sitting on `main` in
**two** files, from **two different mechanisms** (a merge, and a `git stash pop`). Git flags
neither: the conflict *was* resolved, by committing the markers. The guard is verified red on the
pre-fix files and **also runs on the docs-only CI path**, because both instances were `docs/*.md`
and the docs-only skip would otherwise have hidden the very population it exists for.
⚠️ **Match marker CHARACTERS, never label text** — stash-pop markers read `Updated upstream` /
`Stashed changes`, not `HEAD` and a sha. → `docs/os/GITHUB-WORKFLOW.md` §2b/§4b

### On-box generation (the newest capability)
The daily brief's **Analyst's Take** generates on the GaryBuilt box. Today's
`briefing_intel_snapshot` carries a 774-char take with `analyst_take_meta.source =
'onprem_ollama'`; every prior day is length 0. First real on-box narrative.
⚠️ **Re-measured the same evening: that take is a ONE-SHOT, not the pipeline running.**
`generated_at` = **20:51 UTC** against a row created at 10:00 and a cron that fires at **10:18** —
so cron 240 did not produce it; it was generated by hand during the P138 session. P138's tick only
became deployable at the **23:13 UTC redeploy**. **Do not read this row as "producing" until a take
appears inside the 10:18 window.** → backlog **V7**; `docs/architecture/briefing-analyst-take-onprem.md`


### 2a. Shipped rows folded from `PLANNED-BACKLOG.md` (CONSOLIDATE2 round 2, 2026-09-12)

Every row below carried state `✅` in the backlog table and is moved here **verbatim** (byte-identical
row text, table markup included) per the backlog's own "How to keep this file honest" rule — a
shipped row belongs in CURRENT-STATE, not PLANNED-BACKLOG. Nothing here was reworded or summarized.

| # | Item | State | Source |
|---|---|---|---|
| ✅ **TEST-NET-LEAK** | **`npm test` made 14 live HTTPS calls to the production `ai-copilot/chat` edge function on every run** — measured 2026-09-09 two ways: `function_edge_logs` on Dialysis_DB showed 19 bursts of exactly 14 `POST 400` in 3-minute windows from rotating Azure (GitHub-runner) addresses in 24 h plus 71 from a developer machine; a local run of the full suite under a `fetch`-logging shim reproduced exactly 14: `test/lease-extractor.test.mjs` (8) and `test/dossier-generator.test.mjs` (6). Cause: both tests assumed "no AI key → extractor throws", but `api/_shared/ai.js` `invokeChatProvider` defaults `AI_EXTRACTION_PRIMARY` to `edge`, which needs no key and POSTs to the live function unauthenticated; the 400 was swallowed by the fallback chain and the assertion never saw the network. **SHIPPED 2026-09-09**: `test/_helpers/net-guard.mjs` (`--import`ed ahead of every test file, wraps `fetch`, throws on any non-loopback host) + `invokeChatProvider` refuses the `edge` route before fetching under `NODE_TEST_CONTEXT`/`LCC_HERMETIC_TESTS`. Neither leaking test file needed rewriting — their assertions already describe *provider failure*, which the seam now returns deterministically. Guard `test/hermetic-suite.test.mjs`. Full suite re-verified: ~~5,563 / 5,557 pass / 0 fail / 0 live calls, 67s~~ **Cowork re-measured from the merged tree under the original fetch-logging shim: 5,568 / 5,562 pass / 0 fail / 6 skipped / 0 live calls / 181 s vs 376 s before** (faster — no more 35s backoff sleeps against a dead route; CI's `npm test` check shortens the same way). Response: `docs/claude-code/responses/TEST-NET-LEAK-hermetic-suite.response.md`. | ✅ | Cowork 2026-09-09 |
| ✅ **RATINGS-INSERT-COLLISION** | 👤 **CONFIRMED LIVE IN PRODUCTION 2026-09-11 — this session independently queried Dialysis_DB directly (not from Scott's uploaded log excerpt, which didn't reach the ratings phase) and found the real production cutover.** `ingestion_tracker` shows a `cms_medicare_clinics` run starting **2026-09-10 22:05:31 UTC** — after `RATINGS3`'s PR #7402 merged at 18:32:10 UTC — and a direct query of `ratings` shows **all 7,013 rows now carry a fresh `updated_at` between 22:05:32 and 22:17:13 UTC that same run**, up from the March 2026-03-12 baseline that had been stuck through three straight rounds. Zero `ratings_medicare_id_uidx` duplicate-key errors from 22:19 UTC onward through the next 8 hours (checked minute-by-minute in `postgres_logs`) — this is the first time in this saga a full production run has actually landed on the fixed code and succeeded end to end, not just a single hand-run row. **One residual item, not blocking**: during the transition itself (22:05:32–22:17:13), ~219 `ratings_medicare_id_uidx` duplicate-key errors still fired even as all 7,013 rows ultimately succeeded — declining from ~18–21/min down to 0, consistent with either the old and new container briefly overlapping during the actual redeploy cutover, or a benign retry-then-succeed race under concurrency; worth a quick look next time logs come in, but it did not stop a single row from writing. Scott's 2026-09-11 06:16 UTC run (uploaded logs) had not yet reached the ratings-ingestion phase as of the log excerpt provided (only 19 seconds of CFE-RUNAWAY/PROPREV1-stage activity captured) — this ✅ verdict rests on the 22:05 UTC run's live database state, independently queried, not on that excerpt. **Background — how the arc got here:** RATINGS3's Unit 2 found the real explanation for the two prior "still broken" verdicts: the 2026-09-10 test run judged RATINGS2 against started at **17:23:08 UTC, 69 minutes before RATINGS2's own merge at 18:32:10 UTC**, with a flat duplicate-key error rate straight through the merge instant — the running container was executing pre-merge code the entire time ("merged is not running," not a code defect). RATINGS3's Unit 3 then proved the fix live and directly, no mock, against a single row (`id=1`/`medicare_id='012500'`) — no `42P10`, no `23505`, a genuine UPDATE. It also found and fixed a real defect in the process: `ratings.updated_at` had never been stamped by either write path (no update trigger either), so the metric this whole arc used to judge success (`max(updated_at)`) was structurally blind to a real write — now stamped explicitly, gated on column existence. **PR `sbriggssjc/Dialysis#7402` confirmed merged, `main` at `000eda1`; the 2026-09-10 22:05 UTC run is the first production run to actually execute that commit, and it worked at full-table scale.** Full writeup: `docs/claude-code/responses/done/RATINGS3-live-proven-upsert-fix.response.md`. | ✅ | Cowork 2026-09-11 |
| ✅ **PROPREV1** | **CONFIRMED LIVE 2026-09-10 — the first fix in this arc independently verified working, not just tested.** Root cause: CFE-RUNAWAY's client-threading fix WAS present and correct — the real bug was one layer downstream, in `column_exists(refresh=True)` (`src/schema_guards.py:208`), which preferred the on-disk schema cache whenever non-empty, ignoring the live client regardless of `refresh=True`. Fixed by skipping that cache shortcut when `refresh=True`, threading the client explicitly into `propagate_financials_to_properties()`. Test gap also confirmed and closed (CFE-RUNAWAY's 8 tests stubbed the real chain out; a new 4-test file exercises it, RED/GREEN). **Live proof, 2026-09-10: `properties.estimated_annual_revenue`'s null count dropped from 5,113 to 4,840 (273 real writes) across one run** — the exact metric named as the open question, now closed. `clinic_financial_estimates` grew normally in the same run, no runaway-probe recurrence. PR `sbriggssjc/Dialysis#7400`. Full writeup: `docs/claude-code/responses/done/PROPREV1-estimated-annual-revenue-propagation-still-dropped.response.md`. | ✅ | Cowork 2026-09-10 |
| ✅ **PRI1** | 👤 **Fixed, merged, confirmed by Scott 2026-09-11.** `public_record_ingest.py`'s `fetch_properties_for_extraction()` crashed the whole batch on an unhandled `httpx.RemoteProtocolError: ConnectionTerminated` on its very first query. Fixed by routing through the codebase's own existing `src/core_utils.py::safe_execute()` (already used at 113 other call sites, already special-cases this exact error string as transient, 3 attempts/linear backoff 0.5s+1.0s) rather than inventing a new retry helper — reuses a proven pattern. Confirmed (Unit 3) the near-simultaneous `user_interactions RLS check failed` warning shares the same root cause (a process-wide cached Supabase client singleton, same HTTP/2 connection pool). Unit 4 (the 5-hour idle window between crash and "Stopping Container") traced every atexit/signal/thread path in the code and found nothing that explains it — concluded it's Railway-side container scheduling, not a code defect; asked Scott to check for a distinct "process exited with code 1" line to confirm, not yet answered. 4 new regression tests using the real exception type, 64/64 total tests pass. PR `sbriggssjc/Dialysis#7404` confirmed merged. Full writeup: `docs/claude-code/responses/done/PRI1-public-record-ingest-connection-terminated-crash.response.md`. **Note**: this fix does NOT cover the call sites found broken in the fresh crash below (`PRI3`) — those are different files entirely. | ✅ | Cowork 2026-09-11 |
| ✅ **PRI2** | 👤 **Closed — superseded by `PRI3`, now fixed/merged/deployed.** Root-cause escalation finding, now confirmed far more serious than first framed — see `PRI3` for the full, devastating inventory found in a live crash the same day; that inventory is now fixed, merged (`sbriggssjc/Dialysis#7406`), and deployed live, so this row is closed rather than left open pointing at a resolved item. `src.supabase_execute_wrapper`'s calls were first observed failing their first attempt 50% of the time (retry-then-succeed, just wasteful) in one run. A second run, hours later, showed the same root cause causing **actual permanent data loss and zero-output pipeline stages** — not just wasted retries — because several call sites have no retry at all. Same error signature (`RemoteProtocolError: ConnectionTerminated`) in `public_record_ingest.py` (fixed, `PRI1`), `cms_aux_ingestion` (retries, wasteful but survives), `oig_leie_ingestor`, `ownership_linker`, `utils_shared`, and `ingestion_tracker` (all broken, no/insufficient retry — see `PRI3`) strongly indicates one shared root cause (the underlying httpx/HTTP2 client or connection pooling — e.g. a pooled connection Supabase's edge has already reset getting reused on first use), not six independent bugs. `PRI3`'s response addressed the root-cause question directly: **not determinable from this repo** (no lockfile/Dockerfile/Railway config committed to audit actual dependency versions, no deliberate version bump in git history) — the call-site `safe_execute()` approach was the practical path forward, and that approach is now shipped. | ✅ | Cowork 2026-09-11, closed — superseded by PRI3 |
| ✅ **PRI3** | 👤 **Fixed, merged, and deployed live — confirmed by Scott 2026-09-11.** `Dialysis` PR `#7406` (branch `claude/epic-archimedes-jpcknd`) is merged and deployed live. (a) `oig_leie_ingestor`, (b) all 9 `ownership_linker` sub-steps, (d) `utils_shared`'s `pending_updates` fetch, (e) `ingestion_tracker`'s `start_run` — all reported fixed via the same `safe_execute()` pattern `PRI1` proved correct (e's fix included a self-caught regression: moving query construction outside `safe_execute()` broke a test using an incomplete stub client, fixed by wrapping construction+execution in a lambda). (c) the `owners` `UnboundLocalError` — confirmed a genuine, separate Python bug (unassigned local variable referenced after a preceding step failed), fixed independent of the retry work. Full suite: **3183 passed** (up from 3173), 0 failed. Open questions from the original catalog: **(f)** the all-zeros summary resolved as two conflated-but-benign phenomena, not real hidden data loss. **(g)** `census_demographics_ingestor.py` confirmed vulnerable to the same connection issue, but this specific run's actual cause undetermined from available logs — stated plainly, not guessed. **(h)** the actual crash trigger remains **genuinely unresolved** — traced all 25 pipeline steps' error handling, found no unprotected path, concluded possibly an OOM/platform-level kill per a failure mode already documented elsewhere in the repo. **Section 2** (root cause of the connection drops): **not determinable from this repo** (no lockfile/Dockerfile/Railway config committed to audit actual dependency versions) — call-site `safe_execute()` fixes remain the practical path forward. **Gaps flagged, not glossed over**: the response asserts before/after proof for (a)/(b)/(d)/(e)/(g) that wasn't independently visible in what this session could extract from the `.docx`; the file list names 5 of a claimed 6 changed files (`oig_leie_ingestor.py` +23-6, `ownership_linker.py` +202-105, `utils_shared.py` +39-24, `ingestion_tracker.py` +53-31, `test_pri3_connection_retry_sweep.py` +406-8) — the 6th is unnamed. **Merge status confirmed 2026-09-11**: Scott confirmed `sbriggssjc/Dialysis#7406` (branch `claude/epic-archimedes-jpcknd`) is merged AND deployed live — separate from, and in addition to, the `life-command-center` documentation PR that filed this review. The before/after proof detail and the unnamed 6th changed file were never independently re-verified beyond the response transcript itself (no `Dialysis` repo access from this session) — noted as a standing gap, not something this arc can close further from here. Full writeup: `docs/claude-code/responses/done/PRI3-connection-retry-sweep-and-ownership-linker-bug.response.md`. | ✅ | Cowork 2026-09-11, confirmed merged + deployed live |
| ✅ **PRI4** | 👤 **Fixed, merged, and deployed live — confirmed by Scott 2026-09-11.** `Dialysis` PR `#7407` (branch `claude/inspiring-feynman-y8l6mh`) is merged and the redeploy is live. Not yet independently re-verified live (no clean run has been triggered against the redeployed code yet) — that's the next step. (a) The real no-retry location was `src/health.py::preflight_health_check` (corrected from the prompt's own framing) — now routed through `safe_execute()`, plus two more unguarded probes fixed along the way. (d) confirmed safe to kill the hung deployment — no partial state. **(b)**: the response claims the exact failure branch already correctly closes its tracker row via `finish_run()` — **this session re-queried `ingestion_tracker` live and found the actual row (`started_at 15:53:15.083884 UTC`) still open 1.5+ hours later**, contradicting that claim. Either the described code path doesn't match what ran in production, or `finish_run()` itself silently hung under the same connection instability — tying (b) to (c) as one shared symptom rather than two. A second, genuinely uncovered abort branch (`has_blockers`) was found and fixed. **(c)**: root cause not proven (couldn't attach to the live process), but found a real, concrete defect in `core_utils.safe_execute()` — its timeout only stops waiting on the future, while the underlying `ThreadPoolExecutor`'s own `shutdown(wait=True)` then blocks again on the same stuck thread, silently defeating the timeout. Stated as the strongest candidate, not confirmed — but plausibly the same mechanism behind `PRI1`'s own still-unanswered Unit 4 mystery (the 5-hour idle gap). Mitigation applied regardless: abort/cleanup now runs on a daemon thread with a bounded 60s join, then `os._exit(2)`. Full suite: **3222 passed** (up from 3183), 0 failed. **Merge status of `sbriggssjc/Dialysis#7407` unconfirmed** — asked Scott directly. Full writeup: `docs/claude-code/responses/done/PRI4-preflight-abort-hang-and-uncovered-call-site.response.md`. **Update 2026-09-11**: a fresh CMS run did not hang this time, consistent with (not proof of) the daemon-thread + `os._exit(2)` mitigation. | ✅ | Cowork 2026-09-11, no hang observed on next run |
| ✅ **PRI3 live-fix proof** | 👤 **Confirmed live 2026-09-11.** The live-fix proof this arc has been waiting on since `PRI3` shipped: a fresh CMS run's logs show `ownership_linker`'s `Properties → true_owners` returning real, non-zero counts (`from_cms_chain: 6570`) and `Contacts → Salesforce` (`by_company: 19`) — versus the original crash where all 9 sub-steps failed with every counter at `0`. `PRI3`'s fix works end to end in production. | ✅ | Cowork 2026-09-11, confirmed via live run logs |
| C2a-decide | **✅ DECIDED BY SCOTT 2026-08-28 — NO RENT FLOOR, eligible-set only.** *"My inclination is to have no minimum floor… we want to resolve all ownership and pursue the relative next most valuable contact based on all considerations."* **Two facts reframed it:** the gate is on **GROSS ANNUAL RENT, not value** — at ~7% cap the $2M–$20M sweet spot is **$140k–$1.4M of rent**, so the $500k floor (≈$7.1M value) excluded the bottom two-thirds of the target range; and **there is no `--min-rent` in the mint at all** (`lcc_mint_gov_asset_entities(p_rows jsonb, p_batch text, p_dry_run boolean)` takes a row list — the floor is a caller-side convention). ⚠️ **The portfolio-owner argument was MEASURED and does NOT hold in gov**: per-owner aggregation adds only **129 owners** over the $500k per-property floor, and of 16 gov owners with 20+ properties, **0 have all properties under $250k** — the described owner does not exist in the data. **The floor decides what to MINT, not who to PURSUE**; `v_priority_queue` does the ranking. **Mint broadly, rank narrowly.** | ✅ | canonical §4h |
| ⭐ **B6a** | ✅ **SHIPPED 2026-08-28** — `docs/audits/B6a_SKIPPED_STEP_HEALTH_BLINDNESS_2026-08-28.md`. The four dead producers now read **RED** (170/170/150/144d vs a 45-day SLA); skips emit at the guard site; `v_pipeline_task_health` gains `age_days`/`is_overdue`/`skip_reason`. **⚠️ Two corrections to the premise, both load-bearing.** (1) **The registry already existed and was wired end to end** — R56's `feed_freshness_registry` + cross-DB mirror + deduped auto-resolving alert (`feed_stale` has fired 8 times). It read healthy **because nobody registered these four**. No new registry was built. (2) **"A skipped step is NO row" is only half the mechanism** — `gsa_ingest_+_diff` was **not absent**, it carried a **GREEN row 67 days stale** on a 7-day step, so the prescribed fix (*enumerate declared steps*) **would not have caught it**; the missing dimension is **cadence**, not enumeration. Also: **a producer is not a table** (a plain `prospect_leads` row stays green while its `ownership_change` lane is dead → structured `%I`/`%L` filter); **p90 not median** (clustered runs deflate the median and false-positive healthy monthly steps); `is_overdue` **NULL below 3 gaps**; `declared` has **no default**. Detector positive-controlled (silenced a step → red → restored, 0 residue). 23 tests, 18 mutations RED. **No producer restarted — B6b.** | ✅ | B6 §8b |
| ⭐ **B6a-follow-up** | ✅ **SHIPPED 2026-08-28** — `docs/audits/B6a_FOLLOWUP_FRESHNESS_MONITOR_2026-08-28.md`. `feeds_evaluated` **2 → 25**, `feeds_excluded_stale_mirror` **18 → 0**, mirror `synced_at` **33d/30d stale → today**, **6 `feed_stale` alerts opened including all four B6a producers** (170/170/150/144d), plus two `feed_mirror_stale` opened **and auto-resolved** — the monitor seen going red on its own blindness and green again, on the live month-old mirror. **The exclusion was KEPT** (deleting it alerts on ages it cannot vouch for — the wrong fix, worse than the bug); what changed is that the excluded set is now its own deduped auto-resolving alert, and `lcc_finalize_feed_freshness` **counts, records and retries** non-200 into a new `lcc_feed_freshness_sync_status` watermark instead of returning `(0,0)`. **⚠️ The transport was TWO unrelated causes three days apart, not one:** gov a **marginal cold-cache statement timeout** (`500`/`57014`; warm **231 ms** vs a **3 s** anon budget — positive control: identical request `500` cold at 17:41, **`200` warm at 17:44**), dia a **hard revoked `anon` EXECUTE** (`401`/`42501`). **gov NOT touched** (§2c) — mitigated LCC-side by a bounded retry + a `35,40,45` finalize cycle; the dia GRANT is the one domain-side change and **shipped with a registry `REVOKE`**, because `anon` EXECUTE on a SECURITY DEFINER function over a registry `anon` can write is the hole B6a closed on gov, still open on dia. Three further silent paths closed: a `RAISE NOTICE` missing-secret no-op, a `200 []` (P157 shape), and a **`lost` class** — `net._http_response` prunes at ~6h while inflight lingered 24h, so a late response could **never** be consumed. **§2e sweep: this was the only one of ten `lcc_check_*` with the shape**; `lcc_check_bd_sync_freshness` already does it right and is the precedent reused. 17 tests, **15 mutations RED** (4 needed tightening — a body-wide `includes()` is weak wherever the token recurs). **⚠️ NOT fixed: gov's timeout is mitigated, not cured → B6a-follow-up-b; the four producers are still dead → B6b, now unblocked; and B6a's `record_skip` has still not been exercised by a real run (0 skip rows ever, 0 run_log rows since B6a shipped).** | ✅ | B6a §7a · I11 |
| ⭐ **B6b** | ✅ **SHIPPED 2026-08-28** — `docs/audits/B6b_GSA_LANDLORD_CHANGE_RESTART_2026-08-28.md`. `gsa_lease_change_facts` **356,291 → 374,257**, max snapshot **2026-02-01 → 2026-07-01**; `gsa_lease_timeline` **16,471 → 16,779**, max **2025-12-01 → 2026-07-01**; **both `feed_stale` alerts AUTO-RESOLVED** (read on the alert row, not a run log). Scheduled caller wired into the existing Monday `gsa-sync` on **BOTH** paths — the backlog is independent of whether today's pull was new. **⚠️ Three premises corrected.** (1) **The raw feed was never dead**: `gsa_source_pull_log` shows a pull 2026-08-24 with `skipped_duplicate` / `consecutive_unchanged=3` — GSA has not published past 2026-07-01, cadence measured at 28–31d. (2) **The derived layer read a DIFFERENT TABLE** — `derive_change_facts` reads `gsa_inventory_snapshot_lines` (manual, frozen) while the weekly job writes `gsa_snapshots` (live); scheduling the old code unchanged would have derived **nothing**. Repoint gated by a full-history digest (137 shared dates, 136 identical; 22,030 field-level pairs, 0 diffs) **positive-controlled** at 6,223 diffs mis-keyed — and it is **NOT a clean superset**: 10 dates exist only in the manual panel, so it is unioned in per date, not retired. (3) **"Undiffed" ≠ "derivable"** — 21 undiffed dates, **15 already SPANNED by an existing diff** (2018-06-01 sits inside a 2018-03-01→2019-04-01 diff carrying 18,821 facts); deriving them double-observes conveyances, the A2b fan-out in the TIME dimension. Deflated **+72 net-new / +63 properties** (raw +1,336, an 18.6× deflation on the increment); ⚠️ **non-oscillating went DOWN 47** — the new months supplied return legs, so more data made the guard stricter. **Nothing fed to `ownership_history`** (→ B6b-lead). 14 tests, **13 mutations RED**; gov suite 921 pass. | ✅ | B6 §2, §8 |
| ⭐ **B6c** | ✅ **ANSWERED 2026-08-28 — DIAGNOSIS ONLY, NOTHING REPAIRED** ([`docs/audits/B6c_PROPERTY_SALE_EVENTS_2026-08-28.md`](../audits/B6c_PROPERTY_SALE_EVENTS_2026-08-28.md)). **The TABLE has a future; the two LINK COLUMNS do not.** The table is load-bearing — **6 live gov triggers** (close-listing, propagate-sale-to-`properties`, cap-rate snapshot), the LCC detail panel's *declared canonical write target* (2 write paths), read+write allowlisted on both domains — **so keep it.** But **`ownership_history_id` has ZERO readers anywhere**: 0 hits across **620 gov objects** and across dia, 0 in `api/`, 0 of 5,208 gov rows, **1.9% (52/2,730) on dia after four months**, and **no FK on either domain**. Repairing its type builds a link nobody follows (**Class 2**) into a population that is **56% `ownership_change_stub*`**, the retired circular source. **`sales_transaction_id` has exactly one reader, dia-only** (`fn_listing_close_if_sold`); gov has none and gov's own close-listing trigger does not want one. 🚨 **And the audit found something that outranks the type defect — see `B6c-dup`.** Carried the **D2** sweep (now run on all three projects). | ✅ | B6 · D2 · B6c-dup |
| ⭐ **B6c-dup** | ✅ **SHIPPED 2026-08-29 — the collision was REAL, the write path DID leak, and the damage was ZERO** ([`docs/audits/B6c_dup_SALE_STORE_CANONICAL_2026-08-28.md`](../audits/B6c_dup_SALE_STORE_CANONICAL_2026-08-28.md)). **DECISION, in writing: `sales_transactions` is the canonical comps spine; `property_sale_events` is a CAPTURE surface that propagates into it.** 77 of 77 gov views that read a sale store read the spine (all 30 `cm_gov*` CM views); zero read PSE. `detail.js` said the opposite in its own comments — corrected at 4 sites, each `B6c-dup`-marked with the old wording quoted. The leak was confirmed **behaviourally** in a rolled-back transaction (PSE +1, spine **+0**, `latest_sale_price` set), not by reading code. Shipped `trg_gov_pse_propagate_to_sale` (AFTER INSERT on PSE, the SINGLE owner of that transition), `field_source_priority` @5, ledger + kill switch + batch reversal. ⚠️ **BLAST RADIUS TODAY IS ZERO AND THAT IS THE POINT** — the operator path has never produced a row; all 5,208 PSE rows are bulk importers that wrote the spine independently (inserts stopped 2026-04-06). Fix-before-it-bites, so the build is small. | ✅ | B6c · I1 · I7 | ⛔ **ALL THREE PRIOR ORPHAN FIGURES WERE WRONG — 330/$4.48B, then 9/$558.8M, then my own first re-measure of 6/$29.2M. THE TRUE COUNT IS ZERO.** Two root causes, both transferable. **(1) The exact-date join is the wrong key.** `sales_transactions.sale_date` is MONTH-TRUNCATED for its dominant source — `costar_sidebar` **87.4% day-1** (6,871/7,865), ownership stubs **100%**. Re-keyed on `(property, YEAR-MONTH)`: **0 orphans of 1,694**, impossible-price positive control **1,694**. All six named "orphans" have an EXACT price twin 3–21 days apart, **every twin on the 1st**. ⚠️ `dedup_natural_key` already encoded that granularity — the spine had been stating its own join key all along. **Run the neighbouring key before believing an anti-join.** **(2) `property_id IS NULL` ≠ dangling.** Dangling is **0 and structurally impossible**: `fk_pse_property … ON DELETE SET NULL`. The 321 are **376 NULL-link rows**, 374 `costar_export`, **321 detached in ONE batch 2026-04-03** by a bulk property deletion. ⚠️ I reproduced the brief's error before catching it — a `LEFT JOIN … WHERE prop_live=false` lumps NULL in with dangling. **A left join cannot tell "points nowhere" from "points at nothing."** **(3) `transaction_state` was never read.** The "$529.6M invisible to the spine" is **quarantine**: all 3 NULL-price twins are `needs_review`/`duplicate_superseded` with `exclude_from_market_metrics=true`. Population: **1,687 live twins · 7 quarantined ($604.1M) · 0 absent · 0 live twins with a NULL price.** The spine is COMPLETE and had already judged the residue. |
| ⭐ **B6d** | ✅ **SHIPPED 2026-08-29 — and the two alerts everyone assumed were mis-sized SLAs are REAL BREAKS** ([`docs/audits/B6d_FEED_EXPECTATION_GRADING_2026-08-29.md`](../audits/B6d_FEED_EXPECTATION_GRADING_2026-08-29.md)). Every feed expectation is now **measured, not defaulted**: `cadence_class` + a mandatory `expectation_basis` (or `unwatched_reason`), CHECK-enforced on all three registries. Open `feed_stale` **4 → 2**, and **0 of the survivors describes a decision**. ⚠️ **RETIRING AN EXPECTATION MADE ITS ALERT PERMANENT, and it was already live.** `lcc_check_feed_freshness` auto-resolves only a feed that is PRESENT and not stale, so the two retired feeds — **`prospect_leads_ownership_change`** (B6b-lead's refusal) and **`property_sale_events`** (B6c-dup's, expressed as `is_active = false`) — left the surface and their alerts could never close. Fixed with **B6a's own lesson one layer up: an unwatched feed EMITS**, with a NULL bound as a *positive* statement, and a resolve arm keyed on that — **never on ABSENCE**, which also covers a feed whose query errored or whose mirror went blind. The residual is **counted as `alerts_orphaned`, never auto-resolved**: a decision and a disappearance must not close identically. **Retire the BOUND, never the ROW.** ⚠️ **THE POPULATION IS 25, NOT 23** — LCC Opps carries its own registry (`om_intake`, `salesforce_sync`), invisible to a count taken from the domain DBs. ⚠️ **RULE 3c HELD TWICE.** dia `medicare_clinics` (65d/45d): p50 gap 2d, **max gap ever 41d**, so 45 was never the problem — `ingestion_tracker` shows **27 failed + 6 abandoned** CMS runs since the last success 2026-06-25 while `dataset_modified_date` reads **2026-08-25**; widening would have buried a two-month outage against a LIVE source (**B6d-cms**). gov `sam_lease_opportunities` re-scoped 14 → 21 **and deliberately left violated at 33d**: the weekly producer is fine (`usajobs`, same workflow, landed 2026-08-24) and the SAM call itself returns **401 Unauthorized** (**B6d-sam**). ⚠️ **That 401 is NOT the §18 rate limit** — different key, different endpoint; §18's *"the 401 is not real"* correction is exactly what makes a real 401 easy to dismiss. ⚠️ **MEASURING A FEED'S OWN GAPS IS CIRCULAR WHEN IT HAS BEEN DEAD**: an outage is a CLOSED gap and enters its own distribution — `gsa_lease_change_facts` has 2 dates and one 170d gap (*the outage B6b repaired*), so 3×p90 derives a **510-day** bound; and a lifetime window mixes ERAS (`usajobs` p90 31.8/max 95 over life, p50 7/max 16 in the scheduled era). **B6a's p90 rule does not transfer from steps to feeds.** Primary basis = the producer's DECLARED schedule; below 3 gaps the honest verdict is `cannot_be_sized_from_data`, recorded not dressed up. **Weekly family → 21 = 3× cadence** (6 feeds at 14/30/35/45; 14 had already produced a false fire). ⚠️ **The GSA family is FOUR feeds, ONE publisher, and carried FOUR bounds** (65/35/45/45), three below the cycle's own peak: publication is monthly with a **21–51d lag**, peak data age ~82d → `gsa_leases_snapshot` **65 → 90** (it was **6 days from firing** on a healthy feed, with `consecutive_unchanged=3` proving GSA has not published August), derived trio **→ 75**, pinned by a guard so they cannot drift apart again. `gsa_lease_events` was 35 and **would have fired 2026-09-10**: its cadence changed 2026-08-10 when the fingerprint dedupe began skipping, and its bound had not. **`gsa_source_pull` stays tight at 21** — *did WE stop pulling* is a different question from *is GSA publishing*. **`opm_workforce` 120 → 200**: data is 74–75d old **at the moment of a successful import** and the one observed import interval is 119d, so 120 was **unmeetable by the process that feeds it** — it fired 3× in 3 months, every one closed "expected". Controls (rolled back): opm@199d→0 alerts, opm@205d→1, gsa@95d→1, unwatched@1800d→0, orphan-counter→1 named. Guards: LCC 7 tests **12/12 mutations RED**, gov 8 tests **14/14 RED**. ⚠️ **A REVIEW BOT CAUGHT A SECURITY CLAIM THIS ROUND ASSERTED WITHOUT POSITIVE-CONTROLLING IT** (Codex, on Dialysis#7378): `REVOKE EXECUTE … FROM anon, authenticated` on `compute_feed_cadence` was a **no-op** — Postgres grants EXECUTE on a new FUNCTION to **PUBLIC** by default, so both roles still reached the SECURITY DEFINER function; measured live after the "fix", `proacl` carried the leading `=X/postgres` PUBLIC grant and `has_function_privilege('anon', oid, 'EXECUTE')` read TRUE on gov and dia. Fixed with `REVOKE … FROM PUBLIC`, verified false after. A **VIEW** takes no default PUBLIC grant, which is why the view half of the same migration worked. **Assert a privilege with `has_function_privilege()`, never by reading the REVOKE you just wrote** — four artifacts repeated the unverified claim. `compute_feed_freshness` keeps its anon grant BY DESIGN and the guard pins that asymmetry. | ✅ | `prompts/B6d-*.md` · I4 · I11 |
| ⭐ **B6d-cms** | ✅ **ROOT-CAUSED AND FIXED IN CODE 2026-08-29** ([`docs/audits/B6d_cms_INGESTION_REPAIR_2026-08-29.md`](../audits/B6d_cms_INGESTION_REPAIR_2026-08-29.md)); **the restart itself ships on the next Railway run — see the 👤 line below.** ⚠️ **THREE PREMISES IN THE BRIEF WERE WRONG AND EACH ONE MATTERED.** **(1) `cms-ingestion-daily.yml` DOES NOT EXIST** — deleted 2026-07-29 (`5d54fd7`, W1.5b); ingestion is the **Railway `cms-ingestion` cron** (`0 6 * * *`), so the GH Actions logs the brief pointed at hold nothing. **(2) "40 failed + 16 abandoned, still trying daily" is not 56 failed ingestions** — those totals **lump `source='ingestion_lock'` janitor rows in with pipeline rows** (27+6 are `cms_ingestion`; 13+10 are the lock), and the runs are **not daily**: **7 attempt-days in 100**, spaced **31 days** apart. That regularity was the answer, visible before any code was read. **(3) No failure carries a CMS error at all** — every `error_log` is a janitor artifact (*"Orphaned run… stuck in 'started' for 48.0h"*, *"Reclaimed by ingestion_lock after 0.0h stale"*). `run_cms_ingestion` records a real traceback on an exception, so **the ABSENCE of one is the evidence: the process is KILLED, not failing.** **The mechanism is two coupled defects, and neither alone explains it.** `main()` gated on **`if days_ago >= 30`** — a **calendar throttle capping a DAILY cron at ~one ingest/month** against a CMS republish cadence of **p50 2 days**; and `get_last_ingestion_meta()` took the newest tracker row **of ANY status**, so a run killed mid-flight recorded `dataset_modified_date` = CMS's new publish date and **suppressed its own retry** via `SKIP-GATE [last_run_date]`. **A latch: gate opens ~every 31d → run starts → is killed → the crash re-arms both gates.** ⚠️ **Removing the throttle alone would have fixed NOTHING** — the pipeline would run daily and then skip on a watermark written by a crashed run. Watermark now `("success",)` only; `partial` (didn't finish) and `recorded` (marks a *skip*) excluded too — **this check must fail OPEN.** ⚠️ **THE MEASUREMENT THAT REFRAMED IT: find what the job writes on EVERY run.** `cms_dataset_updates` shows **99 of the last 100 days, including 06:02 the morning of the audit** — so the cron is healthy and **CMS egress works**. That killed a plausible "Railway Hobby credit exhaustion" hypothesis (the month-end clustering fit it well) and turned *"the cron isn't running"* into *"it runs and skips"* in one query. **The SLA was never the problem and was not touched** — p50 2d / p90 18.5d / **max 41d ever** vs a 65d age; a bound above a feed's observed maximum is not mis-sized. `expectation_basis` corrected in place (it cited the retired workflow). Guard: `test_b6d_cms_ingestion_throttle.py`, 8 tests, **4/4 mutations RED**, **comments stripped first** — the fix's own comments quote `days_ago >= 30`, so a naive grep would report the bug it just removed. Suite **2919 passed / 52 failed with the failure set BYTE-IDENTICAL to the pre-change baseline** (pre-existing missing optional deps). | ✅ | B6d §11 · I4 | ⚠️ **VERIFIED BY COWORK 2026-08-29 — THE OUTAGE IS NOT YET FIXED, AND 'FIXED IN CODE' MUST NOT BE READ AS 'FIXED'.** State delta measured on the only metric that counts: **`max(medicare_clinics.source_last_seen)` is STILL `2026-06-25`, and `0` clinics have been refreshed since.** ⚠️ **Newest CMS attempt is `2026-08-27 06:12` — status `abandoned`, `rows_upserted` NULL — and there has been NO attempt on 08-28 or 08-29 at all**, against a daily `0 6 * * *` schedule. That silence is itself consistent with the **crashed-run latch** this fix removes: an abandoned run leaves the latch set and nothing runs again. **So the fix targets a real mechanism and is unproven until a run completes.** **The verification is the NEXT 06:00 UTC execution**, and the check is `source_last_seen` advancing + the `feed_stale` alert auto-resolving — **not CI, not the merge, not the redeploy.** ⚠️ **And the deeper cause is still open: WHY each attempt was being killed is unresolved** → **B6d-cms-restart**, which needs **Railway deploy logs Claude Code cannot reach** (👤 Scott).
| ⭐ **B6d-pri** | ✅ **SHIPPED 2026-08-31 — four defects fixed, and the CMS outage turned out to be a DEPLOY GAP** ([`docs/audits/B6d_pri_PUBLIC_RECORD_INGEST_REPAIR_2026-08-31.md`](../audits/B6d_pri_PUBLIC_RECORD_INGEST_REPAIR_2026-08-31.md)). ⚠️ **THE FIRST THING TO ESTABLISH SETTLED THE BIGGEST ITEM: the 2026-08-31 log emits `"CMS ingestion recently run (3 days ago < 30); skipping."`, a format string `fc342b3` DELETED on 2026-08-29** — it exists nowhere in `main`. The arithmetic corroborates to the day: the pre-fix watermark took the newest tracker row of ANY status (`2026-08-27 06:12`, **abandoned**) → 3.99d → `.days` = 3. Under the fixed code the same run reads the last *successful* ingest (2026-06-25, 67d) and applies **no throttle at all**, so it would have proceeded. **So no second throttle fix was written — keying on last SUCCESS is already `INGESTED_RUN_STATUSES = ("success",)` in `main`.** *Merged is not running*, fourth appearance in this arc. **(2) `resolve_applied_updates` omitted `reason` from its SELECT and wrote it back** → 23502 on every stale row; `pending_updates.reason` is NOT NULL (live: **1,959 rows, 0 nulls, 1,952 past the 7d threshold**). Reason now carried forward from the row; a blank one **omits the key** rather than sending `None`; **no placeholder is synthesised** — a reason that restates the source is not a reason. **(3) ⚠️ THE MISSING DSN WAS A SYMPTOM MASKING THE REAL ERROR.** A bare `except Exception: pass` swallowed the PostgREST 23502 and let the psycopg fallback's *"DSN not configured"* become the only visible message — so every diagnosis pointed at an env var. **Setting it would have fixed nothing**: the fallback INSERTs, and `pending_updates` is NOT NULL on `reason`/`entity`/`action`/`payload`/`file_name` too, so it would have failed differently — and a fallback that *did* satisfy them would mint fresh queue rows for what was meant to be a status flip. **Fixing (2) is what makes (3)'s symptom disappear.** Errors now lead with the PostgREST cause; a missing DSN is reported **once per process**, not 486 times. ⚠️ **AND THE FIX NEARLY LANDED ON DEAD CODE** — `logging_helpers` defines `upsert_pending_update` **twice** and rebinds the name to `upsert_pending_update_v2` at the bottom of the module; `inspect` says the live body is line **4528**. Two definitions of one name in one Python module, the later silently wins. Both fixed; guards parametrised over both **and** over the live binding. **(4) `properties._new_property` is a PSEUDO-FIELD, not a column** (0 in `information_schema`, 65 live rows) — the select 42703'd, the `except` swallowed it, and the row **silently read as "no change"**. Now settled by name (no round trip), cached per distinct field, **fails open**, counted as `unreconcilable`. **(5) The honest count is the defect that hid the other four:** `Pending updates cleanup complete` was logged immediately after ~500 consecutive failures and the process exited 0. Now a `DrainReport` (attempted/succeeded/failed), `wholesale_failure = attempted > 0 and succeeded == 0`, ERROR on that shape, and **`EXIT_DRAIN_FAILED = 3`** distinct from the existing preflight `2`. ⚠️ **THE 1,001 LOG LINES ARE A VIEWER CAP, NOT A RUN BOUNDARY** — 496+486+10+~9 = 1,001 against 1,952 stale rows, so the brief's counts are **floors** and the true per-run figure is **~1,950**. Same shape as A5's `815 = 1000 − 185`. **Also fixed: a skip now emits a tracker row with a reason** (B6a's rule one layer down, inside the ingester) under `status='recorded'`, which is deliberately NOT in `INGESTED_RUN_STATUSES` so it can never re-arm the watermark. ⚠️ **One guard passed its own mutation**: `assert "wholesale_failure" in body` stayed GREEN against `if False:` because the local `drain_wholesale_failure` contains that substring (the N15c lesson) — it asserts the **AST attribute access** now. ⚠️ **A second guard sliced past the end of the function it named**, through a module-level `try/except: pass`; the slicer uses **`ast` line spans** now. Guards: **21 tests, 20/20 mutations RED**, comments stripped after slicing. Suite **2,957 passed / 44 failed, failure set BYTE-IDENTICAL to the same-session baseline**. | ✅ | B6d-cms · I4 | 👤 **The restart is still a Railway Redeploy** — nothing here ingests. **Verify on `max(medicare_clinics.source_last_seen)` advancing past 2026-06-25** and the `feed_stale` alert auto-resolving; never `updated_at`, never the log line. |
| 🟡 **DRIFT1-routing-gap** | **REPO-SIDE FIX SHIPPED 2026-09-08 — DEPLOY STILL PENDING.** Sizing confirmed the skip population is structurally uncountable from either domain's staging tables (a 1,064-row re-route replay across dia + gov produced zero flips **by construction**, not as evidence of no gap — a staging table can only hold rows that already resolved to its own vertical); live Salesforce access was not reachable to try Method 2. **Decision: merge into ONE canonical `GOV_SIGNALS`** (now the sole export from `_shared/sf-deal-promotion.ts`, imported by `sf-config.ts`'s `routeVertical`), justified per term — every state-agency phrase kept has a live, word-boundary-anchored precedent in `api/_handlers/sidebar-pipeline.js`'s `GOV_TENANT_PATTERNS` (the Topic-1 TFC audit) already minting gov properties with no reported false positive. **`"motor vehicles"` deliberately excluded** — no sidebar precedent, and this list matches by plain substring (private auto dealers collide) — filed as `DRIFT1-routing-gap-motorvehicles`. Deployed null-on-no-match default kept (no default-to-dia). ⚠️ **Corrects an inherited error**: `GOV_STATE_SIGNALS` was never actually consumed by `sf-promotion-worker` — it had zero production importers, only the test file. 24/24 tests pass. **⚠️ NOT DEPLOYED — `intake-salesforce` is a Supabase edge function; this repo change does nothing until redeployed.** Verify post-deploy on `routeVertical`'s `no_match` rate on new staging rows. ✅ **DEPLOYED 2026-09-09 (Scott, `supabase functions deploy intake-salesforce --no-verify-jwt`): v23 → v24, `verify_jwt` false, bare GET answers. Pre-flight: repo `index.ts` = live v23 snapshot, delta is only the `GOV_SIGNALS` import; `config.toml` pin added so a bare deploy can never flip the JWT check. Proof-next: a `gov_tenant_kw` routing reason on a state-agency record in `sf_sync_log`.** | ✅ | STATUS 2026-09-08 |
| **DRIFT1-retire** | 👤 **Four deployments verdicted RETIRE and deliberately NOT deleted** — LCC Opps `docai-diag` (static 410, self-describing `diagnostic_disabled_safe_to_delete`), dia `sf-test` (SF SOAP smoke test — ⚠️ **holds live `SF_USERNAME`/`SF_PASSWORD`/`SF_SECURITY_TOKEN`**), dia `test-function` and dia `ai-copilot-v2` (static placeholders). All four were verdicted DELETE by `docs/history/EDGE_FUNCTION_AUDIT.md` in **2026-05** and are still deployed four months later. ✅ **Not deleting live infra inside a reconciliation was the right call** (P194: a deployment that still answers is not dead because nobody remembers it). **Operator step: delete from the Supabase dashboard — `sf-test` first, it is the one holding credentials.** ✅ **DONE 2026-09-09 (Scott, Supabase CLI; verified live from Cowork): `sf-test`, `test-function`, `ai-copilot-v2` deleted from Dialysis_DB, `docai-diag` from LCC Opps — gateway returns NOT_FOUND, both lists re-read.** ~~👤 Residue → **DRIFT1-retire-secrets**: … `supabase secrets unset`~~ **DECIDED 2026-09-09 (Scott): the secrets STAY — no planned or built capability is lost.** ⚠️ **And an honest loss to record:** `sf-test`'s body was never committed and the May audit's "source on record in this audit's git history" was false — Cowork fetched the deployed bodies of the two functions it kept but not `sf-test`'s before the delete, so that ~40-line body is gone. The *capability* (SOAP login + SOQL from an edge function, no Connected App) is rebuilt by **SF-DIRECT** below; the three other deletions were static stubs and lost nothing. | ✅ | STATUS 2026-09-07 |
| 🟡 **SF-DIRECT-b** | **Route the direct-Salesforce path through the PA gateway that already works under SSO — Scott's decision 2026-09-09: build the workaround, ask IT later with the product in hand.** The sanctioned path exists: `api/_shared/salesforce.js` ↔ PA flow **"HTTP Switch Salesforce Lookup"** (`sf-http-switch-lookup`) via `SF_LOOKUP_WEBHOOK_URL` — seven typed operations, authenticated as Scott through the connector's SSO token (C1's "the consumer already existed on another surface", again). Gaps this closes: no generic read (add a guarded read-only `soql` case to the flow — Scott builds it from the spec), edge functions cannot reach it (`_shared/salesforce-gateway.ts` + the same secret on Dialysis_DB), and `sf-ping` gets a fallback reporting `via: pa_gateway`. SOAP helper kept as the exhibit for the eventual IT conversation. Prompt: `docs/claude-code/prompts/SF-DIRECT-b-gateway.md`. ⚠️ The trigger URL carries its signature — a secret; Secure Inputs on the flow; every call runs as Scott's user. ✅ **CODE SHIPPED 2026-09-09:** flow spec written into `docs/architecture/flows/http-switch-salesforce-lookup.md` (§ "soql operation") · `supabase/functions/_shared/salesforce-gateway.ts` (`sfGatewayQuery`/`SfGatewayError`, same `ok/reason/detail` + `pickFlowMessage` shapes as `api/_shared/salesforce.js`, SELECT-only client-side guard, 20s timeout, never returns/logs the webhook URL) · `intake-salesforce?action=sf-ping` tries SOAP first, falls back to the gateway **only** on `INVALID_SSO_GATEWAY_URL`/`INVALID_LOGIN`, response gains `via`/`soap_fault_code` · `test/salesforce-gateway.test.mjs` (24 tests: request shape, all four gateway error shapes, positive control that no thrown error carries the URL or its `sig=`) + `test/intake-salesforce-sf-ping-auth.test.mjs` extended (structural: fallback reached only after SOAP, gated on exactly the two named fault codes). `grep -rn "sig=" supabase api test` → only comments/synthetic test fixtures, no literal signature. ~~**Stays 🟡 — the flow's `soql` case, the secret on Dialysis_DB, and the deploy (v25→v26) are all Scott's steps.**~~ ✅ **DONE 2026-09-09 (Scott built the `soql` case in the designer, walked step by step in Cowork; secret set; deployed):** three live defects found and fixed on the way — `sf-ping` gate compared bare codes to `sf:`-prefixed faults (`bareFaultCode()`, red-then-green test, PR merged), the flow's `empty(max_rows)` rejected the integer the helper sends (`equals(…, null)`), and the helper's 20 s abort hid a connector that takes ~49 s (60 s default, `SF_GATEWAY_TIMEOUT_MS`, PR #2205). **Two pings recorded: `via:pa_gateway ok:true open_tasks:5` at 47,716 ms and 49,341 ms** — steady state, see SF-GW-LATENCY. Export filed `private/power-automate/exports/production/2026-09-09/…__2026-09-09.zip`, registry bumped. 👤 **One residual:** Secure Inputs/Outputs is on the trigger only, not on `Execute_a_SOQL_query_1` (read from the export) — one checkbox + re-export. PA2/PA3/PA4 (outbound) are unaffected — this is read-only, and outbound stays PA-only by design. | ✅ | SF-DIRECT · Scott 2026-09-09 |
| **B6c-feed** | ✅ **DONE 2026-08-29 (B6c-dup §4)** — `sql/20260829_gov_b6cdup_retire_pse_freshness_expectation.sql`. **RETIRED, not resolved**: `is_active=false` with the reason recorded in `description`. The 45-day SLA on `property_sale_events` fired whenever nobody typed a sale for six weeks and then sat open forever — the B6a *"expectation nobody chose"*. ⚠️ **The expectation MOVED rather than vanished:** feed `sales_transactions` (the canonical spine) is already registered at 45 days and reads **10 days old**, and B6c-dup is what makes operator sales reach it — so the operator surface is now covered by the SLA that has an actual cadence. Verified: gone from `v_feed_freshness`; 2 stale feeds remain (B6a's registered dead producers); 0 open `feed_stale` alerts. ⚠️ **MECHANISM SUPERSEDED BY B6d (2026-08-29) — the DECISION was right and `is_active = false` was the wrong way to express it.** Dropping the feed off the surface strands its open alert **permanently**: `lcc_check_feed_freshness` auto-resolves only a feed that is PRESENT and not stale, so alert 5376 was left open and unresolvable by any automatic path — the *"0 open feed_stale alerts"* above was read before that alert was re-counted. B6d restores `is_active = true` with a **NULL bound + `unwatched_reason`**, so the feed keeps EMITTING (`status='unwatched'`) and the alert closes with a note saying the expectation was retired. **Retire an expectation by removing the BOUND, never the ROW.** | ✅ | B6c · B6c-dup · B6d · I4 |
| **B2** | ✅ **RE-MEASURED LIVE 2026-09-12 (Cowork) — this row IS `C2e-T2b` (§4k.1 of `connectivity-and-open-threads.md`), not a separate, unsized gap.** The 9,830/6,362/3,468 figures above were the same class of **wrong-key artifact** the `owner_needs_salesforce` postmortem already caught — re-derived from the real join chain (`properties.true_owner_id` → LCC Opps `external_identities(gov,true_owner)` → gov `asset` anchor → `lcc_property_owner`): of 9,842 live gov properties carrying a `true_owner_id`, only **163 true-owner values are unindexed at the identity layer (97.7% already ARE indexed)** — the "never reached the entity graph" premise above is false at that layer. The real bottleneck is **asset-anchor coverage**, exactly as C2e/T2b documented: **2,496 properties have no gov `asset` entity anchor at all** (resolution can't even be attempted), plus a small, not-previously-counted **40-property residual that IS anchored but was never resolved to an owner link** (distinct from the mint gap — worth its own look). T2b's own 2026-08-28 sizing (2,241/2,054, measured safe-to-run, low-value, 👤 Scott's call, no default taken) has simply grown with two weeks of normal property intake. **Recommendation: retire this row into `C2e-T2b` — that row already carries the real decision.** | ✅ | `C2e-T2b` §4k.1 · re-measured live 2026-09-12 (Cowork) |
| ⭐ **D2** | ✅ **SWEEP RUN 2026-08-28 across all three projects** — SQL published as I3's detector in [`B6c`](../audits/B6c_PROPERTY_SALE_EVENTS_2026-08-28.md) §7e. **10 genuine defects + 3 low-severity + 5 accepted false positives.** ⚠️ **Two durable refinements the sweep earned while running:** (1) **a declared FK is authoritative and Postgres already type-checks it**, so D2 need only examine *unFK'd* columns — that alone removes a whole false-positive class (`available_portfolios.portfolio_id` was flagged, but its real FK points at `sales_portfolios`, uuid→uuid, correct); (2) **every genuinely mismatched undeclared column found is 0% populated** — a column that cannot hold its value never gets one — so **populated-ness is the cheapest triage signal**, and a *populated* mismatch is nearly always an external vendor id or a uuid-as-text. ⚠️ **LCC Opps returned zero mismatches, but that is a BOUNDED zero**: it evaluated **151 of 559** `_id` columns (27%); the rest do not resolve to a name-derived target and were not examined. **Nothing repaired — named, sized, ranked** (`D2-dia`, `D2-shape`). | ✅ | I3 · B6c |
| ID2a-cleanup | ✅ **SHIPPED (PR #2337) and live-verified by Cowork 2026-09-12.** Aliases **42 → 207** (seeded from every registry name + `dba_names`, which alone resolved 142 review rows onto 14 operators nobody had aliased); review queue **1,020 → 71 open** (807 category/payer rows routed to the new `properties.operator_class`, backfilled for existing rows too — a gap CC caught itself); duplicates merged; **5 rows parented** (Fresenius/DCI subsidiaries — linked, never merged); 9 person/junk rows reclassified `kind='junk'`; orphan-registry-gap detector shipped (reads 0). `operator_id` coverage **9,307 → 9,449**; parity byte-identical on the 7 pre-existing canonicals (DaVita 4,435 · Fresenius Medical Care 3,769 · US Renal Care 465 · DCI 301 · ARA 244 · Satellite 92) with Northwest Kidney Centers 25 now resolving. The 71 left open are genuinely unregistered names — never guessed. Suite 5,966/0. | ✅ | Cowork 2026-09-12 |
| ID3a-c | ✅ **SHIPPED in the `government-lease` repo (PR #398) and live-verified by Cowork 2026-09-12.** `canonicalize_agency('Immigration & Customs Enforcement')` → **ICE** (root cause: `&` was never normalized to `and`, so ICE fell through to CBP's bare `customs` match); `Border Patrol` → **CBP**; `Dept of Homeland Security` → **DHS**. **Bigger class found and fixed:** 9 federal `Department of X` patterns (VA, DOC, DOT, ED, DOL, EPA, HHS, USDA, DOJ) were matching **state** departments of the same name — `TEXAS DEPARTMENT OF AGRICULTURE` → USDA, confirmed live; now NULL, with `US Department of Agriculture` → USDA intact, via one shared state-qualifier guard. GSA rule now **624/624** consistent (was 474/624 — Scott's own `GSA - Social Security Admin` example was the broken one). Registry **65 → 79** (NAVY, ARMY, DOC, LSC, DOL, USGS, NRC, NIH, NLRB, USAF, TREAS, ED, USSS, EEOC). FK: `properties.agency_id` **7,369 → 8,875**, `property_agencies.agency_id` **119,361 → 120,471**; a dedup bug found while backfilling. Review lane had **no retire mechanism** (1,483 permanently open) → retired 348, now 1,135 open. 35 new mutation-verified guard tests. **Deferred:** USFS/BLM/NSF regex gaps, 10 FK-vs-canonicalizer granularity judgment calls, drift-detector views built but unscheduled. | ✅ | Cowork 2026-09-12 |
| ID3a-gsa-compound | ✅ **RESOLVED by ID3a-b (2026-09-12).** `properties.agency_canonical`/`agency_full` stay GSA (the lease counterparty, unchanged); a second pair of columns `using_agency_canonical`/`using_agency_full` on properties/leases/sales_transactions now carries the occupying agency, parsed and canonicalized from the `GSA - <occupant>` remainder. Measured live: 624 properties / 106 distinct raw strings (not the ~168/944 estimate — that undercounted); 456 of them resolve a `using_agency_canonical` today, the rest use an abbreviated form (`"dept of"` not `"department of"`) the comparator doesn't yet expand — a stated gap, not silently dropped (NULL, not guessed). `property_agencies.agency_id` (the ID3a FK bridge) is untouched — this is the display-column fix only. | ✅ | ID3a-b |
| ID4 | ✅ **Baseline SHIPPED (PR #2329)** — `docs/audits/ID4_IDENTITY_INTEGRITY_BASELINE_2026-09.md`: live populations per class, the framework decision (**per-class comparators, never one shared normalizer** — proven by the broker surname collisions), and the ranked plan. ⚠️ **Two numbers corrected by Cowork 2026-09-12:** its `operators` count (14 → **67**, see ID2a-cleanup) and its I15 retraction (the 2,450/2,450 tie **is** real on eligible rows; the mechanism is a **dedup pass on 2026-07-22 16:01:22** that demoted 346 DaVita / 318 Fresenius rows, not a truncated import → B6d-cms-dedup). **👤 DECISIONS SETTLED (Scott, 2026-09-12): ranking confirmed — ID3a gov agency wiring, then ID3e county vocabulary, then ID3b owners, ID3d guarantors, ID3c brokers last; ID3c WAITS on BR1–BR5; detectors are proven on the agency class FIRST, then generalized (no framework ahead of evidence).** | ✅ | ID4 |
| **C1** | ✅ **DIAGNOSED 2026-08-27 — `docs/audits/C1_SALESFORCE_LANES_CONSUMER_OR_RETIRE_2026-08-27.md`. The answer is RETIREMENT: a working consumer has existed since June on a different surface.** The Decision Center lane **`sf_link_candidate`** holds **3,369 owner↔SF-Account candidates** (dia true_owners 382 · gov recorded_owners 1,618 · gov true_owners 1,369), **every one carrying a resolved `001…` Account id**, behind a verdict path (`api/admin.js:10764`) that **PATCHes the exact column whose NULL-ness defines both research lanes** — null-guarded (a different id raises a conflict card, never an overwrite), provenance-logged, reversible, with an active Ollama pre-rank (cron 213) and **59 human verdicts recorded**. It already covers **360 of dia’s and 1,347 of gov’s** gap subjects. The research lanes are a **capture-less second copy** of it: `completeResearch()` posts `{research_task_id}` alone and writes nothing, neither lane has a capture button (only `owner_contact_manual` and `establish_ownership_history` do — Dead-End **Class 3**), and the seeder dedupes on `status=queued` only, so a completion is re-minted (churn **4.84** tasks/subject on `property_missing_recorded_owner`). ⚠️ **Two of this row’s previous numbers were key-space artifacts and are corrected.** (1) gov “0 of 108 resolve to an entity” is TRUE but structural, not a coverage fact: the lane emits `unified_contacts.unified_id`, an id space `external_identities` has never indexed (only `gov/true_owner` and `gov/asset` exist), so minting cannot change it. Re-keyed via the owner’s property → `true_owner_id`, **111 of 114 resolve** — **but that is a different party 70 of 120 times** (SPE↔sponsor; `ARCP GSPLTNY01, LLC` → **`Nicholas Schorsch`**, `INGOLD FAMILY INVESTMENTS LLC` → **`Robert Ingold`**), the P188 trap, so the safe subset is 55 name-agreeing pairs → **2** with an SF Account. (2) **dia’s two “27”s are DIFFERENT sets, overlap 3** — 27 admitted by the value gate vs 27 whose entity carries an SF Account. ⚠️ **New defect found: the gov lane reads `unified_contacts.sf_account_id` while the only writer writes `recorded_owners.sf_account_id`** — **1,961 gov owners are already linked, 1,292 still read as a gap, only 29 agree**; **96 of the 1,675 admitted ($314.7M) are phantom work.** dia is unaffected (same column both sides). **Creating an SF Account is not buildable, before doctrine is even considered**: LCC’s whole SF surface is a read-only Power Automate proxy and Scott has no admin rights for a Connected App (`_shared/salesforce.js` says so) — a grep for `sobjects`/`/services/data/v` returns nothing — yet both lanes’ instruction text says *“Link **or create** Salesforce account”*. **P131: (a) 27 dia + 2 gov · (b) ZERO · (c) dominant** — a Salesforce id exists only in Salesforce, so a model would fabricate an 18-char id; fourth time in this arc the top-ranked LLM opportunity measured as (a)+(c). **Recommendation: automate 27, retire 945, gate 1,702, repair 1,292 — build no consumer.** See **C1a–C1e**. ✅ **All five units SHIPPED 2026-09-08 (#2152) — see C1a–C1e below and STATUS 2026-09-08.** ⚠️ **CORRECTED same day by UX-T1b's live census: C1c's retirement never ran against production.** `lcc_c1c_retire_sf_lanes()` is dry-run-default and nobody invoked it with `dry_run=false` after merge — verified live (UX-T1b, PR #2160): both `owner_needs_salesforce` (gov) and `true_owner_needs_salesforce` (dia) still read **100% `status='queued'`**, zero rows carry `outcome.reason='c1c_lane_no_consumer'`. The "retire 945" headline above is the PLAN C1c's code implements, not a completed outcome — this is the repo's own "merged is not running" doctrine, paid for on this row eleven hours after it was written. **C1a (mirror repoint), C1b (gate), C1d (27 dia fills) and C1e (ladder rung) are unaffected** — those are self-executing code paths already live; **only C1c's one-time retirement sweep still needs to be run** (an operator/cron action per its own migration runbook, not a code change) — tracked as **UX-T1b-g2**. | ✅ | diagnosed 2026-08-27, executed 2026-09-08 (C1c retirement pending — UX-T1b-g2) |
| **C4** | ✅ **DIAGNOSED 2026-08-28 — `docs/audits/C4_RANKING_LAYER_ROLE_GATE_2026-08-28.md`, canonical §4o. The LAST hop of the chain, measured for the first time.** Every gov deal-timing band (P1/P2/P3/P8) reads one CTE, `gov_owner_props`, whose only filter is **`effective_owner_role IN ('developer','user_owner')`**. It reconciles exactly: gov properties with a current owner fact + an attributes row + a lease expiring ≤24mo = **1,216**; add the role predicate = **74**; observed P1 = **74**. **Not value-gated, not cadence-gated, not opportunity-gated, and not stale** (cache verified fresh 4 min before the read). ⚠️ **`user_owner` is 0 of 66,874 live entities — half the gate has NEVER matched a row** (new Dead-End **Class 22**); `developer` is **715 (1.07%)** from a classifier keyed on `properties.developer_name` that has **drained itself — 285 rows lifetime, 2 candidates left** (working, not broken); **`unknown` is 62,554 (93.5%)** and **72% of the 5,992 resolved owners**. So **only 256 of 5,992 resolved owners (4.3%)** reach the queue at all, and **931 of its 1,267 rows (73%) are data-completion work**, not calls. ⚠️ That classifier is the **N18 view** — whose ranking N18 found was arbitrary; it sits **upstream of the entire ranked call list**, which N18 did not know. | ✅ | diagnosed 2026-08-28 |
| **BROKER1** | ✅ **SHIPPED + APPLIED LIVE 2026-09-11 (Cowork) — the shipping session could not reach production (no DB credentials); this session applied the migration, found and fixed a real bug, then ran it for real.** Migration `20261101160000_lcc_broker1_prospect_broker_assignment.sql` applied to `xengecqvemvfknjvbvrq` — its own privilege self-check (service_role EXECUTE granted, anon revoked) passed. **The first live call failed: `RETURNS TABLE(bucket text, n bigint)` makes `bucket` an implicit PL/pgSQL variable, colliding with `_b1_default.bucket` in three `FILTER (WHERE bucket = ...)` counts (`42702 column reference "bucket" is ambiguous`) — never caught because the shipping session's 13 tests never touched live Postgres.** Fixed by qualifying every reference with the temp table alias (`d.bucket`); no logic change. **Dry run confirmed real, then applied for real:** `already_manual_assignment_left_alone=52`, `defaulted_gov_to_scott=870`, `defaulted_dia_to_kelly=414`, `defaulted_catchall_to_scott=19` (1,355 prospects total in `lcc_priority_queue_resolved`). **The ROE self-signal JS pre-pass (`applyBroker1RoeSelfSignal`) could not be invoked directly — its `/api/broker1-assign-tick` route is auth-gated and this session has no `LCC_API_KEY`** — so its exact query was replicated directly in SQL instead and confirmed genuinely empty, not skipped: **0 of the 1,303 unassigned prospects carry ANY `external_identities` salesforce/account `sf_owner_name` at all**, so there was nothing for the ordering-sensitive pre-pass to write before the default sweep ran — safe to apply the sweep directly. **Nate confirmed untouched by BROKER1**: 7 pre-existing `lcc_entity_owner_override` rows naming him all carry `set_by='reconciled'` (an older deal_owner/sf_task signal, unrelated to this build) — BROKER1 never assigns him and never overwrote these. Spot-checked 8 random post-sweep rows live via `v_lcc_broker1_prospect_assignment_state` — all correctly bucketed (dia→Kelly Largent, gov→Scott Briggs). Railway `/version` confirmed the deployed app (`8716d86d406f`) matches this session's `git` HEAD, so the route itself is live even though this session couldn't authenticate to call it — running the JS pre-pass for real (harmless no-op given the confirmed-empty population, but worth closing the loop) is the one remaining operator action, not a build gap. `BROKER1-sf` (the Salesforce connect-back) remains correctly unbuilt — no write path into Salesforce exists yet. | ✅ | C4c · C7 · applied + spot-verified live 2026-09-11 |
| **C4e** | ✅ **ANSWERED 2026-08-28 by C5 — the `buyer` exclusion is a CATEGORY ERROR, not a bad label.** **578 owners typed `buyer` hold a gov property with a lease expiring inside 24 months, carrying $410.4M** — LARGER than the `unknown` half C4 focused on. The labels are correct: **Boyd Watterson (45 gov assets), Prologis, RMR Group, HC Government Realty Trust** genuinely are buyers *and* are currently the owner of a building whose lease is running out. `entities.owner_role` is a **party-level identity**; the bands ask a **per-asset question** — and the CTE has already joined `lcc_entity_portfolio_facts` on `is_current=true`, so **it is holding the per-asset fact and discarding it for the entity's global label.** ⚠️ Firing the band is NOT choosing the pitch — acquisitions vs disposition are different contacts and tones (`account-based-contact-intelligence.md`); the bucket is C4a. | ✅ | C5 §2 |
| **C5b** | ✅ **ANSWERED 2026-08-28 — and it corrects C5 §5.** The per-asset fix is better founded than widening to `unknown` but **NOT narrower on its own: all five bands, all roles = 4,506 rows / 3,622 owners, a 20× flood.** **P5 is 83% of it** (58 → 1,681) and is the weakest signal in the set. ⚠️ **`aged_props` is NOT gov-scoped** — no `source_domain` filter, so **P5 covers dia** (26 → 565); touching it is a cross-domain change and nothing in this arc has been. **P5 keeps the role gate.** P8 is 76 → 1,497. | ✅ | C5 §5b |
| **C6** | ✅ **SHIPPED 2026-08-29** — migration `20261002110000_lcc_c6_current_holding_seller_bands.sql`; evidence `docs/audits/C6_CURRENT_HOLDING_SELLER_BANDS_2026-08-29.md`. The `effective_owner_role` predicate is **gone from `gov_owner_props`**, replaced by *holds a current gov asset* (the `is_current` join already there) **AND** `EXISTS owner_contact_pivot.active_contact_entity_id IS NOT NULL`. **All four predicted deltas hit exactly: P1 74→149 · P2 32→95 · P3 61→163 · P8 76→213 = 620 rows / 497 assets / 303 owners.** P5, P0.4, P0.5, P-CONTACT, P-BUYER, P4 and all dia held (positive-controlled). **0 unreachable rows emitted.** ⏰ All 14 owners with a gov lease inside 90 days who were contactable-and-invisible now appear in P1. ⚠️ **Reachability is the pivot column, NOT `reachable_hero_qualified`** — that is a single-row aggregate for REPORTING with no per-owner membership to join; CC measured the alternative at 444 rows / 166 owners and documented why it was rejected. | ✅ | shipped 2026-08-29 |
| **C8** | ✅ **SHIPPED 2026-08-31** — migration `20260831120000` (+`is_resolved_owner`, `is_brokerage` on `v_bd_cadence_dashboard`); gate composed in `handleProspectingBrief`; guard `test/c8-prospecting-brief-gate.test.mjs`. **80 → 126 rows** (predicted 127). ⚠️ **The miss was informative: §2 sized the brokerages by reading only the EXCLUDED half — there are 4 of 311, not 3.** `Stan Johnson Co` carries `owner_role='buyer'` and was being SHOWN; the explicit guard drops it, so the delta is **+47 − 1**. **A population counted on one side of a gate is not the population.** ⚠️ P116's false positive appeared (`Clark Matthews`, bare surname) and **costs nothing** — he fails the OR arm anyway; the guard is outcome-bearing for 1 of 4. **At `limit=10`, 9 of 10 slots change** — Easterly enters at rank 2. **A REACH fix, not a count fix.** | ✅ | shipped 2026-08-31 |
| **DOC1 · 8 · 9 · 10 · 11 · 12 · 13 · 16** | ✅⛔ **RESOLVED — full detail in the canonical page, not repeated here.** DOC1 the fixed-window jam (Class 12, 3rd instance) · DOC8 the DocAI cap raise (⚠️ **which unlocks ZERO leases — the 16–30 band holds ONE document corpus-wide**) · DOC9 the counter blind to the escalating path · DOC10 the covered-fragment defect · DOC11 the merged-not-deployed gap · DOC12 the escalation closed **by the MARKER, not the cap raise** · DOC13 `retry_admitted:0` is correct under a full fresh backlog · ⛔ DOC16 refuted (the 30pp cap needs a page-1 start). **Read `document-capture-ocr-and-deeds.md` §0 for any of these.** | ✅ | document-capture-ocr-and-deeds.md §0 |
| **DOC17** | ✅ **ANSWERED 2026-09-02 — the cap is measured against the SELECTION, not the document.** Probed on a real **316-page** PDF: `individualPageSelector {pages:[31..45]}` returned **200 with pages 31–45 and 65,297 chars**, and the positive control (`fromStart:15`) also passed — **both arms passing is what makes it an answer.** ⚠️ **A 31-page selection was refused for being 31, NOT for being part of 316** — the document total never enters the arithmetic. **THE RULE: 30 pages per call contiguously from page 1 (imageless); 15 pages anywhere else.** So a 50-page window is **3 calls**, our 141-page max is **9**, and the whole backlog is **~$3.30** — **no GCS, no IAM, no confidentiality decision.** ⚠️ **Honest gap: the probe document is NOT one of the 42** (`SHAREPOINT_FETCH_URL` is a Railway var, not a Supabase secret), stated rather than glossed. | ✅ | document-capture-ocr-and-deeds.md §0l |
| **EXT2a** | ✅ **SHIPPED 2026-09-03** (PR #2098, `f...` merged). `baseFromPeriodQuote` reads the labelled base out of a schedule period's own quote and emits `plus` components as period-scoped `additional_rent`, deduped `(kind,amount)`; `resolveYear1Rent` returns the selected period so the tenant extractor can merge its components; `resolveYear1TotalRent` gained a `schedule_composition_unknown` guard (null total rather than guessing when a schedule figure's makeup is unknown). 255 now resolves **89,340 / 101,568 / `schedule_period_1`** (was 101,568/113,796 — the double count). Guard: 6 new tests (full-quote shape, no-double-count-with-top-level-duplicate, degraded bare-figure shape, composition-unknown guard, unaffected single-figure schedule, schedule-equals-base-quote), 39/39 in file, 162/162 across the whole `bov-extract`-touching test population. **Found + fixed along the way:** a literal apostrophe inside a regex character class (`[a-z0-9 /&'-]`, in `ext1-lease-rent-basis-quoted-dates.test.mjs`'s own literal-blanking regex — the exact OCR1c apostrophe-in-prose class one syntax level over) was mistaken for a string delimiter and blanked ~20 lines of real code — fixed with `\x27`; transferable to any future comment/literal-stripping guard. Record `responses/done/EXT2a-schedule-line-definition.response.md`. | ✅ | EXT2 residual risk |
| **OCR2** | ✅ **SHIPPED 2026-09-02.** The deed drain now persists `{method, ocr_tier, ocr_engine, ocr_pages, ocr_confidence, page_count, text_len}` into `extracted_data.document_text` on both domains via `<dom>_merge_document_extracted_data` — the **single owner** of writes to that column. ⚠️ **The prompt's premise was incomplete: `extracted_data` had TWO writers and `deed-parser.js` REPLACED the whole column**, so provenance written beside `deed_extraction` was destroyed on every deed and on every re-parse (measured: gov's 185 rows carry exactly those two keys and nothing else; dia carries 10 with a third, from the one call site that already merged). Both call sites now merge; the parser keeps a legacy-replace fallback so a half-applied deploy cannot lose an extraction. `ocrTiered` defaults to `true` and `false` is refused by name — the gpt-4o-direct branch is removed and `ocrPdfToText` survives at exactly ONE call site, tier 3 inside `ocrPdfToTextTiered`. ⚠️ **`revoke ... from public` left explicit `anon`/`authenticated` grants** (Supabase default privileges) — the complementary half of the documented B6d trap, caught only by `has_function_privilege`; both roles now false, service_role true, asserted live. **NO backfill** — 507 rows' tier is unknowable (154 gov extractions predate DocAI; 140 carry no date), and `unrecorded` holding at 325/182 IS the verification. Surfaces `v_gov_deed_ocr_provenance` / `v_dia_deed_ocr_provenance`; **read `provenance_written`, never the computed `ocr_tier` beside it**. Guard `test/ocr2-deed-provenance.test.mjs` (18 tests, 16/16 mutations RED; two presence-greps SURVIVED their mutation via the import line and were replaced with behavioural tests). Follow-ups **OCR2a** (re-parse path — deliberately none: no extraction, no tier) and **OCR2b** (the `needs_ocr` refusal reason is still discarded). Writeup: `docs/audits/OCR2_DEED_OCR_PROVENANCE_2026-09-02.md`. | ✅ | ai-and-ocr-cost-strategy.md §0 · OCR2 audit |
| **C13b** | ✅ **SHIPPED 2026-09-01 — `v_lcc_entity_roles` is live.** One row per (entity, role) with its evidence arm, dates and pacing. **10,655 entities carry ≥1 role (was 4,132); 946 carry ≥2 (was structurally impossible); 0 duplicate pairs.** A VIEW over the existing spine, never a stamped column. **P0.4 555 → 555, deal bands 621 → 621, no consumer repointed, nothing writes.** Migration `20261005120000`; guard `test/c13b-entity-roles-multilabel.test.mjs` (19/19 mutations RED). ⚠️ **THREE OF ITS OWN INPUTS WERE WRONG AND WERE CORRECTED BY MEASUREMENT:** `repeat_buyer` **3,258 → 401** (3,258 counts EDGES; the 2,857 difference is single-asset SPEs whose one conveyance was observed several times — and the `(asset,date)` middle key was measured and rejected at 735); **a manual override REPLACES the column an arm reads** — 119 entities carry `owner_role='developer'` AND an override of `buyer`, so `developer` is **838 → 718**; and **`one_off_owner` rests on `entities.entity_type`, which is wrong in both directions** (C13c). ⚠️ Knock-on: the design's *2,627 repeat buyers dormant 5+ years* is **219**, and `investor_owner`+`repeat_buyer` is **167**, not 772 — both were computed on the edge count. Writeup `docs/audits/C13b_OWNER_ROLE_MULTILABEL_2026-09-01.md`. | ✅ | owner-role-classification.md §7 |
| **C13c** | ✅ **SHIPPED 2026-09-01 — `one_off_owner` carries its CONFIDENCE.** **142 = 13 `_sf_corroborated` + 129 `_unverified`, the COUNT deliberately unchanged**; **21 named institutional rows routed to `v_lcc_entity_role_ambiguity.entity_type_contradicted_by_named_review`** off the `lcc_entity_role_confirmation` ledger, never a name stoplist in the classifier. Every other arm, `v_lcc_user_owner_candidates` (15), multi-role (954) and **P0.4 (555)** unmoved. Migration `20261006120000`; guard `test/c13c-one-off-owner-confidence.test.mjs` (9 tests, **21/21 mutations RED**). ⚠️ **C13b's "no non-lexical corroboration exists" was THREE ABSENCES, NOT A SEARCH** — the fourth column, `salesforce/Contact`, answers on 13 of 142 and **ZERO institutional names carry one** (the positive control is the half that matters). ⚠️ **The routed set is 21, not ~15**: the prompt's list is drawn from the 28 that FAIL `lcc_looks_like_person`, so it structurally cannot contain `Gates Hudson` ($19.6M) or `Metropolitan Life Insurance` ($11.8M) — the arm's #2 and #3 by rent, which PASS the name test. ⚠️ **The uncorroborated 129 read ~80% genuine on a deterministic 10-row sample (8/1/1)** — nobody had measured it, and the 28 failures are not a random sample of the 129. Writeup `docs/audits/C13c_ONE_OFF_OWNER_CONFIDENCE_2026-09-01.md`. | ✅ | owner-role-classification.md §9 |
| ✅ **C13g-costar-stoplist** | **✅ SHIPPED 2026-09-10 — the "never reads the scanner's own verdict back" framing this row started with was WRONG; traced and fixed the real gap.** `contactEntityType()` DOES check `contact.type` first, and CoStar's `_forsale-contacts-parse.js::looksLikePerson()` DOES always stamp an explicit `type` — so its verdict wins outright, `hasFirmSuffix()` was never consulted on this path, and the two lists were never really "two independently drifting copies": `hasFirmSuffix()` already covers almost the entire extension stoplist (Trust/Holdings/Properties/Capital/Realty/Ventures/Management/Company), missing only 4 brokerage brand names (newmark/cbre/jll/colliers) the extension has. **The real defect ran the OTHER direction: the extension's list is missing terms `hasFirmSuffix()` has** — Bancorp, Investments, Development/Developers, Fund, Ptnrs, Cos, Property (singular), Enterprises, Mgmt-abbrev — so a name like "Sentinel Bancorp" or "Meridian Investments" trips the backend's guard but not the extension's, and the extension stamps `type:'person'` on it, which — before this fix — was trusted verbatim and would mint the firm as a person entity. Fix (case **(a)**, but inverted from the row's original framing): `contactEntityType()` now treats an explicit `type:'person'` as a FLOOR, not an absolute — `hasFirmSuffix(name)` still overrides it to `'organization'`, one-way only (an explicit `'organization'`/`'entity'` is never second-guessed, matching the P158a discipline against guessing an org into a person on a heuristic). No second stoplist created; no extension code touched (same precedent as RCA in C13g). Guard: `test/c13g-contact-entity-type.test.mjs` (11 tests, all pass) — added `Sentinel Bancorp`/`Meridian Investments`/`Ashford Development` cases and updated the "explicit type wins" assertion, which the old code would have failed (it asserted `ACME LLC` + `type:'person'` → `'person'`, the exact bug). Forward-mint only, same as C13g — existing mistyped population is `entity_type_review` lane material, not bulk-retyped here. owner-role-classification.md §9i (corrects §9h's wrong "never reads it back" claim). | ✅ | C13g · owner-role-classification.md §9i · test/c13g-contact-entity-type.test.mjs |
| **C16** | ✅ **RESOLVED 2026-09-01 by C13b — shipped as the VIEW `v_lcc_entity_roles`.** ⚠️ **This row previously appeared TWICE with contradictory states** (one ✅ resolved, one 🟢 *"do NOT invent a multi-label table; extend `entities.developer_flag_sources`"*). **Reconciled 2026-09-02:** the second was written before C13b and is superseded — **no table was invented and `developer_flag_sources` was not extended;** the roles are a derived view over the existing spine, re-computed on read, never stamped. **The guidance that still stands: do not add a second cross-DB roll-up — `lcc_entity_portfolio_facts` IS the roll-up.** | ✅ | owner-role-classification.md §7 |
| **C4f** | ✅ **ANSWERED 2026-08-31 — P0.4 needs NO gate, and both proposed fixes were refuted by measurement** (`owner-role-classification.md` §4). **P0.4's 555 rows: 371 (67%) hold no current asset, 469 (85%) have NO KNOWN RENT, and ZERO are contactable.** ⚠️ **A value floor would gate on ignorance, not value** (85% unpriced — the P180 NULL-is-not-zero failure), so **C12's option B is refuted by its own population.** ⚠️ **And C6's reachability precondition is ALSO wrong here — it would take P0.4 to 0 rows.** P0.4 is a **RESEARCH** band (you resolve ownership control by reading deeds and SOS filings, not by calling), so reachability is the right precondition for a call and the wrong one for research; copying it across would delete 555 rows of legitimate work. **The real problem is two kinds of work under one label:** the 555 are research; C4a's 2,949 newcomers are **BD ACTIVATION** — we already know who owns it, nobody has started. **An entity C4a has just classified has had its ownership resolved, so it should never enter a band asking to resolve ownership.** **Route the newcomers to a BD-activation band (P0.5's shape) — 290 reachable today — and leave P0.4 at 555.** The '6× flood' was an artifact of routing to the wrong band. 🔴 **Noted separately, not a defect this creates: P0.4 does upstream research whose output nobody consumes as a call (0 of 555 contactable)** — a Consumption-Layer question of its own. | ✅ | owner-role-classification.md §4 |
| **A1** | ~~Split `establish_ownership_history` into its FOUR real actions.~~ **DONE 2026-08-27** — `v_lcc_ownership_history_lane_split` (one row per open task, LEFT JOIN to its draft, `action` ∈ agrees/mismatch/no_records/all_guarded) + `v_lcc_ownership_history_lane_actions` (chip rollup) + `human_actionable_tasks` appended to `v_lcc_research_lane_summary`. Migration `20260827090000`, applied live; P180 view equivalence-gated at **0 rows differing both directions**. Measured from the STRUCTURED booleans: **agrees 380 / mismatch 73 / no_records 74 / all_guarded 18**, links 450/120/0/0, **0 tasks without a draft** (545/545, no orphans, no dupes). Badge now reads **91**, not 545. Server-side `lane_action` filter in BOTH research branches (v1 + v2 — `V2_MAP` reroutes the endpoint when `queue_v2_enabled` flips, and a filter implemented once would silently unfilter). Guard `test/ownership-lane-split.test.mjs` (15 tests, mutation-verified red on prose-classification, on an INNER JOIN, and on a v2 branch missing the filter). Writeup: `docs/audits/A1_OWNERSHIP_LANE_SPLIT_2026-08-27.md`. | ✅ | **the split is NOT the verification** — this lane has still never completed a task; A2–A4b are what move that |
| **A2** | ~~Auto-apply the 380 "agrees" chains.~~ **DONE 2026-08-27 — and the lane has COMPLETED A TASK FOR THE FIRST TIME: `establish_ownership_history` completed **0 → 288**, open 545 → 257.** Migration `20260827130000` (applied live) + cron **244** (`lcc-a2-ownership-chain-apply`, 06:49 UTC, calling the SQL function directly so no Railway deploy is on the critical path). **304 historical facts written** across 280 owners / $579.9M; `agrees` 380 → 92; 5 current-owner acquisition dates filled; **0 of 304 facts read `is_current`**; `v_lcc_portfolio_ownership_conflict` 0 before and after. Reversible by batch tag (`lcc_a2_unapply_ownership_chains`) and the round trip was **run on real data before the real run** — a reversal path never executed is a claim, not a capability (P195). **Identity is `lcc_ownership_chain_name_key` (lower-then-strip, unambiguous-only, through `lcc_entity_survivor`) — `lcc_owner_strict_core` was TRIED and REJECTED here:** it drops 1-char tokens and sorts, so `BAMMF (8) LLC == BAMMF (3) LLC` and `F R M ASSOCIATES == G B A Associates`, and it matched 393 of 396 grantors, an implausibly clean number that is a bug signal (P182). **Found on the way: `r9_chain_connect` (cron 104) mints a prior-owner entity per chain name and attaches it to NOTHING** — 291 of the 331 resolved grantors are its output, retirable by the gov feeder's own predicate; A2 is the missing consumer. **Seed predicate closed and verified: 0 of the 288 completed properties can be re-seeded into this lane** (the FACT flips `owner_links` to ≥2, so the worklist suggests `trace_ownership_to_developer` instead — 284 moved there, a LIVE lane with 40 lifetime completions). 128 links stay blocked and named — **54 `ambiguous_entity` (LCC duplicate entities: `Duke Realty Limited Partnership` / `DUKE REALTY LIMITED PARTNERSHIP`; **48** of the 92 open tasks are blocked by this alone ($210.6M), so a P195-style merge unblocks them and the next nightly run applies them)**, 28 `repeat_transfer_unrepresentable`, 26 `placeholder`, 20 `no_entity`. **The apply is ALL-OR-NOTHING per task** — writing one link of a chain flips `owner_links` to ≥2 and R60 Sweep A would then close the still-open task as `skipped`, taking its unapplied links out of every surface permanently (measured: 17 partial / 19 would-be-swept before the fix, **0 / 0** after). Guard `test/ownership-chain-apply.test.mjs` (22 tests, mutation-verified red on **eleven** breakages). Writeup: `docs/audits/A2_OWNERSHIP_CHAIN_APPLY_2026-08-27.md`. | ✅ | **the lane's first-ever completion** — and the 92 residue is now a named, ranked worklist rather than an undifferentiated queue |
| **A3** | ✅ **SHIPPED 2026-08-27** — migration `20260827180000`, writeup `docs/audits/A3_OWNERSHIP_MISMATCH_SPONSOR_FAMILY_2026-08-27.md`. Re-measured **74 chains / 46 owners / $403.0M** (A2 drained `agrees` 380→90 in between). Split: **sponsor_family_candidate 32 chains → 12 DECISIONS** (Boyd Watterson 20:1), `unexplained` 31 / 27 owners / $344.6M, `name_variant` 11. ⚠️ **The prescribed key was measured and rejected** — a bare token is unbounded (`east` 226 entities, `boyd` 129) and `sponsor_token` as a PK cannot carry `madison` (two owner entities) or `egp` (Easterly *and* EastGroup), so the confirm registry `lcc_ownership_sponsor_family` is keyed **(entity, token)**; the DETECTOR is shared (P196's guards extracted to `lcc_name_reads_as_street`/`lcc_name_has_spe_marker`, 0 of 696 Tier 0 rows changed). ⚠️ **P196's SPE-marker arm drops 24 of 27 genuine rows here** (a GSA SPE is named for its city, not "Propco") — not applied, predicate not weakened. Contact confirms resolve **0 of 74** and ride the card as evidence only (P188). **`mismatch` stays 74 until Scott confirms**; positive control proved 74→54 / sponsor_spe 0→20 / human_actionable 92→72 with `agrees`/`no_records`/`all_guarded` unmoved. **Residue sized, surface NOT built.** Follow-on **A3b** (named, not built): teach A2 to consume `sponsor_spe`. | ✅ | shipped 2026-08-27 |
| **A4 + A4b** | ✅ **SHIPPED 2026-08-27** — migration `20260827200000`, cron **245** (06:51, after the 06:45 drafter and the 06:49 A2 apply), writeup `docs/audits/A4_OWNERSHIP_LANE_RETIRE_AND_ADJUDICATE_2026-08-27.md`. **`no_records` 74 → 0; open 256 → 182**; `agrees` 90 / `mismatch` 74 / `all_guarded` 18 all unchanged. ⚠️ **The brief's baseline was stale** (it said agrees 92 / mismatch 73 / open 257) — re-measured before anything ran. **A4b refuted the brief's hypothesis: ZERO of the 27 rejected transitions is an oscillating pair**, so the 18 are NOT retired — see the A4b row. | ✅ | shipped 2026-08-27 |
| **A4-detail** | ✅ **DONE 2026-08-27.** `lcc_a4_retire_no_records()` (dry-run default, batch-tagged, reversible via `lcc_a4_unretire`), ledger `lcc_a4_retire_log`, live population in `v_lcc_a4_retired_watch` (retired MINUS reopened/unretired — never count the ledger). ⚠️ **`status='skipped'` alone is NOT terminal to cron 144** — it excludes only `skipped` + `outcome->>'terminal'='true'`, so a bare skip is re-minted at 05:10 (P176). Proven at full scale: all **74 are still in `v_ownership_chain_worklist`** (i.e. all 74 *would* be re-minted) and with the flag stamped **0 would be**. ⚠️ **"No records" is the drafter's verdict, not "nothing recorded"** — the 74 carry **84 raw `gov.ownership_history` rows**, all dropped by the view's base filter; **83 of 84 have no prior owner** (the lane's whole deliverable), hence the reason `a4_no_usable_transition_on_file`. The **1 exception is named**: property **14280**, `SUFFOLK VA III FGF, LLC → Boyd Watterson`, county deed, blocked only by a NULL `transfer_date` → `v_lcc_a4_undated_prior_owner_watch`. Re-open (`lcc_a4_reopen_tasks`) is the P121 inverse and its sensor is the DRAFTER — LCC Opps holds **no mirror of gov.ownership_history** (checked: neither chain view carries a transition count), so the tick is the only possible eye. Round trip proven on real data + a self-rolled-back synthetic gov transition (before 0 → after 1, 0 residue). | ✅ | ⚠️ the re-open pass ships on the **next Railway redeploy**; the retire is live now |
| **A4b** | ✅ **MEASURED 2026-08-27 — verdict: the guards are NOT all correct, so the 18 are NOT retired.** ⚠️ **The brief's leading hypothesis is refuted — ZERO oscillating pairs.** Across all 27 rejected transitions: `prior_owner_unclean` 15 rows / 8 tasks / $23.2M, `new_owner_unclean` 8 / 6 / $4.7M, `self_transition` 3 / 3 / $4.9M, `name_variant` 1 / 1 / $0.7M. Computed per name (not eyeballed), **two arms of the gov `*_is_clean` predicate misfire on street-numbered SPEs**: `\m[0-9]{5}\M` rejects any SPE named after a street number ≥ 10000 (`EGP 17101 BROOMFIELD LLC`, `DE 10990 Wilshire, LLC`, `22690 CACTUS, LLC`); and `^[0-9]+\s` + no recognised legal form rejects 5 more whose legal form is simply **dotted or spelled out** (`830 FIRST STREET L.L.C.`, `1531 UTAH AVENUE SOUTH LIMITED PARTNERSHIP` — `\mpartners\M` cannot match `PARTNERSHIP`). **The digit test is not the defect; the legal-form allowlist is.** Pointed at a known positive (P182): the junk the arm correctly catches has **no legal form** (`Houston, Harris County, Texas 77007`). The other 4 rejections are CORRECT (`Unknown`, a Colliers-polluted name, a CMBS trust, 3 punctuation self-transitions). See **A4c** for the fix. | ✅ | measurement only — no guard changed |
| **A5** | ✅ **DIAGNOSED 2026-08-27 — `docs/audits/A5_TRUE_OWNER_SALESFORCE_STALL_2026-08-27.md`. The premise was wrong: the lane never stalled, because it was never work.** All **596** "completions" are the `gap_resolved` auto-close in `handleGenerateResearchTasks`, and **170 of 183 sampled (93%) still have `salesforce_id IS NULL`** — falsely closed. **`815 open` is `1000 − 185`**: the generator reads a **29,643-row** feed through a call PostgREST caps at **1,000 rows**, and the gap arm is `20 AS priority` — a hard-coded literal with no tiebreak. **5,509 of the 6,324 real gaps have never had a task at all.** P131 category: **(a) 293 on-box and structured** + **(c) ~6,031 not on-box** + **(b) ZERO** — no LLM, there is no corpus to read. Follow-ups: **A5a → A5c → A5d → A5e**, in that order. | ✅ | diagnosed 2026-08-27 |
| AC1b | ✅ **Closed 2026-09-10 (`build/aci-phase0`).** `v_lcc_top_seller_prospects` and `v_lcc_owner_contact_decidability` now call `lcc_owner_name_is_not_prospected` in place of `lcc_owner_name_is_public_body` (predicate-only, column lists unchanged). Measured before/after: top-seller-prospects university rows 14→1 (the 1 residual is a credit union, correctly not a university); decidability university rows unblocked-as-public-body 3→1 (the residual is a pre-existing `lcc_owner_name_is_university` regex gap on "(The)" suffixes, named not patched, out of scope). Migration `20261010120000_lcc_ac1b_*.sql`; guard `test/ac1b-university-scope.test.mjs` (4 tests, mutation-verified). | ✅ | `STATUS.md` 2026-09-10 |
| AC7 | ✅ **Closed 2026-09-10 (`build/aci-phase0`).** Merged live: `d6b0d27e-…` (37 edges, already the Easterly pivot's `active_contact_entity_id`) survives, `537ecdd2-…` (1 edge, no pivot reference) merged into it via the existing reversible `lcc_merge_entity`. `lcc_entity_merge_log` id 170; `v_lcc_entity_merge_reversibility.reversible=true`. Reverse: `select lcc_unmerge_entity('537ecdd2-c0ac-4ded-b407-78602e42a652')`. | ✅ | `STATUS.md` 2026-09-10 |
| AC10 | ✅ **Closed 2026-09-10 (`build/aci-phase0`).** Re-measured before building (the Aug 11/$240.5M figure was stale): 251 owners / $329,379,804.64 suppressed. Built `v_lcc_ac10_promote_candidates` (ranks the winning linked-person candidate per owner, mirroring `owner-reachable-via.js` ordering) + `lcc_promote_linked_owner_contacts(dry_run,limit,batch_tag)` (fill-blanks, ledger-before-write) + reversal `lcc_ac10_unpromote(batch_tag)` + ledger `lcc_ac10_promote_log`. **Forward-running cron `lcc-ac10-promote-linked-contacts` at 06:12 UTC daily** (not a one-shot). Real run applied 249 of 251 (2 have no candidate surviving the junk/brokerage guards, correctly left unpromoted); `v_lcc_ac10_promote_candidates` 251→0. Reversibility proven live in a rolled-back transaction. Migration `20261010140000_lcc_ac10_*.sql`; guard `test/ac10-promote-linked-owner-contacts.test.mjs` (12 tests, mutation-verified on 3 independent mutations). | ✅ | `STATUS.md` 2026-09-10 · `account-based-contact-intelligence.md` §7b |
| PR-scanner-1 | ✅ **Closed 2026-09-10 (`claude/pr-scanner-writeback-wiring`).** Assessor scan → `parcel_records`/`tax_records` and recorder scan → `deed_records`, both fill-blanks/provenance-tagged/reversible via `api/_shared/public-records-writeback.js`, source-tagged `assessor_sidebar_manual`/`recorder_sidebar_manual` (distinct from `costar_sidebar` and the gpt-4o leg — neither touched). New route `POST /api/public-records-capture`; sidepanel UI `loadPublicRecordPropertyView`. Guard `test/pr-scanner-writeback.test.mjs` (12 tests, all passing, behavioural against injected `deps`). | ✅ | `public-records-source-lane.md` §7a |
| PR-scanner-2 | ✅ **Closed 2026-09-10 (`claude/pr-scanner-writeback-wiring`).** SOS scan (incl. CA bizfile) → `llc_member`/`llc_manager` `entity_relationships` edges (`applySosEntityCapture`), the exact edge type AC11 above says is needed first. Officers/registered-agent minted/resolved through `ensureEntityLink`; the residential-vs-agent-service classifier from `address-reverse.js` gates whether an address is ever recorded as a person's residence (positively tested both directions — a CSC/registered-agent address never becomes a residence; a real street address does). `saveOrgBtn`'s no-active-worklist-target path now uses this instead of the old bare `/api/entities` create. | ✅ | `public-records-source-lane.md` §7a |
| PR-scanner-3 | ✅ **SHIPPED 2026-09-12 — `county_records_needed`, the SIXTH action on `v_lcc_ownership_history_lane_split`.** A reclassification of `mismatch`/`all_guarded` (never `agrees`/`sponsor_spe`/`no_records`) when the gov property has no trustworthy `parcel_records`/`tax_records`/`deed_records` on file (`costar_sidebar` parcel/tax, or any deed not tagged `ai_recall_gpt` — the live model-leg tag; the spec's `ai_gpt4o_presumed` does not exist as a literal). Cross-database join solved via a small LCC-Opps mirror table (`lcc_gov_property_record_coverage`) synced by `api/_shared/gov-property-record-coverage.js`; the view's SQL CASE stays the single owner of the classification, the mirror only ever answers "do we have a record" (`IS FALSE`, never `= false`, so an unsynced row is never guessed into the reclassification). Reuses B1's existing human floor unchanged. **Re-measured before shipping (unchanged from the prior measurement): 27 of 68 human_actionable tasks (40%), 126 of 254 fleet-wide (49.6%).** Item (3) closed: predicted-vs-actual `open_tasks` delta was **exact** (mismatch 192→101, all_guarded 62→27, county_records_needed 0→126, human_actionable held at 68 split 37/4/27). Sidepanel button wired (`researchOpenCountyPortal`, `ops.js`) to PR-scanner-5's previously-unwired `/api/recorder-portal` route — its first live consumer. Guards: `test/ownership-lane-split.test.mjs` + `test/gov-property-record-coverage.test.mjs`; full suite 6,022 pass / 0 fail. **Not done (operator step): the coverage mirror sync is not yet on a cron** — `syncGovPropertyRecordCoverageForOwnershipLane()` exists, safe to re-run, seeded once against today's 254-property population. | ✅ | `research-workbench.md` §7d · `ownership-history-lane.md` §5 · `supabase/migrations/20260912150000_lcc_pr_scanner3_county_records_needed_action.sql` |
| **J13-preflight** | ✅ **DONE 2026-09-09.** Enumerated live callers of the retired Vercel host via Supabase `edge_logs` on LCC Opps (24h window, `2026-09-08T01:22Z → 2026-09-09T01:21Z` — `query_logs` caps at 24h/call, this is one call, NOT 14 days), plus sanity checks on Dialysis_DB and government. Found a live caller: ~~an AWS-ephemeral-pool IP (`18.208.213.136`) **POSTed** an upsert to `briefing_intel_snapshot` at 10:00:27 UTC (HTTP 201)~~ ⚠️ **struck (Cowork 2026-09-09): that POST's `user_agent` is `SupabaseEdgeRuntime` — the `briefing-intel-snapshot` edge cron (V4), not Vercel; the frozen build is the 12:30:00 UTC `node` burst with 3 × 400 on schema-drifted views, seen Thu 09-04 / Mon 09-07 / Tue 09-08, not Fri/Sat, read-only** — ~~18 minutes ahead of Railway's own 10:18 cron-240 read+PATCH of the same row, a~~nd a second ephemeral IP (`18.209.20.81`) fired an 18-call composite "render the briefing/My Work dashboard" burst at 12:30:01 UTC — matching the desktop task pattern named in `docs/ops-logs/daily-briefing-cache-2026-09-01.md`. Confirmed the Cowork 21:43–21:44 UTC read-probe did **not** persist a snapshot. Confirmed `/api/intake?_route=mobile-share` is **not mounted** in `server.js` — the iPhone Shortcut has no live Railway target to repoint to yet, a blocker, not a routine repoint. Confirmed `extension/background.js::pickIntakeHost()` is Railway-first (shipped manifest `1.0.52`); confirmed `docs/os/FLOW-REGISTRY.yaml` + the sampled 2026-08-11 PA export zips carry 0 Vercel hits; named (not identified) the pre-existing "3 flows outside registry and `retired_flows`" boundary. Full measurement + every query run: `docs/audits/J13_TEARDOWN_PREFLIGHT_2026-09-09.md`. Runbook: `docs/os/RUNBOOK_vercel_teardown.md`. | ✅ | J13 · edge_logs 2026-09-09 |
| ✅ **OWN-T0j** | **SHIPPED + LIVE + VERIFIED END-TO-END 2026-09-11.** Cross-database Node classifier splitting `OWN-T0a`'s gov disagreement population into `sponsor_family_confirmed` (explained by an already-confirmed `OWN-T0e` sponsor family) vs a genuine `unclassified_rival` residual — reads gov (`scknotsqkcheojiaewwh`) and LCC Opps (`xengecqvemvfknjvbvrq`) directly since no SQL join is possible across the two projects. `api/_shared/ownt0j-sponsor-classifier.js` (pure functions, 11 tests) + `api/_handlers/ownt0j-sponsor-classify-tick.js` (GET dry-run / POST writes) + cache table `lcc_ownt0j_sponsor_disagreement_cache` + reporting view `v_lcc_ownt0j_sponsor_disagreement_report` + cron `lcc-ownt0j-sponsor-classify-refresh` (`39 */4 * * *`). Does not touch gov's `properties.true_owner_id`/`ownership_history` (that shape was already tried and refused — `OWN-T0`/`RO2`); routes a genuinely unclassified pair to `OWN-T0e`'s existing confirm lane rather than building a second mechanism. **Two real production bugs found and fixed post-ship, both in the one part of this feature with no test coverage (the classifier's own 11 tests only cover pure functions, none exercise the handler's HTTP calls):** (1) the `true_owners` fetch batched up to 1,000 UUIDs into one `in.(...)` filter (~39KB URL), which Railway's edge rejected outright — fixed by paging `true_owners` unfiltered instead; (2) the POST auth check called `authenticate(req)` with the wrong signature (missing `await`, wrong return-shape assumption), so every POST 401'd regardless of the key — fixed to match the real `authenticate(req, res)` convention every other handler uses. **Verified live, in full, after both fixes deployed:** triggered the real POST via `lcc_cron_post` (the exact call the cron makes) — `200`, `written: 2462` — and the cache table now holds exactly `sponsor_family_confirmed=482` / `unclassified_rival=1,980`, byte-for-byte matching every prior independent measurement; the reporting view reads them back correctly. ⚠️ **Precision caveat, not fixed:** the confirmed token `gov` (2 sponsor entities, 3 characters) will match any true_owner name containing "gov" anywhere — a real generic-token risk distinct from Boyd/UIRC/NGP/Highwoods, worth a look before curating more short-token confirms. Full narrative across all three passes in STATUS.md 2026-09-11. | ✅ | OWN-T0a · OWN-T0e · OWN-T0b |
| ✅ **OWN-T0e-b** | **BUILT 2026-09-09 (Cowork, `build/own-t0e-b-same-party-merge`) — design doc §7.** `same_party` + `payload.merge_now` merges the operator-named duplicate INTO the sponsor via `lcc_merge_entity` (reversible; ONE loser per verdict; guards read live: members, distinct, not tombstoned, **same recorded `entity_type`**). Card: duplicate picker + "Merge duplicate now" behind a confirm. 13/13 new mutations RED; rolled-back positive control on InCommercial (merge log 145→146→145, reversible=true). ⚠️ **Reading the 5 pairs: 2 clean merges (Incommercial, Four Springs), 1 REFUSED by the type guard until retyped (`Gardner-Tanenbaum` is typed `person` — C13g), 1 NOT a duplicate (Truist Bank ↔ Truist Financial = parent/subsidiary), 1 wrong card (`NGP Group` belongs to NGP Capital's card).** JS ships on the next Railway redeploy. **2026-09-09: the type guard's refusal has an answer — C13g-min's `lcc_retype_entity` is live (2 cards blocked: Gardner-Tanenbaum, MassMutual Life); the lane to ask it is `C13g-min-lane` (✅ live 2026-09-09, deploy `3cd0e782`). ⚠️ Gardner retype+merge clears 4 of 14, not 14 — 10 carry a third claimant (its RTD/TEP SPEs).** **Original:** `same_party` forwards to `merge_duplicate_entities`, which cannot show 5 of the 13 duplicate-suspect pairs (no member on `v_lcc_merge_candidates`). | ✅ | OWN-T0e · C13g |
| ✅ **OWN-T0e-c** | **A group can need BOTH verdicts and the card offers one.** NGP Capital / `ngp` ($43.2M) carries 27 real SPEs and 2 duplicate entities of the sponsor (`NGP Group`, `National Government Properties (NGP)`); `confirm_family` is right for 27 and leaves 2 classified as family. Options: per-member checkboxes on the card (confirm family for these, merge these), or run OWN-T0e-b first on the duplicates so the family confirm follows clean. Read the 13 `duplicate_entity_suspect` groups before choosing. Design §6. **MEASURED after the live `ngp` confirm (2026-09-09, design §8): the residue is 2 properties (1 each on `NGP Group` / `National Government Properties (NGP)`), not 30 — a `same_party` from the `NGP Group` card clears the same 2. Small; sequence after C13g.** ⚠️ **2026-09-09, tried live: it does NOT — `same_party` on the NGP Group card routes to the merge lane, which has no card for `NGP Group` (P189 NULL normalizer), and `merge_now` there can only name an SPE. A card whose SPONSOR is the duplicate needs a "merge this sponsor into <X>" affordance; until then the fix is a direct reversible `lcc_merge_entity` call on the recorded verdict.** ✅ **Done 2026-09-09 (merge log 151, reversible): NGP Group → NGP Capital; residue is now ONE property, `National Government Properties (NGP)`, which has had no verdict.** ✅ **2026-09-10 — the affordance itself is BUILT.** Reused the existing cache signal (`spe_ids` + `spe_props_max >= 2`, no new detector) via `findSponsorDuplicateTarget`/`annotateSponsorDuplicates` (`sponsor-family-planner.js`), a fifth verdict `merge_into_sponsor` (loser = this card's own sponsor, winner = the recognised target, re-derived LIVE at verdict time via a `spe_ids=cs.{...}` cache filter, never client-supplied — P188), the same `lcc_merge_entity` writer/refresh/reversal as OWN-T0e-b. Card button in `dc-lanes.js`. Guard 23 tests, spot-mutation-verified RED on 3 new assertions. ⚠️ **No live instance exists today to exercise it end-to-end** — the one historical case was already merged by hand; a live query of the cache for a sponsor_id that is ALSO listed as a duplicate-suspect spe_id elsewhere returns 0 rows. Built and unit-tested, not yet fired in production. | ✅ | OWN-T0e-b |
| ✅ **DOCMAP2** | *(row filed by the UX-T1c window 2026-09-08 while the Cowork reconcile was stranded in an unapplied patch — kept verbatim, three claims struck in place; the verified accounting is the row below.)* **PARTIALLY EXECUTED 2026-09-08** (`docs/claude-code/responses/done/DOCMAP2-retired-dependency-sweep.response.md`, commit `9913db76`). Done: Unit 1 for the `Vercel` term — 59 files, **2 real defects fixed** (`docs/architecture/flows/rcm-power-automate.md` and `loopnet-power-automate.md` stated the retired `life-command-center-nine.vercel.app` host as the LIVE Power Automate POST endpoint; bannered in place, pointing at J13 + `infrastructure-topology.md`; ~~the third J13 file, `lcc-personal-calendar-sync.md`, was already clean~~ — ⚠️ Cowork 2026-09-08: it was **never dirty**; `git log -S` finds no commit ever holding the host, and its endpoint is the Dialysis `ai-copilot` edge fn (ACTIVE v77), not Railway); `vercel.json` 17 hits all correctly historical. Unit 2: `docs/audits/README.md` shipped (cite an audit for its mechanism, never its live number). Unit 3: 9 repo-root `.md` classified, **0 moved** (`WRITE_SURFACE_POLICY.md` is canon-bound by path + tested; `BRIGGS-WRITING-VOICE.md` / `SALESFORCE_LCC_INGESTION_PLAN.md` are path-referenced from live docs/code — flag, don't move blind; the two `SPEC_BOV_Lease_Extractor_Unit4*.md` are self-labelled BUILT → `docs/history/` candidates). **NOT REACHED (the higher-yield half):** Unit 1b deep-read — zero files deep-read, candidate list never enumerated; 5 of 7 terms (`SOS-direct`, `CONTACTS_HUB`, `owner-contact-websearch`, `GOV_STATE_SIGNALS`, `queue_v2_enabled`, `exec_sql`) got a file count only — note `SOS-direct` must be re-scoped to "claims ENABLED while `W9_1_SOS_DIRECT` is off", it is blocked not retired; `docs/setup/` 24, `canon/` 22, `copilot/` 6, `data-quality/` 2, `resolver/` 3 never opened; ~~`docs/architecture/` grew 181 → 232~~ (⚠️ Cowork: it was 232 at DOCMAP1's own commit — 181 is `-maxdepth 1`; the 51 are subdirs) and the ~51 ~~new~~ subdirectory files are unclassified by either pass → ✅ classified by DOCMAP3 (47 rows + 4 added in the reconcile); no orphan-audit sweep over the ~~108~~ 110 audits; `SPEC_forsale_om_and_webpage_ingest.md` and `SPEC_sos_direct_scraper.md` unclassified. J13 remains open on its own terms (the flow docs now WARN, the endpoint text is still the dead one). | ✅ | J13, DOCMAP1 |
| ✅ **DOCMAP2** | **SHIPPED 2026-09-08 (PR #2173) and RECONCILED the same day (Cowork) — five claims corrected in place in the response.** Verified reproducing at base `c69ca680`: scope 855 · `Vercel` 59 · `vercel.json` 17 (0 defects, a RESULT) · `SOS-direct` 18 · `CONTACTS_HUB` 13 · `owner-contact-websearch` 6 · `GOV_STATE_SIGNALS` 6 · `queue_v2_enabled` 7 · `exec_sql` 5 · `docs/architecture/` 232 · root `.md` 9 classified / 0 moved (each reason re-read and holds). **Corrected:** `docs/audits/` is **110** not 108; **"58 of 59 correctly framed" was 57 unread files recorded as clean** — re-keyed as 23 hostname hits all read → **12 defects** (2 fixed by the PR, 10 bannered in the reconcile — the six `flows/` misses had the host only inside a lowercase URL and a case-sensitive `Vercel` grep skipped them) · 11 history · 36 counted-only; `lcc-personal-calendar-sync.md` **never** held the term and points at a live Dialysis edge fn, not Railway; **`docs/architecture/` did not grow 181→232** — 181 is the top-level count, the 51 are subdirs (`flows/` 45) DOCMAP1 never listed. **J13 was a known row, not a find** — DOCMAP2 closed a filed row and did not say so plainly. Shipped: banners ×2, `docs/audits/README.md` (true: rule + spot-check, not exhaustive). **Boundary carried to DOCMAP3:** Unit 1b zero files; 5 terms count-only; `flows/` 45 + 3 other subdirs unclassified; `docs/setup/` 24 · `canon/` 22 · `copilot/` 6 · `data-quality/` 2 · `resolver/` 3 never opened. | ✅ | DOCMAP1 NOT-REACHED · Cowork 2026-09-08 |
| ✅ **DOCMAP3** | **SHIPPED 2026-09-08 (PR #2178, `1f93e973`) and RECONCILED (Cowork, same day).** Deep-read by consequence: Unit A 51 subdir files enumerated/read, **9 defects fixed** (7 retired-host flow docs incl. the 6 DOCMAP2 missed + `http-init-llc-repair-runbook.md`; 2 retired flows `todo-lcc-sync.md`/`unflag-completed-email-tasks.md` bannered against `retired_flows`); Unit B six terms re-grepped case-insensitively across all types, **1 defect** (`CONTACTS_SPLIT_BRAIN_CUTOVER_RUNBOOK.md` presented the completed `CONTACTS_HUB=ops` flip as pending) and **five terms read clean — a RESULT, do not re-grep**; Unit C 60 named files, **8 defects** (6 `docs/setup/` procedures naming Vercel as a live target, `SPEC_sos_direct_scraper.md` superseded by the government-lease `sos_detail_fetcher.py` behind the egress proxy, one heading); Unit D 56 files title+skim, 0. **Verified:** every banner exists; `BRIGGS-WRITING-VOICE.md` read by path from 4 `api/` files + 2 tests ✓; CONTACTS_HUB refutation is `CLAUDE.md`:186 ✓; `retired_flows` carries both flow ids ✓. **Corrected in place (STATUS entry):** Unit A wrote **47 rows, not 51** — `loopnet-`/`rcm-power-automate.md`, `vercel-github-direct-alert.md`, `weekly-retention-sweep.md` had no row (added in the reconcile); Unit B's recount **included `docs/history/` + `docs/capital-markets/`**, which the prompt excluded — excluding them: 61/27/18/12/18/11 (all types), 29/18/10/9/10/7 (`.md`); `docs/setup/` is 24 `.md`, not 23; the `AUTH_ENFORCEMENT_ROLLOUT.md` §5 heading was **rewritten, not bannered** (original survives only in git). **Disagreement recorded:** DOCMAP3 left `LCC_OneDrive_Upload_Setup_2026-04-21.md` unbannered as "dated narrative"; Cowork bannered it — its step 63 and the PowerShell snippet are a procedure that POSTs to the retired host, and a dated title does not make a procedure historical. **Boundary → DOCMAP4 (🟢, not scheduled):** `AI_CHAT_ROLLOUT_CHECKLIST.md` "policy: balanced" unverified; `SPEC_forsale_*` Parts B/C; 27 HISTORICAL `flows/` title+skim only; canon block facts unchecked; DOCMAP1's 87 title+skim rows. **Recommendation: stop sweeping, start enforcing → J13a-guard.** | ✅ | DOCMAP2 boundary · Cowork 2026-09-08 |
| **PDR1** | ✅ **BUILT + LIVE-VERIFIED 2026-09-10 — planner + flag-gated auto-merge tick + Decision Center lane, per Scott's P13#1 decision ("auto-merge the clear cases, queue the rest").** `api/_shared/ambiguous-entity-merge-planner.js` (pure scoring, documented threshold: one candidate ≥100 pts — has an address — leading the runner-up by ≥50 pts) + `api/_handlers/ambiguous-entity-automerge-tick.js` (GET ungated dry run / POST gated `AMBIGUOUS_ENTITY_AUTOMERGE`, off by default) + the new `ambiguous_entity_resolution` Decision Center lane, both routing every write through the SAME `rpc/reconcile_entity` — no second merge writer. ✅ **The real split, run live against production 2026-09-10 (Cowork, re-implementing the planner's exact scoring in SQL): of 189 entities, 22 auto-mergeable / 167 needs_human (`margin_too_close`) / 0 with no candidate clearing the minimum score.** DaVita/Donna-TX itself resolves correctly and is one of the 22 — winner `d90be440…` ("1006 I-2, Donna, TX 78537", score 172: addressed + normalized + 11 real signal rows) over runner-up `3c2dc7d3…` (score 104: addressed but not normalized) and the bare-city placeholder `c94991a3…` (score 40, no address at all) — exactly the ranking PDR1's own worked example called for. Migration `20260910120000` (run-log table + flag seed) **applied live** (Cowork); `AMBIGUOUS_ENTITY_AUTOMERGE` registered, state `off`, notes carry the real split. No recurring cron (population is closed, nothing minted since 2026-08-04). ✅ **EXECUTED 2026-09-10 (Scott: "run all 22 now") — 19 of 22 merged live via direct `reconcile_entity` calls (run log id 1, `lcc_ambiguous_entity_automerge_run_log`), all 19 `ok:true`.** ⚠️ **3 held back and routed to the review queue instead: the Rock Hill, SC cluster (Kohl's / Hobby Lobby and Shops / 1522-1526 Meeting Blvd) — their `ambiguous_resolution` candidate lists reference EACH OTHER as candidates, a case the scoring planner's address/signal check doesn't detect** (a candidate that is itself another unresolved placeholder can still score highest on address quality). Auto-merging these 3 could conflate up to three separate deals into one asset — filed as **PDR12** below, a real gap in the planner found during execution, not assumed safe. See STATUS.md 2026-09-10 for the full execution record, including DaVita/Donna-TX's confirmed downstream effect (PDR2/PDR4/PDR6/PDR7). | ✅ | shipped + live-verified + executed |
| **PDR3** | ✅ **CONFIRMED LIVE 2026-09-11 — PDR14b closed the LCC-side gap.** The Feb-2019 sale (`sale_id 311`, $3,639,317, buyer Phil Decarion — **not "2017-18" as recalled**, no trace of an earlier sale anywhere) is on the merged dia property (`39874`); `bd_opportunities` already linked the deal to the LCC entity correctly all along. PDR14b repointed entity `d90be440…`'s `metadata.domain_property_id` `37722 → 39874` (via PDR14a's `dia_resolve_property_id` redirect), and `get_property_context` now returns the transaction live — confirmed by direct query, not inferred: `sales_transactions where property_id=39874` reads 1 row. | ✅ | confirmed live 2026-09-11 |
| **PDR4** | ✅ **RESOLVED by the PDR1 merge — confirmed live 2026-09-10.** Post-merge, the canonical property now shows **3 documents** (an OM from CREXi, two OM emails) where the orphan showed zero. The entity-consolidation fix was exactly the right root cause for this one. | ✅ | fixed by PDR1 merge |
| **PDR6** | ✅ **CONFIRMED LIVE 2026-09-11 — PDR14b closed the LCC-side gap.** `property_cms_link` (`property_id 39874`, `medicare_id 672843`) was always on the row PDR13's merge kept as canonical. PDR14b repointed entity `d90be440…` to `39874` (same fix as PDR3), so `get_property_context` now resolves the CMS link — confirmed live: `properties.recorded_owner_id`/`true_owner_id` for `39874` both non-null, and the entity's `domain_property_id` reads `39874`. | ✅ | confirmed live 2026-09-11 |
| **PDR13** | ✅ **SHIPPED + LIVE-VERIFIED 2026-09-11 (Cowork independently re-queried every claim against live Dialysis_DB before trusting the shipped commit message).** Dialysis-repo session shipped a second, address-independent twin detector (`dia_find_property_twins_strong_id` / `dia_merge_strong_id_twins`, migration `20260911123900_dia_pdr13_property_twins_strong_id.sql`) keyed on `parcel_number` (len ≥6) UNION effective `medicare_id` (CCN len=6, `COALESCE(property_cms_link.medicare_id, properties.medicare_id)`), reusing the SAME `dia_property_twin_review` lane + `dia_merge_property_reversible` round trip the existing geospatial detector uses — no second merge mechanism. **Real fleet-wide population, confirmed live: 48 pairs (32 `review_name` / 16 `review_conflict` / 0 `auto_blank` / 0 `review_ambiguous`).** **The Donna, TX group specifically confirmed merged**: `properties` now holds exactly 2 rows (`39874` canonical, `45543` — a genuine address data-entry error, correctly left alone), `sales_transactions` for the group is exactly 1 row (`sale_id 311`, $3,639,317, 2019-02-01, now on `39874`), `property_cms_link` still correctly on `39874`. `dia_property_merge_backup` rows `587/588/589` confirmed live with the exact nested `rewired` payload the response claimed — checked at the JSON-payload level, not assumed from the response's prose. **⚠️ New gap found during this verification, out of PDR13's own scope (an LCC-side gap, not dia-side): see PDR14.** | ✅ | shipped + live-verified 2026-09-11 |
| EB1 | ✅ **SHIPPED (schema + contracts + measurement; NOT applied to any live DB, no rendering).** Migration `20260911165100_lcc_eb1_exec_briefs_foundation.sql`: five tables (`market_brief_facts`, `market_brief_issues`, `build_brief_snapshots`, `operator_notes`, `producer_runs`) + two views (`v_market_brief_live`, `v_market_brief_staleness`), RLS on the lockdown pattern. Contracts: `market_brief_payload_contract.md` + `operator_note_contract.md`. Seed: `scripts/eb1-seed-dialysis-exemplar.mjs` (dry-run default; plans 16 facts from the dialysis exemplar, excludes its 5 `[UNVERIFIED]` items). ⚠️ **§1 measurement was repo-only static analysis — this session had no DB/network access.** Confirmed from code: the 4 RSS streams exist exactly as spec'd; the Analyst's Take Anthropic call carries **no `tools` field at all** (web-search is not enabled in the current code path, a static fact); `intake-tagged-comm.js` exists as a viable `outlook_tagged` channel; MCP `log_memory` is one POST to `cortex_memory` — the template for `log_operator_note`. **UNMEASURED, needs a DB-reachable session:** per-stream RSS item counts/failures, `ANTHROPIC_API_KEY` live presence/success, tagged-Outlook-intake flag state + 30-day row count. `cortex_market_intel`'s schema could not be found in this repo (RLS-enabled but no CREATE TABLE, no `api/` reader) — MB-a should re-check it live before deciding reuse. Guard: `test/eb1-market-brief-foundation.test.mjs` (16 tests) + `test/eb1-seed-exemplar.test.mjs` (7 tests); full suite 5,791/0/6-skipped. Branch `feat/eb1-exec-briefs-foundation`, pushed, no PR opened. **Live-measured 2026-09-11 (Cowork, Supabase read-only)**: **migration NOT applied** (0 of 5 tables on LCC Opps); RSS 4 streams live, capped 6/stream, gov 0 items on 09-07/08 and tax 0 on 3 of 8 days; Analyst's Take on-box daily (316–680 chars, `onprem_ollama`); `ANTHROPIC_API_KEY` set but **every snapshot 09-02→09-11 fails "credit balance too low"**; `TAGGED_COMM_INTAKE` flag on but **dormant** (6 rows ever, last 2026-08-07, 0 in 30d); **`cortex_market_intel` EXISTS live** (922 rows, last write 2026-09-11 06:30 UTC; tenant/property_type/cap_rate/price/psf/dom — writer outside repo). Merged PR #2291. | ✅ | STATUS.md 2026-09-11 (EB1 + reconcile) |
| EB1a | ✅ **Applied live 2026-09-11 (OC-a step 0).** All 5 tables + 2 views confirmed on LCC Opps; 16 exemplar facts inserted (11 `[UNVERIFIED]` items correctly excluded); `v_market_brief_staleness` returns 20 (lane, section) cells, 5 populated / 15 `is_missing` (expected — only the `dialysis` lane has facts until MB producers ship). | ✅ | OC-a 2026-09-11 |
| OC1 | ✅ **Shipped 2026-09-11 — `POST /api/operator-notes`, three of four channels live.** In-app Note button (`operator-note-client.js`, floating button + modal, auto-captures route/entity/last-10-errors) · MCP `log_operator_note` (sibling of `log_memory`) · Outlook, with the **LCC-Note dormancy diagnosed and fixed**: `parseLccCategoryHint`'s `^lcc$`/`^lcc[:=](.+)$` regex structurally cannot match a category literally named `LCC-Note` (the hyphen fails both), so every note tagged that way silently fell through to `no_lcc_category` — this is narrower than and separate from the broader "flag-on, 0 rows in 30d" dormancy of the whole tagged-Outlook flow, which this fix does not by itself resolve (needs a live post-through-the-flow check). Teams is endpoint-ready (`channel:'teams'`, `PA_WEBHOOK_SECRET` auth) with the PA flow itself an operator step. Idempotent on a channel-native id via `context.idempotency_key`. Never triages inline. | ✅ | OC-a 2026-09-11 |
| OC3 | ✅ **Shipped 2026-09-11.** `scripts/render-operator-inbox.mjs --write` → GENERATED `docs/os/OPERATOR-INBOX.md`, grouped by `routed_to` thread, severity-sorted. MCP `get_operator_inbox` read tool + `/api/operator-inbox` HTTP route read the identical query. Wired into `.claude/hooks/session-start.sh` (non-blocking — no-ops honestly when `OPS_SUPABASE_*` creds aren't in the session's `.env.local`, which is the sandbox default). | ✅ | OC-a 2026-09-11 |
| HP1-P0 | ✅ **SHIPPED + DEPLOYED 2026-09-12 (PR #2358, `42158f17`; `/version` confirms live).** `Promise.allSettled` + `settledQueryResult()` so a thrown source degrades only its own lane (1b); `timeoutMs` 20 s on the seller-prospect read, 12 s on the other three (1a); `countMode` `'exact'` → `'estimated'` on all four, safe because `.count` is read nowhere (1c); per-lane `source_error` rendered by `app.js` plus a degradation note folded into the existing `named_gaps` contract, replacing the **"Nothing here right now. ✓"** green checkmark a failed lane used to show (1e). A 7th previously-unguarded `opsQuery` in the same handler (the entity-name lookup) was found and wrapped. Guard `test/today-sections-degraded-source.test.mjs` asserts a thrown source empties exactly one lane and the endpoint still returns 200. Suite 6,013/0/6-skipped. | ✅ | STATUS.md 2026-09-12 |
| HP1-P1a-fix | ✅ **CLOSED 2026-09-12 — verified on `UPDATED_not_inserted = 608`, `brand_new_rows = 0`.** PR #2371 + both services redeployed; **two silent defects found on verification and fixed live**: (a) the migration had **never been applied** (`pg_proc` had no such function — the redeploy would have 404'd all 608 deals per run, i.e. made the feed worse), (b) the function as written raises **42702** on its first call (OUT params `sf_opp_id`/`entity_id` collide with columns; a plpgsql body is not parsed at CREATE time). Corrected by `20261101170100_lcc_hp1p1a_upsert_rpc_fix_out_param_ambiguity.sql` (OUT params `out_`-prefixed, INSERT aliased `AS t`; no JS change). **The 12:47 UTC PA run wrote 608 UPDATEs** — the first this path has ever produced. Six weeks of drift landed as **10 stage changes / 6 close-date changes / 4 newly closed / 0 amount changes / 0 new rows**, all recognisable live deals. ✅ **Unit 4 held** — `closed_at` moved on exactly the 4 genuine transitions; the 569 already-closed rows kept their original timestamps. ⚠️ **One backward stage move** (*Essentia Health — Hinckley MN* `loi_executed` → `listing_signed`) — 👤 Scott's eye: real re-trade or SF hygiene. LCC now mirrors Salesforce faithfully, errors included. ⚠️ **The mass-auto-retire warning was misdirected** — there is no `deal_next_step` table; `lcc_generate_deal_next_steps()` writes into **`action_items`**. | ✅ | HP1-P1a-fix verified 2026-09-12 |
| HP1-P1a-rpc | ✅ **DONE 2026-09-12 — recorded so the class is not repeated.** A merged migration is not an applied migration, and an applied plpgsql function is not a working one. Both halves failed here silently. **Standing check for any RPC-shipping prompt from now on:** after deploy, (1) assert the function exists in `pg_proc`, and (2) execute it once inside a rolled-back transaction against a real row — `CREATE` success proves neither. A `RETURNS TABLE` OUT parameter that shares a name with a column of the table the body writes is a 42702 waiting for its first call; prefix them. | ✅ | HP1-P1a-fix verify 2026-09-12 |
| HP1-P2a | ✅ **SHIPPED 2026-09-12 — the Inbox surface excludes `new_contact_qualify` (captured-contact hygiene, 879 rows) at `v_inbox_triage`, the source every consumer (`handleInbox`/`/api/inbox`, `v2GetInbox`/`/api/queue-v2?view=inbox`, v1 `case 'inbox'`) reads.** `contact_misparse_review` (117) stays ON the Inbox — no resolution surface exists anywhere in the repo (P131 gap, filed not fixed). `mv_work_counts.inbox_new`/`inbox_triaged` carry the same exclusion so the header agrees with the list. Every response now carries `hygiene_pointer {source_type,count,label}` — an EXACT uncapped count against `inbox_items` (never the filtered view), rendered as a persistent row on the Inbox page linking to the pre-existing `renderContactQualifyWorklist()` surface (868 live rows). Inbox reads **182**, not the prompt's stale 65 (that figure assumed both hygiene lanes AND the personal `email_alert` class would leave; only one does). Migration `20261101190000`; guard `test/hp1-p2a-inbox-hygiene-pointer.test.mjs`. Suite 6,083/0/6-skipped. | ✅ | STATUS.md 2026-09-12 |
| ✅ **C13g-min-lane** | **BUILT 2026-09-09 — the human-verdict LANE `entity_type_review` over C13g-min's retype write.** All four registries wired (`FEDERATED_DECISION_TYPES`+`federatedSubjectRef` = `etype:<entity_id>`, `_DC_FEDERATED`+tile, `_DC_FED_META`+card, `review-shared.js` lane `entity_merge`), pure planner `api/_shared/entity-retype-planner.js` (verdicts `retype_organization` → `rpc/lcc_retype_entity`, never a direct PATCH / `keep_person` record-only / `research`), card re-read from `v_lcc_entity_retype_candidates` at verdict time (P188), a success forward to the `sponsor_family_confirm` lane when the retyped entity was blocking a sponsor card. Guard `test/c13g-min-lane.test.mjs` (16 tests; one assertion mutation-sampled, not a full mutation pass). Live census re-run (not merely re-quoted): lane population is now **19 rows**, not 18 — re-derivable views move; `merge_candidates`/`auto_mergeable` confirmed to hold on retype alone and move only on the subsequent merge (5,205→5,204 / 3,012→3,011); the broader Gardner+sponsor `unclassified_rival` count moved 65→47 across a full rolled-back retype→merge→unmerge→unretype round trip, 0 residue. Neither Gardner nor MassMutual Life carries any Tier 0 corroboration column, so retyping either removes nothing from that bench. `v_lcc_entity_role_ambiguity` and the 14-co-claimed re-verification were NOT re-measured this pass — owner-role-classification.md §9f. | ✅ | C13g-min · OWN-T0e-b |
| ✅ **C13g-min-lane-mutation** | **DONE 2026-09-09 — `test/c13g-min-lane.test.mjs` is now 14 tests / 46 mutations RED / 46.** Added 2 assertions (candidate-view migration reads the OWN-T0e cache not the slow view; `p_decision_id` bigint matching `lcc_decisions.id`, old uuid overload dropped — the repo-side twins of the two same-day hotfixes' apply-time DO blocks). Two assertions survived their first mutation and were rewritten: the ordering test's own row names happened to sort the same by name as by rent (renamed anti-alphabetically); the registry-membership regex matched the unrelated `research_type: 'entity_type_review',` literal in the verdict branch instead of the `FEDERATED_DECISION_TYPES` entry (re-anchored on the `sponsor_family_confirm` adjacency). Both unmeasured items closed, rolled back: `v_lcc_entity_role_ambiguity` is 0 rows for Foulger Pratt in either type (not every retype moves that view); the Tier 0 bench gained **10 cards across 3 of the 11 non-tombstoned retyped entities** (UIRC 7, Global Net Lease 2, Foulger Pratt 1) that could not exist while person-typed — §9f's Gardner/MassMutual-only corroboration check was right for those two, incomplete as a claim about the lane. owner-role-classification.md §9g. | ✅ | C13g-min-lane |
| ✅ **C13g-min-lane-placeholder** | **DONE 2026-09-10 — `Research In Progress` is a placeholder ENTITY holding 2 current portfolio facts; it no longer reaches the retype lane.** Measured live first that none of the three existing name guards fires on it (`lcc_is_placeholder_owner_name`/`lcc_p131_is_document_row_label`/`lcc_a2_is_placeholder_party` all `false`); widened `lcc_is_placeholder_owner_name`'s exact-match list with the one literal after a blast-radius check (2 entities fleet-wide, both genuine placeholders). `v_lcc_entity_retype_candidates` restated (migration `20261101150000`) to exclude it from both population sources; candidates **4 → 3** (exact predicted delta). New `v_lcc_entity_retype_placeholder_excluded` view + one-shot `api/admin.js?action=entity-retype-placeholder-seed` route the excluded row to `junk_entity_review` (retire, never merge) — seeded live, review_id 386, `dismiss`, naming the 2 held facts. | ✅ | C13g-min-lane |

## 3. Feature-flag state — LIVE registry snapshot, measured 2026-08-26

`select flag, state from feature_flags_registry` on LCC Opps: **30 `on` · 27 `off` · 2 `partial`.**
The registry is the authority; **seed migrations and older docs have drifted from it more than once**
(that miss is what re-ranked the whole local-model audit on 2026-08-24).

**ON (30).** `BRIEFING_ANALYST_TAKE_ONPREM` · `DEAL_COMMS_PROPAGATE_CRON` · `DEAL_EMAIL_MATCH_CRON` ·
`DRAFT_ASSIST` · `MAILBOX_MIRROR` · `MATCH_DISAMBIG_ASSIST` · `MOVE_QUEUE_EXECUTOR` · `NEXT_STEP_AI` ·
`OCR_CLOUD_DOCAI` · `OLLAMA_CLEAN_ASSIST` · `OLLAMA_EXTRACTION` · `ORE_USE_RESOLVER` ·
`OWNERSHIP_CHAIN_DRAFT` · `PROPERTY_TWIN_ASSIST` · `SHAREPOINT_LIST_URL` · `TAGGED_COMM_INTAKE` ·
`W51_PARTY_EXTRACT` · `W74_ROLE_ISSUES` · `W75_ACTION_SUMMARY` · `W8_U1_JUNK_PRESCREEN` ·
`W8_U2_DUP_PAIRS` · `W8_U3_LINK_PROPAGATION` · `W8_U4_FINDINGS_REPORT` · `W8_U5_NAMING_HYGIENE` ·
`W9_1_CONTACT_ACQUISITION` · `W9_2_REACHABILITY_HARVEST` · `W9_3_DONOR_HANDOFF` · `W9_3_RESCORE` ·
`W9_3_SF_ASSIST` · `W9_6_COMMS_OWNER_ATTRIBUTION`

**PARTIAL (2).** `CONTACTS_HUB` (currently pointing at **`ops`** — LCC Opps is the live
`unified_contacts`; the gov copy is a frozen pre-cutover snapshot. ⚠️ `govQuery()` reads whichever
side the flag names, so **confirm the flag before quoting any contact count**) ·
`RESOLVER_RETRAIN_LOOP`.

**OFF (27) — grouped by WHY, because "off" is not one thing.**

| Why it is off | Flags | What it would take |
|---|---|---|
| **External egress / bot-wall** — the handlers are correct and honest-blocked | `W9_1_SOS_DIRECT`, `SOS_STATE_ADAPTERS.FL/CA/TX`, `OWNER_ENRICH_SOS_URL`, `OWNER_ENRICH_ADDRESS_URL`, `OWNER_ENRICH_DEED_URL`, `OPENCORPORATES_API_KEY` | Client fidelity in `sos-proxy/fetcher.js` (per-host cookie jar + browser-grade TLS). **Never a CAPTCHA solver.** TX is *paid*, not blocked. → `government-lease/CLAUDE.md` §25 |
| **Paused by doctrine** | `OWNER_ENRICH_WEBSEARCH_URL` | Nothing — contact acquisition is public-records-only by decision. |
| **Third-party key / URL not provisioned** | `GEOCODIO_API_KEY`, `GOOGLE_MAPS_API_KEY`, `CM_TREASURY_REFRESH_URL`, `SF_LIST_IMPORT_URL`, `SF_LIST_SEED_INSTITUTION` | Provision the key/webhook; the code no-ops honestly meanwhile. |
| **Held pending a dry-run grade** (built, deliberately not flipped) | `OWNERSHIP_CHAIN_ROLE_LABELS` (→ open Prompt 140), `LISTING_PAGE_PROACTIVE_EXTRACT`, `DEED_IMPLIED_PRICE_FILL`, `CADENCE_TEMPLATE_AUTOSELECT`, `CADENCE_OPEN_TRACKING_ACTIVE`, `DECISION_PROVENANCE_LEARN`, `DECISION_OWNER_DEED_WINS`, `DECISION_GOV_WRITEBACK`, `GOV_EVIDENCE_WORKBENCH`, `SF_CONTACT_WRITEBACK`, `TEAMS_COLD_ALERTS_ENABLED` | Pull a dry-run sample, eyeball 10–20 proposals, flip. **Grade before flipping** — `OLLAMA_CLEAN_ASSIST` failed its first grade (6/12 content-free) and needed P134 context enrichment before it passed. |
| **Producer gate, deliberately closed** | `ENABLE_OWNERSHIP_RESEARCH_QUEUE` | A decision, not a defect — it gates 9 gov insert sites. |
| **Operator/tenant step outstanding** | `PA_OUTLOOK_DRAFT_FLOW` (`off_since` 2026-08-21 — ⚠️ the same day the draft seam was proven working end-to-end; **re-measure before acting on either reading**) | Confirm the PA flow + `PA_OUTLOOK_DRAFT_URL` against the tenant. |

> **The generalised check for every ON flag:** `state=on` is not production. **Assert on the write
> delta over the last 7 days**, never on the flag and never on the worker's own tally — a
> re-discovery counter (`already_annotated`, `already_attributed`, `already_drafted`) reads exactly
> like throughput while nothing moves. This has been proven three times (P135, P136, P159a).

## 4. Local-model (GaryBuilt / Ollama) surface state

Doctrine: **private corpora never go to a cloud model.** All traffic flows through
`api/_shared/ai.js` (`invokeExtractionAI` / `invokeOnPremGeneration` fail-closed /
`invokeOnPremEmbeddings`). Master gate `OLLAMA_URL`, per-surface gate `OLLAMA_SURFACES`.
Full map: **`LOCAL-MODEL-LEVERAGE-MAP.md`** · ranked gaps: **`LOCAL-MODEL-GAP-AUDIT.md`** ·
box playbook: `docs/setup/garybuilt-local-model.md`.

**Live uses:** extraction (OM/deed/lease/BOV/owner-name/party) · narrative (dossiers, deal-comm
summaries, action summaries) · drafting (`draft-assist` + `nomic-embed-text`) · voice distillation ·
junk pre-screen · next-step derivation · **on-box brief narrative (new)** · optional chat provider.

### Production-health of the assist lanes — re-measured 2026-08-26

`select source, count(*), count(*) filter (where created_at > now()-interval '7 days')
from lcc_clean_assist_proposals group by source`:

| assist | flag | total | last 7d | last write | verdict |
|---|---|---|---|---|---|
| ownership-chain draft | `OWNERSHIP_CHAIN_DRAFT` | 545 | 545 | 2026-08-26 | ✅ healthy — full corpus drafted; cron 239 keeps it fed |
| sf-link assist | `W9_3_RESCORE` (source `w9_3_sf_assist`) | 247 | 47 | 2026-08-22 | ✅ healthy (caught up) |
| ollama clean-assist | `OLLAMA_CLEAN_ASSIST` | **63** (45 earlier the same day) | 63 | 2026-08-26 | ✅ producing and still climbing since the P134 re-grade + P137 ladder wiring |
| **property-twin assist** | `PROPERTY_TWIN_ASSIST` | 200 | **0** | 2026-08-19 | ⏳ **DIAGNOSED 2026-08-26 evening — THE FIX WAS NEVER DEPLOYED, not broken.** P135 merged 18:16 UTC; the build running all day was cut at **16:03 UTC**. Cleared by the 23:13 UTC redeploy (#1789). **Verify on cron 220 @ 05:45 UTC — the count must pass 200.** |
| reachability harvest | `W9_2_REACHABILITY_HARVEST` | **`reachability_harvest_target_marker`: 60 markers, all written 2026-08-27, last 04:40:19** — inside cron 212's run | 60 | 2026-08-27 | ✅ **HEALTHY (confirmed 2026-08-27).** ⚠️ **Do NOT judge this lane by `reachability_harvest_review`** — it is still 4 and that is *correct*: P136's design emits a **negative marker** (*checked, and empty*), so targets with no evidence produce no proposal. The proposal count is the one metric that reads zero while the lane works. Also note cron 212 logs `timed_out: true` at 60s and the work completes regardless (P123). |
| junk pre-screen · naming hygiene · dup-pair · match-disambig · next-step | `W8_U1` · `W8_U5` · `W8_U2` · `MATCH_DISAMBIG_ASSIST` · `NEXT_STEP_AI` | *(other stores / inline)* | | | Doc-sourced ✅ healthy as of 2026-08-26 — see `LOCAL-MODEL-LEVERAGE-MAP.md` §2; not independently re-measured here |

## 5. Surfaces & the instruction canon

- **Canon version: `CANON_VERSION 1.5.0`** (2026-08-20) — `docs/os/canon/00-INDEX.md`. Rules live in
  `canon/blocks/*.md`; **never hand-edit a file whose header says GENERATED.**
- **To change any rule:** edit the block → bump `CANON_VERSION` → run
  `node docs/os/tools/render-surfaces.mjs --root=docs/os --write-live` → follow
  `SURFACE-SYNC-PROTOCOL.md` to push to every surface. `tools/check-parity.mjs` exits non-zero on drift.
- **One master paste-file per surface:** Copilot Deal Agent → `docs/copilot/agent-instructions.md`
  (auto) · ChatGPT → `docs/os/surfaces/chatgpt.canon.md` as the LCC-CANON knowledge file (auto) ·
  Northmarq Claude → `_WORKFLOW/NORTHMARQ_PROJECT_PROMPT.md` (manual) · Personal Claude / Cowork →
  `~/.claude/skills/*` (manual).
- **Legacy, do not treat as authoritative:** `docs/claude/northmarq-claude-instructions.md` and
  `docs/claude/personal-claude-instructions.md` self-label "AUTHORITATIVE" and are not.
- Full mechanics: **`AI-SURFACES-OPERATIONAL-REFERENCE.md`**.

## 6. Canonical doc map — one source per topic

| Topic | Canonical source |
|---|---|
| **Where we are** (this file) | `docs/os/CURRENT-STATE.md` |
| **Everything unbuilt-but-intended** | `docs/os/PLANNED-BACKLOG.md` |
| OS entry point / architecture | `docs/os/README.md` → `docs/os/REGISTRY.md` |
| Rules (per topic) + version | `docs/os/canon/*.md`, `docs/os/canon/00-INDEX.md` |
| Surface update procedure | `docs/os/SURFACE-SYNC-PROTOCOL.md` |
| Surfaces / comps engine / deploy | `docs/os/AI-SURFACES-OPERATIONAL-REFERENCE.md` |
| Local model — where it is used | `docs/os/LOCAL-MODEL-LEVERAGE-MAP.md` |
| Local model — ranked gaps | `docs/os/LOCAL-MODEL-GAP-AUDIT.md` |
| Invariants, footguns, doctrines | `CLAUDE.md` (LCC) · `Dialysis/CLAUDE.md` · `government-lease/CLAUDE.md` |
| API/routing reference | `.github/AI_INSTRUCTIONS.md` |
| Repeatable defect detectors | `docs/audits/DEAD_END_AUDIT_PLAYBOOK.md` |
| **The FULL ownership → true-owner → CRM pipeline, property to Salesforce/Outlook/WebEx, stage by stage** | **`docs/architecture/ownership-truth-pipeline-state.md`** — written 2026-09-10, the one page that walks all five stages end to end and names the gaps between them; every other row in this cluster covers one stage only |
| **Tier 0 owner-contact: person↔owner matching, the confirm lane, the sponsor map, owner merges** | `docs/architecture/tier0-owner-contact-system.md` — **one door into thirteen rounds (P186–P198)**; live state, decisions already made, traps already paid for. **Sibling of the row below — they share `lcc_merge_entity`, `lcc_owner_sponsor_domain` and the owner entities, and now cross-link** |
| **Ownership-history lane** (the whole subsystem — read this, not the 7 audits) | **`docs/architecture/ownership-history-lane.md`** |
| **What the PROPERTY PANEL reads for ownership (ONE view)** | ✅ **OWN-T0 SHIPPED 2026-09-02** (#2074, `47d0a934`) — `v_lcc_property_ownership_reconciled` / `_current` is the single store the Ownership tab reads; `v_lcc_property_multi_current` counts the class the old detector was blind to (**756 / $903.3M**); P117's fill-blanks predicate fixed at the PROPERTY grain (480 phantom current owners prevented). ⚠️ **End-dating was refuted on named rows** — the pairs are sponsor↔SPE, both true. **7,678 properties have NO owner on file** (`only_non_owner_claims`), now visible. ✅ **OWN-T0e BUILT 2026-09-09** — Decision Center lane `sponsor_family_confirm` (one confirm = one `lcc_ownership_sponsor_family` row, clears a family; reads a 4-hourly cache of the ~20 s proposals view; **JS pending the Railway redeploy**, DB half live). ✅ **OWN-T0h DECIDED 2026-09-11**: the reconciled store (`v_lcc_property_ownership_reconciled`, `property_state='conflict'`) is canonical for "conflict properties," not `v_lcc_property_multi_current`'s 756 (that's a narrower fact-ledger-vs-itself check). ⚠️ A same-day re-measurement briefly (and wrongly) reported this had doubled to 4,478 — a `count(*)`-vs-`count(distinct property)` bug. **Corrected: 2,065 today (gov 1,752 / dia 313), essentially flat vs. 2,097**, explained by OWN-T0e confirm-lane activity. Open: OWN-T0e-b (5 duplicate pairs the merge lane cannot show). → `docs/architecture/ownership-history-lane.md` § OWN-T0 |
| **Owner-role classification** | **C13g-min LANE LIVE 2026-09-09, arc CLOSED 2026-09-10; capture-path producer fix (`C13g` + `C13g-costar-stoplist`) SHIPPED 2026-09-10 too** (placeholder routing, the sponsor-is-duplicate merge affordance, and both RCA/CoStar capture-path producers — `docs/architecture/ownership-truth-pipeline-state.md` Stage 3, `owner-role-classification.md` §9h/§9i) — the `entity_type_review` Decision Center lane over C13g-min's DB half (`lcc_retype_entity` / `_unretype_` / `lcc_entity_retype_log` / `v_lcc_entity_retype_candidates`, re-derivable population now **19 rows**), backlog `C13g-min-lane` ✅; owner-role-classification.md §9f. ✅ **LIVE 2026-09-01 (C13b) — `v_lcc_entity_roles` on LCC Opps**, one row per (entity, role) with its evidence arm, dates and pacing; **10,655 entities carry ≥1 role, 946 carry ≥2, 0 duplicate pairs.** A VIEW over the existing spine, never a stamped column; nothing writes and no consumer is repointed (P0.4 555 → 555). **Read `docs/architecture/owner-role-classification.md` §7 FIRST** — it corrects three figures this page and the design carried: `repeat_buyer` **3,258 → 401** (3,258 counted purchase EDGES), `investor_owner`+`repeat_buyer` **772 → 167**, `developer` **838 → 718** (a manual override REPLACES the column that arm reads). ⛔ **`developer` is BUILT, not unbuilt** (§2e, `v_gov_owner_at_first_gen`) — it is READ, never re-implemented. 🟠 **C18 still blocks the part that matters most, but for a corrected reason: the 50.7% `ownership_start_date` blindness belongs to `investor_owner` pacing; repeat-buyer pacing is 98.8% dated.** ✅ **C13c SHIPPED 2026-09-01 — `one_off_owner` carries its CONFIDENCE: 142 = 13 `_sf_corroborated` + 129 `_unverified`, the COUNT unchanged, plus 21 named institutional rows on `v_lcc_entity_role_ambiguity.entity_type_contradicted_by_named_review` (a LEDGER read, never a name stoplist). P0.4 still 555.** ⚠️ **C13b's "no non-lexical corroboration exists" was three absences, not a search** — `salesforce/Contact` answers on 13 of 142 and ZERO institutional names carry one. ✅ The underlying `entities.entity_type` defect was SIZED, FILED, and the producer FIXED 2026-09-10 (**C13g** + **C13g-costar-stoplist**: non-lexical floor 414 entities / $181.8M; ⚠️ the lexical 13,225 measures the regex, not the population) — both the RCA and CoStar capture-path producers now route through the shared `hasFirmSuffix()` guard; forward-mint only, the existing population stays `entity_type_review` lane material. |
| **BD ranking / the priority queue** (the chain's LAST hop — read this, not the C4–C11 audits) | **`docs/architecture/bd-ranking-and-priority-queue.md`** — one door into C4→C11. ✅ **The call-sheet arc is COMPLETE (2026-08-31): C6** (gate on current holding + reachability) **· C8** (admit resolved owners, 80 → 126) **· C10** (legibility) **· C11** (contact basis). **126 rows, gated, legible, each stating why that person is the contact; ~4 defective rows remain** (C11b, C11c, one C9 split) — see **§4b**. ⚠️ **C11a REFUTED** (sponsor↔SPE, not a defect); ⚠️ **C9's 45 true splits touch exactly 1 sheet row.** ✅ **C4a's ordering half is ANSWERED (canon 1.8.0, app-ux-review §0b).** 🚨 **UX-T1a Part A (2026-09-03): the queue is 89.6% DISJOINT from the doctrine's population** — P1/P2/P3 select assets LATE in term, the sweet spot is the first 2–3 years; funnel 8,858 → 3,529 in band → 259 newer lease → 31 reason to sell → 23 not reached; **58% of queue rows are automation/plumbing/buyer work.** Part B deliberately NOT built: two gates are coverage gaps (dia lease dates absent from the mirror; no loan table in LCC despite 192 maturities ≤24 mo and a `loan_maturity` slot on Today that was never implemented). ✅ **UX-T1a-gates SHIPPED 2026-09-03 (#2088):** dia lease dates in the mirror 0 → 1,747; `loan_maturity` owner-attributed 0 → 172 rows / 109 owners; operator surface 1,635 → 694 (941 hidden via `human_surface`, not deleted). ⚠️ The audit's "no producer" was half-wrong — the HANDLER always fanned out to the domain watch views; what was missing was OWNER ATTRIBUTION (`entity_id: null`), which is what actually blocked the queue. ✅ **UX-T1a-queue SHIPPED 2026-09-03 (#2092): `v_lcc_seller_prospect_queue` = 520 rows / 453 owners** (variant F, gates as named columns; `no_linked_person` 384 — links are the binding constraint; debt arm asset-scoped, a stated 95-row decision). ⚠️ The 89.6% disjointness is the NEWER-LEASE half only — the whole queue overlaps the band queue 34.8%, so it **sits beside** the bands, not replacing them. ✅ **UX-T1a-today SHIPPED 2026-09-03: Today is now Significant / Important / Urgent** (`GET /api/operations?action=today_sections`, pure classification in `api/_shared/today-sections.js`). Significant = the whole seller-prospect queue (520 rows, first front-end at `pageSellerProspectQueue`); Important = `bd_opportunities` open rows (47 — named gaps: no BOV-generation or marketing-live-listing producer exists); Urgent = `action_items` deal correspondence (58 open) unioned with `v_lcc_bd_worklist`'s `contact_writeback`/`owner_source_conflict` (loan_maturity + ownership_chain deliberately excluded). Replaces the old unlabelled "Work Your Outreach" + "Top BD Actions" Home cards. **Next: -cadence** (after -touchcount). **C7a** (mailbox coverage) still upstream. 🚨 **HP1 (2026-09-12): that endpoint had been 500ing ALL THREE lanes on a single slow source** — six queries in one `Promise.all`, and `opsQuery`'s 8 s `AbortController` makes `fetch` THROW rather than resolve `{ok:false}`, so the handler's `ok`-guard was dead code and the rejection reached `withErrorHandler`. ✅ **FIXED + DEPLOYED (#2358, `42158f17`, `/version` confirms live):** `Promise.allSettled` + per-lane `source_error` + `named_gaps` degradation notes, explicit `timeoutMs`, and `countMode` → `'estimated'`. A failed lane now says so instead of rendering **"Nothing here right now. ✓"**. 🔴 **Still wrong: `total_open` is the CAPPED page length, not the population** — Significant's badge reads 200 against a true **517** (the 520 above, re-measured), Urgent's ≤200 against **1,587**; ranking is unaffected, only the count (backlog **HP1-badge**). ⛔ **That "P1 blocked — Salesforce-hygiene vs PA-scope is Scott's to establish" line was WRONG and is retracted (2026-09-12).** It was never a hygiene or scope question. The Power Automate flow is healthy and has delivered 608 records every 30 minutes throughout; the **write** failed. `mcp/opportunity-sync.js` passed `{Prefer: ...}` as the 4th positional arg to the standalone MCP's `opsQuery(method, path, body, prefer)`, which stringified it to `"[object Object]"`; PostgREST could not parse that, dropped `resolution=merge-duplicates`, and ran a plain INSERT — 608 duplicate-key violations per run under an HTTP 200. **No stage change or close has ever propagated from Salesforce into LCC.** The live handler is the MCP's copy because `mountLccMcp(app)` at `server.js:176` registers the same route 193 lines ahead of `server.js:368-369` (**HP1-P1a-dup**). ✅ Fixed by routing through `rpc/lcc_upsert_bd_opportunities` (PR #2371, deployed) — but the migration had not been applied and the function as written raised 42702 on its first call; both corrected and the UPDATE proven live on 2026-09-12. ✅ **PROVEN END TO END 2026-09-12 12:47 UTC: `updated_not_inserted = 608`, `brand_new_rows = 0`** — the first UPDATE this path has ever produced. Six weeks of drift landed as **10 stage changes / 6 close-date changes / 4 newly closed / 0 amount changes**; `closed_at` moved only on the 4 genuine transitions (Unit 4 held). Two further defects were found and fixed during verification — the migration had never been applied, and the function as written raised 42702 on its first call. ✅ **HP1-P1d SHIPPED + verified independently 2026-09-12:** `lcc_check_sf_opportunity_freshness()` asserts on `max(last_synced_at) FILTER (WHERE sf_opp_id IS NOT NULL)`, cron `lcc-sf-opportunity-freshness-check` hourly at :20, opening an `lcc_health_alerts` row. **The B6a trap test passes:** with the SF feed back-dated 30 days *and* a fresh write by the non-SF producer, the alert still fires (`stale:true`, age 720.2 h) — it is producer-scoped, not table-keyed in disguise. ⚠️ Row split corrected: **612 SF-linked / 7 non-SF** (the 7 are the `priority_queue` producer, all `last_synced_at IS NULL`); the 612-vs-608 gap is a separate finding — **HP1-P1a-orphan**, stale SF ghosts in the open queue. ✅ **HP1-P1d SHIPPED 2026-09-12**: `lcc_check_sf_opportunity_freshness()` (migration `20261101180000`) alerts on `max(last_synced_at) FILTER (WHERE sf_opp_id IS NOT NULL)` going stale past 3h (6× the measured 30-min PA cadence), into the existing `lcc_health_alerts` / Teams-push surface — confirmed `salesforce_sync` in `feed_freshness_registry` watches a DIFFERENT pipe (`sf_sync_log`) and is untouched; `ingestBatch` now logs one `producer_runs` row per batch (`facts_written` = the RPC's own inserted+updated tally). All four required positive controls run live (rolled back) and pinned as automated tests. See backlog **HP1-P1a-fix** / **HP1-P1a-nullsf** (still open, deliberately not resolved here). |
| **AI & OCR cost strategy** (local vs Microsoft vs Google) | **`docs/architecture/ai-and-ocr-cost-strategy.md`** — the canonical page carries the full state; summary: 🟢 **The case for a local engine is the PAGE CAP, not money** — DocAI spend is ≈ $0.86 total, 51% of documents already extract free; every hard problem in the arc was Google's 15/30-page limit. ⛔ Microsoft refuted (no batch OCR API; IT blocks Azure AD app registrations). 🟡 **OCR1 verdict for tesseract: §5 row 2** — free pre-filter with no page cap (141 pp in one pass), ~13 pp below DocAI against a 93–94% model self-agreement floor. **Surya/Paddle still ungraded** → 👤 the GaryBuilt run (`OPERATOR-ACTIONS.md` §2; the first box run installed the CPU paddle wheel and never switched surya's backend). ✅ **OCR2 (#2050)**: deed-lane OCR provenance persisted via the single-writer `<dom>_merge_document_extracted_data` RPC (the deed parser had been REPLACING the jsonb). ✅ **EXT1 → EXT1b → EXT2 (#2078, `f83c2d99`)**: the extractor now QUOTES (never computes), the quote outranks the model's labels (rent + expiration self-agreement 100%), and the LEASE'S OWN definition decides base rent / year 1 (at Rent Commencement, `year1_rent_source`) / the tenant (`tenant_legal_entity`; `credit_entity` = express guarantor else the counterparty; a parent merely named is never the credit). ✅ **EXT2 floor re-run DONE 2026-09-03** — 7/8 decided docs agree on `year1_rent_source`. ✅ **EXT2a SHIPPED 2026-09-03 (PR #2098)** — ground-truthed against the real Chesterbrook lease Scott supplied: `baseFromPeriodQuote` reads a schedule period's own base/additional split so equipment stops double-counting into `year1_total_rent`; doc 255 now resolves 89,340 base / 101,568 total / `schedule_period_1`. Record `responses/done/EXT2a-schedule-line-definition.response.md`. ⚠️ `LEASE_TEXT_SLICE_CHARS` (90k) still caps CONSUMPTION at ~52 pp median even when acquisition is uncapped. ⚠️ No pricing constant exists in code (OCR5). |
| **Document capture / OCR / deeds** (read this, not the five older files) | **`docs/architecture/document-capture-ocr-and-deeds.md`** — **opens with a CURRENT STATE block.** Re-measured 2026-09-02 ~16:00 UTC: deeds **325/325** · undrained **771 → 401** · **`bov_ready` 5 → 43** · **88 DocAI / 2 gpt-4o since the 09-01 redeploy, both in its first two ticks, ZERO since.** ✅ **DOC17 settled the decider: the DocAI sync page cap is measured against the SELECTION, not the document — 30pp/call from page 1 (imageless), 15pp/call anywhere else.** ✅ **DOC18 LIVE, verified on a real document 2026-09-02 15:30 UTC** (doc 80: 31pp in 2 calls, 0 gaps). ⚠️ **Its first tick FAILED because the `docai-ocr` edge function was undeployed — a third deploy surface beside the migration and Railway; v25 deployed 15:29.** Cron drains one over-cap document per tick at :07/:37 — verify on `v_lcc_cre_longdoc_backlog`, backlog row **DOC18**. 👤 **DOC14 should probably be closed — Scott's call.** ⛔ Do NOT widen cron 160 (DOC7). |
| **Owner-role classification** (how an owner gets its role) | **`docs/architecture/owner-role-classification.md`** — C4a's design, written to Scott's accuracy-first constraints. ✅ **BUILT AND LIVE 2026-09-01 — §7 is the shipped state.** Derived + re-computed (never stamped); recorded facts only, no lexical classifier; `former_owner` shipped at **3,786**. §6's open questions are all answered. Writeup `docs/audits/C13b_OWNER_ROLE_MULTILABEL_2026-09-01.md`. |
| **The app as the operator sees it** (2026-09-02 walk-through: 48 comments, tiered) | **`docs/architecture/app-ux-review-2026-09-02.md`** — §0 is the doctrine (minimum human dose; seller-first queue), §1 the catalog with what is already known, §2 the tiers; backlog **§P16**. Queues behind the OCR thread. |
| **Where operator time actually goes** (lane-by-lane throughput) | `docs/audits/DATA_PROCESS_AUTOMATION_AUDIT_2026-08-26.md` |
| Intake channel provenance (grade by channel, never fleet-wide) | `docs/audits/W53_INTAKE_CHANNEL_PROVENANCE_2026-08-26.md` |
| Wave rollout ledger | `ROLLOUT_STATUS.md` |
| Running work log | `docs/claude-code/STATUS.md` |
| **Connectivity map + open threads — the property→owner→contact→cadence chain** | `docs/architecture/connectivity-and-open-threads.md` — **§4e is the current chain state (2026-08-28)**; §4b's counts are superseded. ⚠️ **BREAK-2's "cadence has no consumer" verdict is OVERTURNED** (Scott 2026-08-27: the cadence layer is core, it is un-started not orphaned), and **BREAK-3's 49.2% is *of assets*, not properties** (13% on the property denominator) |
| Cowork session setup | `docs/os/COWORK-SETUP-AND-FUTUREPROOFING.md` |
| Fresh-chat kickoff | `docs/claude-code/NEW-CHAT-KICKOFF.md` |

## 7. Stale claims this consolidation overturned (measured 2026-08-26)

Recorded here so the next chat does not re-inherit them, per the fix-the-note-in-the-same-change rule.

1. **`AI-SURFACES-OPERATIONAL-REFERENCE.md` §1 said `CANON_VERSION` is `1.2.2`.** It is **1.5.0**
   (2026-08-20). Corrected in that file.
2. **`briefing-analyst-take-onprem.md` and the STATUS entry say `BRIEFING_ANALYST_TAKE_ONPREM` is
   OFF awaiting the operator gate.** The registry says **`on`**, and today's snapshot carries a real
   774-char on-box take. The gate was passed; the docs had not caught up. *(The edge fn
   `briefing-intel-snapshot` is at v21, updated 2026-08-26, consistent with the omit-when-null guard
   deploy having been run — **confirm the deployed source carries
   `if (row.analyst_take == null) delete row.analyst_take;` before any manual snapshot re-fire**, or
   the re-fire upserts NULL over the on-box take.)*
3. **`LOCAL-MODEL-LEVERAGE-MAP.md` §3 points at "ROLLOUT_STATUS W10 Stage 3, `⬜`".** There is no
   `⬜` anywhere in `ROLLOUT_STATUS.md` — the W10 Stage 3 (template library) and Stage 4 (LoRA)
   intentions live only in the prose of the **W10.1** row. Both are preserved in
   `PLANNED-BACKLOG.md` so the pointer's rot cannot lose them.
4. **P135 / P136 are written up as fixed.** The code fixes shipped and dry-ran clean, but **neither
   lane has yet produced a live write delta** (property-twin still 200/0-in-7d; reachability-harvest
   4/0-in-7d). By this repo's own rule that is *not yet* fixed in production. Carried as an open
   verify in `PLANNED-BACKLOG.md`. **Re-confirmed the same evening — both unchanged.**

### Added 2026-08-26 (evening) — three more, from picking up the W5.3 / Ollama-hygiene thread

5. **`ROLLOUT_STATUS.md` W5.3 says the Prompt-61 hardening is "VALIDATED at production quality"
   (NOI 89% / tenant 79%).** That number averages **three channels with different input types**,
   and the sidebar channel — **56% of rows, 0 hardened-schema extractions out of 350** — has never
   run the hardened prompt. The verdict is **unproven, not refuted**; the evidence offered cannot
   carry it. Row corrected in place. → `docs/audits/W53_INTAKE_CHANNEL_PROVENANCE_2026-08-26.md`
6. **The post-93 note "stamp coverage now 100% (87/87 backfilled)" reads as a fixed writer.** It
   was a **backfill**; the daily new-row rate decays straight back to zero (2026-08-26: **0 of
   21**). Carried as backlog **V6**.
7. **`CLAUDE.md` said "CI keeps the hard fail."** No workflow runs `npm test` on a PR at all — the
   4,551-test suite never executes in CI, which is how #1786 merged green with a red suite.
   Corrected in `CLAUDE.md`; fix scoped as backlog **N9** (✅ resolved 2026-08-27).

### Added 2026-09-02 — from landing B6e-ci-last5

8. **"The Dialysis code sits within 0.3% of the live reconciled model"** — in the prompt, the
   backlog, `CLAUDE.md` and `producer-health-and-ci-enforcement.md`. CC measured **−4.90%**, no
   segment within 1%. The decision it supported stands on the FY table; the figure was wrong on
   four pages and is corrected on all four.
9. **`PLANNED-BACKLOG.md` N9 still read "partially shipped and currently red"** six days after
   `CURRENT-STATE.md` §1 recorded it resolved and required. Two stores, one fact — corrected in the
   backlog.
10. **"Unmask the pytest line so a red suite fails a merge"** (B6e-ci-unmask's own wording) was
    only half true by construction: on Dialysis a red job blocks nothing without branch protection.
    Re-filed as **B6e-ci-required-check**.
11. **"Ruff is red on `main` with 11 errors"** — this file, `CLAUDE.md`, the backlog, the handoff
    and the canonical page all said it, from me, the same morning. **GitHub caps step annotations
    at ten.** Real count **5,746** (`ruff check .`), 1,293 files (`ruff format --check`). Corrected
    everywhere; ruff stays masked, which is the right call the wrong number would have reversed.

## 8. Where the history went (nothing was deleted)

| Archive | What it holds |
|---|---|
| `docs/history/CLAUDE_full_2026-07.md` | The full per-round worklog R5→R64 (ORE, CONNECTIVITY, UI phases, SF reconcile, T9d, CM) through 2026-07 |
| `docs/history/AGENTS_full_2026-07.md` | The AGENTS.md counterpart |
| `docs/history/STATUS_claude-code_2026-08-03_to_2026-08-12.md` | **New 2026-08-26** — the STATUS tail: comps arc prompts 19–60, Wave 8 hygiene, Wave 9 connectedness, ChatGPT/Copilot rollout |
| `docs/history/DOCS_CONSOLIDATION_2026-08-26.md` | **New** — what this consolidation moved, and the full preservation manifest |
| `docs/history/worklogs/` + its `INDEX.md` | **New 2026-08-27** — 31 one-off per-round worklogs moved verbatim from the **repo root**. ⚠️ Seven carried unfinished work, now recovered into `PLANNED-BACKLOG.md` P10 as **K13–K20** (five measured Capital-Markets chart defects among them). **P141 swept `docs/` and never looked at the root**, which is how they stayed invisible for 17 days. |
| `docs/history/INDEX.md` | The archive index |
| `docs/claude-code/done/` | Completed Claude Code prompts (66 files) |

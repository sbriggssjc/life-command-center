# Ownership & Sales — RE-AUDIT (2026-05-29)

Live, data-driven re-verification of every audit gap (G1–G15) + the original
symptoms, after the full remediation + the final sweep. Queries run against
dia (`zqzrriwuavgrquhisnoa`), gov (`scknotsqkcheojiaewwh`), LCC Opps
(`xengecqvemvfknjvbvrq`).

## Gap-by-gap verdict

| Gap | Verdict | Evidence (live) |
|---|---|---|
| **G1** cross-source sales dedup | ✅ resolved + structural | **0** live duplicate `dedup_natural_key` groups on dia **and** gov; UNIQUE partial index + B1 tick prevent new. |
| **G2** sales completeness | ✅ resolved | completeness views live; C9 ingest contract gates new captures; dia **0** live NULL-price / **0** NULL sale_date. |
| **G3** deed/parcel property_id | ✅ N/A | system uses the `property_public_records` join-table model (audit overstated). |
| **G4** owner-entity dedup at write | ✅ resolved | C4 BEFORE-INSERT trigger + A1 backfill + B2 owner-merge-tick. |
| **G5** SOS enrichment | ⬜ **workstation-bound** | framework live + `SPEC_sos_direct_scraper.md` turnkey (contract/skeleton/wiring); adapters can't be built/verified here (all 5 SOS sites **403** from remote env). |
| **G6** implausible cap rates | ✅ resolved (tiny lag) | gov: **2,756 / 2,777** out-of-band live cap rates tagged (`implausible_unverified` 1,395 / `stated_only` 1,337 / `market_implied` 24); **21** untagged await the nightly cap-rate-quality tick. Exclusion is a consumer-side filter on the quality tag. |
| **G7** unified_contacts | ✅ gov done + cutover LIVE; ⏳ dia partial | gov consolidation + Contacts cutover **live on the LCC hub** (29,640 rows). dia: `properties.unified_id` column added (+index); backfill staged (`A9b_dia_property_unified_id.mjs`); **3,189** dia org entities exist as targets; full coverage gated on BD-entity-sync activation. |
| **G8** owner→SF link | ✅ resolved | A7 backfill. |
| **G9** gov NULL-price ownership stubs | ✅ resolved + continuous | dia **0** live NULL-price; gov **3** — all *today's* `costar_sidebar` captures awaiting the hourly `sales_needs_review_tick` (self-healing by design). |
| **G10** ownership overlap/gap | ✅ resolved + structural | C5 `excl_oh_no_overlap` EXCLUDE governs 3,220 active rows (617 grandfathered residual review queue); B4 chain-tick for gaps. |
| **G11** silent ownership_research_queue failures | ✅ resolved | C6 neutralized both swallow sites. |
| **G12** RCM/LoopNet 401s | ✅ resolved | C8 (RCM live; lead-name sanitization on both paths). |
| **G13** sales chain continuity | ✅ resolved | B4 `v_sales_chain_breaks` + nightly tick → research tasks. |
| **G14** ownership-field priority rules | ✅ resolved | **0** owner-field ingestion gaps (drift detector); `ownership_source` now ranked (8 rules); 81+ owner rules total. |
| **G15** ownership provenance doc | ✅ resolved | `Ownership_Data_Provenance_Schema_Design.md` authored. |

## Original symptoms — re-verified

| Symptom | Verdict |
|---|---|
| Duplicates for the same sale | ✅ **0 live** on both domains; UNIQUE index + dedup tick prevent recurrence. |
| Missing many sale elements | ✅ completeness instrumented; C9 contract; 0 live NULL-price/date on dia. |
| Ownership history not in unison | ✅ CHECK + A6a auto-close trigger + C5 EXCLUDE — overlaps structurally impossible for non-grandfathered rows. |

## What genuinely remains (none remote-doable; all documented)

1. **G5 / C7 — SOS adapter implementation.** Workstation task (live SOS access + verify-before-enable). Framework + turnkey spec ready; 624 gov queue rows persist harmlessly. *(Owner action.)*
2. **G7-dia full backfill.** Run `A9b_dia_property_unified_id.mjs` for the ~1,159 authoritative links now; full ~9,488 coverage follows **activating the BD entity sync** (set dia/gov vault secrets on LCC Opps → `dia.true_owners.lcc_canonical_entity_id` populates → re-run). *(Owner action: workstation run + vault secrets.)*
3. **Self-healing residuals (auto-clear, no action):** gov 3 fresh NULL-price captures (hourly tick); 21 untagged out-of-band cap rates (nightly tick).
4. **Pre-existing, non-audit:** the Contacts "Engagement (High)" sort is a no-op (`engagement_score` always 0 — computed badge ≠ stored column). Minor UI bug, optional fix.

   **Investigated 2026-05-29 (not fixed — needs a product decision):** the stored `engagement_score` can't simply be backfilled. `computeEngagementScore()` scores from `last_call/email/meeting_date` + `total_calls`/`total_emails_sent`, but **all of those are empty on the hub** (0 rows) — gov only populated `total_touches` (16,946) and `last_activity_date` (16,892). The UI badge is derived from *those* fields, not the stored score. A real fix requires (a) a new scoring formula over `total_touches` + `last_activity_date` recency, (b) a product call on weighting, and (c) keeping the going-forward `computeEngagementScore` writers consistent with it — plus live UI verification. Out of audit scope; deferred as a scoped follow-up.

## Bottom line

**13 of 15 gaps fully resolved and verified; G6 with a 21-row tagging lag; G5 and the dia-half of G7 are the only open items — both gated on owner-side actions (live SOS access / vault-secret activation) the remote environment can't perform.** All three original symptoms are fixed and structurally protected. No new material issues surfaced.

## Re-audit query log → audit_run_log

This re-audit was read-only. Sweep changes recorded in audit_run_log: 56 (G14),
57 (G7-dia), 58 (G15), 59 (C7). A9 cutover: 54–55.

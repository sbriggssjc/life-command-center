# LCC Audit Rollout — Living Status Tracker
**Baseline audit:** `docs/audits/LCC_Data_Architecture_Audit_2026-07-29.md`
**Execution plan:** `docs/audits/LCC_Audit_Rollout_Plan.md`
**Convention:** update this file at the end of every working session (Cowork or Claude Code). One line per unit: status, date, session type, verification result. Statuses: ⬜ not started · 🟨 in progress · ✅ done · ⛔ blocked · 🚫 skipped (with reason).

## Wave 9 — Data connectedness (owner → contact → SF → Outlook; kickoff `docs/audits/W9_CONNECTEDNESS_KICKOFF.md`)

> **Progress ledger (2026-08-11, Cowork):** W9.3 LIVE 3 nights — re-score walked 1,200 of 23,817
> (32 auto-links, 8 review; conservative ratio steady); sf-assist walking the 3.3k pool 20/night
> post-92; donor handoff stamped 14 night one then stalled on a fixed scan window (→ prompt 93A).
> Donor-key coverage: gov 19 / dia 15 of the 2,305 addressable. W9.2 stays gated on that count
> climbing. **W5.3 re-grade (08-11): the hardened extraction prompt VALIDATED at production
> quality (OM NOI 89% vs pre-61 4%) — ollama-primary intake confirmed; attribution caveat →
> prompt 93B.**

Doctrine (inherited): value-gate everything (rank_value order); fill-blanks only; provenance + fsp rows in-migration; reversible; never fabricate a contact; human lanes for judgment; honest per-source counts; new units START with the house pattern (windowed scans, budget floors, crash-proof envelopes, batched lookups, loud per-source errors).

| Unit | What | Status | Notes |
|---|---|---|---|
| W9.2 | Contact-reachability internal harvest (Prompts 88/96/101) | ✅ **LIVE 2026-08-13 — flag `W9_2_REACHABILITY_HARVEST` ON (Cowork), after the `unified_contacts` name-accelerator lit up historical yield. FIRST live cron 04:40 UTC produced 4 real owner-email proposals (Boyd Watterson / UIRC / BLOOMINGTON IRS), health `green`, 0 dropped / 0 LLM / 0 applied — awaiting Scott's lane verdicts.** | Claude Code + Cowork gating. Wave 9 unit 1 (recommended-first: internal harvest, zero external dependency, reuses U3's verbatim-evidence pattern). Reuses the W8 U1–U5 house shapes wholesale (pure planner `api/_shared/reachability-harvest-planner.js`, GET dry-run / `?score=1&n=` inline sample / POST flag-gated apply tick, nightly cron, in-migration flag, reversible ledgers, bounded+resumable scoring, loud `scan_errors`) — does NOT fork a new pattern. **THE GAP (grounded live):** domain contacts with NEITHER email nor phone — dia **4,234/5,951 (71%)**, gov **10,542/15,434 (68%)** (the gov reachability count measured for the FIRST time this unit). **TWO ARMS, deterministic-first (the U5/85 lesson):** (1) **deterministic** (arm=deterministic, NO LLM, confidence 1.0, provider `none`) — the SAME person's synced record matched by an EXACT identity key (`sf_contact_id`/`salesforce_id`, batched `in.()` over BOTH domains, NOT name-fuzz) carries an email/phone the target lacks → a fill-blanks proposal with an exact source pointer; bulk-confirmable, excluded from any distribution guard. (2) **llm** (surface `clean_assist`) — an email/phone in an intake extraction snapshot naming this contact; ONE bounded name-indexed scan (never per-row); Ollama proposes with a **VERBATIM quote** (the W7.4/U3 validator: value must appear verbatim in the quote or the proposal is DROPPED → `reachability_harvest_dropped_log`, the precision floor); no evidence ⇒ no LLM call (counted `no_evidence`). **Value-gated** by owner-linked rank (owner-linked blanks: dia 1,955 / gov 4,313 drain first). Proposals → the **NEW `reachability_harvest_review` Decision Center federated lane** (all 6 touches wired — `_DC_FEDERATED` + SUBLANES + `_DC_FED_META` + render branch + `FEDERATED_DECISION_TYPES` + worklist-fetch + verdict branch + `/api/review-counts` lane; `test/w8-federated-lane-wiring.test.mjs` structural guard passes) with a **bulk-confirm for the deterministic fills only** (`dcFedBulkReachabilityFills`). A HUMAN **confirm** runs the **deterministic fill-blanks writer**: re-checks the field is STILL blank (a now-populated field → conflict card, **never a clobber**), fill-blanks the domain contact email/phone via `domainQuery` PATCH, stamps `field_provenance` via `lcc_merge_field` (source per arm: `w9_2_internal_harvest`@60 / `comms_observed`@40, both BELOW manual/recorded/salesforce/om_extraction), all logged to the reversible `reachability_harvest_apply_log`; **reject** keeps the row (rubric fuel). Migration `20260826120000_lcc_w9_2_reachability_harvest.sql` applied live to LCC Opps: `reachability_harvest_review` + `reachability_harvest_batch` + `reachability_harvest_apply_log` + `reachability_harvest_dropped_log` + `v_reachability_harvest_review_open` + `v_lcc_reachability_harvest_health` + **8 `field_source_priority` rows** (`v_field_provenance_unranked`=**0** for both sources — no drift) + flag **`W9_2_REACHABILITY_HARVEST` (OFF)** in-migration + nightly `reachability-harvest-tick` cron (**04:40 UTC**, after the W8 chain; no-ops while OFF). New route `/api/reachability-harvest-tick`. **⚠ Honest yield (grounded):** the deterministic arm is **input-starved today** — only 15 dia + 5 gov blank contacts carry an SF identity key (within-domain donors resolve to ~5/~4) — so it scales as W9.3's SF-linkage drain lands keys; the LLM arm has broad name coverage (4,128 + 10,542 named blanks) but fires only where an intake snapshot names the same person (609 owner + 606 seller + 8,228 broker snapshots). Tests: `test/reachability-harvest-planner.test.mjs` (22): arm routing, value-in-quote + verbatim validator, deterministic builder, cursor-resume stability, bounded scorer, + structural guards (batched in.() / single intake scan — no per-row fan-out; deterministic-first short-circuit; tick proposal-only; fill-blanks-writer conflict-not-clobber; fsp registration; flag OFF + staggered cron; pure planner). Dry-run sheet: `docs/audits/W9_2_reachability_harvest_dryrun_2026-08-08.md`. **Operator step before flip:** redeploy Railway → GET `/api/reachability-harvest-tick?score=1&n=8` (deterministic pointers + verbatim LLM quotes + `scan_errors:[]`), review, then flip `W9_2_REACHABILITY_HARVEST`→on. |
| W9.3 | SF linkage drain + live re-score (Prompt 90) | ✅ **LIVE since 2026-08-08 (all 3 flags ON). Verified healthy 2026-08-13 (Cowork reconcile — docs previously mis-stated OFF).** Live yields over 5 nights: **re-score** gov 2,000 / dia 2,000 scored → **~72 exact-unique auto-links applied** (gov 52 / dia 20), **1 conflict correctly guarded (not overwritten)**, 12 → `needs_review` lane — conservative (~1.8%), the null-guard working as designed; **SF assist** 80 annotation pre-ranks in `lcc_clean_assist_proposals` (source `w9_3_sf_assist`, never a verdict — zero curated-data writes); **donor handoff** unique-match SF-key fills accruing slowly (gov 5 last night; input-thin as expected — grows with re-score). | Claude Code + Cowork gating. Unlocks W9.2's input-starved donor pool (measured: only ~20 blank contacts carry an SF key; SF is where the emails/phones live — linkage is the unlock). **Three workstreams, house tick pattern throughout** (windowed / budget-floored / resumable / batched-lookup / loud `scan_errors` / flag-gated / reversible ledger). Migrations applied live: `20260827120000_lcc_w9_3_sf_linkage_drain.sql` (LCC Opps: 3 flags OFF + splink_v2/sf_account_contact_expansion `field_source_priority` rows + `lcc_flush_provenance_events` first-class allowlist extended + `lcc_w9_3_sf_assist_measure` + `lcc_record_sf_assist_agreement` + `v_lcc_w9_3_sf_assist_accuracy` (U4) + `lcc_w9_3_donor_coverage_log` + `v_lcc_w9_3_donor_coverage` + 3 staggered crons 04:50/05:10/05:30 no-op while OFF) + `government/`+`dialysis/` `20260827120000_*_w9_3_rescore_and_donor_ledgers.sql` (`w9_3_rescore_log` + `w9_3_donor_handoff_log`). New routes `/api/sf-link-assist-tick`, `/api/sf-link-rescore-tick`, `/api/sf-donor-handoff-tick` (mounted in server.js + admin.js). **WS1 assist:** the `sf_link_candidate` lane is mint-at-verdict, so — per prompt-80 — the Ollama pre-rank is stored in `lcc_clean_assist_proposals` (source `w9_3_sf_assist`, the store the lane already renders/attaches), **never a verdict** (`annotation-never-verdict` guard: the tick writes only that store, never PATCHes the queue or `entity_match_labels`). Nightly bounded (~20) resumable pass ranks each owner↔SF-account pair merge/not/uncertain + confidence + one-line evidence; the lane sorts **easy-first** (`attachSfLinkAssist`→`sfAssistSortKey`); each human verdict self-measures agree/disagree into `v_lcc_w9_3_sf_assist_accuracy`. **WS2 re-score:** no JS FS-math exists (W4.3 scored offline, never committed; libpostal-port drifts bands) — so the re-score reproduces W4.3's actual **exact/near-exact** auto-link tier **deterministically** (bands 0.9/0.1): exact clean-name UNIQUE→auto_link (0.99, `splink_v2` provenance, **null-guarded** — a different pre-existing id→conflict→review, never overwrite); exact-ambiguous or core-only→needs_review (assist-ranked lane); else no_match. Re-scored against the CURRENT registry (**16,210** accts, was 15,987 at W4.3 → +223 new). Resumable via `score_resolved`; reversible via `w9_3_rescore_log` + tag `w9_3_splink_v2_<date>_refreshed_registry`. **Live projected-match probe (top-200 gov no_match names vs the live registry, planner-identical normalization): 5/162 exact-unique auto_link (~3%), 0 ambiguous** — conservative gate validated (`postal realty trust`→unique account; REITs not force-linked). Backlog: gov 21,499 + dia 2,361 no_match. **WS3 donor handoff:** W9.2 keys on person-level `contacts.sf_contact_id`; W4.3/W9.3 land org-level owner `sf_account_id`/`sf_company_id`. The gap is an **account→contacts expansion** (a direct copy is semantically invalid) — enumerate the linked account's SF contacts from the local bridge (gov `sf_contacts_import` 34,002 / dia `salesforce_contacts` 5,002), **UNIQUE** name-match against the owner's blank contacts, **fill-blanks** stamp the real `sf_contact_id` (provenance `sf_account_contact_expansion`, reversible `w9_3_donor_handoff_log`; ambiguous skipped, existing never overwritten). **Acceptance metric (live baseline):** blank contacts carrying an SF key = gov **5** / dia **15** (=20, matches W9.2); **2,305 blank contacts already sit under an SF-linked owner** (1,813 gov + 492 dia) — the addressable pool TODAY, growing as WS2 lands links; `v_lcc_w9_3_donor_coverage` trends it — **rising = W9.2's unlock** → Cowork flips `W9_2_REACHABILITY_HARVEST`. SF stays minimum-necessary (no writes to SF; account=org-edge). Tests: `test/w9-3-sf-linkage.test.mjs` (44 pass): band conservatism (auto-link ONLY on exact-unique), annotation-never-verdict, donor propagation, ledger reversibility, agree/disagree measurement. `v_field_provenance_unranked` unchanged (33 pre-existing, none W9.3). Dry-run sheet: `docs/audits/W9_3_sf_linkage_drain_dryrun_2026-08-08.md`. **Operator step before flip:** redeploy Railway → GET the three ticks `?score=1`, review samples, then flip the 3 flags → on. |
| W9.1 | Contact-acquisition engine — STAGE 1, internal sources (Prompt 98) | ✅ **LIVE since 2026-08-12 (flag `W9_1_CONTACT_ACQUISITION` ON). Verified healthy 2026-08-13 (Cowork reconcile — docs previously mis-stated OFF).** Health green; nightly 04:55 UTC produced 5 open proposals (2 crossref attach / 3 mint+broker), 0 dropped, 0 applied — proposal-only, human-gated in `contact_acquisition_review`. Stage 2 (SOS-direct egress) remains OFF/walled (Prompt 99). | Claude Code + Cowork gating. Wave 9 unit 1 (the lever on the 68-73% no-contact gap). STAGED: this is **Stage 1 — INTERNAL sources only** (Stage 2 SOS-direct via non-datacenter egress is Prompt 99, pending Scott's infra call; web-search proxy stays PAUSED). **Pool** = ops `v_owner_contact_worklist` (value-ranked true owners with NO contact — **3,151 · 300 ≥ $1M · dia 628 / gov 2,523**). Per owner the engine runs a **pluggable, cost-ordered stage list, stopping at first success** (the Stage-2 seam): **1a crossref** (`lcc_resolve_owner_cross_reference` — a person already a contact under a related owner → ATTACH) + **1a institution** (`lcc_resolve_institution_contact` → ATTACH); **1b deed signatory** (a name in the owner's own deed text → MINT, VERBATIM-quoted); **1c broker-of-record** (the OM listing broker who sold the owner the building → MINT, ALWAYS typed `broker_of_record`, never a direct owner contact). **Proposal-only** — the tick writes ONLY the proposal/ledger tables; a **human verdict** resolves into the **ops entity graph** via the shared contact-attach helpers (`ensureEntityLink`/`linkPersonToEntity`/`stampContactOnActiveCadence`), reversible via `contact_acquisition_apply_log`. House pattern throughout: windowed+cursored pool walk (anti-joins its own proposals — 92-class), value-gated+capped, budget-floored, batched cross-DB reads (owner→property map built ONCE), per-stage counts + loud `scan_errors`, in-migration flag OFF, staggered nightly cron. **New Decision Center lane `contact_acquisition_review`** — fully wired (75-guard): admin.js FEDERATED set + fetchFederatedSource + review-counts lane + verdict; ops.js `_DC_FEDERATED` + count-map + lane list; dc-lanes.js meta + card + `dcFedBulkContactAttach` (bulk-confirm the deterministic attaches). **No new `field_source_priority` rows** — Stage-1 verdicts hit the ops graph, not domain-contact fields (`v_field_provenance_unranked` stays 0). Migration `20260812130000_lcc_w9_1_contact_acquisition.sql` applied live to LCC Opps (`contact_acquisition_batch`/`_review`/`_apply_log`/`_dropped_log` + open-view + health-view + flag OFF + cron `55 4 UTC`). New route `/api/contact-acquisition-engine-tick` (distinct from the R16 SF `contact-acquisition-tick`). **⚠ Honest yield (grounded live):** top-100-value pool → **6 crossref attaches (~6%)**, top-300 → 17; **institution 0** (registry thin), **deed 0** (`deed_records.raw_payload` carries a signatory on **0/5,771** gov deeds — the parser writes it to `property_documents`; wired for the signatory/OCR backfill), broker = a person-gated tail off 2,830 broker-bearing sales. Same "correct but input-thin, surface don't fake" posture as W9.2/W9.4. Tests: `test/contact-acquisition-planner.test.mjs` (21) — stage-order/stop-at-success, Stage-2 seam, attach-vs-mint routing, verbatim validator on deed quotes, broker_of_record typing guard, cursor/value-gate, read-only-until-verdict structural guards; guard suites 74/74. Dry-run: `docs/audits/W9_1_contact_acquisition_dryrun_2026-08-12.md`. **Operator step before flip:** redeploy Railway → GET `/api/contact-acquisition-engine-tick?score=1&n=10` (crossref attach proposals + `scan_errors:[]`) → review → flip `W9_1_CONTACT_ACQUISITION`→on. |
| W9.3 | SF linkage drain + re-score *(duplicate placeholder — see the LIVE W9.3 row above)* | ✅ | Superseded by the Prompt-90 W9.3 row above (LIVE 2026-08-08). Kept for the plan-numbering trace; feeds W9.2's deterministic arm by landing SF identity keys on blank contacts. |
| W9.4 | Comms-harvest arm (Prompt 94) | 🟡 **BUILT as the THIRD arm of the W9.2 tick — flag `W9_2_REACHABILITY_HARVEST` still OFF; grounded INPUT-STARVED today, yield gated on ONE upstream fix.** | Claude Code. Extends the live W9.2 tick / lane / fill-blanks writer / cron / flag with **correspondence (`activity_events`) as a third input source** — does NOT fork a unit ("extend, never fork"). **Three sub-arms, reusing the existing two-arm split:** (1) **header pairs → deterministic** (`parseHeaderAddress` over `metadata.from/to` + mailbox-mirror `from_email/to_emails/cc_emails`; a display name bound to a valid non-internal/non-generic email/phone matching a blank contact's normalized name → arithmetic fill, confidence 1.0, provenance `comms_observed`, source pointer = message id); (2) **signature phones → llm** (`extractSignaturePhones` over the body signature region → assembled under the sender name; the SAME verbatim-quote validator keeps only a proposal whose quote contains the name AND the value); (3) **create-contact → `target_kind='owner'`** (a thread participant attributable to an owner — ops entity → domain `true_owner` via `external_identities` — with ZERO contacts → propose-CREATE; minted ONLY via a human verdict, never auto; bulk-confirm excludes it). **Privacy-scoped:** harvests ONLY business-attributed, `visibility<>'private'` threads (`commsRowHarvestable`). **⚠ Grounded honestly (live 2026-08-12):** the arm is correct + complete but **input-starved TODAY** — of 7,751 business-attributed correspondence rows, **0 carry a header display name** (Outlook ingestion flattens Graph `{name,address}` → bare email), so header pairs = 0 and the 2,410 phone-bearing signature bodies are **not keyable** (no name key); and the 309 linked correspondence entities are deals/properties, **0 map to a `true_owner`**, so create-contact = 0. **The single unlock (recommended next):** preserve the header display name at Outlook ingestion (`metadata.from_name`/`to_names` on the mirror + loggers, forward-only) — that one field lights up all three sub-arms. Same honest posture as W9.2's input-starved deterministic arm / SOS-yields-nothing. **Verdict path:** `target_kind='owner'` confirm MINTS a domain contact (name+email+phone, `data_source='comms_observed'`), stamps name/email/phone provenance, idempotent (dup email under owner → conflict), reversible (`reachability_harvest_apply_log`, reversal.record_id = new contact_id). Migration `20260827120000_lcc_w9_4_comms_harvest.sql` applied live: 2 NAME-field `comms_observed@40` `field_source_priority` rows (`v_field_provenance_unranked`=0 for the reachability fields) + flag notes refreshed (3 arms); **no new table**. Tests: `test/reachability-harvest-planner.test.mjs` (34, all pass) — header extraction, privacy-scope exclusion, phone regex + verbatim validator, create-contact-never-auto, arm routing, cursor walk, structural guards; `w8-federated-lane-wiring` (10) + `operations-subroutes` (5) green. Dry-run + grounding: `docs/audits/W9_4_comms_harvest_dryrun_2026-08-12.md`. **Operator step before flip:** land the header display-name ingestion capture → redeploy → `GET /api/reachability-harvest-tick?score=1&n=10` shows non-zero `comms_counts` → review → flip `W9_2_REACHABILITY_HARVEST`→on (one flag, three arms). **➡️ UNLOCK SHIPPED (Prompt 96, 2026-08-12):** the four Outlook correspondence writers (`handleOutlookSent`/`outlook_sent`, `handleOutlookMessage`→`logInboundCorrespondenceDualAnchor`/`outlook_inbound`, `handleOutlookMessageExtract`/`outlook`, `intake-tagged-comm`/`outlook_tagged`) no longer flatten Graph's `{name,address}` — they persist `metadata.from_name` + `metadata.to_names[]` (name↔email pairs) via the shared `api/_shared/outlook-recipients.js` parser (accepts Graph object arrays + `Name <email>` delimited strings; additive jsonb, fill-blanks, never fabricates). `harvestBuildCommsIndex` now binds those structured pairs directly. **Forward-only** (no backfill of the 7,751 bare-header rows). FROM name is code-only-live (already in the payload); TO/CC names need a one-time PA flow change (exact Select+Join steps in `docs/audits/W9_4_display_name_capture_2026-08-12.md`). Second finding (`0 correspondence entities → true_owner`) probed: **separate linkage gap** (correspondents are parties/deals, not owner LLC entities), orthogonal to names — flagged for a follow-on, not scoped here. Tests `test/outlook-recipients.test.mjs` (15) + full suite green. Flag stays OFF pending accrual + the score=1 verify. **➡️ ACCELERATOR SHIPPED + APPLIED LIVE (Prompt 101, 2026-08-13):** Prompt 96 was forward-only, so accrual was weeks-slow (0 `from_name` on the 7,051 historical outlook rows). **⚠ The accelerator's premise was REFUTED live** — `email_bodies.from_name` is populated on **0/23,071** rows (the Prompt-96 doc's "already stored" claim was wrong); no structured historical display name exists. The real source is **`unified_contacts`** (17,527 named). New `api/_shared/outlook-name-backfill.js` (pure builder, reuses `outlook-recipients.js` `parseAddress` — one code path) + route `GET/POST /api/outlook-name-backfill` (cursored/resumable, `?reverse=1&batch=`) + migration `20260813120000_lcc_outlook_name_backfill_log.sql` (audit ledger + case-insensitive RPC `lcc_names_for_emails`, applied live). Reconstructs `metadata.from_name`/`to_names` from the EMAILS ALREADY ON THE ROW → `unified_contacts`, fill-blanks, provenance-marked `metadata.name_backfill.source='unified_contacts'`, reversible, idempotent. **Applied live (batch `nb_sql_20260813`):** `from_name` **0 → 2,357**, `to_names` rows **→ 2,438** (3,768 recipient pairs), **simulated `header_name_pairs` 0 → 6,050**. The harvest reads live `activity_events`, so `GET /api/reachability-harvest-tick?score=1&n=10` now shows non-zero `comms_counts.header_name_pairs` (harvest's attribution gate applies → subset of 6,050, but decisively >0). Tests `test/outlook-name-backfill.test.mjs` (13) + full suite green. Doc `docs/audits/W9_4_name_backfill_accelerator_2026-08-13.md`. **Cowork:** review the now-populated `?score=1` sample → flip `W9_2_REACHABILITY_HARVEST`→on (real historical yield, not future-only). |
| W9.5 | Propagation-integrity tick (Prompt 97) | ✅ **LIVE — read-only measure, no flag (nothing to gate).** | Claude Code. Wave 9 unit 5 ("measure, last"): the standing cross-DB link-coverage audit that proves "full propagation" stays true. **NO LLM anywhere — pure counts.** Pure planner `api/_shared/link-coverage.js` (IO-free, fixture-tested); tick `GET/POST /api/link-coverage-tick` (GET = computed JSON dry-run, POST = also persist the monthly snapshot; read-only otherwise). Migrations applied live: `20260828120000_lcc_w9_5_link_coverage.sql` (LCC Opps: `v_lcc_w9_5_link_coverage` — mirror-consistency + correspondence links + canonical conformance; `lcc_w9_5_link_coverage_snapshot` MoM-delta store, `period` UNIQUE) + `dialysis/`+`government/` `20260828120000_*_w9_5_chain_coverage.sql` (`v_{dia,gov}_w9_5_chain_coverage` — per-link recorded→true→contact→reachable→SF, counts-only/no-PII, anon-granted). The tick UNIFIES all three + a tick-computed owner→ops-mirror row into one coverage table (per link: link_name, domain, group, total, linked, pct). **Reports INTO U4** — new **Connectedness section** in the systemic-findings aggregator (`buildConnectednessSection`), wired into `assembleReport` builders; severity heuristic = **a link whose pct DROPS month-over-month → high + a fix-unit stub naming the failing producer** (a low-but-stable link is the campaign's known work, not a regression). The monthly snapshot rides the **U4 cron POST** (no second cron; `fetchSystemicFindingsInputs` computes coverage, U4 apply-path persists it). **Grounded live baselines (2026-08-12):** chain `true_to_contact` dia 32.1% (2,280/7,107) / gov 27.0% (4,398/16,270); `contact_to_reachable` dia 29.1% / gov 32.3%; `true_to_sf` dia 16.9% / gov 11.8%; `contact_to_sf_contact_id` dia 6.6% / gov 5.8%; mirror `domain_owner_identity_entity_bound` 100% (0 dangling), `external_identity_canonical_conformance` 100% (0 banned spelling), `owner_to_ops_mirror` dia 92.4% (6,568/7,107) / gov 54.8% (8,912/16,270); correspondence `correspondence_to_entity` 98.8% (7,746/7,837), **`correspondence_entity_owner_llc` 2.5% (6/241)** = the prompt-96 parties-vs-owner-LLC split baseline for the linkage follow-on. Tests: `test/link-coverage.test.mjs` (18: pct/row/delta, weakest-link, regression severity, U4 section wiring, read-only + counts-only structural guards) + `test/systemic-findings.test.mjs` updated (9→10 sections). **Operator step:** redeploy Railway → GET `/api/link-coverage-tick` for the live unified table; U4's next monthly report (1st 05:00 UTC) carries the Connectedness section with these baselines + first deltas from month 2. |
| W9.6 | Correspondence→owner-LLC attribution (Prompts 102/103) | ✅ **LIVE 2026-08-13 — flag `W9_6_COMMS_OWNER_ATTRIBUTION` ON (Cowork), after Prompt 103's Path-B precision pass + a live re-review of the tightened sample. Nightly cron 05:05 UTC proposes owner-attribution edges (Path A property bridges + tightened Path B) into `comms_owner_attribution_review` — proposal-only, human-gated, reversible.** **Prompt 103 precision (verified live):** internal-team exclusion (reused `INTERNAL_DOMAINS` — Scott/Toby never an owner subject), brokerage-target guard (`lcc_is_brokerage_owner_name`, drops brokerages mislabeled `true_owner` — logged as a KNOWN upstream ORE labeling issue), tie-tightening (`relationship` → ownership/employment roles only; `active_contact` kept). Path B clean **28** (0 internal / 0 brokerage; Boyd Watterson/Kingsbarn/Realty Income/Easterly survive); dropped-when-included 13 (brokerage 10 / internal 2 / loose 1). Path A unchanged (3). 20 tests green. Also Part B: `folder_feed_lease` fsp rows registered (drift 39→34 baseline). Branch `claude/w9-6-path-b-precision-uwsds6` (PR #1717). | Claude Code + Cowork gating. The last major INTERNAL linkage gap: correspondence is stamped with the deal/party/property entity the resolver found (brokers/buyers/sellers), never the owning LLC → W9.5 measured `correspondence_entity_owner_llc` at just **2.5% (6/241)**. Closes it via **two deterministic-first paths → pure planner `api/_shared/comms-owner-attribution.js` → proposals**: **Path A `property_bridge`** — a correspondence entity that resolves to an ASSET → its single current `true_owner` via the ops owns edge (arithmetic, conf 1.0, unambiguous-only, value-ranked by owner portfolio rank); **Path B `person_match`** — a correspondent PERSON tied to a single owner via `owner_contact_pivot` active contact or an unambiguous person→owner edge (verbatim correspondent header evidence; a shared-token-only name bridge is REJECTED — the W9.1 false-bridge lesson). **Mechanics (full house pattern):** tick `/api/comms-owner-attribution-tick` (`?score=1&n=` dry-run, POST flag-gated, per-path counts, loud `scan_errors`, budget cap, batched joins); NEW DC lane `comms_owner_attribution_review` fully 75-wired (backend set/subject-ref/count/fetch/verdict + frontend `_DC_FEDERATED`/SUBLANES/`_DC_FED_META`/render). **Confirm = a deterministic writer** that appends the owner ops entity to the correspondence rows' `metadata.linked_entity_ids` (reversible ledger + `field_provenance` `comms_owner_bridge`) — that ONE anchor feeds BOTH consumers: the owner-record correspondence history AND the W9.2/W9.4 reachability create-contact arm (the arms compound). **Verified live (LCC Opps):** migration `20260829120000` applied; flag OFF (off_since 2026-08-13); fsp row on `public.activity_events.linked_entity_ids @ priority 45 record_only` (`v_field_provenance_unranked` shows NO `comms_owner_bridge` drift); **W9.5 baseline held at exactly 6/241 = 2.5%** (owner-restricted union — pre-existing non-owner links do NOT dilute; it rises only as owner bridges confirm). Path A 3 candidates / Path B 40 unambiguous (16 pivot / 24 relationship) live; high-value owners lead (Boyd Watterson rank 1175, Kingsbarn). Honest finding: some pre-existing brokerage-as-owner entities appear at rank 0 for the human to reject — noise the confirm lane catches, never auto-attributed. Tests: `test/comms-owner-attribution.test.mjs` (14) + lane-wiring/partition/subroute guards — 27 green. Metric: W9.5's `correspondence_entity_owner_llc` extended to count owner bridges via `linked_entity_ids`. Branch `claude/comms-owner-attribution-6flfnt` (PR #1714). Dry-run: `docs/audits/W9_6_comms_owner_attribution_dryrun_2026-08-13.md`. **Operator step before flip:** redeploy Railway → GET `/api/comms-owner-attribution-tick?score=1&n=20` → review → flip `W9_6_COMMS_OWNER_ATTRIBUTION`→on. |

## Wave 10 — Voice & drafting (Scott's authored corpus → grounded BD/response drafts; kickoff `docs/audits/W10_VOICE_AND_DRAFTING_KICKOFF.md`)

Doctrine (inherited): never auto-send (every output is a DRAFT for Scott's edit); never fabricate facts (voice = HOW, the LCC deal spine = WHAT); strategy stays VERBAL (offer-submission doctrine); corpus-cleaning is a data-quality step FIRST; the distill pass runs ON-PREM via ollama (Scott's decade of client mail never egresses); reversible (the profile is a versioned, regenerable doc). Four staged builds — start light, earn the heavy step.

| Stage | What | Status | Notes |
|---|---|---|---|
| 1 | Voice PROFILE (no training, cheap, reversible) — Prompt 100 | ✅ **SHIPPED — no drafting surface changed; awaiting Scott's read of `BRIGGS-WRITING-VOICE.md` before Stage 2.** | Claude Code. Corpus located live (LCC Opps 2026-08-13): **~926 distinct Scott-authored sent emails** (`activity_events` `outlook`/`outlook_sent` + `email_bodies`, dedup by internet-message-id; sabriggs@/teambriggs@northmarq.com + Stan-Johnson-era, family excluded), Nov 2022→Aug 2026. **⚠ Honest cap finding:** the store keeps only Graph `bodyPreview` (~255-char cap); `body_text`/`body_html` are empty → the real signal is Scott's email *openings* (~31 words), strong for greeting/opening voice, LOW-confidence for sign-offs/long-form (flagged, not faked). Context mix (493 distinct): internal reply 219 / external reply 180 / internal new 75 / cold-BD outreach just 14 (THIN — flagged); 81% replies. Deliverables: pure `api/_shared/voice-corpus-clean.js` (strips reply chains, inline Briggs sig block, disclaimers, mobile sigs; deterministic `classifyDraftType`; **19 tests**), on-prem `scripts/voice-distill.mjs` (calls ollama directly, **refuses to run if `OLLAMA_URL` unset** — corpus never egresses), `BRIGGS-WRITING-VOICE.md` (overall voice + per-context variants, each with anonymized evidenced excerpts + the U4 draft-vs-sent edit-distance hook for Stage 2). v1 authored from deterministic SQL + a small anonymized opening sample — **no LLM read the prose**; the ollama distiller is the operator's on-prem enrichment step (same "mechanism built, heavy pass is Scott's" pattern as SOS/SAM). Branch `claude/voice-profile-scott-corpus-qofank` (PR #1715). **Scott's step:** read `BRIGGS-WRITING-VOICE.md` — does it sound like you? — before any Stage 2 build. |
| 2 | Retrieval-grounded drafting (RAG over the sent corpus) | ⬜ | Future prompt. Retrieve Scott's 3–5 nearest PAST examples of a draft type (embedding-KNN over the cleaned corpus) + deal facts from the spine → draft grounded in both; lands in Outlook Drafts / Cowork, never sent. Gated on Scott's read of the Stage-1 profile + the corpus-cap question (the 255-char preview limits long-form RAG until fuller bodies are ingested). |
| 3 | Template library (the "automatic templates" ask) | ⬜ | Future prompt. Cluster the corpus by draft-type; synthesize a Scott-voiced parameterized template + the trigger that offers it (new listing → announcement draft; LOI → offer-submission already fires). |
| 4 | LoRA fine-tune on GaryBuilt (optional, playbook phase 3) | ⬜ | Last resort — only if Stage 1–2 few-shot voice isn't tight enough. Profile+RAG likely suffices; fully reversible. |

## Wave 8 — Ollama data-hygiene campaign (Scott's directive; kickoff `docs/audits/W53_AND_OLLAMA_HYGIENE_KICKOFF.md` Part 2)

Doctrine: Ollama PROPOSES; a deterministic gate or a human lane DECIDES. No LLM in auditable gates. Evidence-grounded (verbatim quote). Never hard-delete; never retire an FK-referenced row. Reversible ledgers, flag-gated. Four units, sequenced (U1 → U4), each its own prompt.

| Unit | What | Status | Notes |
|---|---|---|---|
| U1 | Junk-entity pre-screen (Prompt 62) | ✅ 2026-08-07 | Claude Code. Extends the prompt-32 clean-assist machinery (does NOT fork a parallel agent). Migration `20260807120000_lcc_w8_u1_junk_entity_prescreen.sql` applied live to LCC Opps: `junk_entity_review` (proposals) + `junk_review_batch` (reversible ledger) + `v_junk_entity_review_open` (federated-lane source) + `v_lcc_junk_prescreen_health` + flag **`W8_U1_JUNK_PRESCREEN` (OFF)** registered in-migration (36y) + nightly `junk-prescreen-tick` cron (03:40, no-ops while OFF). New route `/api/junk-prescreen-tick` (GET dry-run / `?score=1` inline model sample / POST apply, flag-gated) + `junk_entity_review` Decision Center federated lane (confirm→reversible soft-retire; **FK-referenced→conflict card, never delete**; rename/parse_contact→edit lane; keep→close). Deterministic candidate filter (`api/_shared/junk-prescreen.js`, NO LLM) + Ollama scores ONLY the candidate pool via `invokeExtractionAI`, few-shot-grounded on accrued `junk_entity_name` human verdicts. **Live scope pass (2026-08-07):** candidates — ops entities ~194 (90 all-digits + 104 too-short), dia 19 (ro 1 / to 1 / contacts 17), gov 8 (ro 2 / to 3 / contacts 3). Tests: `test/junk-prescreen.test.mjs` (28) + partition guard updated. Flag OFF ⇒ cron no-ops; zero writes without a human verdict. **Operator step before flip:** GET `/api/junk-prescreen-tick?score=1` for a sampleable proposal sheet, then flip `W8_U1_JUNK_PRESCREEN`→on. |
| U2 | Duplicate proposals → resolver fuel (Prompt 63) | ✅ 2026-08-07 | Claude Code. Reuses U1's shapes (pure planner module, GET dry-run / POST flag-gated apply tick, nightly cron, in-migration flag) — does NOT fork a new pattern. **Doctrine: dupes are the RESOLVER's job (Fellegi-Sunter `/match` + `entity_match_labels`); Ollama emits CANDIDATE PAIRS only — ZERO merges from this unit.** Targets the blocking blind spot: a deterministic near-miss generator (`api/_shared/dup-pair-planner.js`, NO LLM) over ops `entities` + gov/dia `true_owners` finds pairs the resolver's token-blocks skip — (a) high trigram/levenshtein name similarity with **no shared word token**, (b) same normalized mailing address / different names, (c) abbreviation/expansion (DVA↔DaVita, initials). Excludes pairs already in `w8_u2_dup_pair`, already labeled (`entity_match_labels` seeder w8u2), or merged; capped per run. Ollama gives a same_party/distinct/unsure second look via `invokeExtractionAI({surface:'clean_assist'})`; **unsure + below-`DUP_PAIR_MIN_CONFIDENCE` (0.6) are DROPPED (counted only)** — the LLM verdict writes nothing. Proposable pairs land in the **EXISTING `owner_reconcile` resolver review pool as a 4th folded seeder `w8_u2_ollama_pair` (NOT a new lane)**; a human verdict flows exactly as today → `writeEntityMatchLabel` (`same_party`/`distinct`, both training fuel incl. the hard-negatives W4.3 flagged missing) → nightly W4.4 retrain. The verdict branch dispositions the `w8_u2_dup_pair` row + writes the label and **never calls `lcc_merge_entity`**. Migration `20260807160000_lcc_w8_u2_dup_pair_proposals.sql` (LCC Opps): `w8_u2_dup_pair` (proposals, status CHECK has no 'merged') + `w8_u2_dup_pair_batch` (reversible ledger) + `v_w8_u2_dup_pair_open` (seeder-D source) + `v_lcc_w8_u2_dup_pair_health` + flag **`W8_U2_DUP_PAIRS` (OFF)** in-migration (36y) + nightly `dup-pair-tick` cron (03:50, no-ops while OFF). New route `/api/dup-pair-tick` (GET dry-run / `?score=1` inline sample / POST apply, flag-gated). **W4.4 corpus reader: the `w41-corpus-export` edge fn reads `entity_match_labels` with no seeder allowlist to update, so `seeder='w8_u2_ollama_pair'` rows are picked up automatically** (no in-repo seeder-IN list gates them). Tests: `test/dup-pair-planner.test.mjs` (43). Flag OFF ⇒ cron no-ops; zero writes without a human verdict; zero merges ever. **Operator step before flip:** GET `/api/dup-pair-tick?score=1` for a sampleable pair sheet, then flip `W8_U2_DUP_PAIRS`→on. |
| U1 | Junk-entity pre-screen (Prompts 62/64/65/66/67) | ✅ **LIVE 2026-08-07 13:51 UTC** — flag `W8_U1_JUNK_PRESCREEN` flipped ON by Cowork after FIVE gated dry-runs, each catching a distinct failure before any write: (1) precision — LLM parroted heuristics, real SPEs/acronyms proposed dismiss → **64** SPE/abbrev/connection/address guards + judge-don't-parrot rubric; (2) scope — pool exploded to 6,946, abbrev/address classes flag real entities → **65** true-junk-only candidacy, ~6.5k `naming_hygiene_backlog` counted-not-enqueued (future rename unit), scan-time connection exclusion, keeps never persisted; (3) latency — ollama 16s/call 502'd the proxy → **66** `&n=`/budget-bounded inline scoring + resumable `JUNK_SCORE_BATCH_SIZE` cron batches + `junk_prescreen_scored` cursor ledger (mig `20260807140000` live); (4) calibration — 0.5 dismiss-share guard blocked the now-correct pre-filtered outcome → **67** `JUNK_DISMISS_GUARD_THRESHOLD` (0.9) + deterministic surname guard (WALDSCHMITT fixture). Fifth run PASSED all acceptance on ollama/qwen2.5:14b (SPE→keep, surname→keep 1.0, garbage/orphans→dismiss w/ independent evidence, distribution clean). Nightly 03:40 cron drains ~25/night (~247 pool ≈10 nights) → Decision Center junk-review lane; every verdict human, every write reversible. | Claude Code. Extends the prompt-32 clean-assist machinery (does NOT fork a parallel agent). Migration `20260807120000_lcc_w8_u1_junk_entity_prescreen.sql` applied live to LCC Opps: `junk_entity_review` (proposals) + `junk_review_batch` (reversible ledger) + `v_junk_entity_review_open` (federated-lane source) + `v_lcc_junk_prescreen_health` + flag **`W8_U1_JUNK_PRESCREEN` (OFF)** registered in-migration (36y) + nightly `junk-prescreen-tick` cron (03:40, no-ops while OFF). New route `/api/junk-prescreen-tick` (GET dry-run / `?score=1` inline model sample / POST apply, flag-gated) + `junk_entity_review` Decision Center federated lane (confirm→reversible soft-retire; **FK-referenced→conflict card, never delete**; rename/parse_contact→edit lane; keep→close). Deterministic candidate filter (`api/_shared/junk-prescreen.js`, NO LLM) + Ollama scores ONLY the candidate pool via `invokeExtractionAI`, few-shot-grounded on accrued `junk_entity_name` human verdicts. **Live scope pass (2026-08-07):** candidates — ops entities ~194 (90 all-digits + 104 too-short), dia 19 (ro 1 / to 1 / contacts 17), gov 8 (ro 2 / to 3 / contacts 3). Tests: `test/junk-prescreen.test.mjs` (28) + partition guard updated. Flag OFF ⇒ cron no-ops; zero writes without a human verdict. **Operator step before flip:** GET `/api/junk-prescreen-tick?score=1` for a sampleable proposal sheet, then flip `W8_U1_JUNK_PRESCREEN`→on. |
| U2 | Duplicate proposals → resolver fuel (Prompts 63/68) | ✅ **LIVE 2026-08-07 16:26 UTC** — flag `W8_U2_DUP_PAIRS` flipped ON by Cowork after two gated dry-runs. Run 1 failed (similarity degenerate on generic CRE vocab, gov/dia silent zero-coverage — root cause: missing/misnamed address columns 400ing at PostgREST swallowed to `records:0`, value inversion: easy-distinct persisted while Invester↔Investar typo-dupe dropped unsure). **68 fixed all three:** distinctive-core similarity (generic-noun stoplist + `coreIsPairable`, initials unpairable by name), loud `scan_errors` + resumable keyset cursors (lcc 60k paged; dia full/wrapped), three-way disposition (propose / needs_human for high-sim unsure / drop). Run 2 passed: NorthStar↔North Star Realty→same_party, Harrison↔Garrison→distinct (hard negative), MAINSTREET↔Main Street Group→needs_human, dropped_unsure 0. Nightly 3:50 cron 25/night → owner_reconcile lane (seeder `w8_u2_ollama_pair`) → human verdicts → `entity_match_labels` → W4.4 nightly retrain (corpus reader has no seeder allowlist — consumed automatically). Zero merges structurally enforced. **Watch:** abbrev_expansion class (84% of generated) unsampled — eyeball first lane batches. | Claude Code + Cowork gating. |
| U3 | Connection propagation — evidence-grounded link proposals (Prompts 69/71/72) | ✅ **LIVE — flag ON 2026-08-07 (Cowork), after 3 gated dry-run rounds.** 71 added per-source evidence counts + loud scan_errors + first-class `different_people` verdict (confirm → merge candidate resolved distinct + `entity_match_labels` seeder `w8_u3_shared_email`); 72 fixed the exposed evidence-column bugs (gov deed PK `deed_id` vs dia `id`; activity `id`/`title`; intake dia/gov alias footgun → `in.(gov,government)`). Final verify post-deploy: scan_errors [], deed 83 / activity 12 evidence hits, honest no_evidence where evidence doesn't name the party. Nightly 04:10. Watch: chain `intake` source 0/60 (measurable via per-source counts). | Claude Code. Reuses U1/U2's shapes (pure planner `api/_shared/link-propagation-planner.js`, GET dry-run / `?score=1&n=` inline sample / POST flag-gated apply tick, nightly cron, in-migration flag, bounded+resumable scoring) — does NOT fork a new pattern. **PREMISE CORRECTION (verified live):** `lcc_chain_unresolvable` is EMPTY (0 rows) — built against the LIVE gap surfaces instead: `v_ownership_chain_worklist` (**3,405**: developer_unidentified 1,226 / no_prior_owners_recorded 2,179; 3,249 valued rank>0) + `v_lcc_person_email_merge_candidates` (**258**). **Doctrine: Ollama PROPOSES a missing link ONLY from evidence LCC already holds, quoted VERBATIM (W7.4 validator — the quote must be a whitespace-normalized substring of the supplied evidence or the proposal is DROPPED + logged to `w8_u3_dropped_log`, never shipped). NEVER fabricates: no evidence ⇒ NO LLM call (`no_evidence` counter). No web search (`owner-contact-websearch` PAUSED — internal evidence only).** Value-gated (valued pool drains first, rank order); evidence assembly (deterministic, bounded topK/char-capped) from domain sale/deed notes + ops activity/correspondence, with a resumable evidence-hash scored-marker so a row re-scores only when its evidence changes. Ollama gives `link_proposal` / `no_evidence_found` via `invokeExtractionAI({surface:'clean_assist'})`; survivors (validated + ≥`LINK_MIN_CONFIDENCE` 0.6) land in `w8_u3_link_review` → Decision Center **`w8_u3_link_review` federated lane** (a NEW lane; added to `FEDERATED_DECISION_TYPES` + ops.js `_DC_FEDERATED`). A HUMAN **confirm** runs the **deterministic writer**: resolve/mint the proposed entity, create the `entity_relationships` edge (`developed`/`owns`), stamp provenance via `lcc_merge_field` (source `w8_u3_link_propagation`@85, below all record sources), all logged to the reversible `w8_u3_link_apply_log`; **reject** keeps the row; a person-email-merge confirm routes to the resolver (research_task, never a silent merge). Migration `20260807180000_lcc_w8_u3_link_propagation.sql` applied live to LCC Opps: `w8_u3_link_review` (verdict CHECK is `link_proposal`/`no_evidence_found` — no merge verdict) + `w8_u3_link_batch` + `w8_u3_link_apply_log` + `w8_u3_dropped_log` (precision floor) + `v_w8_u3_link_review_open` + `v_lcc_w8_u3_link_health` + **2 `field_source_priority` rows** (`entity_relationships.developed`/`.owns`@85) + flag **`W8_U3_LINK_PROPAGATION` (OFF)** in-migration (36y) + nightly `link-propagation-tick` cron (**04:10**, after U1 03:40 + U2 03:50; no-ops while OFF). New route `/api/link-propagation-tick`. **Live verify: `v_field_provenance_unranked`=33 — ALL the pre-existing W6.6 baseline (costar_sidebar 18 / om_extraction 10 / rca_sidebar 3 / …), ZERO `w8_u3_link_propagation`** (my source is ranked; U3 adds no drift). Tests: `test/link-propagation-planner.test.mjs` (36): value-gate ordering, evidence-assembly bounds, verbatim validator (accept substring / reject paraphrase+fabrication), no-evidence short-circuit, proposal normalization + confidence gate, evidence-hash stability, + structural guards (writer never merges, tick never auto-writes edges, no web search, fsp row + flag-off in migration). **Operator step before flip:** GET `/api/link-propagation-tick?score=1&n=6` for a sampleable sheet where every would-propose carries `quote_verbatim=true`, then flip `W8_U3_LINK_PROPAGATION`→on. | Claude Code. |
| U4 | Systemic-findings monthly report (Prompts 70/73) | ✅ **LIVE — flag ON 2026-08-07 (Cowork). WAVE 8 BUILD-OUT COMPLETE (4/4 units live).** 73 hardened the tick after a first-run 502: crash-proof JSON envelope, inline narrate retired from GET (narration only on POST/cron, budget-bounded), per-section try/catch → loud section_errors. Final verify: fast GET, 46 findings (6 critical/7 high), section_errors [], 18 ready-to-send fix-unit stubs. Day-one systemic findings for the fix backlog: PGRST204 schema drift (dia 3,702 + gov 3,243), `Unflag Completed Email Tasks` flow 524 fails (flow supposedly retired — zombie-logging?), dia 23505 dedup collisions 1,837, chain completeness 8.2%. | Claude Code. Wave 8 build-out COMPLETE. The campaign's REPORTING layer: aggregates the systemic-defect signal streams already logged but never synthesized into ONE monthly doc that feeds the W6.6 audit. Reuses the U1/U2/U3 shapes (pure aggregator `api/_shared/systemic-findings.js`, GET dry-run / `?narrate=1` / POST flag-gated tick, in-migration flag OFF, monthly no-op-while-off cron) — does NOT fork a new pattern, and creates **NO new review lane** (the doc IS the consumer, opened as a `research_tasks` artifact `systemic_findings_report`). **Doctrine (W7.2): numbers are computed DETERMINISTICALLY (no LLM) from the `v_lcc_w8_u4_*` views; Ollama drafts ONLY the narrative FROM the computed table, and the FIGURE VALIDATOR (`validateFigures`) drops any prose whose numbers don't all match a computed value — regenerate once, then ship the raw tables. Never a fabricated number.** Eight sections: ingest/flow failure clusters (grouped by error signature, top-N, each with a candidate Claude Code fix-unit), provenance drift (baseline 33) + conflicts, chain completeness + U3 drain, precision floors (dropped ratios), U1/U2 lane throughput + human accept rates, naming-hygiene backlog (from the latest U1 apply snapshot), extraction provider mix (post-61 `_provider` stamp). Month-over-month deltas via a persisted `lcc_w8_u4_findings_snapshot` row (deltas exist from month 2). Migration `20260807190000_lcc_w8_u4_systemic_findings.sql` applied live to LCC Opps: snapshot table + 10 aggregation/health views (`v_lcc_w8_u4_*`) + flag **`W8_U4_FINDINGS_REPORT` (OFF)** in-migration (36y) + monthly `systemic-findings-tick` cron (**1st 05:00 UTC**, after the nightly chain; no-ops while OFF). New route `/api/systemic-findings-tick`. **First doc generated + committed: `docs/audits/systemic-findings/2026-08.md`** (46 findings; 6 critical / 9 high / 10 medium; deterministic header — model narrative attaches on the first cron run). Live grounding: ingest_write_failures 11,890 (top cluster: PGRST204 schema-drift 3,722 dia + 3,271 gov), flow_run_failures 829, chains 311/3,783 complete (8.2%), provenance unranked 33 = baseline (0 drift), extraction 2/567 provider-stamped (attribution gap flagged). Honest zeros where U1/U2 lanes still empty. Tests: `test/systemic-findings.test.mjs` (34): per-section builders, figure validator (match/mismatch/format-tolerant), snapshot/delta, doc renderer, + structural guards (no new lane, flag OFF, monthly cron). **Operator step before flip:** GET `/api/systemic-findings-tick` for the computed JSON (+`?narrate=1` for the figure-validated prose), review `2026-08.md`, then flip `W8_U4_FINDINGS_REPORT`→on. |
| U5 | Naming-hygiene campaign — rename/link proposals (Prompt 79) | 🟡 **BUILT — flag OFF, awaiting dry-run review → Cowork flip.** | Claude Code. Consumes the naming-hygiene backlog U1 deliberately deferred (lcc 5,091 = 1,113 known_abbreviation + 3,978 address_as_name; gov 973; dia 395). Reuses the U1–U4 shapes wholesale (pure planner `api/_shared/naming-hygiene-planner.js` importing U1's `classifyName`/targets/bounded-scoring; GET dry-run / `?score=1` inline sample / POST flag-gated apply tick; nightly cron; in-migration flag; reversible ledger) — does NOT fork a new pattern. **Two proposal types, DETERMINISTIC-FIRST:** (a) `known_abbreviation` → **RENAME** — an unambiguous dictionary (`ABBREV_EXPANSION`: Prtnrs→Partners, Mgmt→Management, Hldgs→Holdings…) expands with **NO LLM**; genuinely ambiguous tokens (`AMBIGUOUS_ABBREV`: Cos, Assoc, Comm, Cap, Fin, Dev, Prop; Corp kept as a valid form) get Ollama in context via `invokeExtractionAI({surface:'naming_hygiene'})` with a judge-don't-parrot rubric + an anti-hallucination floor (expansion must lengthen + keep the first word, else → uncertain). (b) `address_as_name` → **LINK-DON'T-RENAME** — resolve the property at that address (exactly-one normalized-address match; >1 = ambiguous/human, 0 = unresolved; **never guess**) and propose an `ensureEntityLink` asset link + a **fill-blanks** display name from the property owner. **Value-gate (Prompt 79):** connected entities (relationships/portfolio, via U1's batched FK probe) rank FIRST — renaming a connected entity improves every surface. **Volume:** ~50 deterministic renames/night (cheap) + ~15 LLM-assisted (budget-bounded) + ~25 address links/night; `naming_hygiene_scored` resume cursor (re-scores only on a name change). **Apply (human-gated):** rename writes the display name via the house normalizer (LCC `entities` also recompute `canonical_name` via `lcc_normalize_entity_name`; a **unique-canonical collision routes to a conflict, never a silent clobber**), records `field_provenance` (source `w8_u5_naming_hygiene`) + a reversible `naming_hygiene_batch` ledger row (old name captured); link applies via `ensureEntityLink`. Migration `20260808120000_lcc_w8_u5_naming_hygiene.sql` applied live to LCC Opps: `naming_hygiene_review` (proposals; class + action CHECKs) + `naming_hygiene_batch` (reversible ledger) + `naming_hygiene_scored` (resume cursor) + `v_naming_hygiene_review_open` (federated-lane source, deterministic-first) + `v_lcc_naming_hygiene_health` + **8 `field_source_priority` rows** (`w8_u5_naming_hygiene`@40 for every writer-touched name/canonical field — verified `v_field_provenance_unranked` shows **0** `w8_u5` drift) + flag **`W8_U5_NAMING_HYGIENE` (OFF)** in-migration (36y) + nightly `naming-hygiene-tick` cron (**04:25**, after U1 03:40 / U2 03:50 / U3 04:10; no-ops while OFF). New route `/api/naming-hygiene-tick` + **`naming_hygiene_review` Decision Center federated lane** (all three touches wired — `_DC_FEDERATED` + SUBLANES + `_DC_FED_META` + render branch + `FEDERATED_DECISION_TYPES` + fetch-source + verdict branch; the `test/w8-federated-lane-wiring.test.mjs` structural guard passes) with a **bulk-confirm for the unambiguous deterministic renames** (`dcFedBulkHygieneRenames`, one click per page — never bulks the LLM/link cards) and a live `/api/review-counts` badge. Tests: `test/naming-hygiene-planner.test.mjs` (51): dictionary/ambiguous split, address-link planning, LLM guardrails, apply plan, target catalogue, + migration structural guards (fsp rows, flag OFF, reversible ledger, staggered cron). Flag OFF ⇒ cron no-ops; zero writes without a human verdict. **Operator step before flip:** GET `/api/naming-hygiene-tick?score=1` for the sampleable sheet (deterministic renames listed separately from the LLM/address proposals), review, then flip `W8_U5_NAMING_HYGIENE`→on. |

## Baseline metrics (2026-07-29, from live DBs — re-query at each wave verification)

| Metric | dia | gov |
|---|---|---|
| Properties without ownership_history | 8,382/12,312 (68%) | 13,148/20,175 (65%) |
| Live sales missing buyer | 1,315/3,575 | 1,174/5,689 |
| Live sales missing listing broker | 2,216/3,575 | 3,373/5,689 |
| true_owners without contact | 5,588/7,021 | — |
| llc_research_queue deferred | 1,155 | 885 |
| sf_link_research_queue queued | 3,106 | 27,605 |
| ownership_research_queue unverified | — | 57,130 |
| staged_intake_feedback rows (LCC) | 1 | |
| template_sends replies (LCC) | 0/60 | |
| field_provenance enforce: strict/warn/record_only (LCC) | 76 / 78 / 1,851 | |
| CMS patient counts latest real monthly | 2025-03-01 | |

## Wave 0 — Hygiene & guardrails

| Unit | What | Status | Notes |
|---|---|---|---|
| W0.1 | Repo root cleanup (.gitignore, move worklogs to docs/history/) | ✅ 2026-07-29 | **PR #1492.** 54 docs → docs/history/ + INDEX.md; 14 artifacts untracked/removed; 18 refs repointed; tests green (2236 pass). AUDIT_PROGRESS.md + SALES_AND_AVAILABLE_COMPS...AUDIT stayed at root (referenced from .js comments — revisit in W6.5 front-end split). |
| W0.2 | Distill CLAUDE.md ≤30KB | ✅ 2026-07-29 | **PR #1493.** CLAUDE.md 569KB→24.5KB (theme-organized: topology, write-surface rules, doctrines, footguns); AGENTS.md 32KB→3.5KB (also corrected stale Vercel invariants); full originals archived verbatim to docs/history/. |
| W0.3 | Archive schema + bloat + agency_debt_programs move | ✅ 2026-07-29 | Cowork. archive schema + manifest on dia+gov. Moved: 36 dia backup/ledger tables (incl. field_cleanups — NOT empty as audit claimed: 838k rows, legacy correction log dormant since 2025-10; and agency_debt_programs 1.6GB, zero code refs, Scott-approved) + 15 gov backup tables + 4 gov DEPRECATED tables. Excluded: _sweep_candidates_2026_06_11 (referenced by cleanup-contaminated-hertz-lease.mjs). VACUUM FULL pending_updates: 196MB→2.2MB. Drop-after dates in archive.manifest (90d default). |
| W0.3v | Independent verification of W0.3 (Code session) | ✅ 2026-07-29 | **PR #1494.** Manifests 1:1 both DBs (dia 35 / gov 17 post-restores), 0 orphans/collisions. Found 2 sibling-repo code refs the Cowork sweep missed (repo-only grep can't see DialysisProject/GovernmentProject): see W0.3h + W0.3i. Standing rule extended: archive sweeps must also grep the SIBLING repos. |
| W0.3f | Remove agency_debt_programs ref from merge_dialysis_dup_property, then re-archive the table | ✅ 2026-07-29 | Claude Code (Dialysis `claude/peaceful-meitner-167bsi`, migration `20260729_dia_w0_3f_...`). Dropped the (always-NULL, always-no-op) `agency_debt_programs` repoint from `merge_dialysis_dup_property()`; moved the table (1.55M rows / 1.6GB) to `archive` + manifest row (`federal data misplaced in dia; approved 2026-07-29`, drop_after 2026-10-27). Reference re-check: **0** pg functions/triggers/views/inbound-FKs reference it after; outbound FK→properties travels inert (matched_property_id NULL on all rows). ⚠️ Repo grep surfaced `Dialysis/src/ingest_agency_debt.py` — a MANUAL write-only CLI (`python -m src.ingest_agency_debt`, not in run_cms_ingestion/pipeline_runner/any workflow); archiving takes the table out of the API so that CLI can no longer write. It is the misplaced federal ingester → retire in a later DialysisProject session (a .py change, out of scope for this migration-only unit). |
| W0.3g | Restore gov lease_lifecycle_events (reachable user path was erroring) | ✅ 2026-07-29 | Cowork migration `w0_3g`. research_artifacts.py lease-activity promotion → write_services.py:537 INSERT was failing post-archive. |
| W0.3h | Repoint lease-activity promotion path to gsa_lease_events, then re-archive lease_lifecycle_events | ✅ 2026-07-30 | **CLOSED with corrected verification basis.** Repoint merged (gov PR #352, 2 unit tests). The planned "one live promotion" verification is IMPOSSIBLE in prod: `renderGovEvidenceWorkbench()` returns '' unconditionally (gov.js:3854 — "requires the GovernmentProject FastAPI service; until deployed, skip rendering"), so the Gov Evidence card Scott was sent looking for CANNOT render, and the FastAPI promotion path has no deploy target. Table (0 rows) re-archived via `w0_3h_rearchive_lease_lifecycle_events`; only-writer unreachable. Workbench registered in feature_flags_registry as GOV_EVIDENCE_WORKBENCH (off, hard-disabled) → surfaces in the Dormant Capabilities briefing digest. Deploy-or-retire decision folded into W6.4. |
| W0.3i | field_cleanups decision + code-path retirement | ✅ 2026-07-30 | Re-verified in the cleanup bundle: run_field_cleanups() has 0 callers, ingest_agency_debt.py deleted, 0 runtime refs to either archived table (tombstones/docs only); separate live engine apply_all_cleanups correctly retained. (Substantive removal shipped earlier as W0.3i-x.) |
| W0.4 | Inert-feature registry + briefing digest | ✅ 2026-07-29 | **PR #1495.** feature_flags_registry live on LCC Opps, 22 flags (19 env vars + 3 SOS adapters, 0 phantoms); "Dormant Capabilities" section in briefing email. Corrections: audit's OWNER_ENRICH_SOS_ADDRESS_URL is actually OWNER_ENRICH_ADDRESS_URL; SF_LIST_IMPORT_URL is a PA-flow gate not an env check. Registry is operator-curated — flip state to 'on' when actually enabling. |
| W0.5 | Retention policies for mega-tables | ✅ 2026-07-29 | Cowork. dia: `dia_retention_prune()` + daily 03:20 cron + retention_prune_log; windows: learning_logs 90d / ingestion_run_errors 90d / data_corrections 365d (conservative — first deletes ~2026-12). Backlog cleared in-session: 1,271,186 learning_logs + 160,769 ingestion_run_errors rows. field_aliases EXCLUDED (operational alias→canonical reference, not a log). LCC Opps existing prunes verified active (field_provenance 90d, sf_sync_log 30d, context packets 7d, research tasks, intake artifacts). |
| W0.5b | gov gsa_inventory_snapshot_lines parquet offload decision | ⬜ | Proposal registered: 866k of 1.16M rows (75%) are >24mo old (2013–mid-2024). Offload = export by year to Storage as parquet + delete. Deferred: verify historical analyses read gsa_lease_change_facts/timeline, not raw lines, first. |
| W0.6 | dia Postgres 15.8 → 17 upgrade | ⛔ 2026-07-29 | pgjwt dropped (unblocked the wizard) and LCC leaked-password ✅, but the **upgrade itself FAILED after ~15 min** with a contact-support prompt; Supabase support ticket filed (Scott). Post-failure health check from Cowork: dia rolled back cleanly to 15.8, ACTIVE_HEALTHY, queries fine, retention cron ran 12:56 UTC same morning — **no damage**. Awaiting Supabase support; re-attempt after their response. |
| W0.7 | anon EXECUTE revoke on SECURITY DEFINER fns (103 LCC / 61 dia / 38 gov) | ✅ 2026-07-29 | Claude Code (all 3 repos `claude/peaceful-meitner-167bsi`; migrations `*_w0_7_revoke_anon_execute_secdef`). Front-end anon-RPC audit → **exception lists**: LCC = **none**; dia = `match_marketing_lead_to_sf`, `upsert_lease_field` (both SECURITY INVOKER, already out of the DEFINER loop); gov = `gov_auto_resolve_ownership`, `gov_auto_resolve_intel`, `gov_resolve_gsa_link_review`, `gov_resolve_sale_link`, `gov_create_property_from_pending`, `gov_resolve_portfolio_sale_link` (+`upsert_lease_field`, absent on gov). ⚠️ **Grounding correction:** the browser calls ZERO RPCs with the anon key — every SPA RPC routes through server-side **service_role** proxies (`/api/dia-query`, `/api/gov-query`, admin.js `opsQuery`, `/api/data-query`); `auth.js`'s anon client is auth-only. So the exceptions are *defensive* (belt-and-suspenders). ⚠️ **Effectiveness fix:** anon executed via BOTH an explicit grant AND `PUBLIC` (`proacl ...,anon=X,...,=X`), so `REVOKE FROM anon` alone was cosmetic — the migration also GRANTs authenticated+service_role (preserving access reached only via PUBLIC) then REVOKEs FROM PUBLIC. Folds in `function_search_path_mutable` (`SET search_path=public` where unset). Verified live: anon-executable secdef = **0 LCC / 0 dia / 6 gov (=exceptions)**; authenticated+service_role preserved; missing_search_path → 0 on all 3. |
| W0.8 | Duplicate-index cleanup (28 dia / 13 gov / 3 LCC, from performance advisors) | ✅ 2026-07-29 | Claude Code (all 3 repos; migrations `*_w0_8_drop_duplicate_indexes`). pg_index signature match cross-checked vs the advisor duplicate_index lists (28/13/3). Dropped **27 dia / 14 gov / 1 LCC** redundant NON-constraint copies. Left untouched (both members back a constraint — never drop a constraint index): dia `cm_nm_broker_patterns`, `listing_snapshots`; LCC `domains`, `workspace_memberships`; gov none. Every survivor is on identical columns/uniqueness (ON CONFLICT inference intact). Verified live: 0 droppable duplicate groups remain on all 3; all dropped indexes gone; survivors intact. Reversible via pg_get_indexdef. |
| W0.V | Advisors triage (all 3 projects) | ✅ 2026-07-29 | Cowork. 0 ERROR-level on all 3. WARNs: anon/authenticated SECURITY DEFINER execute (→W0.7); function_search_path_mutable (130/150/228 — defer, bundle with W0.7 migration); rls_policy_always_true (6 gov/16 dia — review in W0.7 session); materialized_view_in_api (2/5/13 — review); dia vulnerable_postgres_version (→W0.6); leaked-password protection off on LCC (dashboard toggle). 136/120/224 RLS-no-policy INFO = service-role-only by design, accepted. |
| W0.3v | Archive verification + code-reference audit | ✅ 2026-07-29 | Claude Code. See "W0.3 verification" below. Manifest ↔ archive 1:1 on both DBs (dia 35/35, gov 17/17, 0 orphans, 0 public name-collisions). **2 archived tables still referenced by RUNTIME code** (contradicts W0.3 "no code refs"): dia `field_cleanups` (LOW, graceful) + gov `lease_lifecycle_events` (MEDIUM, user-path errors). dq5_/dq7_ correctly left in public. |
| W0.3h | Repoint gov lease-activity promotion → `gsa_lease_events` | ✅ 2026-07-29 | Claude Code (government-lease `claude/supabase-schema-cleanup-wxcz9x`). Fixes the W0.3v MEDIUM finding: `research_artifacts.py` + `write_services.py` lease-activity screenshot promotion now writes canonical `gsa_lease_events`, not deprecated `lease_lifecycle_events`. Field map + NOT-NULL guards in the commit; 2 unit tests pass. `lease_lifecycle_events` was restored to public 2026-07-29 (w0_3g) so nothing errors now. **⏳ After this deploys AND one successful lease-activity promotion is verified live, `lease_lifecycle_events` gets re-archived** — do NOT drop/archive it before that. |
| W0.3i | field_cleanups decision + code-path retirement | ✅ 2026-07-30 | Re-verified in the cleanup bundle: run_field_cleanups() has 0 callers, ingest_agency_debt.py deleted, 0 runtime refs to either archived table (tombstones/docs only); separate live engine apply_all_cleanups correctly retained. (Substantive removal shipped earlier as W0.3i-x.) |

### W0.3 verification (2026-07-29, Claude Code session)

Verified the already-applied W0.3/W0.3c archive moves against live DB state and repo code (`api/`, `mcp/`, `supabase/functions/`, domain `src/`). **No new DB migrations created — verification only** (per Scott's directive to stand down on further moves).

**Manifest consistency — PASS (current live state):**
- **dia** `archive.manifest` = 35 rows ↔ 35 archive tables (excl. manifest), 1:1 both directions, 0 tables missing a manifest row, 0 manifest rows without a table, **0 name-collisions with `public`**.
- **gov** `archive.manifest` = 17 rows ↔ 17 archive tables, same 1:1 / 0-orphan / 0-collision result.
- `_sweep_candidates_2026_06_11` correctly **retained in dia `public`** (not archived) — referenced by `scripts/cleanup-contaminated-hertz-lease.mjs`. ✅
- `dq5_*` / `dq7_*` correctly **left in `public`** — live merge ledgers, not backups (`apply_owner_merge()` wrote `dq5_owner_merge_log` 2026-07-23, 6 days before this audit). ✅

**⚠️ State moved during the session (concurrent Cowork/MCP editing):** counts dropped from my first read (dia 37→35, gov 19→17) because these tables were **un-archived (moved back to `public`)** mid-session: dia `agency_debt_programs` (1.6 GB — the contested federal-data table) + `cap_recompute_backup`; gov `cap_recompute_backup` + `gov_sale_lease_date_backup`. Manifests were updated to match, so the sets remain internally consistent. `gov.public.agency_debt_programs` remains the empty 0-row counterpart (never archived). Net: the W0.3 log's "36 dia + agency_debt_programs / 15 gov + 4 deprecated" is now **dia 35 / gov 17** live.

**Code-reference audit — 2 genuine findings (the W0.3 "zero code refs" claim was incomplete):**
1. **dia `field_cleanups` — LOW (graceful degradation).** ~15 files in `Dialysis/src` still target it, incl. read/write via `client.table("field_cleanups")` in `cleanup_manager.run_field_cleanups()`, `utils_shared.py`, `ai_scrubber.py`, `learn_aliases.py`, `data_cleaner.py`, `post_data_cleaning.py`, digest modules. `run_field_cleanups()` degrades gracefully (`safe_execute` → "aborted: failed to fetch field_cleanups", returns — **no crash**), but the field-cleanups engine is now permanently dead-lettered since the table left `public`. Table was dormant since 2025-10 (superseded by `field_provenance`), so functionally near-neutral. **Action:** accept (dormant) OR un-archive `field_cleanups` to `public` to keep the engine live; ideally retire the code path in a later wave.
2. **gov `lease_lifecycle_events` — MEDIUM (user-triggered path errors).** Reachable runtime path: `research_artifacts.py` "lease_activity" screenshot-observation promotion does a SELECT (`research_artifacts.py:793`) then `apply_evidence_promotion(..., lifecycle_event_row=...)` which INSERTs (`write_services.py:537`). Both now raise (table moved out of `public`) if a gov user promotes a lease-activity observation. Table was already DEPRECATED (replaced by `gsa_lease_events`), so the branch should be retired — but archiving broke it in the interim. **Action:** confirm whether that promotion path is exercised; if yes, repoint it to `gsa_lease_events` OR un-archive `lease_lifecycle_events` before its 2026-10-27 `drop_after`.

**Benign (no action):** dia `data_corrections` refs = the **live** `dia.public.data_corrections` (795k rows), not the gov-archived 0-row table. gov `db_audit.py` lists `data_corrections`/`email_log` as catalog entries ("not used yet") — diagnostic only. All other archived-name hits are the original `CREATE TABLE` statements in `supabase/migrations/**` and `sql/**` (one-time historical DDL, harmless).

**Advisors (Step 5 / W0.V) — re-confirmed consistent:** 0 ERROR-level on all 3 projects. Safe do-now not yet actioned: drop redundant **duplicate indexes** (28 dia / 13 gov / 3 LCC WARN — each is two identical indexes, drop-one is reversible); enable LCC **leaked-password protection** (dashboard toggle); dia **Postgres 15.8→17** patch (→W0.6). Deferred: `function_search_path_mutable`, SECURITY DEFINER anon EXECUTE (→W0.7), `materialized_view_in_api`, `rls_policy_always_true`. Accepted: RLS-enabled-no-policy INFO (service-role-only by design; incl. the new `archive.*` tables, which are not in the exposed API schema).

## Wave 1 — Close the built loops

| Unit | What | Status | Notes |
|---|---|---|---|
| W1.1 | Feedback capture (match picks → staged_intake_feedback + backfill) | ✅ 2026-07-29 | Claude Code (`claude/match-decisions-feedback-loop-dkir0k`). Canonical writer `writeIntakeFeedback()` extracted in `intake-feedback.js` (single writer; HTTP path behavior-neutral). Wired into admin.js Decision Center (`match_disambiguation` pick=approved/corrected, create_property=no_match, research=deferred; also `intake_disposition` open_property=approved, create_property=no_match — same signal class) + intake.js `handleIntakePromote` (implicit approve, snapshots the machine's latest **non-manual** match row for honest confidence bands). Backfill migration `20260810120000_lcc_w1_1_feedback_backfill.sql` (reversible ledger `staged_intake_feedback_backfill_w1_1_log`, `source=backfill_w1_1`, idempotent NOT-EXISTS guard) applied live. **Verified:** staged_intake_feedback 1→3,729 (3,728 backfilled approvals: 3,536 @0.95-1.00, 164 @0.85-0.95, 28 @0.70-0.85); nightly `matcher-accuracy-rollup` cron active (`15 2 * * *`); `v_matcher_accuracy_recent` returns 34 per-band rows (no double-count — view sum = 3,728). Rates all 1.000 until live reject/corrected/deferred signal accrues. |
| W1.2 | Reply propagation → template_sends + template_response signal | ✅ 2026-07-29 | **PR #1504** (`claude/template-loop-closure-o9xx5h`). `recordTemplateResponse()` in templates.js: inbound two-way reply → latest unreplied template_sends row ≤45d PATCHed replied=true + `template_response` signal (same payload keys as template_sent → lands in high_performing_templates correctly). Honest counts: one send flipped per reply. `recordTemplateSend` now writes opened/replied/deal_advanced = **null** ("not observed") matching the schema's partial index. evaluateTemplateHealth + chooseBestTemplate verified null-safe. Tests: template-response.test.mjs + 3 wiring cases; suite 2264/0. JS-only — live on Railway redeploy. |
| W1.3 | Bug-fix bundle | ✅ 2026-07-29 | **PR #1507** (`claude/admin-signal-audit-fixes-96r9sc`, 6 commits). (1) fetchExcludedRefs pages past the 1000-row cap in strides, throws on error (was: 1,086 decided refs, only 1,000 excluded → 86 decided subjects re-shown). (2) `get_contact_recommendation_weight` fixed IN the function (migration 20260811120000, live): resolves unified_id→entity_id internally — was silently 1.0 for every contact, now real weights (verified 0.5 on test). (3) `recommendation_ignored` producer in research-loop.js + Research-page Dismiss + migration 20260811122000 adds `domain` to ignored_recommendation_contacts (generator skip-set query was 400ing). Caveat: signals.entity_id is UUID — 11k numeric property-kind feed rows can't suppress via the contact view (30k UUID owner/entity rows can); documented. (4) chain-connect-tick: ledger-as-cursor page walk replaces the starving min(2000) window — gov had 2,938 candidates vs 731 logged; tick now reaches the tail. (5) gov `recorded_owners.sos_compared_at` watermark (migration 20260811121000, live) + ORDER BY + one-shot stamp — 1,233 exact-matched owners drain exactly once (~1,133 were unreachable). DB migrations applied live; JS ships on redeploy. |
| W1.4 | LLC research revival → REDEFINED as the Free-First Ownership Plan | 🟨 2026-07-29 | FL requeue = no-op (0 FL deferred both DBs; already drained by the Sunbiz adapter). **Superseded by `docs/audits/OWNERSHIP_RESEARCH_FREE_FIRST_PLAN.md`** (Scott directive: free public resources first, per the pre-LCC manual doctrine). Five lanes: (1) SAM feed widening — 380/885 gov deferred rows sit on GSA-leased properties; lessors must be SAM-registered; `sam-entity-lookup` + `sam_propagate_to_owners()` already built, just underfed (SPEC_owner_data_ingestion fix); (2) county mailing capture (=W3.1); (3) sidebar rapid-ingest bundle (3 written specs: host-permission fix, rapid-ingest UX, worklist front door); (4) bulk-mirror expansion on the FL pattern for free-bulk states (verify from residential egress: CO/NY/NC/VA/TX-Comptroller promising; CA/GA/IL/AZ likely sidebar-only); (5) institution registry seeding for the Tier-A SPE tail. **OpenCorporates decision DEFERRED to ~Aug 28**, re-priced against the residue. Cross-match evidence: existing ingested data closes <10% of the 2,043-row backlog (gov: 66 registry / 57 GSA-lessor / 1 SAM; dia: 10 registry, 0 parcel-mailing). Deferred-by-state: dia unknown 237, CA 143, TX 98, GA 72, IL 59, NC 50; gov unknown 455, TX 46, CA 38, VA 33, AZ 33. |
| W1.4-L1 | Free-First Lane 1: SAM feed widening | ✅ 2026-07-31 | **gov PR #354 + LCC PR #1509** (`claude/sam-feed-widening-free-first-drpulr`). `sam_lookup_candidates()` RPC (recorded+true owners lacking SAM match, gross-rent-ranked, GSA-lessor deferred rows tier 0) + sam-entity-lookup v4 batch mode (recorded_owners were 100% unfed) + `sam_propagate_to_owners()` auto-retires deferred queue rows on POC landing (source='sam_entity', sos-writeback contract, self-rollback-gate proven). Baselines locked: deferred=885, sam_entity=0, recorded sam_checked=0. **Key refreshed by Scott 2026-07-29 ✅** (PRs merged, redeploy live). As of 16:55 UTC sam_entities still 228 / latest 2026-06-11 — first enriching cron cycle pending (every 2h). 48h-delta check scheduled in Cowork for 2026-07-31: deferred<885?, source='sam_entity' retirements, new data_source='sam' contacts. **48h CHECK PASSED (session 32, 2026-07-31): deferred 885→880, 6 queue rows auto-retired source='sam_entity', +7 sam contacts (416 total), 5 recorded managers set, sam_entities enriching again (latest 2026-07-31 00:15, was stuck 2026-06-11). WATCH STANDS: pace slow — +6 entities/48h vs 50-per-2h capacity; re-check weekly, investigate cron batch consumption if it stays flat.** |
| W1.4-L3 | Free-First Lane 3: sidebar rapid-ingest bundle | ✅ 2026-07-29 | **PR #1510, merged + LIVE-VERIFIED.** Scott's real capture (CHULA VISTA SOCIAL SECURITY BUILDING, LLC / CA @ 16:49 UTC) confirmed the full chain from Cowork: recorded_owners got filing_state=CA, filing_id 200631810125, status Terminated, filing_date 2006-10-13, agent "Individual JOHN ISAAC", source=sos_manual_sidebar; principal-address observation written (3525 Del Mar Heights Rd #765, San Diego) with source_surface=sos_sidebar and **ext_version=1.0.36** stamped; llc_research_queue row auto-retired status='done'. Note: a mixed-case duplicate recorded_owner row exists for the same entity (capture landed on the uppercase row) — expected to collapse via the canonical-name merge tick; watch at W1.V. The 50-state human loop is operational. |
| W1.4-L3b | Live Intake promote fired on an empty capture → silent pipeline failure | ✅ 2026-07-29 | Fixed (`claude/sidebar-promote-empty-captures-20aevj`). Server surfaces pipeline_failed + classifier diag in the response; extension gates Save/Promote on hasExtractableContent() with tooltip + renders failure toast (1.0.36→**1.0.37**, needs reload); pipeline-failed promotes emit deduped lcc_health_alerts (sidebar_promote_pipeline_failed). 15 new tests; suite 2279/0. JS live on redeploy. |
| W1.5 | CMS patient-count fix | 🟨 2026-07-29 | **Code complete** (`claude/cms-ingest-cleanup-objsdn`, DialysisProject, 4 commits + LCC tracker commit). Honest DB write counters (never derivation counts), new-source zero-write alert via ingestion_tracker→lcc_health_alerts, backfill dispatch executor, 11 tests. **DISPATCH PATH CORRECTED 2026-07-30:** the GH Actions workflow is the LEGACY path and fails on the free plan — per INFRASTRUCTURE.md (2026-05-16 consolidation), CMS ingestion runs on the **Railway `cms-ingestion` cron service** (handsome-luck project, `0 6 * * *`); GH Actions is CI-only by decision. Backfill must run on Railway: W1.5b prompt wires a `PATIENT_MONTH_BACKFILL` env-flag path + runbook + retires the GH dispatch. Caveat stands: if CMS hasn't published past 2025-03, a loud no-op is CORRECT. |
| W1.5b | Railway-native CMS backfill dispatch + retire legacy GH workflow | ✅ 2026-07-30 | **Dialysis PR #7352** (substantive) + **LCC PR #1514** (tracker). `run_patient_month_backfill_if_flagged()` reads `PATIENT_MONTH_BACKFILL` env on the Railway cms-ingestion service; idempotent via ingestion_tracker marker `patient_backfill_done` (failed run = no marker = retries; `PATIENT_MONTH_BACKFILL_FORCE=true` for deliberate re-run after CMS advances); honest count(*) before/after delta; non-blocking to the daily run. Runbook in DialysisProject INFRASTRUCTURE.md. Legacy `cms-ingestion-daily.yml` DELETED (red runs falsely implied broken ingestion). 14 backfill tests + 71 related pass. **Scott operator steps:** merge both PRs → Railway → handsome-luck → cms-ingestion → Variables → add `PATIENT_MONTH_BACKFILL=true` → Redeploy → watch logs for `[W1.5b][DATA] ... rows written (DB count delta): N` → remove the variable. N=0 is a correct loud no-op if CMS hasn't published past 2025-03. |
| W1.5c | Backfill completion-marker hardening | ✅ 2026-07-30 | **Dialysis PR #7353.** Root cause: two workers raced start_run — 2 orphan started rows, neither finished. Marker now a single idempotent success-upsert written only after completion; partial unique index `uq_ingestion_tracker_patient_backfill_marker` (applied live, orphans collapsed → 1 success row); failed never demotes success; skip-guard verified to read exactly what the writer stores. 19 tests. |
| W0.3i-x | Retire field_cleanups engine + ingest_agency_debt.py CLI | ✅ 2026-07-29 | Same branch. 0 runtime refs to either archived table (comments/migrations only); obsolete tests dropped; suite green vs baseline. Closes the last W0.3 loose end. |
| W1.4-L3c | Live Intake UX bundle (3 defects from Scott's live testing) | ✅ 2026-07-30 | **PR #1513** (`claude/live-intake-defects-fixes-sql13z`, one commit per defect). (1) `liveIngestBindPreflight()` gate + one-click "Bind <label>" suggestion from the open detail record; (2) gov Phase-2 ownership_history SELECT was missing property_id — added (root cause confirmed = my diagnosis); (3) 0-ops proposal now renders "Everything extracted already exists — nothing to apply ✓", Apply disabled via hasApplicableOps, extractor emits already_on_file[] annotations. 19 new fixture cases; suite 2304/0. JS-only — live on Railway redeploy of merged main. MERGE NOTE: branch also edits this tracker file — on conflict keep the working-tree version (this row already reflects the branch). Original defect list preserved in the session log. | (1) Original 400: no record bound → opaque error; add pre-flight message + auto-bind suggestion. (2) **Ownership research row drops property_id**: 2026-07-30 retry bound context carried ownership_id 580d4b94 but property_id null — yet the DB ownership_history row HAS property_id=3811. The gov ownership research-row payload / getLiveIngestCurrentContext loses it → red "property_id is null" callout + property-field ops suppressed. Fix the row payload to include property_id. (3) **0-ops proposal reads like success-then-failure**: model narrative says "Updated/Inserted..." (proposal phrasing) while ops=0 (extracted facts already in DB), and "Apply Selected" errors "Select at least one proposed operation." Show an explicit "Everything extracted already exists — nothing to apply ✓" state (with per-fact already-present annotations) instead. Screenshots on file (2026-07-30). |
| W1.V | Wave 1 verification | ✅ 2026-07-30 (final trend check 07-31) | **All five Wave-1 loops verified wired + live against baseline:** (1) Feedback: 1 → **3,730** incl. 1 organic live row beyond the backfill — the Decision Center capture works in production. (2) Template loop: 7 new sends carry replied=NULL ("not yet observed") — new write-path live; replies/template_response signals await organic inbound (0 so far, expected). (3) Accuracy: 34 bands, nightly cron alive. (4) LLC/SAM: gov deferred 885→**882**, sam_entities 228→231, +4 SAM contacts — draining but SLOWER than the 50/2h cadence implies (only ~1 enriching cycle observed; watch at 07-31 check). dia deferred 1,155→1,166 (grew — dia has no SAM lane; sidebar Lane 3 is its drain). (5) recommendation_ignored: 0 (no dismissals yet, organic). **Coverage metrics intentionally ~unchanged** (ownership gap 8,382 dia / 13,147 gov; broker/buyer gaps flat) — coverage moves in Waves 3–5; Wave 1's job was closing feedback loops, and they're closed. **Overnight items:** CMS backfill fires at the 06:00 UTC cron (variable set; marker check 07-31) — ingestion_tracker also shows NO runs since 07-26, so tonight doubles as a is-the-cron-alive test. Enforce modes unchanged 76/78/1,851 (Wave 2's job). |

## Wave 2 — Provenance spine

| Unit | What | Status | Notes |
|---|---|---|---|
| W2.1 | lcc_merge_field concurrency + unique invariant | ✅ 2026-07-30 | **PR #1515**, migration `20260812120000` applied live. Deduped **1,798 duplicate live-write keys / 2,964 rows** (reversible ledger `field_provenance_dedup_w2_1_log`); partial unique index `uq_field_provenance_live_write`; lcc_merge_field rewritten with per-key advisory lock + supersede-before-insert; 20-way race test → exactly 1 live write / 19 superseded / 0 violations. BONUS CATCH: `lcc_record_field_resolution` (live Decision Center path) inserted-before-superseding and would have thrown unique_violation the moment the index landed — reordered in the same migration. **Cowork verified live 07-30:** index present, ledger=2,964, dup live keys=0. |
| W2.2 | Record effect not intent (sidebar provenance ordering) | ✅ 2026-07-30 | **PR #1516**, migration `20260812130000` applied live + sidebar-pipeline.js patched (§F method, 11,387→11,486 lines). decision CHECK widened +failed_write +cleared (Cowork-verified live); new `lcc_record_field_write_outcome()` shares the W2.1 advisory-lock domain; sales provenance flushes from the FILTERED payload gated on write result; diagnostics read computeWrittenSalesFlags(writtenPayload); updated_at → BOOKKEEPING; explicit nulls → cleared (refillable under warn/strict). Metric re-baseline noted on v_sales_parser_miss_rates_7d (cutover 2026-08-12 comment). 17 fixture tests; suite 2315/0. Judgment call (endorsed): failed PATCH retracts our own optimistic same-source write — ledger under-claims rather than over-claims, self-heals next capture. JS ships on redeploy. |
| W2.3 | Watermark mirror sync (gov at 18-page ceiling NOW) | ✅ 2026-07-30 | **PR #1517**, 3 migrations applied live (LCC + gov/dia companions). Keyset walk with composite (updated_at, property_id) cursor — caught that 1,126 gov rows share one timestamp (strict > cursor would skip 126). Two grounded redesigns: pg_cron forbids intra-job COMMIT → one-page-per-tick state machine (`lcc_mirror_tick` every 3 min); listing_events kept as sale_date recency feed (updated_at cursor would flood 150× from recompute churn — caught + cleaned a 1,990-row flood mid-build). Stale-guard proven; gov parity 13,518=13,518. Per-leg freshness rows + watchdog coverage. **Cowork verified live:** gov legs ok/advancing; dia legs alarming suspect_empty_source → see W2.3d. |
| W2.3d | dia mirror frozen (pre-existing): anon-read decision | ✅ 2026-07-30 | dia's mirror-feed views returned [] to anon (no anon-read path; mirror was silently frozen BEFORE W2.3 — the new alarms exposed it). **Scott chose scoped definer views** (narrowest exposure): Cowork migration `w2_3d_dia_mirror_scoped_definer_views` flips v_property_attributes_portfolio + v_property_owner_facts_portfolio to security_invoker=off; anon SELECT stays view-only. Verified: anon reads 12,321 rows. Mirror self-heals via the 3-min tick (~1h to parity; confirm at 07-31 check). |
| W2.4 | Null semantics + listing_events retraction | ✅ 2026-07-30 | **PR #1519** (merged), migration applied live. attributes=FILL-BLANKS + cleared-tombstone (v_field_provenance_live_cleared → lcc_apply_cleared_tombstones); owner_facts=LWW-incl-NULL stated explicitly; listing_events=INSERT-ONLY + retraction (guarded, reversible, self-healing; all 4 consumers filter retracted_at IS NULL). Backfill: 3 gov events retracted; P-BUYER 616→617 (stale counterparty exclusion correctly lifted); dia safely no-opped via min_live guard (input-starved → W2.3d2). |
| W2.3d2 | dia sales feed for listing-events leg + retraction | ✅ 2026-07-30 | **Dialysis PR #7353 + LCC PR #1520.** dia `v_sales_feed_portfolio` (security_invoker=off, anon SELECT view-only, 8 non-PII cols); LCC `lcc_mirror_tick` dia branch + `lcc_retract_listing_events_fetch` repointed (gov branches byte-identical). Cowork-verified live: dia listing_events leg status=ok reading 6 rows/tick (was frozen at 0); retraction now evaluates dia (live_sales=287, retracted=0 — correct, all still live). DBs already migrated; PRs land files to match. |
| W2.5 | provenance_event_log flush crons (gov backlog 16,860) | ✅ 2026-07-30 | **PR #1518**, migration `20260813120000` applied live. `lcc_flush_provenance_events()` RPC (normalizes gov's bare table names, skips bulk markers, merges source='domain_trigger' — registered record_only, drift view unchanged at 29); admin.js orchestrator route; watermark state table; both crons scheduled + watchdogged + queue-depth health check. **Drained live: dia 94→0; gov 16,860→16,760 trending** (cron finishes after merge+redeploy — until then the route 404s harmlessly). **Conflict report: 0 disagreements across 191 flushed keys** — the phantom state was pure ledger silence (trigger writes absent, not conflicting); no strict-mode cohort to promote from this batch. 9 tests; suite 2324/0. |
| W2.V | Wave 2 verification | ✅ 2026-07-30 | **All Cowork-verified live:** W2.1 invariant holds (0 dup live keys); W2.2 enum live; W2.3 mirrors — gov parity + advancing, **dia HEALED post-W2.3d (22,546 attrs / 23,667 owner-facts walked in, watermarks current 10:37 UTC)**; W2.5 — dia 0 AND **gov 0 undrained** (all 16,860 flushed by 02:39 UTC post-redeploy); W2.4 retraction live (3 gov backfill retractions). Sole open edge: W2.3d2. **BONUS: CMS backfill ran at the 06:00 cron — latest monthly 2025-03→2025-09, +44,868 rows; CMS itself has only published through 2025-09, daily run tracks from here. W1.5 FULLY VERIFIED → Wave 1 completely closed.** Cosmetic follow-up: backfill marker wrote status=started (not success) and duplicated (2 rows 65ms apart) — skip-guard may not engage; Scott removing the Railway variable is the real guard; tighten in next DialysisProject session. |

## Wave 3 — Ownership engine + queue triage

| Unit | What | Status | Notes |
|---|---|---|---|
| W3.1 | County scraper fix + recurring cron | ✅ 2026-07-30 | **gov PR #355 + LCC PR #1521** (merged). Root cause WORSE than audited: parcel_records/deed_records had NO property_id column at all (link lived only in the junction table). Migration adds first-class property_id+county_fips; fetch-time persistence (fill-blanks); FIPS from the in-DB census gazetteer (3,144 counties). **Historical linkage recovered in pure SQL: parcels 0→11,075/11,100, deeds 0→5,635/5,744 (Cowork-verified live).** Worklist views (1,328 due counties); county_ingest_pull_log ledger; mailing flow-through confirmed (03:25 cron → recorded_owners → SOS/LCC signals). Honest ceiling: NEW mailing capture=0 until per-county assessor fetchers (Phase A1b) + Railway re-scrape run. **OPERATOR STEP: schedule the Railway county-ingest cron (0 8 1 * *, python -m src.public_record_ingest --county-cron) per docs/RUNBOOK_county_ingest_cron.md.** |
| W3.2 | ORE activation + review lane + entity_match_labels | ✅ 2026-07-30 | **LCC PR #1522 + gov PR #356** (merged, redeploy live). Engine rescheduled 25/hr verdicts-only (merge=0); watchdog + depth alert (threshold 1500; live 1,447 — Cowork-verified, cron `25 * * * *`). owner_reconcile federated Decision Center lane with per-candidate evidence trace; verdicts write lcc_decisions + entity_match_labels (Wave-4 training corpus; table live). **Three orphan queues folded into ONE drain:** LCC reconcile review (660) + gov owner_unification_review_queue (2,574, disposition columns live) + entity_match_candidates (866 gov + 447 dia). 12 lane tests; suite 2336/0. ⚠️ GROUNDING CORRECTION → corrections log. |
| W3.3 | Retire lossy owner_merge_tick + substring true-owner resolver | ✅ 2026-07-30 | **LCC PR #1523 + gov PR #357** (merged). `gov_owner_strict_core()` strips ONLY pure legal forms (keeps CO/COMPANY/GROUP/PARTNERS/HOLDINGS); tick auto-merges only on strict-core equality, routes semantic collapses to entity_match_candidates→owner_reconcile lane. **90d merge audit: 448 pairs → 441 (98.4%) legit legal-form variants, 7 (1.6%) false-merge candidates in `gov_owner_merge_unmerge_worklist` with evidence + per-row reactivate_sql (Cowork-verified: 7 rows).** Tick dormant since May (unique index bounds it) — gate is preventive. Substring resolver replaced: exact-normalized auto-links; non-exact creates fresh owner + files costar_true_owner_fuzzy candidate; NEVER links from substring. Honest scoping: the 2 "ORE merges" were LCC-side entities merges, already neutralized by W3.2. |
| W3.4 | Orphan queue sweep — final four units | ✅ 2026-07-30 | **LCC PR #1524 + gov PR #358** (merged, redeploy live). (1) Comp-review drains: list/resolve MCP tools + HTTP routes + Decision Center lane (dia 86 + gov 96 open, now drainable). (2) metadata_backfill worklist page via service-role read (gov 3,073 + dia 687). (3) Six review-counts lanes rendered as source-backlog strip. (4) **ownership_research_queue: writer GATED** (ENABLE_OWNERSHIP_RESEARCH_QUEUE default OFF across all 9 producer inserts, registered in feature_flags_registry, regrowth alert >200/24h) **+ triaged 57,130 → 17,665 auto-resolved in place + 39,465 archived + 520-row stratified sample (Cowork-verified: sums exactly).** Per-queue SLA framework (v_gov/dia_queue_sla_status + hourly checks). Grounding correction → log. |
| W3.5 | Listing-BD consumer | ✅ 2026-07-30 | **PR #1525.** 📣 Listing BD inbox chip → grouped one-card-per-listing view with per-listing select-all; "Draft outreach for selected" wired to the never-called draft&action=listing_bd endpoint (new CONSUMER branch on inbox_item_ids[]); cross-listing 7-day dedupe (one draft/contact, honest template_sends counts); daily 04:10 expiry sweep (respects W2.4 retracted_at, reversible, live-verified with synthetic sold listing); template_sends replied=null wiring → W1.2 loop measures this channel. Real end-to-end draft rendered from the live Millersburg DaVita listing (2 items → 1 draft / 1 send). 9 fixtures; suite 2348/1 — the 1 failure is PRE-EXISTING on clean main (review-shared.test.mjs lane rationalization map) → watch item. Migration live; JS on redeploy of merged #1525. **ALL WAVE 3 BUILD UNITS COMPLETE — W3.V remains.** |
| W3.1d | County scraper null entity_name guard | ✅ 2026-07-31 | Writer-level guard (save_entity_record skips null/blank names, never raises); entity_skipped_no_name counted in pull-log notes + run summary. DEEPER ROOT CAUSE: the raise aborted mid-flight AFTER parcel/tax commits → ledger under-counted (live proof: 91 parcels written, 81 tallied — the 10 delta = exactly the entity failures). Part-2 premise REFUTED honestly: all 91 parcels (incl. failures) carry property_id + junction link → already excluded from the worklist; nothing to retry, only the nameless entity row was lost (correctly unwritable). |
| W3.6 | Lane usability defects from live testing | ✅ 2026-07-30 | **PR #1526** (4 commits). (1) Display-name resolution: owner_reconcile batch-resolves unified_contacts (renders "Owner ↔ Micah Pinney (Legend Development LLC)" + real comparison facts); Listing-BD joins entities for listing/contact names. (2) Comp lane: explicit action labels, Open property → deep link, cap INPUTS shown with source + as-of dates, un-resolve/reopen. (3) CoStar property-specific search URL. (4) **Root cause found+fixed for id=1 (Cowork-verified: noi=326,480 confirmed_sale, implied=reliable=3.71%, resolved):** comp producer divides price into stale properties.noi (estimated_comp_ratio @2026-03-31) while the engine already reconciles to reliable cap — **54/61 open cap_mismatch rows share the pattern → W3.6b.** 18 render tests; suite 2366/1 (pre-existing review-shared drift). JS needs redeploy of merged #1526. |
| W3.1b | Railway county-ingest cron service | ✅ 2026-07-31 | **RUN 3 SUCCEEDED END-TO-END** (00:55 UTC, Cowork-verified): 8/8 counties ok (DC, Arlington, Montgomery, NY, Fairfax, Baltimore, LA, Prince George's) → **91 new parcels, 100% property-linked, +76 tax records**, 8 pull-log rows. Three bugs found+fixed across the three runs (dia env vars → gov; rollup timeout → W3.1c matview; both via Scott's manual-run discipline). Monthly cron live (0 8 1 * *). Runtime-proven ceiling: mailing_captured=0 — per-county assessor fetchers (Free-First Phase A1b) are the ORE-fuel unlock. NEW small defect → W3.1d. |
| W3.6b | Systemic comp-NOI fix | ✅ 2026-07-30 | **PR #1527.** Producer derives implied_cap from ENGINE income (gov_engine_noi_batch / dia_engine_rent_batch RPCs, labeled implied_basis.source, safe fallback); bulk-refresh with reversible ledgers: **gov cap_mismatch 60→7, dia 50→23**; 40 gov properties got confirmed-sale NOI write-through (high-confidence active-lease anchor only, 80 provenance rows). Remaining gov 7 = 5 genuine conflicts + 2 engine-null (correct). dia same pattern confirmed + fixed (no properties write-through — conservative, no provenance ledger there). JS on redeploy of merged #1527. |
| W3.7 | OM-financials → automatic comp resolution | ✅ 2026-07-30 | **PR #1529.** om-comp-resolver handler (dry-run default): comp row → sf_comp_staging → sf_files → reuse existing extractor snapshots → guarded reversible gov_apply_om_confirmed_noi RPC (confirmed_document rank-3, never overwrites confirmed/manual) → auto-resolve ±75bps with om_confirmed:<file_id> note. Fort Wayne fixture proves would-resolve; 22 tests; owner_b rider fixed + backfilled 50/50. **HONEST CEILING: OM corpus is rich (gov 588 / dia 1,448 extracted) but comp→file LINKAGE is thin — only 4 of ~100 open rows reachable; gov 6 have 0 reachable OMs.** The unlock is Wave 4 SF-link resolution (30k queue) + W3.7b. |
| W3.7b | SF Listing-page attachments → sf_files | ✅ 2026-07-30 | **PR #1530** (merged, redeploy live). Root cause grounded: sf_files was 100% Comp__c both DBs; Fort Wayne comp fully linked to Listing+Deal with 0 files. Server-side ?action=discover SOQL sweep over ContentDocumentLink for Comp/Listing/Deal + traversal columns (applied live, 588 gov backfilled) + discover cron; resolver reaches Listing/Deal OMs. Coverage: +1 gov open (id=60) + 3 resolved-history incl. Fort Wayne; dia +0 (comps not listing-linked — honest). Live sweep needs SF Connected-App creds on the edge runtime per runbook; acceptance via real-id fixture. |
| W3.7c | Listing/Deal OM discovery via Power Automate collector | ✅ 2026-07-31 | **PR #1532.** Three webhook-secret edge actions (discovery-worklist GET with re-serve lease; discover-webhook dedup on content_version_id; file-content sha256-verified + 15MB cap → bucket → hourly stage-queued). Connected-App path RETIRED (410 + doctrine tombstone; token code deleted; dead cron migrations removed). Worklist: **gov 679 (comp 532/listing 79/deal 68) + dia 465** discoverable ids. Spec: docs/flows/FLOW_sf_file_discovery.md + flow-sf-file-discovery.json skeleton. **Realization: merged; edge fn DEPLOYED by Cowork 2026-07-31 (Dialysis_DB v21, payload sf-files-2026-07-v7 — info endpoint lists the 3 new actions, worklist 401s without the secret so PA_WEBHOOK_SECRET is enforced). Flow rebuilt per session-30 fix list. **CLOSED session 31: first successful production run 2026-07-31 04:59 UTC — 8 gov OMs stored+queued (SSA WisconsinRapids/Houston/Ahoskie, MOB FortWayne, TDCJ ElPaso, SOT-TDCJ HorizonCity, DFPS Wharton, USDVA Cincinnati), dia 20 already stored+extracting. One env defect found+fixed en route: GOV_SUPABASE_URL secret was a redirecting (http) form → 301 → fetch silently converted every gov POST to a body-less GET (inserts no-oped while reporting ok); Scott corrected the secret, Cowork verified write path + reset 76 burned worklist leases.** |
| W3.V | Wave 3 verification | ✅ 2026-07-30 | **Infrastructure: ALL LIVE AND DRAINING.** Comp queues gov 96→41 / dia 86→59 open (halved); ORE draining hourly (1,447→1,432) with verdicts-only; entity_match_labels 50/50 complete; orq HOLDING at 17,665 (writer gate working, no regrowth); feedback stable 3,730; 19 template sends awaiting replies (Listing-BD channel now feeds the W1.2 loop); parcels 11,075 linked; CMS 2025-09. **Coverage metrics intentionally not yet moved** (gov ownership gap 13,148≈baseline; dia 8,422 slightly up as new properties enter; mailing addresses still 26): the fuel lanes are days old — county re-scrape hasn't run (env fixed, re-trigger pending), SAM drain pace is SLOW (only 5 retirements — watch item: verify the cron is actually consuming 50/2h), sidebar sessions and OM discovery just began. Coverage movement is now the standing measure for the monthly audit, not a wave gate. **WAVE 3 COMPLETE.** |

## Wave 4 — Entity-resolution modeling  ✅ COMPLETE (W4.1–W4.4, 2026-07-31)

| Unit | What | Status | Notes |
|---|---|---|---|
| W4.1 | Training-data export (labeled pairs) | ✅ 2026-07-31 | **DELIVERED (session 32).** `storage://entity-resolution/w4_1/labeled_pairs.jsonl` on LCC Opps — 5,342 pairs (4,044+/1,298−, 76/24), 4,750 entity groups, split-by-entity 80/10/10 (train 4,277/valid 518/test 547, labels balanced). Sources: gov+dia dq5 owner/true-owner merge maps (2,318 after dedupe), ops soft merges incl. Boyd Watterson (1,671), entity_match_labels (36), owner_reconcile approvals (19); negatives generated same-state/same-city with rare-token + known-positive guards (1,298). 1,648 cross-source dupes dropped. Built by the re-runnable deterministic `w41-corpus-export` edge fn (Dialysis_DB) — re-run = W4.4 corpus refresh. Honest gaps: sf_account/email mostly null (owner_sf + contact models still fixture-fed until W4.3 decisions feed back); intake feedback excluded (filenames, not entity names); match_disambiguation has 0 decided rows. Full report: docs/resolver/W4_1_CORPUS_REPORT.md. **Next operator step: Scott creates the Railway resolver service (W4.2 runbook) → /train against the real corpus → calibration approval gates W4.3.** |
| W4.2 | Splink + libpostal resolver service | ✅ 2026-07-30 | **PR #1531.** resolver/ FastAPI: /normalize (libpostal + W3.3-discipline company normalization), /match (Fellegi-Sunter calibrated probability + per-comparison explanations, splink-trained m/u persisted as JSON so /match never needs splink at request time), /train, /health (honest backend reporting). Token blocks ∪ bge-small embedding-KNN fallback. Graceful degradation with deterministic fallbacks. Stub corpus + fixtures (incl. W3.3 false-merge negatives) pending W4.1; auto-link band precision 1.0 @ ≥0.995 target on stub split. Dockerfile + Railway runbook (CPU-only, handsome-luck). 31 tests. SCORES ONLY — never writes. **Scott operator steps: create the Railway resolver service per runbook; /train re-run after W4.1 corpus lands.** **SERVICE LIVE + TRAINED ON REAL CORPUS (session 33, 2026-07-31):** Railway `gracious-radiance-production-eeaf` up after 3 build/config fixes (GCC-14→bookworm pin; libpostal v1.1→master pin 25099c50 for pypostal API compat; RESOLVER_STORAGE_KEY must be the legacy anon JWT, Bearer-only code path). /health: libpostal true, sentence-transformers, splink true. All 3 models trained with corpus=storage://entity-resolution/w4_1/labeled_pairs.jsonl (4,795 train/547 test): auto-link precision 1.0 (0 false links, recall 0.9926) ≥ 0.995 target; auto-reject 49 rejects/0 positives lost; 94→review. CALIBRATION.md + model JSONs regenerated from the real corpus and committed; gen script now honors SUPABASE_URL/RESOLVER_STORAGE_KEY (was stub-hardcoded). Caveats: trainer=count_estimator (splink present but fell back — investigate at W4.4; count estimator is exact-given-labels so calibration valid); owner_sf/contact effectively name/addr-calibrated (corpus gap). **W4.3 GATE: calibration meets target — awaiting Scott's explicit approval to run the 30k backlog.** |
| W4.3 | SF-link 30k backlog run | ✅ 2026-07-31 | **EXECUTED (session 33, Scott-approved bands).** All 30,711 queued rows (gov 27,605/dia 3,106) scored offline against the local ops SF-Account registry (15,987 accounts via external_identities→entities) with the real-corpus owner_sf model — same FS math as the service, 5s wall. **Bands: conservative 0.9/0.1, NOT the calibrated 0.005/0.0008** — live-backlog distribution proved the calibrated band doesn't transfer (would auto-link 26k incl. '2200 Main LLC'→'900 South Main LLC'; corpus negatives too easy — measured finding, feeds W4.4 retrain). Results: **linked 3,442** (gov 3,077: 1,424 true_owners + 1,653 recorded_owners sf_account_id; dia 365 sf_company_id; all P≥0.98929, exact/near-exact name matches, 15/15 clean sample) with source='splink_v1' provenance_event_log rows (confidence=probability) + null-guarded writes; **needs_review 3,452** (gov 3,064/dia 388 incl. 18 dia conflicts where a DIFFERENT sf_company_id pre-existed — flagged w4_3_conflict, candidate stored) visible in v_sf_link_review_queue (W3.4 lane); **no_match 23,817** batch-tagged w4_3_splink_v1_2026_07_31_local_registry (REVERSIBLE; caveat: judged vs the local registry, not live SF). Ledger: w43_splink_batch table in both DBs (band/probability/prior value/conflict/applied_at) + Storage archive w4_3/{queue,sf_accounts,staged_batch}.jsonl. Review verdicts → entity_match_labels = the hard negatives W4.4 needs. Review band 3,452 vs <3,000 target (+15%) — accepted over loosening auto-link. **REVIEW LANE SHIPPED (session 33, 2026-07-31):** the 3,452 needs_review candidates are now a Decision Center sub-lane — `sf_link_candidate`, rendered via `renderFederatedLane`, source `v_sf_link_review_queue` (gov+dia; view extended with `last_error` so the 18 dia conflict rows render a three-way keep/switch card). Verdicts: **Link** applies the SF id via the existing attach semantics (gov `sf_account_id`+`sf_last_synced` / dia `sf_company_id`) — **null-guarded: a pre-existing DIFFERENT id never overwrites, it re-renders the conflict card**; **Not a match** → `no_match`; **Keep existing** (conflict) records the kept id without touching the owner row; **Switch** is the deliberate override (provenance old_value preserved); **Research** spawns a task. Every applied link writes a `provenance_event_log` row (`source='sf_link_review_human'`) and **every verdict writes an `entity_match_labels` row (`seeder='sf_link_review'`, verdict `same_party`/`distinct`) — the W4.4 hard-negative corpus** (label-write failure surfaces a 502, never silent). Mint-at-verdict (no 3,452-row `lcc_decisions` pre-mint); the sublane badge reads the live view depth via `/api/review-counts`. Pure planner `api/_handlers/sf-link-review.js::planSfLinkVerdict` + 18 unit tests + 5 wiring guards (`test/sf-link-review.test.mjs`). |
| W4.4 | Model as provenance citizen + retrain loop | ✅ 2026-07-31 | **SHIPPED (Claude Code) — Wave 4 CLOSES here.** (1) **Provenance citizenship:** `splink_v1` (@50, record_only, min_conf 0.9) + `sf_link_review_human` (@1, manual-equivalent) registered in `field_source_priority` for the SF-link fields (gov `true_owners`/`recorded_owners.sf_account_id`, dia `true_owners.sf_company_id`); migration `20260819120000` also upgraded `lcc_flush_provenance_events` to PRESERVE these first-class sources instead of the domain_trigger collapse (self-maintaining; can't trip `v_field_provenance_unranked`), and re-attributed the 3,493 already-flushed rows to their true source. **Verified live: 8 registry rows, fp splink_v1=3,442 + sf_link_review_human=51, unranked drift=0, ladder proof human>model (test/sql/w4_4_*.sql, 0 residue).** (2) **Nightly retrain loop:** `w44-retrain-tick` edge fn (Dialysis_DB, committed) → refresh corpus → `/train` ×3 → `lcc_record_resolver_retrain` (LCC Opps `resolver_calibration_history` + drift alerts: precision drop >1pt / band-floor hit / unexpected trainer, + loud `resolver_retrain_failure`). Cron `w44-resolver-retrain-nightly` (30 7 * * *) scheduled+active on Dialysis_DB. **RPC verified live via synthetic gate (2 drift + 1 failure + 3 history rows, 0 residue).** Operator steps: deploy the edge fn + set `RESOLVER_URL` secret (runbook §6). (3) **ORE candidate scorer → resolver:** `api/_shared/resolver-client.js` + `owner-reconcile-engine.js` gate every auto-merge on `/match` (owner_owner); veto or **fail-closed** fallback (service down/timeout/unset) → `needs_review`, never a merge. `ORE_USE_RESOLVER=1` + `RESOLVER_URL` (revert without redeploy). **15 fail-closed unit tests pass; 37 existing reconcile tests green.** (4) **Calibration defects fixed:** band floor 0.5 (`RESOLVER_AUTO_LINK_BAND_FLOOR`, hard invariant + 5 unit tests) — committed models re-clamped 0.005→0.5 (the transfer-gap band); splink training smoke-test **retired** (never fed the model; count estimator is exact-given-labels — honest README note). Feature flags `RESOLVER_RETRAIN_LOOP` (partial) + `ORE_USE_RESOLVER` (off) registered. **Corpus now accrues `sf_link_review` seeder rows** (refreshed nightly).<br/><br/>_Original ⬜ note:_ **Corpus now accrues `sf_link_review` seeder rows.** As Scott works the `sf_link_candidate` review lane (W4.3), each verdict appends an `entity_match_labels` row (`seeder='sf_link_review'`; `same_party` on Link/Switch, `distinct` on Not-a-match/Keep-existing) with `evidence_json.batch='w4_3_splink_v1_2026_07_31'` + probability + conflict flag. These are the human-adjudicated hard negatives the W4.4 retrain reads (the W4.3 finding: the calibrated band didn't transfer because corpus negatives were too easy). W4.4's corpus reader must include `seeder IN ('owner_unification','sf_link_review', …)` — one consistent `same_party`/`distinct` enum across seeders. **OPERATOR STEPS DONE + LIVE VERIFICATION GREEN (session 33, 18:26 UTC — WAVE 4 CLOSED):** Cowork deployed `w44-retrain-tick` to Dialysis_DB and forced one full tick: corpus refreshed (4,890 train/530 test), all 3 models trained, `resolver_calibration_history` rows in ops, `/match` serves **auto_link 0.5, auto_link_floored=true on all models** (the 0.005 hazard structurally gone), precision 1.0 at the floored band. 3 `resolver_calibration_drift` warn alerts open BY DESIGN (band-hit-floor = negatives still too easy; self-resolve as review-lane hard negatives accumulate). Nightly cron `30 7 * * *` UTC active. `RESOLVER_URL` + `ORE_USE_RESOLVER=1` set on LCC Railway; fail-closed exercises on next reconcile tick. Optional polish: regen committed CALIBRATION.md/model JSONs (runbook §6). |

## Wave 5 — Extraction models + signal automation

| Unit | What | Status | Notes |
|---|---|---|---|
| W5.1 | GLiNER party extraction over sale_notes_raw | ✅ gov 2026-08-05 (session-35d): `--apply --domain gov` executed + verified — 105 agreement-only fill-blank writes / 83 sales (buyer 36, seller 52, listing 9, lender 8), 100% ollama, dedup 102/165, ledger+provenance reconcile 1:1, all rows ranked. Windows entry-guard bug found+fixed locally (commit pending). dia deferred (note pool is bookkeeping; near-zero yield under agreement-only). 502 row re-run same session → 106 total applied writes, unit CLOSED for gov; disagreement pool (~152) available as future review/training signal. Open hygiene: CF token rotation deferred to build completion. | **BUILT (Claude Code, session 33).** Two-channel extractor + adjudicator, all pieces in place: **Channel A** `resolver/app/parties.py` + `POST /extract-parties` (GLiNER `gliner_medium-v2.1` baked in Docker; deterministic cue-phrase heuristic fallback in the bare venv — both span-anchored, `/health` reports `gliner:`; 13 tests). **Channel B** via the EXISTING `ai.js invokeExtractionAI` seam (no second Ollama client) — `api/_handlers/party-extract.js::buildExtractionPrompt` strict-JSON extraction. **Adjudication** (`party-extract.js`): normalized-core agreement (mirrors `normalize_company`, prefix/suffix firm-name match) → write `party_extract_agree` 0.60; A-only (LLM abstained) → `gliner_extract` 0.55; **value CONFLICT or B-only → NO write, logged** to `party_extract_disagreements` (deliberate tightening of the plan's "or disagreed" wording toward the never-guess doctrine). Writes are fill-blanks-only, through the **field-priority guard**, span-grounded in the reversible ledger `party_extract_batch`. **Migration `20260731120000` applied live to LCC Opps** — 18 `field_source_priority` rows (party_extract_agree@80 / gliner_extract@85, below all aggregators), the ledger + disagreements tables, `W51_PARTY_EXTRACT` flag; `v_field_provenance_unranked` unaffected. **Runner** `scripts/party-extract-backlog.mjs` — `--sample N` (review sheet, no writes), dry-run default, `--apply` (ollama-gated unless `--allow-cloud`/`W51_ALLOW_CLOUD=1`), resumable via the ledger, per-domain before/after report. 18 JS adjudication tests + 48 resolver tests green. **⚠️ GROUNDING (measured live):** the `sale_notes_raw`-only backlog is SMALL (dia 281 / gov 163 live sales have a note); folding dia's `notes` (CoStar marketing description) column lifts dia's addressable set to ~2,242 (988 missing-buyer, 2,195 missing-broker) — gov stays note-starved (~163). So the plan's ">20pt missing-broker drop" is reachable on dia, NOT on gov from notes alone; report per-domain. **Operator prereqs (unchanged, realize value):** RESOLVER_URL (Channel A) + GaryBuilt tunnel + Railway OLLAMA_URL/OLLAMA_MODEL (playbook docs/setup/garybuilt-local-model.md; flag OLLAMA_EXTRACTION off) + domain service keys; then run `--sample 100` → Scott approves → `--apply`. **Cowork verification (session 33): mig live confirmed — 18 fsp rows, both tables, flag off; deploy note: resolver memory bump needed (torch+GLiNER > 2GB) and the redeploy reverts to committed real-corpus floor-clamped models (nightly tick re-trains within 24h, or force one). Side-find during verification: `v_field_provenance_unranked`=31 — all PRE-EXISTING costar_sidebar sales_transactions rows since 2026-07-13, NOT W4.4/W5.1 — see KNOWN_ISSUES entry (W6.6 audit treats as known baseline).** |
| W5.2 | Signal → task automation (state lease / agency risk / NPI) | ✅ CLOSED 2026-08-06 (session 36e) — all three consumers LIVE-VERIFIED end-to-end: state-lease 8/8/0 (dry→apply→idempotent re-run) with 8 structured research_tasks confirmed in ops (all instructions=NULL, all deep-linked); agency-risk + NPI dry-runs green; W5.2b seam fix (PR #1566) proven — LCC ledger via research_tasks, no gov processed_at reads/writes, types partitioned with gov lead-gen. TX chain fully closed-loop (auto-fetch → snapshot → diff → events → leads + distress tasks + dual alarms). BUILT 2026-08-05 (Claude Code) — migrations live, code + tests green, crons scheduled. **Three consumer ticks** in `api/admin.js` (GET dry-run / POST apply, mounted in `server.js`, pg_cron on LCC Opps): `state-lease-consume` (gov distress→research_tasks; consumption ledgered ops-side via research_tasks — W5.2b 2026-08-06 stopped it reading/writing gov `processed_at`, which is lead-gen's seam), `agency-risk-consume` (staleness alarm + auto-dismiss safety valve for the pull lane), `npi-consume` (dia missing/new NPI→research_tasks + ops ledger; dup→digest). **Two Decision-Center lanes** (R48 pull model): `agency_risk_action` (pursue_disposition/monitor/dismiss), `npi_dedup_review` (data_error) + `npi_dedup_autoapprove` (auto_resolvable — human APPROVES the deterministic survivor, NEVER a silent auto-collapse). Migrations live: gov `20260817130000` (agency_risk_signals.processed_at + partial index), ops `20260817131000` (lcc_npi_signal_consumed ledger, hash keyed), ops `20260817132000` (3 crons). Payloads = entity/property ids + a `deep_link` (`#/gov`, `#/dia?d=prop:dia:<clinic>:Overview`); `instructions` always NULL (no prose/SQL). Tests: `test/w5-2-signal-task-automation.test.mjs` (5, green) + partition parity. **Producer-staleness finding surfaced:** state_lease_events is silent since 2026-06-23 (backfill-stamped) — the tick opens a `state_lease_producer_stale` health alert past 45d, so a consumer never runs silently over a dead producer. Grounding refinements vs the prompt: state_lease_events.property_id is 100% NULL so lessor_change can't gate on a tracked link → digest-only (never guess); high agency signals have NULL agency_code/affected_locations → linkage keyed on the `agency` name (`details` jsonb carries the spending/expiration signals). NEXT: Railway deploy of merged main lights the routes; run `npm run verify:deploy`, then dry-run each `_route` before the crons apply. | Deterministic rank thresholds by design — NO LLM in the value gate (keep it auditable). **Grounded prompt at `audit/data-flow-2026-05-30/CLAUDECODE_PROMPT_w52_signal_task_automation.md`.** Live grounding (2026-08-05) corrected the audit premise: (1) `state_lease_events` 577 rows ALL backfill-stamped processed_at=2026-06-23 AND the producer is SILENT since 2026-06-23 — the unit adds a producer-staleness alarm alongside the consumer; (2) `agency_risk_signals` grew to 15,299 (3,508/30d, all signal_type=composite, high=15/elevated=3,124) and has NO seam column — gov migration adds processed_at; (3) `mv_npi_inventory_signals` 1,452 rows is a MATERIALIZED VIEW — consumption ledgered ops-side by stable hash. Routing: research_tasks for research-work, Decision Center queues for decisions (modeled on R48 listing_event consumer: 20260619210000 + api/admin.js queue/resolver shapes); NPI auto_resolvable dupes get a human batch-approve lane, never silent auto-resolve. NEXT: send prompt to Claude Code. |
| W5.3 | Local LLM tail evaluation | ✅ CLOSED 2026-08-06 (Cowork) — **VERDICT recorded, Wave 5 closes.** Graded the real ollama-primary production window (234 extractions since Aug 1) against live LCC Opps data; full report `docs/audits/W5_3_LOCAL_LLM_EVALUATION_2026-08-06.md`. **Split verdict:** (1) **KEEP ollama-primary** on narrative/proposal surfaces (W7.2 summaries — 33 since Aug 1; W7.4 roles/issues — dropped-log 8, validator working; W7.5; next-step): abstain-don't-fabricate behavior confirmed, latency fine (p50 16.1s/artifact). (2) **Intake OM extraction: TUNE + interim revert recommended** — material recall/schema gap vs same-window cloud-fallback extractions (NOI 4% vs 93%, tenant 6% vs 64%, responsibility keys never emitted; sale-schema key drift; PSAs/listing agreements misclassified `om`; one signature-block wrong-party grab; NO fabrication observed). (3) **Seam gaps found:** provider attribution EXISTS (`staged_intake_items.raw_payload.extraction_result.diagnostics[].ai_final_provider`) but only 88/238 items carry it and **17/50 attributed artifacts bypassed ollama entirely** (chain starts edge/400 — a process runs intakes without OLLAMA_URL); true attempted-fallback rate only 2/31 (6.5%). Human-verdict grading NOT measurable yet (feedback corpus = one bulk Jul-27 batch, zero Aug rows) — re-grade when organic verdicts accrue. **Needs Scott:** decide interim revert (env flip) now vs after the prompt-hardening fix-unit. **RE-GRADE PENDING (2026-08-07, Prompt 61 fix-unit landed):** prompt hardened (full-key JSON schema + doc-type rubric incl. psa/listing_agreement/valuation_proposal + party-role + sale-record guards, pure builder `api/_shared/intake-extraction-prompt.js`); Ollama native `response_format:json_object` (OLLAMA_JSON_FORMAT, default on); provider stamp now on `staged_intake_extractions.extraction_snapshot._provider` (no raw_payload join needed); per-surface gating `OLLAMA_SURFACES` (revert intake to cloud while narrative stays local — set `OLLAMA_SURFACES=summaries,roles,narrations,next_step`); loud one-time warn when `OLLAMA_EXTRACTION` on but `OLLAMA_URL` unset (identifies the bypassing process in Railway logs); edge-primary 400 root-caused (copilot `/chat` caps max_tokens=1500 + injects sales system prompt — set `AI_EXTRACTION_PRIMARY=openai` to route extraction off it). **Re-measure after redeploy + ~50 fresh intakes** using the W5_3 report queries: OM ollama field coverage within 15pts of cloud on noi/tenant/cap; zero sale-comp keys on OM docs; zero edge-first chains while the flag is on; every new extraction row provider-stamped. | **Infrastructure REALIZED by GaryBuilt** (post-audit addition, playbook `docs/setup/garybuilt-local-model.md`): Ollama + qwen2.5:14b on the home desktop (RTX 3060 12GB), provider seam ALREADY SHIPPED in api/_shared/ai.js (local primary → cloud fallback, gated on OLLAMA_URL; flag `OLLAMA_EXTRACTION` off pending Railway cutover). Remaining = the EVALUATION the unit asked for: flip intake extraction to local, measure quality/latency vs the edge chain on ~50 real intakes, record verdict here. Session-33 expansion map (beyond the playbook's phase list): (a) W5.1 party-extraction channel B; (b) OM extraction cost-avoidance as W3.7c file discovery scales (gov 679 + dia 465 ids); (c) OPTIONAL future — review-lane pre-screen annotations (non-binding LLM second opinion to triage-order the 3,357 sf_link candidates; labels stay human) and LLM-PROPOSED hard-negative candidate pairs for human adjudication (the shared-token pairs the W4.1 guard refused to auto-label). Neither optional item ships without its own consumer + gate per the producer/consumer doctrine. |

## Wave 6 — Structural consolidation

| Unit | What | Status | Notes |
|---|---|---|---|
| W6.1 | Fetcher/extension CI contract + extension_version stamping | ⬜ | |
| W6.2 | dia/gov SQL templating + drift alarm | ⬜ | |
| W6.3 | unified_contacts home decision (→ LCC Opps) | ⬜ | |
| W6.4 | Delete dead code (api/sync.js copy, sos-lookup stubs, pipeline/) | ⬜ | |
| W6.5 | Front-end decomposition | ⬜ | |
| W6.6 | Monthly standing audit (scheduled task) | ✅ 2026-07-31 | **LIVE (session 33).** Cowork scheduled task `LCC monthly standing audit (W6.6)` (trig_01179VYojpP5SZ1Hegkm7F8b), 1st of each month 14:00 UTC (9am CT), first fire 2026-08-01. Seven-point scorecard: open health alerts (incl. resolver drift-alert self-resolve tracking + current auto_link band), cron health (w44 nightly retrain freshness), the W3.V standing coverage metrics (ownership gaps, SAM drain, review-lane burndown, corpus growth by seeder), consumption-doctrine spot-check (producers without consumers; v_field_provenance_unranked=0), dormant capabilities (>30d off flags, OLLAMA_EXTRACTION called out), disk health, staging/extraction freshness. Writes scorecard to Cortex memory + ROLLOUT_STATUS session log; always notifies Scott (reds first). |

## Corrections log

- **2026-07-31 (W3.7b architecture correction, Scott-grounded):** The discover sweep was built on Connected-App client-credentials (SF_CLIENT_ID/SECRET) — but the org CANNOT provision Connected Apps (SSO-gated, no admin; documented in SALESFORCE_LCC_INGESTION_PLAN.md §2, INVALID_SSO_GATEWAY_URL). STANDING RULE for all future prompts: **every Salesforce read/fetch goes through Power Automate flows → X-PA-Webhook-Secret edge endpoints; never request SF_CLIENT_ID/SECRET or Connected-App provisioning.** W3.7b's server-side discovery is inert as shipped → re-planned as W3.7c (PA-collector variant reusing the same insert logic). The SF-creds operator item is REMOVED from Scott's list.
- **2026-07-30 (W3.4 grounding correction):** `ownership_research_queue.ai_response` is a SEARCH PLAN, not a resolved owner — the "auto-verify" is honestly an auto-RESOLVE of rows whose ownership premise is already satisfied by current recorded/true-owner data. Implication for Wave 4: this queue is NOT a source of resolved-owner labels; entity_match_labels (W3.2) is the training corpus.
- **2026-07-30 (W3.2 grounding correction):** The audit claim "ORE drain deliberately left unscheduled" was REFUTED live — the engine cron was already scheduled AND auto-merging (source=candidates, no merge=0 → doMerge defaulted true; 2 pairs already merged before the fix). W3.2 replaced it with the verdicts-only queue drain; watchdog + depth alert now make mis-scheduling visible. Implication: W3.3's merge audit must include the ORE candidate-mode merges, not just owner_merge_tick.
- **2026-07-30 (walkthrough error, mine):** I twice directed Scott to find "Extract Latest Screenshot" (the Gov Evidence card) — that card is HARD-DISABLED by an unconditional early return in renderGovEvidenceWorkbench() and has never been renderable in production. Lesson: before giving a UI walkthrough from code, check the render function for early returns / deployment gates, and check the feature registry. (The registry now carries GOV_EVIDENCE_WORKBENCH so this class of confusion is visible going forward.)

- **2026-07-29 (W0.7):** The audit premise "front-end calls RPCs with the anon key" is REFUTED. The browser calls **zero** DB RPCs as anon — every SPA-triggered RPC goes through a server-side **service_role** path (`/api/dia-query` + `/api/gov-query` use `*_SERVICE_KEY`; LCC RPCs go through admin.js `opsQuery`/`/api/data-query`). `auth.js`'s anon `supabase` client is auth-only (no `.rpc()`/`.from()`). The W0.7 exception lists are therefore *defensive*, not load-bearing. **Effectiveness correction:** anon reached SECURITY DEFINER fns via BOTH an explicit `anon=X` grant AND the `PUBLIC` (`=X`) grant, so a bare `REVOKE FROM anon` (as the unit title implies) is cosmetic — the migration also revokes `PUBLIC` (after granting authenticated+service_role to preserve access some reached only via PUBLIC). Rule for future anon-revoke work: check `proacl` for the `PUBLIC` (`=X`) entry, not just the role grant.
- **2026-07-29 (W0.3f):** `agency_debt_programs.matched_property_id` is NULL on all 1.55M rows, so the `merge_dialysis_dup_property()` repoint the W0.3e check flagged was always a no-op; removing it changes no behavior. The dia table also still has a live-ish reference the "zero code refs" claim missed: `Dialysis/src/ingest_agency_debt.py` (a MANUAL `python -m` CLI, not automated, write-only). Archiving is safe (takes it out of the API); the CLI itself should be retired in a DialysisProject session.
- **2026-07-29 (W0.8):** The advisor treats indexes as "identical" only when uniqueness ALSO matches (it did not flag unique-vs-non-unique pairs on the same columns, e.g. dia `idx_cfe_unique_primary_per_clinic`/`idx_cfe_medicare_primary`). Dropping a group's non-constraint member can leave a residual BOTH-constraint pair (e.g. dia `property_embeddings` pkey/`property_id_key` after `property_id_uq` was dropped) — expected; those need `ALTER TABLE DROP CONSTRAINT`, out of scope.
- **2026-07-29:** W0.3 prompt in the plan wrongly listed `dq5_*`/`dq7_*` as archive candidates. They are LIVE ledgers (`apply_owner_merge()` writes dq5_owner_merge_log; hourly gov owner_merge_tick is an active caller; W3.3 audits them). They were never moved; plan text corrected. Lesson applied: before archiving anything, check pg function bodies (`pg_get_functiondef`) for table references, not just views + app code.
- **2026-07-29:** field_cleanups "0 rows" claim was stale planner stats; actually 838k rows (legacy ledger, archived not dropped).
- **2026-07-29 (W0.3e):** The pg-function reference check found 4 archived tables that are live write-targets of running functions: `cap_recompute_backup` (dia AND gov — written by the nightly `*_recompute_caps_*` functions), `gov_sale_lease_date_backup` (written by `gov_guard_sale_lease_dates`), and `agency_debt_programs` (`merge_dialysis_dup_property` repoints `matched_property_id` on every dia property merge). All 4 restored to public via `w0_3e_restore_function_referenced_tables`; manifest cleaned. Post-check: 0 remaining function references to archived tables on both DBs. FOLLOW-UP (W0.3f, Code session): remove the `agency_debt_programs` UPDATE from `merge_dialysis_dup_property`, then re-archive that table per Scott's standing approval. Rule for all future archive/drop work: the sweep MUST include `pg_proc.prosrc` + trigger + view + app-code checks.
- **2026-07-29:** gov's 4 DEPRECATED tables: archived with drop_after=2026-10-27 is the INTENDED end state (Claude Code question answered — no further action; the manifest policy handles the hard drop). EXCEPTION: lease_lifecycle_events restored 2026-07-29 (W0.3g) pending path repoint (W0.3h).
- **2026-07-29 (from PR #1494):** the "zero code refs" check for archived tables was repo-local only. Sibling repos (DialysisProject, GovernmentProject) had live references: field_cleanups (~15 Dialysis/src modules, graceful degradation) and lease_lifecycle_events (erroring user path — restored). RULE: archive/drop sweeps must grep pg functions + triggers + views + THIS repo + BOTH sibling repos.
- **2026-07-29 (from PR #1495):** audit env-var name correction: OWNER_ENRICH_ADDRESS_URL (not OWNER_ENRICH_SOS_ADDRESS_URL); SF_LIST_IMPORT_URL gates a Power Automate flow, not a process.env check.
- **2026-07-29 (from PR #1493):** the "§F Edit-truncation warning" lives in GAPS_AND_FINDINGS_REGISTER.md §F (not in CLAUDE.md/AGENTS.md — the W0.2 session grepped only the latter two and reported it missing). The session-rules block in the rollout plan cites the right source; no change needed, noted to avoid future confusion.

## Session log

- **2026-08-07 (Claude Code, Prompt 61 — W5.3 ollama extraction hardening + seam fixes):** Landed the intake-tune fix-unit the W5.3 verdict called for. **(1) Prompt hardened + made pure/testable** — extracted the OM extraction prompt into `api/_shared/intake-extraction-prompt.js::buildExtractionPrompt` (one prompt, model-agnostic): strict FULL-key JSON schema (`EXTRACTION_SCHEMA_KEYS`, exact keys, null for absent, no extras), a doc-type rubric with the real vocabulary incl. `psa`/`listing_agreement`/`valuation_proposal` (+ one-line defs) to stop the PSA→om drift, a party-role guard (signature/broker blocks are NOT seller/buyer), a sale-record guard (no `sold_*`/buyer on an on-market OM), and the responsibility keys the model was dropping — abstain-don't-fabricate preserved. **(2) Ollama native structured output** — `invokeOllamaExtraction` sends `response_format:json_object` gated on `OLLAMA_JSON_FORMAT` (default on; flip to 0 to revert to prompt-only). **(3) Provider stamp on the snapshot** — `staged_intake_extractions.extraction_snapshot._provider` (`{final_provider, final_model, fell_back, chain}`) via pure `buildProviderStamp`, so grading no longer needs the raw_payload join (only 88/238 carried it). **(4) Per-surface gating** — `invokeExtractionAI({prompt, surface})` + `OLLAMA_SURFACES` env map (unset = all surfaces, prior behavior; set = only listed). Intake tagged `intake`, narrative lanes tagged `summaries`/`roles`/`narrations`/`next_step`, W8 hygiene `clean_assist` — so the interim revert is `OLLAMA_SURFACES=summaries,roles,narrations,next_step` (intake→cloud, narrative→local). **(5) Bypass detection** — loud one-time warn (`warnIfOllamaMisconfigured`) when `OLLAMA_EXTRACTION` is on but `OLLAMA_URL` is unset in the executing process; grep Railway logs for it to identify the OLLAMA_URL-less service (the 17/50 edge-first bypass). **(6) Edge-400 root-caused** — the default cloud primary routes through the ai-copilot `/chat` edge, built for short sales chat: it caps `max_tokens=1500` (truncates OM extractions) and injects a sales system prompt (biases JSON); set `AI_EXTRACTION_PRIMARY=openai` to route extraction off it entirely (self-contained OpenAI, no 400, no bias) — default preserves prior `edge` behavior; edge `max_tokens` also made env-configurable (`AI_COPILOT_MAX_TOKENS`, needs a Dialysis-project deploy). **Service audit (#3):** intake extraction executes only in `server.js` (Railway service `tranquil-delight`) via `/api/intake-extract[-drain]` + the copilot ingest_pdf action; the MCP server (`mcp/server.js`, separate service) proxies to `LCC_API_BASE` and does NOT run extraction — so the bypassing process is a `server.js` instance whose Railway env lacks `OLLAMA_*` (verify OLLAMA_URL/MODEL/CF_ACCESS_* on every service/replica that boots server.js; the new warn now names it in the logs). Tests: `test/ollama-extraction-hardening-prompt61.test.mjs` (18, green). Full suite green; 116-comps untouched. **Operator (Scott):** decide the interim revert env (`OLLAMA_SURFACES`), confirm/set `OLLAMA_*` on all server.js services, optionally `AI_EXTRACTION_PRIMARY=openai`; then re-grade per the W5.3 row's acceptance queries after ~50 fresh intakes.
- **2026-08-06 (Claude Code, W7.4 — role evolution + open-issues surfacing; the last Wave 7 unit):** BUILT, flag off (`W74_ROLE_ISSUES`). Migration `20260822120000` applied live to LCC Opps: `lcc_deal_dossier_analysis` (versioned role-set/issue-set per deal, kind=roles|issues, `is_current` flip, watermark), `v_lcc_deal_dossier_analysis_current`, `lcc_deal_analysis_dropped_log` (validator drops, audit-only), +5 W7.4 counters on the run-log, flag registry row. New pass on the W7.2 tick (AFTER summaries/cues/to-dos, isolated try/catch so a failure never blocks them; own watermark over the comm set → idempotent 0-writes on an unchanged corpus): one Ollama call/deal (`invokeExtractionAI`) proposes party ROLES + OPEN ISSUES, each carrying evidence (comm id + verbatim quote). `api/_shared/deal-role-issues.js` VALIDATES every proposal — a quote not appearing verbatim (whitespace-normalized) in the CITED comm is dropped + logged (wrong-comm-id also dropped), so nothing fabricated reaches the dossier. Issue lifecycle: a later comm answering an open issue flips it to resolved (with closing evidence) via a new versioned row; the panel shows open + recently-resolved. Stage awareness (`api/_shared/deal-stage-line.js`) is 100% deterministic from the milestone set — latest stage + a regression flag when an earlier stage re-occurs after a later one (verified against the live Banning shape: diligence→3×loi flags regression; Villages same-day psa+escrow does NOT false-flag). Dossier gains a "What's Coming / Open Issues" panel + an emerging-roles note under Parties, both labeled ANALYSIS with collapsible evidence; `buildDealPacket` loads the analysis rows so the dossier `source_hash` regenerates when they change. Read-only dry-run: `GET /api/deal-comms-propagate-tick?dry_run=1&force=1&roles=1` returns proposed roles/issues + evidence for the top comm-active deals (Ollama runs on Railway, not the sandbox — the model-facing dry-run is captured there). Tests: `test/deal-role-issues.test.mjs` (28, green) — validator (fabricated/verbatim/normalized/cross-comm/unknown-comm), idempotency watermark, issue lifecycle (open/close/carry/fabricated-close-rejected), deterministic stage line (Banning + Villages), render. Full suite green. **Operator (Scott):** eyeball the dry-run on Railway, then set `W74_ROLE_ISSUES` to enable (the parent `DEAL_COMMS_PROPAGATE_ENABLED` still gates the tick). WAVE 7 build work is now complete (all units built; flags stepwise).

- **2026-08-06 (Cowork, session 36q — the THIRD Copilot surface: agent instructions/canon):** Scott re-imported v4.1.0 but couldn't see the new tools — correctly so: dispatch-routed actions are enum values inside `dispatchCopilotAction`, NEVER separate tiles in the Studio tool list; the agent uses them only when its INSTRUCTIONS say so. Those instructions are rendered from the canon system (`docs/os/canon/blocks/` → CANON_VERSION → `render-surfaces.mjs --write-live` → `docs/copilot/agent-instructions.md` → paste → Publish) — a surface the 36p drift sweep missed. **Fixed:** `blocks/logging-and-touchpoints.md` now teaches `log_call_note` + `tag_comm_to_deal` (dispatch usage, pick-list-on-ambiguity, restamp-refusal); CANON_VERSION → **1.4.2** with changelog; COPILOT_STUDIO_SETUP banner expanded to the **three-surfaces rule** (server registry + connector enum + canon/instructions move TOGETHER; parity guarded by check-parity.mjs + the connector drift test). **Operator (Scott):** `node docs/os/tools/render-surfaces.mjs --write-live` → paste rendered agent-instructions.md into Copilot Studio → LCC Deal Agent → Instructions → Publish → then re-test the two actions in Copilot chat (invoke by ASKING, e.g. "log a call on the Banning deal…" — not by looking for a tool tile).

- **2026-08-06 (Cowork, session 36p — Copilot drift review PROJECT-WIDE + permanent guard):** Full sweep of every Copilot action surface. **Result: after the 36o fix, ZERO functional drift** — `operations.js ACTION_REGISTRY` (48) === v4 spec dispatch enum (48) exact both directions; the 3 comps actions in the docs registry route via DEDICATED spec operations (QueryComps/SynthesizeComps/GenerateComps — by design, reachable); `copilot/actions/*.yaml` (3 declarative specs) map to dedicated v4 operationIds (contextRetrieveEntity/intakeStageOm/memoryLogTurn — present); the Teams/declarative-agent manifest package (`docs/setup/copilot_studio_manifest/`) contains no action list of its own (declarativeAgent → ai-plugin → the connector) so it cannot drift; the docs registries (.md/.json, 33 actions) are a CURATED subset by design — rule stands that new actions get added there (W7.3 did). **Future-proofing shipped: `test/copilot-connector-drift.test.mjs`** — 3 CI assertions: (1) ACTION_REGISTRY ≡ dispatch enum exact-set (either-direction drift fails the build with a fix-instruction message), (2) every documented registry action reachable via enum or dedicated operation, (3) exactly ONE spec file in `copilot/` (legacy specs must live in docs/archive/openapi-legacy/). Logic verified green against current state (python emulation — no node in the bridge VM; the .mjs runs in CI/next Claude Code session). Working-tree additions (36o+36p): v4.1.0 spec, v3 archived, SETUP banner, drift test, this log — COMMIT WITH NEXT PULL/PR.

- **2026-08-06 (Cowork, session 36o — Copilot connector single-source-of-truth fix):** Scott flagged the swagger-editor spec drift. Confirmed: **`copilot/lcc-deal-intelligence.connector.v4.swagger.json` is THE canonical spec** for the one "LCC Deal Intelligence" Power Platform custom connector (v4.0.0, last touched Aug 1) — and the W7.3 actions were registered server-side + in the architecture registry but NEVER added to the spec's `dispatchCopilotAction` → `copilot_action` enum (46 entries), so Copilot couldn't see them. Fixed: enum → 48 (added `log_call_note` + `tag_comm_to_deal`), version bumped **4.1.0** with changelog in info.description. **De-dup:** legacy v3 spec (Jul 27, 94 ops, duplicate snake/Pascal naming) moved to `docs/archive/openapi-legacy/` alongside v2 — `copilot/` now holds exactly ONE spec. COPILOT_STUDIO_SETUP.md gains a canonical-spec banner + the standing rule: **server-side ACTION_REGISTRY/ACTION_SCHEMAS and the v4 spec move TOGETHER; re-import updates the EXISTING connector, never create a second one.** Operator: re-import v4.1.0 via the swagger editor into the existing connector, then the Copilot verify steps (log_call_note + tag_comm_to_deal live). These edits are working-tree — commit with next pull.

- **2026-08-06 (Cowork, session 36n — W7.2c + W7.3/M365 prompts drafted):** Two prompts per Scott's direction (refinements first, then call/M365 capture — states still parked). **W7.2c `CLAUDECODE_PROMPT_w72c_tick_refinements.md`:** (1) milestone same-key collapse — first occurrence = THE milestone, re-occurrences roll to metadata ×count/last_seen (+ migration collapsing Banning's 6+ loi rows; deterministic new-row exception for stale+regressed re-openings); (2) briefing "what changed on your deals" delta from the propagation ledger (deterministic, one line/deal, omit-when-empty); (3) incremental summary compression (compressed_block + watermark in summary metadata — keeps GaryBuilt latency flat as threads grow); (4) reply-SLA to-dos (inbound unanswered >3 business days → `reply_overdue` via the Phase-1 guarded path; dry-count before first live generation). **W7.3 `CLAUDECODE_PROMPT_w73_call_notes_m365_capture.md`** (expanded per Scott's Microsoft ask): three capture paths all landing as deal-stamped dual-anchor activities the LIVE tick already propagates — (A) in-app quick-log call (+ optional Ollama structuring, proposal-only, never blocks); (B) TWO new Copilot Studio connector actions per the registry standard: `log_call_note` + `tag_comm_to_deal` (ambiguous deal → pick-list, NEVER guess; tag refuses cross-deal restamp) — usable from Copilot in Outlook/Teams as Scott works; (C) Outlook CATEGORY tagging at send time (`LCC` / `LCC:<hint>`) → PA flow → `POST /api/intake-tagged-comm` receiver (idempotent, unresolved → park lane). NEXT: Scott → Claude Code (either order; W7.2c is smaller), then live verify each capture path end-to-end through the tick.

- **2026-08-06 (Cowork, session 36m — W7.2 LIVE-VERIFIED → flag ON; the comms→context loop Scott asked for IS RUNNING):** Dry-run reviewed + approved (10 deals / 261 backlog comms, 28 deterministic cues incl. Banning 19, todo_eligible=0 — the 7-day window correctly zeroed historical to-do spam). Live tick fired: **239 comms ledgered, 11 is_current summaries** — spot-checked GROUNDED + CITED (IRA Capital OM receipt on Pops Mart Barnwell w/ 3 cited activities; Sal Cammarata inquiry on 519 N Main; Fremont Plaza listing facts), **24 milestones written, every one carrying its source email as detail_ref**, 0 to-dos (correct). `DEAL_COMMS_PROPAGATE_CRON` → **on**; hourly cron (:32) drains the remaining backlog batches then runs steady-state. GaryBuilt now carries the summary workload hourly (skips counted when the box sleeps — W6.6 watches `summary_skipped`). **First-live-batch refinement → W7.2b queued (WAVE7 plan updated):** repeat same-key cues write one milestone PER occurrence date — Banning shows 6+ 'loi' rows spanning months of LOI negotiation; collapse policy = first occurrence is THE milestone, later re-occurrences roll into metadata (count/last_seen). Small + reversible. **Remaining Wave 7:** W7.2b collapse → W7.3 call notes (Scott's capture-option decision) → W7.4 role/issues. End-to-end proof pending Scott's next real deal email (summary+to-do+dossier should all move unprompted).

- **2026-08-06 (Cowork, session 36l — W7.2 grounded + prompt drafted):** Propagation-tick prompt written: `audit/data-flow-2026-05-30/CLAUDECODE_PROMPT_w72_propagation_tick.md`. Grounding wins: **next-step engine Phase 1 is ALREADY BUILT and firing on live inbound** (`next-step-ai.js::deriveNextStep` → `lcc_advance_todos` +3 AI params, deterministic-first + AI escalation) — W7.2 REUSES it for newly attributed recent inbound (7-day window, existence-guard dedupe) instead of forking; milestone/summary tables verified (is_current versioning, milestone_key/detail_ref shapes); dossier source_hash reuse-if-fresh confirmed. Tick design: own ops-side ledger (`lcc_deal_comm_propagated`, W5.2b seam doctrine), hourly cron offset after the matcher, flag-gated, bounded 10 deals/tick for calm backlog catch-up; four propagations — (1) Ollama-authored correspondence summaries under the no-fabrication contract (LCC's biggest recurring local-LLM workload, by design), (2) milestone detection: deterministic cues write with detail_ref / LLM candidates → `milestone_confirm` Decision-Center lane (never-guess), (3) Phase-1 to-dos for recent inbound only, (4) dossier regeneration via changed source_hash + context-packet refresh. NEXT: Scott → Claude Code → dry-run → flag on → live verify (15 deals summarized, Woodland Hills gold-standard check, fresh-email end-to-end).

- **2026-08-06 (Cowork, session 36k — W7.1 VERIFIED LIVE + ORE DRAINING; alert 982 closed):** Post-merge re-fire of both crons, all green. **Matcher (W7.1):** live pass attributed +69 new deal-stamped activities (243→312 `lcc:deal_match`), email-derived roster edges 60→81; the immediately-following hourly cron pass proved idempotency perfectly — `ok:true, scanned 36, with_matches 15, emails_attributed 0, already_attributed 251, errors 0`. Per-deal distribution matches the approved dry-run shape (Queens 46 / The Villages 45 / Banning 44 / Tucson 33 / ECU Greenville 27 …). The delta vs the ~306 dry-run candidates = emails already attributed once to another deal (one-deal-per-email rule) — correct behavior. Hourly cron + run-log live; W7.1 mechanical layer CLOSED (remaining: Outlook `deal_thread_search` PA flow for the 21 zero-match deals — operator, spec in docs/setup/). **ORE:** dequeue fix verified — one tick: 25/25 processed, 0 failed, queue 2,064→2,039 / done 15→40; resolver gate active (fail-closed). Queue clears ~3.5 days at 25/hr. **Alert 982 RESOLVED** with the two-part root cause (Jul-31 PostgREST overload collision + zero-pair dequeue skip). Alert board now: 3 by-design drift alerts + the domain-propagation re-capture check (Scott sidebar test pending). **NEXT: W7.2 propagation-tick prompt** — the attribution fuel is landing; the deal spine is ready to tick.

- **2026-08-06 (Cowork, session 36j — ORE dead-engine ROOT-CAUSED: name collision + dequeue bug; collision FIXED live):** The scheduled ORE check found the queue still frozen (2,064/15) with the tick reporting `failed:25` every run. **Root cause #1 (FIXED live):** migration `20260818160000_lcc_owner_reconciliation_core` (applied Jul 31, the SF-signal owner-evidence subsystem) created `lcc_reconcile_owner(uuid,numeric,boolean)` COLLIDING with the ORE's `lcc_reconcile_owner(uuid)` pair-generator → PostgREST 42725 "function is not unique" on every `rpc/lcc_reconcile_owner` call since Jul 31 — the ORE engine has been dead since that exact day. Migration `lcc_fix_reconcile_owner_name_collision`: renamed the evidence scorer to `lcc_reconcile_owner_evidence` + repointed its single SQL caller (`lcc_reconcile_all_owners`); no JS callers. Verified: next tick `owners_processed:25, failed:0`, resolver gate active (enabled+configured). Registry flag `ORE_USE_RESOLVER` → **on**. **Root cause #2 (JS fix pending):** the engine only marks a queue row done in the has-pairs branch — zero-pair owners (`if (!pairs.length) continue`, the COMMON case) are never dequeued and re-picked every hour forever. One-line fix: dequeue (source=queue && !dryRun) BEFORE the zero-pairs continue. Alert 982 stays open until the dequeue fix lands; queue drains ~25/hr thereafter (≈3.4 days). **Hazard-class note for CLAUDE.md/W6.6: function-name collisions across subsystems on PostgREST-exposed RPCs — a new overload silently kills every existing rpc caller (42725); consider a standing advisor check for multi-overload public functions.**

- **2026-08-06 (Cowork, session 36i — W7.1 grounded + prompt drafted):** Wave-7 kickoff per Scott's approval. Grounding: the deal-email-matcher is MOUNTED (engine `POST /api/pipeline/match-deal-emails`) and has run at least once — 90 deal-tagged activity_events, 60 email_derived deal_party edges (252 total) — so W7.1 = ran-once → always-on. **Prompt: `audit/data-flow-2026-05-30/CLAUDECODE_PROMPT_w71_correspondence_attribution_live.md`**: (A) full-corpus DRY-RUN report → Scott approves → live run → hourly cron (W5.2 pattern, idempotent, loud-failure); (B) ingest-time deal stamping (lcc_resolve_contact → deal mapping + conversation_id continuity) so known-thread mail doesn't wait for the matcher; (C) connector-ready backfill plumbing (lcc_deal_correspondents fn + POST /api/intake-deal-backfill; PA/Graph flow spec documented for the operator). Precision doctrine locked in the prompt (city required; no LLM in attribution). Ollama employment noted for W7.2+ (summaries/proposals only).

- **2026-08-06 (Cowork, session 36h — PR #1568 merged; PA reds resolved; WAVE 7 registered as the next build):** Triage-fixes PR merged (domain-code normalizer + thin-capture alert suppression), redeploy in flight — verify with a sidebar re-capture of a dia entity (expect propagation, not unknown_domain). Prune-migration repo mirror written to `supabase/migrations/20260820140000_lcc_prune_skip_resolution_referenced_provenance.sql` (ALREADY APPLIED LIVE — mirror only, do not re-apply; commit with next pull). PA reds resolved from Scott's run histories: RCM = Outlook mark-as-read race (object moved/deleted before the action; harden with Configure-run-after), HTTP-Switch = transient Aug-2 spike (thousands green since). ORE: Scott set ORE_USE_RESOLVER + RESOLVER_URL (`https://gracious-radiance-production-eeaf.up.railway.app`); drain verification scheduled (+80min self-check) → then registry flag → on + alert 982 close. **DIRECTION (Scott): more states pushed to later chats; NEXT BUILD = comms-driven context propagation** — plan written: `docs/architecture/WAVE7_COMMS_CONTEXT_PROPAGATION_PLAN.md` (consolidates the existing dossier program + correspondence-ingestion design + deal spine + next-step engine into 4 sequenced units: W7.1 correspondence attribution live → W7.2 propagation tick → W7.3 call notes → W7.4 role/issue surfacing). Grounding: 22,881 emails flowing, deal spine live-but-empty (1 dossier / 1 summary / 3 milestones), matcher built, generator built — the gap is purely the propagation connective tissue. Backlog #19 in AUDIT_REFRESH; sequencing revised there.

- **2026-08-06 (Claude Code — ALERT TRIAGE FIXES: domain-code normalization + thin-capture alert noise, branch `claude/alert-triage-domain-noise-m5b07t`):** Implemented the two fixes from session 36g's `CLAUDECODE_PROMPT_triage_fixes_domain_codes_and_alert_noise.md` (AUDIT_REFRESH §3A). **Fix 1 (REAL BUG #1):** `propagateToDomainDb` (`api/_handlers/sidebar-pipeline.js`) rejected the SHORT domain codes `entities.domain` stores exclusively (dia 14,167 / gov 23,622 / lcc 22,006 / cre 99; zero long-form rows) → every sidebar RE-capture of an existing dia/gov entity (preserve-existing branch of `classifyAndUpdateDomain` returns the stored short code) silently skipped domain-DB propagation with reason `unknown_domain`. Added pure exported `normalizePropagationDomain()` (dia→dialysis, gov→government, accept both; lcc/cre/other→null=unknown_domain) at the single dispatcher choke point (both callers in `processSidebarExtraction` route through it; `classifyAllApplicableDomains` already emits long forms so no caller change needed). **Inverse-hazard note documented in-code:** the reason zero long-form rows exist is `canonicalEntityDomain()` normalizes every `entities.domain` WRITE down to short form + the `chk_entities_domain` CHECK enforces it — the two conventions (short in the column, long for the propagation router) are now both spelled out at the function. **Fix 2:** thin-capture `no_domain` alerts (8 of 9 open `sidebar_promote_pipeline_failed`: searchTextLen 56–97, no sale notes, no PDFs — correctly-declined CoStar contact fragments) were noise. Added pure exported `shouldAlertPipelineFailure()` — suppresses the per-capture `lcc_health_alerts` row only when `reason='no_domain'` AND thin (`searchTextLen<150` && `!hasSaleNotes` && `!hasPdfTexts`); substantive no_domain (rich text / sale notes / PDFs = potential classifier gap) KEEPS its alert; `unknown_domain` + every other reason alert unchanged. Suppressed thin captures are counted + surfaced in the promote response diag (`thin_no_domain_suppressed`) + a console line. Tests: `test/sidebar-alert-triage.test.mjs` (21 cases: normalizer short/long/lcc/cre/edge + alert thin/rich/salenotes/pdf/unknown/boundary-150) green; `test/sidebar-promote-outcome.test.mjs` still green; syntax-checked. JS-only → live on Railway redeploy of merged main; **VERIFY after redeploy:** Scott triggers a re-capture of an existing dia entity from the sidebar → response diagnostics must show domain propagation (not `unknown_domain`). Context already handled per prompt (NOT redone): field-provenance-prune FK migration (applied live session 36g), sf_sync_queue apostrophe row (closed), ORE queue depth (operator flag ORE_USE_RESOLVER, out of scope).
- **2026-08-06 (Cowork, session 36g — ALERT TRIAGE SWEEP: 19 → 7 open, 2 real bugs found, 1 fixed live):** Backlog #1 executed; AUDIT_REFRESH §3A rewritten with full dispositions. **Fixed live:** field-provenance-prune nightly cron was failing on FK 23503 (prune deleted provenance rows still referenced by `field_provenance_resolutions.current_provenance_id` — 3rd instance of the never-delete-referenced hazard class) → migration `lcc_prune_skip_resolution_referenced_provenance` excludes referenced ids; verified clean (8,551 pruned). **Root-caused + data-fixed:** SF→LCC Retry & Dead-letter flow failures — the flow re-picked the single ancient failed sf_sync_queue row (Apr-2026 acknowledged SOQL-apostrophe bug) every run; row closed failed→done with terminal note. **Self-healed:** gov loans feed (latest 2026-08-03). **Noise dispositioned:** 8× thin-capture no_domain sidebar alerts (classifier correctly declined 56–97-char contact fragments). **REAL BUG #1:** `entities.domain` uses short codes exclusively (gov 23,622/dia 14,167) but `propagateToDomainDb` accepts only long names → re-captures of existing dia/gov entities silently skip domain propagation → prompt `CLAUDECODE_PROMPT_triage_fixes_domain_codes_and_alert_noise.md` (also downgrades thin-capture alerts to digest). **REAL FINDING #2:** ORE queue depth (2,064 queued / 15 done ever) is NOT capacity — `ORE_USE_RESOLVER` flag is still OFF: the W4.4 operator step (set ORE_USE_RESOLVER=1 + RESOLVER_URL on LCC Railway, flip registry flag) never completed; safe to enable now (fail-closed gate, trained+floored resolver). **Remaining open (7):** the 2 PA-side reds (RCM ×5, HTTP-Switch ×14 — Scott to open run histories), 3 by-design drift alerts, + the two bug/operator items above until fixed/flipped.

- **2026-08-06 (Cowork, session 36f — AUDIT REFRESH + project backlog register):** Full catalog written: **`docs/audits/AUDIT_REFRESH_2026-08-06.md`** — scorecard (W0 16/18, W1 11/13, W2–W4 CLOSED, W5 2/3, W6 1/6), verified-live systems inventory, gap assessment, and an 18-item PROJECT BACKLOG (one pick-up-anytime unit per future chat; convention: "Pick up backlog item N from AUDIT_REFRESH_2026-08-06"). Live gap findings: **19 open health alerts incl. a fresh Aug-5 cluster (9× sidebar_promote_pipeline_failed + lcc_health_red ×3 + cron_failure + flow_failure) → recommended NEXT session: alert triage sweep (backlog #1)**; unranked provenance 33; W5.1 disagreement pool 113 (no consumer yet, deliberate); retrain history 21 runs (7 nights × 3 models — loop healthy); labels 145. Recommended sequencing: #1 alert triage → #2 LA onboarding → #4 W5.3 eval → #5 W6.5 front-end split. Refresh convention added: re-catalog at each wave close or monthly with W6.6.

- **2026-08-06 (Cowork, session 36e — W5.2 CLOSES; TX state-lease chain closed-loop):** W5.2b (PR #1566) live-verified exactly to spec: GET scanned=8 (6 removed + 2 agency_change, relocated correctly in digest), POST created 8, second POST scanned 0 / deduped 8. Ops verification: 8 `research_tasks` (source_table=state_lease_events), 8/8 instructions=NULL, 8/8 with deep_link. Digest healthy (renewed 17 / new_lease 5 / lessor_change 9 / relocated 1); producer age 0d. **W5.2 row → CLOSED.** The full TX chain now runs hands-off: registry-driven auto-fetch (monthly workflow) → snapshot → diff → events → gov prospect_leads (lead types) + LCC distress research_tasks (no-lead types) with the producer-staleness alarm and workflow loud-failure as the double net. **NEXT: LA onboarding (multi-state plan U2)** — source recon for Louisiana Division of Administration lease inventory → `state_lease_sources` row + parser adapter (148 tracked state-gov props waiting). Standing open items: CF token rotation (deferred to build completion), W5.3 evaluation (diagnostics accruing), W0.5b/W0.6/W1.5/OpenCorporates parked decisions.

- **2026-08-06 (Claude Code — W5.2b state-lease seam-contention FIX, branch `claude/state-lease-consumer-seam-ehsk8c`):** Surgical fix in `api/admin.js::handleStateLeaseConsume` + tests (no gov change, no migration). **The tick no longer reads OR writes gov `state_lease_events.processed_at`** — that column is gov lead-gen's seam (stamped by `state_events_to_leads` inside pipeline step 44 on EVERY event, including the `no_lead_event_type` distress rows), so the old `processed_at=is.null` filter scanned 0 forever and the no-lead distress tail died silently. **LCC now tracks its OWN consumption ops-side**: `research_tasks` (`source_table='state_lease_events'`, `source_record_id=<event id>`, any status) IS the per-event ledger; candidates are fetched by distress event_type + `created_at >= 2026-08-01` floor (excludes the 2026-06-23 backfill), then filtered against the ledger — re-runs create ZERO duplicates (the `uq_research_tasks_open_source` open-dedupe index backstops races). **Type partition with lead-gen (no double-surfacing):** LCC task set = `removed`/`footprint_reduction`/`agency_change` (the types lead-gen dispositions as no_lead_event); **`relocated` dropped to the digest** (lead-gen already mints a prospect_lead for it). Digest counts (`renewed`/`new_lease`/`lessor_change`/`relocated`) re-keyed on a 30d `created_at` window (can't use processed_at either). Producer-staleness alarm unchanged (keys on max(created_at)). Tests updated: distress set without `relocated`, consumption independent of processed_at (events WITH processed_at still task), idempotent re-run via the ledger, and an assertion the tick NEVER writes gov `state_lease_events` — 5 green. NEXT (live, after merge + Railway redeploy): GET `/api/state-lease-consume` should show scanned=8 (6 removed + 2 agency_change); POST/cron creates exactly 8 research_tasks; second run creates 0.
- **2026-08-06 (Cowork, session 36d — TX producer re-run GREEN + seam-contention finding + W5.2b prompt):** Run #554 (gov PR #364 merged) verified end-to-end: date-fallback rescued the 3 undated files as HISTORICAL BACKFILL (in-workbook as-of dates 2018-01/2018-05/2019-06 — snapshot history now 6 dates back to 2018), contacts reconcile soft-retired 59 referenced contacts (zero FK errors), August snapshot diffed vs June → **40 events** (new_lease 5 / removed 6 / lessor_change 9 / renewed 17 / relocated 1 / agency_change 2), 13 prospect_leads inserted, producer age reset (staleness alarm satisfied). **NEW FINDING — seam contention:** ALL 40 events immediately carry `processed_at`, stamped by gov `state_events_to_leads` (runs inside step 44 right after the diff; marks even `no_lead_event_type` rows). The W5.2 LCC tick filters `processed_at=is.null` → scans 0 forever; the no-lead distress events (6 removed + 2 agency_change) die silently between the two consumers. The 2026-06-23 "backfill stamp" was actually lead-gen's seam all along — W5.2 grounding misread it. **Fix prompt drafted: `audit/data-flow-2026-05-30/CLAUDECODE_PROMPT_w52b_state_lease_seam_fix.md`** (LCC repo, surgical): LCC tick gets its OWN ops-side consumption tracking (stops reading AND writing processed_at), event types PARTITIONED with lead-gen (LCC takes removed/footprint_reduction/agency_change — the no-lead tail; relocated moves to digest since lead-gen leads it), stranded 8 events recovered by watermark, June backfill stays excluded. NEXT: Scott → Claude Code → merge → GET dry-run should show scanned=8 → 8 research_tasks.
- **2026-08-06 (Claude Code, GovernmentProject repo `claude/tfc-contacts-sync-fk-fix-sotsw6` — TFC contacts-sync FK fix + filename-date fallback + Phase-2 auto-fetch BUILT):** Fixed the two run-#552 producer bugs and built the §10 Phase-2 unit. **Bug 1 (BLOCKING, fixed):** `ingest_texas_tfc.ingest_tfc` no longer delete-and-recreates TFC contacts. New non-destructive `_reconcile_source_contacts` upserts on the stable identity `(name, lessor email, lessor company)` — matched rows updated in place (contact_id + all FK back-refs preserved), new inserted, disappeared rows deleted ONLY when unreferenced by ANY FK (checked across true_owners/recorded_owners/contact_aliases/properties/sales_transactions×2; a referenced disappeared contact is soft-retired + logged, never deleted; ref-check errors fail SAFE = keep). Migration `sql/20260806_gov_tfc_contacts_reconcile.sql` (applied live to gov `scknotsqkcheojiaewwh`): partial unique index `ux_contacts_tfc_identity` on the identity `WHERE data_source='tfc_state_inventory'` (544 TFC contacts, all distinct → clean build) + additive `state_lease_sources.last_content_sha`. The contacts sync is wrapped so a failure logs loudly (ERROR + `contacts_sync.status='error'`) but NEVER aborts the property/child propagate or the downstream snapshot diff/eventing; `ingest_tfc_file`'s curate call is likewise wrapped (snapshot is source-of-record, already written). **Bug 2 (fixed):** `discover_files` date resolution is now a chain — dated-filename suffix → in-workbook as-of date (`ingest_texas_tfc.workbook_asof_date`) → file mtime month with a LOUD, actionable warning (prints the exact `'<ReportName> - <Month YYYY>.xls'` rename pattern). An undated fresh download now ingests via mtime instead of being silently skipped. **Phase-2 auto-fetch (built):** registry-driven, multi-state `run_auto_fetch` in `state_inventory_sync.py` — for each active `state_lease_sources` row with `dataset_urls`, downloads `dataset_urls[0]` (https-only, 120s timeout), SHA-256s the payload, skips cleanly on `== last_content_sha` (touches `last_run_at`, no snapshot), else stages a dated file (`<glob-stem> - <Month YYYY><ext>`, month=current) into the adapter dir and runs the normal snapshot→diff→events→leads. Nothing TFC-specific is hardcoded (stem/ext derived from the adapter glob + registry `format`). Wired to step 44 via `--state-auto-fetch` / `run_pipeline` kwarg; loud failure = an `ingestion_tracker` 'failed' run per source, per-source isolation. **Scheduling:** `.github/workflows/state-lease-ingest.yml` — monthly (2nd @ 09:00 UTC; GitHub runners reach the versionless TFC endpoint, unlike the OneDrive folder), `workflow_dispatch` defaults to a dry-run gate (apply=false). **Tests:** `tests/unit/test_ingest_texas_tfc.py` (+3 reconcile: FK-referenced-preserved regression, in-place-update+idempotent, ref-check-fail-safe-keep) + `test_state_inventory_sync.py` (undated→mtime, workbook-asof-before-mtime) — 57 state/tfc tests green. NEXT (live verify): run step 44 with the referenced contact present (must complete GREEN through diff/events); confirm new `state_lease_events` (created_at > 2026-06-23) + counts; `state-lease-consume` dry-run scan non-zero + producer age reset.

- **2026-08-06 (Cowork, session 36c — W5.2 dry-runs VERIFIED + TX producer refresh attempt → 2 producer bugs found + gov-repo prompt drafted):** All three consumer ticks dry-ran GREEN via authenticated GET (X-LCC-Key): state-lease 0-to-consume with producer age 43d (alarm trips at 45d — correct); agency-risk 15 actionable-high, 3,124 elevated held in digest; NPI scanned 217 (504-missing pool + 216 data_error queued lanes, clean structured samples). **TX manual refresh (run #552) FAILED with two producer bugs:** (1) BLOCKING — step 44's TFC contacts sync does delete-and-recreate on `data_source='tfc_state_inventory'`; contact `bd236f10…` is now referenced by `true_owners` (a lessor promoted to tracked owner — the system working) → FK 23503 → whole step errors → diff/events never run; recurs every run until fixed. (2) Fresh downloads are silently skipped — snapshot date parses ONLY from a `- Month YYYY` filename suffix (3 undated files skipped); rename convention given to Scott. **Prompt drafted: `audit/data-flow-2026-05-30/CLAUDECODE_PROMPT_govrepo_tfc_sync_fix_and_autofetch.md`** (GovernmentProject repo): non-destructive contacts reconcile (upsert + never-delete-referenced + sync failure must not abort eventing), filename-date fallback chain, and the §10 Phase-2 auto-fetch (registry-driven, content-hash skip-if-unchanged, monthly, loud-failure) — recon already done, stable URL in `state_lease_sources.dataset_urls`. NEXT: Scott sends prompt to Claude Code (gov repo) → merge → renamed-file/auto-fetch re-run → first live distress events → state-lease-consume goes non-zero.

- **2026-08-06 (Cowork, session 36b — W5.2 post-merge verification + state-lease producer diagnosis + multi-state plan):** PR #1562 merged, redeploy live. Verified live: gov seam (processed_at+processed_reason+partial index, 15,299 unprocessed awaiting first tick), ops `lcc_npi_signal_consumed` ledger, 3 crons scheduled (lcc-state-lease-consume / lcc-agency-risk-consume / lcc-npi-consume). **Producer diagnosis — NOT a broken scheduler:** `state_lease_snapshots` holds exactly TWO snapshot dates (2023-07-01 baseline + 2026-06-01); the 2026-06-23 session (see `docs/STATE_GOV_LEASE_GAP_MEMO_2026-06-23.md` session wrap) built the full TX engine with recurrence MANUAL-BY-DESIGN (folder-drop + `python -m src.run_pipeline --steps 44 --state-source tx_tfc`); Phase 2 auto-pull was the documented follow-up (dataset_urls empty pending TFC browser recon). The W5.2 staleness alarm will fire on this state — correctly. **New doc: `docs/STATE_LEASE_MULTI_STATE_ROLLOUT_PLAN.md`** — producer cadence decision (manual-now + Phase-2 recon recommended), the registry-driven add-a-state recipe (gov plan §9), and a data-driven rollout order from live footprint: TX done → **LA (148 tracked state-gov props, standout #2) → CA (70/1,361) → FL → GA → batch WA/NC/AZ/TN**; per-state source recon at onboarding, not upfront. Proposed units U1 (TX refresh + cadence + `Old/` folder back-fill check), U2 (LA onboarding), U3 (CA/FL/GA). W6.6 audit to add a state-source last_run_at age check.

- **2026-08-05 (Claude Code — W5.2 BUILT):** Executed the W5.2 prompt end-to-end across the three repos (branch `claude/signal-task-automation-state-lease-gij25r`). **Migrations applied live:** gov `20260817130000` adds `agency_risk_signals.processed_at` + `processed_reason` + a partial `WHERE processed_at IS NULL` index (the seam the audit found missing); ops `20260817131000` creates `lcc_npi_signal_consumed` (the matview has no seam — consumption is ledgered by a stable md5(signal_type|clinic_id|npi|winner) hash, validated 1,452/1,452 distinct live); ops `20260817132000` schedules three pg_cron ticks. **Code (LCC `api/admin.js` + `ops.js` + `server.js`):** three `_route` ticks — `state-lease-consume`, `agency-risk-consume`, `npi-consume` (GET dry-run / POST apply, R48 tick shape) — plus three federated Decision-Center types (`agency_risk_action`, `npi_dedup_review`, `npi_dedup_autoapprove`) wired through `FEDERATED_DECISION_TYPES` / `federatedSubjectRef` (`arisk:`/`npi:`) / `fetchFederatedSource` (cross-DB reads via `domainQuery`) / the `handleDecisionVerdict` resolver, mirrored into `ops.js _DC_FEDERATED` + sublanes + render branches. Research-work streams (state-lease distress; NPI missing/new) push straight to `research_tasks` with **structured metadata (ids + `deep_link`), `instructions` always NULL** — no prose/SQL. **Grounding corrections made during the build (surfaced, not built over):** (1) `state_lease_events.property_id`/`annual_rent` are 100% NULL — the payload is enriched from `state_lease_snapshots` (address/agency/lessor) and `lessor_change` is digest-only (no tracked-link to gate on — never guess); (2) high `agency_risk_signals` carry NULL `agency_code`/`affected_locations`, so linkage is keyed on the `agency` NAME → `properties.agency` and the risk is in `details` jsonb (spending/expiration); unlinked-elevated + low/moderate are auto-dismissed (reversible) so the pull lane stays high + linked-elevated only. **Producer-staleness alarm** (`state_lease_producer_stale`, cheap helper reused for agency) opens a deduped `lcc_health_alerts` row when the gov producer's `max(created_at)` ages past 45d — state_lease has been silent since 2026-06-23 (mass backfill stamp), so this is the loud-never-silent guard the prompt asked for. **Tests:** `test/w5-2-signal-task-automation.test.mjs` (5, green — distress-only gate, payload shape/no-prose, seam idempotency incl. 409-still-marks, ledger dedupe, agency auto-dismiss targeting, autoapprove verdict spawns task+ledger with NO destructive dia write, prior-decided 409 don't-re-ask); partition + subroutes guards green. **All ticks are correct-but-inert on today's data for state_lease (0 unconsumed — backfill) and light for agency-high, but real for NPI (504 missing + 17 new + 216 data_error + 462 auto_resolvable) — the durable Consumption-Layer fix; state-lease fires the moment the producer resumes.** NEXT: Railway deploy of merged main + `npm run verify:deploy`, dry-run each route, then let the crons apply.
- **2026-08-05 (Cowork, session 36 — W5.2 grounding + prompt draft):** Wave-5 continuation per Scott's approval. Grounded W5.2 against live DBs before drafting; three corrections to the audit premise recorded in the W5.2 row (state_lease_events consumed-AND-stale — producer silent since 2026-06-23, mass backfill stamp burned the seam; agency_risk_signals 15,299 seamless/composite-only; NPI signals live on a matview needing an ops-side consumption ledger). Verified the consumer surface: research_tasks (18,863 rows) + Decision Center queue framework + lcc_decisions (3,730) + the R48 listing_event consumer as the reference implementation. Prompt written: `CLAUDECODE_PROMPT_w52_signal_task_automation.md` — three deterministic consumer ticks with fixed thresholds proposed FROM the measured distributions (state-lease distress/movement types always task; agency risk high always + linked-elevated only; NPI missing→research_task, data_error dupes→decision queue, auto_resolvable→human batch-approve lane), structured payloads + deep links, seams/ledgers + don't-re-ask, producer-staleness alarms. W5.3 rides behind W5.2 (live ollama-vs-openai diagnostics keep accruing since Aug 1 cutover — wider eval window for free).

- **2026-08-05 (Cowork, session 35d — W5.1 gov `--apply` EXECUTED + VERIFIED; unit closes for gov):** Scott ran the real runner on his workstation. **Windows blocker found+fixed first:** the runner's direct-invocation guard (`import.meta.url === \`file://\${process.argv[1]}\``) NEVER matches on Windows (backslash argv vs file:// URL) → `main()` silently never ran, exit 0, zero output. Patched locally to `pathToFileURL(path.resolve(process.argv[1])).href` comparison — **uncommitted local edit; Claude Code to commit + grep `scripts/` for the same pattern elsewhere.** Run results (mode=APPLY, gov, limit 500): **scanned 165, ai_calls 63 (dedup served 102/165 — 62% cost cut on the portfolio-heavy gov pool), 105 fill-blank writes across 83 sales, disagreements 112 (a_only 34 / b_only 69 / conflict 9), provider_hist 100% ollama, prefilter 1 skip, one transient resolver 502** (sale 72b7566f — resumable; a re-run picks it up as the ledger skips the done rows). **Before→after missing (gov):** buyer 1171→1135, seller 1292→1240, listing_broker 3356→3347, lender_name 4925→4917 (deltas sum exactly to 105). **Cowork verification GREEN:** ledger 105 applied=true / 0 unapplied, all `party_extract_agree`, by-field matches the runner deltas 1:1; 105 `field_provenance` rows all RANKED (zero W5.1 rows in `v_field_provenance_unranked`); spot-checks — Saban buyer+seller ✓, SVEA buyer written + buyer-as-listing-broker correctly withheld ✓, Philips Center buyer landed as clean channel-B surface ✓, and the CoreCivic row proved the design: the FULL production note names "David Chandler the developer" as seller (agreement → written correctly) while buyer went b_only and was properly withheld. `W51_PARTY_EXTRACT` registry flag → **on**. Side-find: `v_field_provenance_unranked` drifted 31→33 with new composition (costar_sidebar 18, om_extraction 10, rca_sidebar 3, ops_asset_metadata_loan 1, salesforce 1) — pre-existing registry-coverage gap widening in NON-W5.1 sources; fold into the KNOWN_ISSUES entry / W6.6 audit. **CLOSED same session:** 502-row re-run completed (ledger skipped all 83 done sales, 1 new write — the Sperry VanNess listing_broker; total 106 applied, verified in ledger); Windows guard fix committed by Scott. dia stays deferred pending a richer note source. **Open hygiene item: CF Access service-token rotation DEFERRED by Scott until the build wraps** — the pair used from the Cowork container (sessions 35/35c) remains live; rotate at build completion (Zero Trust → Access → Service Auth) + update Railway CF_ACCESS_CLIENT_ID/SECRET.

- **2026-08-05 (Cowork, session 35c — W5.1b re-sample VERIFIED GREEN; gov `--apply` cleared pending token rotation):** 30-row gov-weighted re-sample (24 gov / 6 dia narrative) run from the Cowork container against the MERGED W5.1b module (PR #1557 semantics verified in the module before running: a_only → skip/log, agreement value prefers channel B's surface). Results: 30/30 rows, zero errors, 100% `ai_final_provider=ollama`; note-hash dedup served 20/30 rows from 19 unique extractions (~35% channel cost cut, matching the runner's new cache). **17 field would-writes, ALL `party_extract_agree`, zero `gliner_extract` writes; 17/17 entity-correct.** Every previously-wrong A-only write from the session-35 sample now correctly parked in the a_only log (Ace-Hardware-as-seller, CoreCivic-as-seller, SVEA-as-listing-broker, Craig Fisher). Surface fix confirmed live: Philips Center buyer emerges as clean "American Realty Capital Healthcare". Only residual blemishes are source-note-inherited dirty surfaces ("AZ_based" prefix, "c/o Joe Ersek" baggage) — right entities, acceptable at 0.60. Review sheet delivered (`W51b_resample30_review_sheet.csv`); Scott to approve → rotate CF Access service token + update Railway `CF_ACCESS_CLIENT_ID/SECRET` → run the real runner `--apply --domain gov` on a host with domain service keys (Scott's machine or an egress-capable Claude Code run), then record per-domain before/after missing-field rates here.

- **2026-08-05 (Claude Code, session 35b — W5.1b agreement-only write path + prefilter/dedup SHIPPED; --apply re-targeted to gov):** Applied the 5 runner/handler changes the session-35 sample demanded. **(1) A-only lane DEMOTED to log-only** — `party-extract.js::adjudicateField` `a && !b` now returns `decision:'skip', disagreementKind:'a_only'` (logged to `party_extract_disagreements` like the other non-writes); **agreement (`party_extract_agree`) is the sole write path**, removing all 19 serious sample errors. `SOURCE_GLINER`/`CONF_GLINER` stay exported (the `field_source_priority` row stays registered, gains no producers). **(2) Agreement surface = channel B preferred** (`value = b || a`) — kills the address-bleed case (A's "Philip Blvd. American Realty Capital Healthcare" → B's clean surface). **(3) Note-quality prefilter** in the runner's `candidates` — skips `^master_xlsx_backfill`, `^historical_csv_import`, `^Office - Sub` stubs + `<MIN_NOTE_LEN`, before either channel runs; per-pattern skip counts in `stats.prefilter_skips`. **(4) Portfolio-note dedup** — FNV-1a hash of the trimmed note; one `extractParties` per unique note, fanned to all member rows (`stats.dedup_hits`, no-network iterations skip the pace delay). Contains the CoreCivic ×8 / SVEA ×4 blast radius + ~30% GLiNER cost cut on gov. **(5) --apply re-targeted to gov** in the runner docs/usage (dia yielded 0 writes in 60 sample rows under agreement-only; dia stays runnable, expected near-empty). No schema change (migration `20260731120000` + registered priorities unchanged); disagreement gate, fill-blanks + field-priority guard, ledger/resumability, and the ollama `--apply` gate all untouched. Tests: `test/party-extract.test.mjs` updated (a_only branch → skip regression guard; agreement-prefers-B; adjudicate buyer A-only now skip) — **24/24 green**. **Next: small gov-weighted re-sample (`--sample 30 --domain gov`) → Scott approves the sheet → `--apply --domain gov`; report per-domain before/after missing-field rates here.** Reminder (independent of this PR): rotate the CF Access service token used for the 2026-08-05 container run.
- **2026-08-05 (Cowork, session 35 — W5.1 100-row sample run COMPLETE; verdict: HOLD --apply pending one lane change):** Claude Code's runner env was egress-blocked, so the sample ran from the Cowork container against the REAL staged modules (party-extract.js + CF-patched ai.js), zero writes, 100/100 rows `ai_final_provider=ollama` (tunnel + Access token held throughout; GaryBuilt 1–3s/call, GLiNER 20–41s/call warm). Deliverables to Scott: `W51_sample_review_sheet.csv` + `W51_SAMPLE_FINDINGS.md`. **Decisive result — the two write lanes have opposite quality:** agreement lane (`party_extract_agree`, 27 field-writes) ~93% entity-correct (only 2 surface-pollution cases, right entity/dirty string); A-only lane (`gliner_extract`, 24 field-writes) **~80% WRONG** — every serious error lives there: tenant-as-seller ×6 (Fresenius/DaVita stubs, Ace Hardware), buyer-as-seller ×8 (CoreCivic portfolio fan-out), buyer-as-listing-broker ×4 (SVEA portfolio), buyer-as-procuring ×1 (Ridgeline). Disagreement gate worked perfectly (withheld "The buyers", "$26.1M CMBS loan"-as-lender, and real-but-span-less claims like Ventas/HTA/Seritage). **Required runner changes before --apply (all small):** (1) DEMOTE `gliner_extract` A-only lane to log-only — agreement becomes the only write path (removes all 19 serious errors); (2) agreement surface rule: prefer channel B's surface (current longer-wins picked the address-polluted span); (3) dia note-quality prefilter (54/60 dia notes were bookkeeping strings — `master_xlsx_backfill*`, tenant stubs); (4) portfolio-note dedup by hash (duplication multiplied the CoreCivic error ×8, SVEA ×4; also ~30% GLiNER cost cut on gov); (5) **re-target first --apply at gov, not dia** — under agreement-only dia yielded ZERO writes in 60 rows (the ~2,242 "addressable" dia set is addressable on paper only); gov yielded 26 write-rows in 40. Hygiene: CF Access service token was used from the container — Scott to rotate (Zero Trust → Access → Service Auth). **Next: Claude Code prompt to apply the 5 runner changes → re-sample (small) → gov --apply.**

- **2026-08-04 (Cowork, session 34 — four-day systems check, all green):** (1) **Nightly retrain loop: 4/4 nights ran** (Aug 1–4; 3 models each; band floored 0.5; n_train stable ~4,880 — labels froze at 95, review lane untouched since Jul 31, so drift alerts correctly remain open awaiting new hard negatives). (2) **W6.6 monthly audit fired on schedule Aug 1** (next Sep 1); weekly SAM check fires Aug 7. (3) **GaryBuilt IS CARRYING PRODUCTION:** per-artifact diagnostics (staged_intake_items.raw_payload.extraction_result.diagnostics) show ai_final_provider by day — Jul 31: 5 openai (pre-cutover); Aug 1: 4 openai/1 ollama; Aug 3: 13 openai/1 ollama (desktop likely off — clean cloud degradation, the designed fallback); **Aug 4: 22 ollama / 1 openai** — local-primary extraction verified in production at zero cloud cost. Note for max savings: keep-awake settings on GaryBuilt matter (off-days route to cloud silently — by design, but visible in the diagnostics). This ALSO seeds the W5.3 evaluation: ollama-vs-openai comparative quality can now be graded from live diagnostics. (4) Gov staging fresh (Aug 5 02:37 sync), sf_files extraction backlog ~4 queued (draining). **Next: W5.1 `--sample 100`** — recommend running via Claude Code (it holds domain service keys; W51_ALLOW_CLOUD=1 acceptable for the 100-sample per the prompt; warm both channels first re: GLiNER cold start) → Scott reviews sheet → `--apply`.

- **2026-07-31 (Claude Code, W5.1 — party extraction MECHANISM SHIPPED):** Two-channel party extractor over sale notes, built end-to-end (not yet run — awaits operator prereqs + the 100-sample gate). **Channel A:** `resolver/app/parties.py` + `POST /extract-parties` — GLiNER `gliner_medium-v2.1` baked into the resolver Docker image; a deterministic cue-phrase heuristic backend covers the bare venv / CI (both span-anchored, honestly reported at `/health`). **Channel B:** the EXISTING `ai.js invokeExtractionAI` seam (GaryBuilt/qwen2.5 local primary → cloud fallback) — no second Ollama client. **Adjudicator** `api/_handlers/party-extract.js`: agreement on the normalized company core (mirrors resolver `normalize_company`; prefix/suffix firm-name match) → write `party_extract_agree` 0.60; A-only (LLM abstained) → `gliner_extract` 0.55; **value conflict OR span-less B-only → NO write, logged** to `party_extract_disagreements` (deliberate tightening of the plan's ambiguous "or disagreed" toward the never-guess doctrine — documented in the handler). Fill-blanks-only, through the field-priority guard, span-grounded in the reversible ledger `party_extract_batch`. **Migration `20260731120000` applied live to LCC Opps** (18 `field_source_priority` rows party_extract_agree@80/gliner_extract@85 below every aggregator; ledger + disagreements tables; `W51_PARTY_EXTRACT` flag). **Runner** `scripts/party-extract-backlog.mjs` — `--sample N` review sheet (no writes), dry-run default, `--apply` ollama-gated (`W51_ALLOW_CLOUD=1`/`--allow-cloud` to override), resumable via the ledger, per-domain before/after report. Tests: 18 JS adjudication + 48 resolver (all green). **⚠️ GROUNDING (measured live, not the audit's premise):** `sale_notes_raw` alone covers only ~281 dia / ~163 gov live sales; the runner folds dia's `notes` (CoStar marketing description) column, lifting dia's addressable set to ~2,242 (988 missing-buyer, 2,195 missing-broker). gov has no generic `notes` column, so gov stays note-starved (~163) — the ">20pt broker-rate drop" is reachable on dia, not gov-from-notes; report per-domain. **Operator steps to realize value:** set RESOLVER_URL + OLLAMA_URL/OLLAMA_MODEL + domain service keys, run `--sample 100`, Scott approves the sheet, then `--apply`; flip `W51_PARTY_EXTRACT`/`OLLAMA_EXTRACTION` on. Resolver Docker image now pulls torch+gliner (past the 2GB floor) — measure and bump the resolver service memory at deploy.
- **2026-07-31 (Claude Code, W4.4 — Wave 4 CLOSES):** Model as provenance citizen + retrain loop. **Provenance:** registered `splink_v1` (@50) + `sf_link_review_human` (@1) in `field_source_priority` for the SF-link fields; upgraded `lcc_flush_provenance_events` to preserve first-class sources (self-maintaining, never trips `v_field_provenance_unranked`) + re-attributed 3,493 historical rows (mig `20260819120000`, applied live — 8 rows, fp splink 3,442 / human 51, unranked drift 0, SQL ladder proof 0-residue). **Nightly loop:** `resolver_calibration_history` + `lcc_record_resolver_retrain` drift RPC (mig `20260819130000`, applied live; synthetic gate proved 2 drift + 1 failure + 3 history, 0 residue), `w44-retrain-tick` edge fn (committed, deploy = operator step), cron `w44-resolver-retrain-nightly` `30 7 * * *` scheduled+active on Dialysis_DB (mig `dia/20260819140000`). **ORE:** `resolver-client.js` + `owner-reconcile-engine.js` gate auto-merges on `/match`, **fail-closed** to needs_review (flag `ORE_USE_RESOLVER`, mig `20260819150000`); 15 fail-closed unit tests + 37 reconcile tests green. **Calibration defects:** band floor 0.5 (hard invariant + 5 tests; committed models re-clamped 0.005→0.5); splink training smoke-test retired (count estimator is exact given labels — never fed the model; honest README note). Resolver suite 34 pass (1 known env-only `/match` failure needing Docker bge-small/libpostal). Docs updated (RUNBOOK §6 nightly loop + operator steps, CALIBRATION.md band-floor note, W4_1_CORPUS_REPORT.md v2-accruing). **Operator steps to realize value:** deploy `w44-retrain-tick` to Dialysis_DB + set `RESOLVER_URL` on its edge secrets; set `RESOLVER_URL` + `ORE_USE_RESOLVER=1` on the LCC Railway app; then flip `RESOLVER_RETRAIN_LOOP`/`ORE_USE_RESOLVER` flags to `on`. **WAVE 4 COMPLETE.**
- **2026-07-31 (Cowork, session 33 — GaryBuilt cutover LIVE + W5.1 live test):** Permanent transport built and verified: named CF tunnel `garybuilt.briggscrelccopps.com` (cloudflared Windows service) + Access **Service Auth** token; unauth probe → 403 (GPU locked), authenticated /api/tags → qwen2.5:14b. **Auth correction shipped:** Access service tokens need `CF-Access-Client-Id/Secret` headers — Bearer does NOT pass — ai.js patched (merged) + playbook §2.4 corrected; Railway env: OLLAMA_URL (**https** — Scott's initial http value caught before it silently 301'd POSTs to GETs, third instance of that hazard class today), OLLAMA_MODEL, AI_EXTRACTION_PROVIDER=ollama, CF_ACCESS_CLIENT_ID/SECRET. `OLLAMA_EXTRACTION` flag → **on**. Resolver: gliner:true on /health post-#1538; real-corpus models restored via forced retrain tick (0.5 floored bands, history recorded). **Live channel-A test on real dia notes:** sale 14787 buyer/seller/price extracted perfectly (Bilnia Investments/JBL Asset Management/$17.743M); sale 14808 broker correct but TENANT-as-seller miss (DaVita) — validates the channel-B + sample-gate design; GLiNER cold-start latency high (warm-up + generous timeouts required in the sample runner). **Playbook §7 added: full Ollama employment map** (live now: intake OM stream + next-step engine; gated: W5.1 channel B; near-term: junk-entity pre-screen, review-lane sort + hard-negative proposals, email-intake triage assist, briefing narrative, research synthesis; mid-term: voice-grounded drafts, match_disambiguation assist, LoRA; never: auditable gates). Side-find from W5.1 verification: 31 pre-existing costar_sidebar unranked provenance rows → KNOWN_ISSUES. **Next gate: W5.1 `--sample 100` → Scott approves sheet → `--apply` (dia ~2,242 addressable).**
- **2026-07-31 (Cowork, session 33 — Wave-5 kickoff + GaryBuilt integration):** W4.4 live verification GREEN recorded (see W4.4 row — WAVE 4 CLOSED). **GaryBuilt local model reviewed** (post-audit addition, docs/setup/garybuilt-local-model.md — Ollama qwen2.5:14b, ai.js seam already shipped, flag OLLAMA_EXTRACTION off pending Railway cutover): it REALIZES W5.3's infrastructure; integration map added to the playbook §6 (W5.1 channel B, OM-extraction cost avoidance as file discovery scales, + two OPTIONAL doctrine-gated ideas: review-lane pre-screen sort, LLM-proposed hard-negative candidates). **W5.1 prompt drafted + unit REVISED for the local model** (two-channel GLiNER + local-LLM agreement writes, ollama-gated bulk, 100-sample human gate kept): CLAUDECODE_PROMPT_w51_party_extraction_local_llm.md. W5.2 annotated: NO LLM in value gates (auditable determinism). **W6.6 monthly standing audit LIVE** as a Cowork scheduled task (1st monthly 9am CT, first fire 2026-08-01 — seven-point scorecard, always notifies). Remaining decisions parked for Scott: W0.5b parquet offload, W0.6 Supabase ticket, W1.5 CMS dispatch confirm, OpenCorporates re-price ~Aug 28, advisors duplicate-index drops, GaryBuilt Railway cutover (OLLAMA_URL).
- **2026-08-06 (Cowork, session 36u):** **Agent instructions brought under Copilot Studio's 20,000-char limit** (rendered paste region was 24,372). Zero rules dropped — all compression + de-duplication: **canon v1.4.3** (comps block 6,376→4,774 dense phrasing; naming convention deduped to Filing with BOV pointing there; offer-submission/logging/personal/filing tightened; re-rendered across all 5 surfaces, parity 0 drift) + Copilot-only sections condensed (sections restating canon — email-routing prohibition, intake classification — became one-line canon pointers; flows/gates/approve-all tightened). **Now 19,235 chars, 765 headroom.** **Future-proofed: render-surfaces.mjs gained a paste-region length guard** driven by `render.manifest.json` (`copilot.pasteLimit: 20000`, `pasteSoftLimit: 19500`) — every `--write-live` render measures the region below `---`, warns inside the soft ceiling, exits 1 over the hard limit. Operator: paste below `---` (canon v1.4.3) → Instructions → Publish → test log_call_note/tag_comm_to_deal by asking.
- **2026-08-06 (Cowork, session 36cc — W5.3 + Ollama hygiene campaign KICKED OFF):** Scott directed: start the local-LLM evaluation AND expand the cleaning agent's reach (duplicates/junk/unconnected records across DBs; Ollama cleans, propagates, connects, reports systemic code errors). **Corpus confirmed: 234 staged_intake_extractions since Aug 1** (ollama-primary window) — provider attribution not in snapshots (locating it = first W5.3 task). Hygiene grounding verified in LCC Opps: v_lcc_person_email_merge_candidates, lcc_chain_unresolvable + chain-completeness views, cross_domain_contacts, ingest_write_failures/flow_run_failures all exist — the campaign builds ON these. **Kickoff doc written: `docs/audits/W53_AND_OLLAMA_HYGIENE_KICKOFF.md`** — W5.3 grading method (human-anchored via staged_intake_feedback + 30-row stratified spot audit + W7-surface metrics incl. the free precision floor from lcc_deal_analysis_dropped_log) and the W8 campaign (U1 junk pre-screen → junk_review lane; U2 duplicate proposals → resolver pool/labels; U3 connection propagation w/ verbatim-evidence validator → confirm lane; U4 systemic-findings monthly report feeding W6.6; doctrine: propose-never-gate, soft-retire-never-delete, flag+registry per unit; order: W5.3 verdict first). **Pick up in a fresh chat: "Pick up W5.3 + the Ollama hygiene campaign from docs/audits/W53_AND_OLLAMA_HYGIENE_KICKOFF.md"** (this session at context depth — grounding complete, heavy work belongs in the new chat).
- **2026-08-06 (Cowork, session 36bb — W7.6 LIVE + drain + outbound verified + sent-sweep NOT needed):** **Mailbox mirror ACTIVE**: PR #1597 merged, mover flow built (folder ids resolved: source "Intake Staged, Not Completed", dest "Processed"; parent-folder safety condition included), `MAILBOX_MIRROR=true` set, registry → on; **first batch of 25 moved+acked cleanly, backlog draining 3,908→3,885** (~13h to converge at 25/5min). Subfolders (old Deals/Duplicates/General/Infra/Leads suggestion): **flat Processed chosen** — LCC is the retrieval system now; taxonomy folders = duplicated state; optional later `destination_hint` (deal vs no-deal) if Scott ever browses rather than searches; Duplicates/Infra rejected outright (dedupe upstream; infra mail should be diverted pre-intake by an Outlook rule). **DISCOVERY: an existing PA flow ALREADY feeds `/api/intake?action=outlook-sent`** (59 outlook_sent rows, current through today 18:38) — the 36z "unfed" finding was wrong for Scott's mailbox (feed predates this week); **"LCC — Sent Mail Sweep" does NOT need to be built** — struck from the operator list. **W7.5 outbound verified live**: the 18:38 "Re: The appraiser" send → Villages open to-dos reconcile-stamped at 18:41 (ball-in-court/de-stale); completion arm correctly idle (no open offer_review/follow_up — deal_next_step never auto-closes, doctrine); narration correctly silent when nothing completed — auto-complete + first narration will prove organically on the next send that satisfies a completable to-do. Evening checkpoint scheduled: drain progress + parked errors + any organic advance/narration/issue-close.
- **2026-08-06 (Cowork, session 36aa — W7.5 live + W7.6 decided + prompt):** PR #1596 merged/redeployed; `W75_ACTION_SUMMARY` env set + registry → on (first narrations to be quality-checked at next verification). Scott's live test: tagged his comps send to the broker (not the client) — sweep catches Sent Items, outbound advance now fires; "Comps for Appraisal" close expected at next tick (verify: stamp + advance + issue close + first narration in ONE pass). **Sent-sweep flow ("LCC — Sent Mail Sweep") still to build** — simplified copy/paste steps issued in-chat (top-25 idempotent sweep, no high-water variable; auth = X-LCC-Key, which maps to an operator identity per auth.js). **W7.6 DECIDED: mailbox mirror = YES** — Scott's spec: the "Intake Staged, Not Complete" Outlook folder must mirror open LCC work exactly; emails auto-move to Processed when the follow-up is sent or the to-do completes. **Prompt drafted** (`CLAUDECODE_PROMPT_w76_mailbox_mirror.md`): pull-model — deterministic (NO-LLM) worklist endpoint (closed = all linked to-dos done OR later outbound reply in thread OR terminal triage; offer_review guard) + ack ledger w/ backoff-retry-cap-alert + PA mover spec (Graph move + unflag + mark read; move-only, never delete). Still parked for later decision: email-body filing to deal folders; SF call parity.
- **2026-08-06 (Cowork, session 36z — loop-closure review + W7.5 prompt):** Scott asked whether every completed action closes end-to-end. **Verified map:** calls → `lcc_advance_todos` (channel call, outbound) resolves offer_review/follow_ups + schedules seller follow-up ✓; inbound mail → advance+reconcile+deriveNextStep ✓; dossier/context packets regenerate every tick ✓; deal_next_step deliberately non-destructive (doctrine, kept) ✓. **THE GAP: `handleOutlookSent` — the outbound completion engine (sent email auto-resolves the to-dos it satisfies) — is BUILT but UNFED**: no live flow posts sent mail to it, and the tagged-comm receiver only advances to-dos for INBOUND (a tagged outbound send stamps the spine, completes nothing). Also not built: mailbox write-back (unflag/move/file/mark-read), email-body filing to deal folders, per-action Ollama narration; SF gets no task for calls (LCC-full/SF-generic doctrine currently offer-only). **W7.5 prompt drafted** (`CLAUDECODE_PROMPT_w75_outbound_loop_closure.md`): (A) outbound advance branch in the tagged receiver — the 5-min Sent sweep then closes to-dos with zero new infra; (B) untagged sent-mail feed → handleOutlookSent + cross-path de-dupe on internet_message_id + PA spec doc w/ 36y lessons; (C) flag-gated `W75_ACTION_SUMMARY` per-action Ollama narration (validator: may only name to-dos actually touched). Mailbox write-back / email filing / SF call parity = explicit Scott decisions, listed out-of-scope in the prompt. Also confirmed: no new env var needed for the 36y park fix.
- **2026-08-06 (Cowork, session 36y — W7.3 path C LIVE + park-lane bug found/fixed):** **Outlook category tagging is LIVE end-to-end.** PA flow "LCC — Tagged Comm Capture" built by Scott with Cowork triage across four defects: (1) condition expression pasted as literal TEXT not an fx expression (always false); (2) Apply-to-each foreach doubled into a string; (3) **`Get emails (V3)` does not return the `categories` field at all** — replaced both fetches with Office 365 Outlook **"Send an HTTP request"** → Graph `me/mailFolders/{inbox|sentitems}/messages?$select=...categories...&$top=25` (standard action, also sidesteps premium licensing on the fetch side); (4) foreach then missing `?['value']`. **Proof:** tagged email "comps for Davita" → stamped `Received (tagged)` on the spine → auto-resolved to **The Villages** via `sender_resolver` (plain LCC tag, no hint). `TAGGED_COMM_INTAKE_ENABLED` set in Railway; registry row `TAGGED_COMM_INTAKE` → on (**row was never registered by W7.3 — created retroactively, gap noted**). Receiver health also proven directly via `lcc_cron_post` (synthetic post → 200). **BUG FOUND VIA THAT TEST: the park lane never wrote** — `research_tasks.domain` is NOT NULL but `parkUnresolved` sent `domain: null` → every park insert failed AND the catch returned misleading state; receiver reported `parked:true` with no row (silent message loss in the never-guess lane). **Fixed in working tree** (`intake-tagged-comm.js`: `domain:'lcc'` + loud error + honest `parked:false`/`park_error` on failure) — commit + redeploy with the pending bundle, then re-verify with an unresolvable tag. W7.2 tick will fold the Villages tagged comm in at the next :32; the 10:15 CT scheduled check verifies W7.4 analysis accrual.
- **2026-08-06 (Cowork, session 36x — W7.4 verified + FLAG ON):** Claude Code's W7.4 delivery (PR #1590, merged + redeployed) **verified live**: `lcc_deal_dossier_analysis` + current-view + `lcc_deal_analysis_dropped_log` present (0 rows — inert pre-flag), 5 new run-log counters on the propagation run log, `W74_ROLE_ISSUES` registered off. Dry-run plumbing proven via `lcc_cron_post` (200, run_id 19, zero writes) — first fire scanned 0 deals (W7.2 had consumed the backlog), so a **one-deal ledger rewind** was executed with Scott's approval: Banning's 8 most-recent `lcc_deal_comm_propagated` rows deleted (0 inbound FKs checked; ids captured in-session; re-ledgered idempotently at the next live tick). **Second dry-run (run_id 20): REAL PREVIEW** — Villages (2 organic new comms): role Scott→broker w/ 2 verbatim quotes + 2 open commitments ("Provide appraisal inspection info", "Send comps to Phil Lamb"), 1 dropped; Banning (8 rewound): 5 deterministic cues, 0 surfaced, **2 dropped by the evidence validator** (paraphrase-not-quote — the designed failure direction). **Scott approved the gate → `W74_ROLE_ISSUES` registry row flipped ON.** Operator: set `W74_ROLE_ISSUES=true` in Railway vars (code gates on the env var). **Watch item: dropped-proposal ratio** — if it stays high, tune the extraction prompt to demand exact copied spans (never loosen the validator). Verification (next :32 tick + 10:15am organic re-check): analysis rows accrue, dossier panel renders, idempotent re-run, issue-close on a later comm. Also noted: 3 pre-existing full-suite failures (MCP /api/comps route, owner_reconcile card) — future small cleanup session (backlog candidate #20).
- **2026-08-06 (Cowork, session 36w — W7.4 prompt issued):** With Copilot parked on IT (36v), the build proceeds to the LAST Wave 7 unit. **W7.4 prompt drafted:** `audit/data-flow-2026-05-30/CLAUDECODE_PROMPT_w74_role_evolution_open_issues.md` — role evolution + open-issues surfacing + deterministic stage awareness into the living dossier. Doctrine encoded: LLM proposes into Analysis sections only (never facts/gates); **no-fabrication contract with a verbatim-quote evidence validator** (proposal dropped if its quote isn't in the cited comm); own-seam pass appended to the propagate tick with independent watermark; is_current versioning incl. issue lifecycle (close needs evidence); Ollama primary via the ai.js seam; flag `W74_ROLE_ISSUES` (inert until flipped); dry-run report over the 5 most comm-active deals in the PR for Scott's quality eyeball. Scott: paste the prompt into a Claude Code session when ready.
- **2026-08-06 (Cowork, session 36v — Copilot topic PARKED pending IT):** Continued diagnosis after the agent re-broke (ConnectorAuthorizationError → then global failure). THREE more real defects found + fixed in Copilot Studio: (1) **the dispatch tool tile exposed only `copilot_action` — the `params` input was missing entirely** (why the agent said "parameters can't be passed through the gateway" and improvised into unauthorized tools) → added `params` (Dynamically fill with AI) + saved; (2) **broken MCP tool tile "LCC Deal Intelligence"** ("No tools available", pointed at the stray connector) → DELETED from the agent (Scott-approved); (3) **agent model was set to GPT-Realtime** (voice preview — wrong orchestrator) → switched to **GPT-5-Chat** (Scott-approved; only non-realtime option offered). Scott's v1.4.3 instructions paste confirmed present in draft. Publish attempted (Force newest version) — header stuck at 8/5. **ROOT CAUSE OF THE REMAINING GLOBAL FAILURE (working hypothesis, strong): Copilot Studio message capacity exhausted — Monitor shows "Copilot credits: 0".** Custom agents bill against tenant/environment Copilot Studio message capacity (packs or PAYG), SEPARATE from the per-seat M365 Copilot license — which is why regular Copilot chat works while every custom agent fails instantly on every message (draft + published). Fits timing (worked 01:43 UTC, dead by 02:13). **Blocked on: NorthMarq IT** — Power Platform admin center → Billing → Copilot Studio: allocate message packs to env Default-fccf69d3 or link Azure PAYG. **Resume checklist when capacity lands:** Publish (verify date flips from 8/5) → fresh chat → "Log a call on the DaVita in Banning…" → expect pick-list (free-text matched no open deal in the connector test) → pick → Cowork verifies the call on the deal spine + next propagation tick. If IT reports capacity is fine: next suspect is the pasted instructions (bisect with a minimal instruction set). All session 36o–36v fixes remain valid regardless.
- **2026-08-06 (Cowork, session 36t):** **AGENT CONFIRMED WORKING.** After Scott re-imported spec v4.2.0 (one more speed bump first: Power Platform caps the connector `info.description` at 1000 chars — the accumulated changelog blew past it; description trimmed to ~470 chars, full history moved to `copilot/CHANGELOG.md` with the limit documented), "What are my work counts today?" returned a full live snapshot (44 open actions, 15 overdue, 5,389 inbox, 0 sync errors) — both root causes (36r auth header flip, 36s orphaned compat operations) resolved. One connection-manager reverify prompt appeared on first use, then cleared. **Remaining operator steps:** (a) paste rendered agent-instructions (canon v1.4.2) → Instructions → Publish; (b) test log_call_note + tag_comm_to_deal by ASKING (expect a pick-list — test showed "Banning DaVita" free-text matched no open deal); (c) decide MCP tool "LCC Deal Intelligence" fate (still disabled from debug); (d) stray "LCC Deal Intelligence" connector → ZZ-DEPRECATED rename after confirming no flow references; (e) commit the working-tree bundle (v4.2.0 spec + CHANGELOG.md + setup banner + 36r–36t entries).
- **2026-08-06 (Cowork, session 36s):** **SECOND outage root cause found + fixed — ConnectorOperationNotFound.** After the auth fix (36r) the agent STILL failed every message with ConnectorOperationNotFound: the old connector had 94 operations incl. 41 legacy snake_case ops at `/api/copilot/compat/<kebab>`; the v4 spec dropped them (54 ops), but the agent's tool tiles (added in the snake_case era, 66 connector tools) still bind those operationIds — orchestrator tool-loading fails on EVERY message, even ones needing no tool (e.g. get_work_counts → "What are my work counts today?"). Verified server-side before fixing: `server.js` still routes `/api/copilot/compat/:action` → dispatch, and ALL 41 actions remain in ACTION_REGISTRY. **Fix: spec v4.2.0** — the 41 compat operations restored verbatim from the archived v3 spec (security normalized to apiKey-only, x-ms-requestBody-name ensured), total 95 ops; drift test 3/3 green. **Standing rule added to COPILOT_STUDIO_SETUP banner: never remove a connector operation while an agent tool tile references it — retire the tile in Studio → Publish → then drop the op → re-import.** Operator: re-import v4.2.0 into "LCC Intelligence" via swagger editor → Update connector → **verify Security tab still says X-LCC-Key after import** → retest agent.
- **2026-08-06 (Cowork, session 36r):** **COPILOT AGENT OUTAGE ROOT-CAUSED AND FIXED.** Symptom: LCC Deal Agent failed EVERY message (incl. plain "What are my work counts today?") with the generic fallback; earlier M365-channel error ConnectorOperationNotFound. Diagnosis in Power Automate (env Default-fccf69d3): the agent's dispatch tool binds the **"LCC Intelligence"** custom connector; its Test tab call to dispatchCopilotAction returned **401 "Authentication required. Provide Authorization header or X-LCC-Key."** Security tab showed API-Key parameter name = `Authorization` — the v4.1.0 spec carried TWO securityDefinitions (bearerAuth `Authorization` + apiKey `X-LCC-Key`); Power Platform honors only ONE and the re-import picked bearerAuth, so the saved connection (created 07-17) stopped sending `X-LCC-Key` → every call 401 → agent globally dead. **Fix applied in-portal:** Security → Parameter name `Authorization`→`X-LCC-Key` → Update connector. Verified via Test tab (Raw Body): `{"copilot_action":"log_call_note","params":{...Banning DaVita...}}` → **200 `ok:true, requires_pick:true, wrote:false`** — auth healed, dispatch gateway + registry + never-guess pick-list all working (existing connection carried over; no key re-entry needed; nothing written). **Durability:** spec bumped to **v4.1.1** — bearerAuth securityDefinition REMOVED (apiKey/X-LCC-Key is the only one; changelog warns never to re-add a second); COPILOT_STUDIO_SETUP.md banner adds the one-security-definition rule + post-import checklist (check Security tab, run Test before touching the agent) + names "LCC Intelligence" as the agent-bound connector ("LCC Deal Intelligence" = stray/legacy duplicate, do not import into it). **Still open:** (a) MCP tool "LCC Deal Intelligence" in the agent was disabled during debug (showed "No tools available") — re-enable or remove deliberately; (b) Scott: retest the agent, then paste rendered agent-instructions (canon v1.4.2) → Publish; (c) test log_call_note/tag_comm_to_deal by asking; note test showed "Banning DaVita" matched NO open deal (empty candidates) — check the deal's stage/naming when testing; (d) decide the stray connector's fate (ZZ-DEPRECATED rename after confirming no PA flows reference it); (e) commit the working-tree bundle (now incl. v4.1.1 spec + setup-doc banner + this entry).
- **2026-07-31 (Cowork, session 33 — closing):** W4.3 review lane verified END-TO-END in production: Scott cast **95 verdicts** (50 Link, 2 Switch, 43 Not-a-match). Exact reconciliation confirmed: 95 `entity_match_labels` (seeder=`sf_link_review`) = 95 queue transitions (gov 83 / dia 12); every human link carries its `sf_link_review_human` provenance row (zero gaps checked). **The corpus now holds 43 real hard negatives (was 0), 45% negative mix** — the calibration-transfer fix fuel. Review pool remaining: 3,357 (gov 2,981 / dia 376) — no deadline; each verdict = live SF link + training signal. **W4.4 prompt drafted** (`audit/data-flow-2026-05-30/CLAUDECODE_PROMPT_w44_provenance_citizen_retrain_loop.md`): provenance registration, nightly corpus-refresh + /train + drift alert (also heals Railway's ephemeral models), band-floor invariant, splink-fallback investigation, ORE→resolver fail-closed integration. **INTERIM SAFETY (live, set by Scott): `RESOLVER_AUTO_LINK=0.9` + `RESOLVER_AUTO_REJECT=0.1` env overrides on the Railway resolver** — the trained models carry the non-transferable 0.005 band; nothing consumes /match in prod yet (ORE integration IS W4.4); W4.4 removes the override with the proper fix.
- **2026-07-31 (Cowork, session 33):** **W4.3 review lane SHIPPED.** The 3,452 `needs_review` SF-link candidates from the W4.3 splink run are now the `sf_link_candidate` Decision Center sub-lane (source `v_sf_link_review_queue`, gov 3,064 + dia 388). One-click verdicts on top of the EXISTING attach semantics (no new architecture): **Link** applies the SF id (gov `sf_account_id`+`sf_last_synced` / dia `sf_company_id`), **null-guarded** — a pre-existing DIFFERENT id never overwrites, the card re-renders three-way (Keep existing / Switch / Research); **Not a match** → `no_match`; **Keep existing** records the kept id without touching the owner row; **Switch** is the deliberate override (provenance old_value preserved). Every applied link writes a `provenance_event_log` row (`source='sf_link_review_human'`, `target_database` gov_db/dia_db); **every verdict writes an `entity_match_labels` row (`seeder='sf_link_review'`, `same_party`/`distinct`) — the W4.4 hard-negative corpus** (a failed label write surfaces a 502, never silent). Mint-at-verdict (no 3,452-row `lcc_decisions` pre-mint); the sublane badge reads the live view depth via `/api/review-counts` (gov+dia). Added `last_error` to `v_sf_link_review_queue` (append-only CREATE OR REPLACE, applied live to both DBs) so the 18 dia conflict rows (`w4_3_conflict_existing_sf_company_id_<ID>`) render the three-way card. Pure planner `api/_handlers/sf-link-review.js::planSfLinkVerdict` + `test/sf-link-review.test.mjs` (18 unit + 5 wiring guards); fixed a pre-existing stale count in `review-shared.test.mjs` (21→23, `comp_review`+`sf_link_candidate` were unaccounted). Suite green (2 prior fails were this + the partition sync, both resolved). Provenance insert shape validated live against gov (transactional, 0 residue). **Operator verification (post-deploy): open Decision Center → "Salesforce link — confirm candidate" (~3,452); approve a gov row → owner `sf_account_id` set + provenance + queue `linked` + `entity_match_labels` row; one of the 18 dia conflict rows renders three-way, Keep-existing labels without touching `true_owners`.**
- **2026-07-31 (Cowork, session 32):** SAM 48h check PASSED (W1.4-L1 → ✅, slow-pace watch stands). **W4.1 DELIVERED:** labeled-pairs corpus assembled server-side by a new deterministic `w41-corpus-export` edge fn on Dialysis_DB (the one project holding gov+dia+ops creds; auth X-PA-Webhook-Secret; preview/export actions; no domain writes — Storage upload only) and landed at the resolver's exact default path `entity-resolution/w4_1/labeled_pairs.jsonl` (LCC Opps Storage) + corpus_report.json. 5,342 pairs, sha256 47c41a59…, composition + methodology + honest gaps in docs/resolver/W4_1_CORPUS_REPORT.md. Discovery notes: ops `entities.merged_into_entity_id` soft merges recover loser names (incl. all Boyd Watterson + merge_duplicate_entities losers) — richer than the plan's entity_merge_log route (which carries no names); exact_name_merge pairs fully norm-collide with dq5 case variants (0 net); staged intake feedback excluded (OM filenames ≠ entity names — plan's implicit-positives idea documented as not viable in current shape). Wave 4 remaining: Scott's Railway resolver service → /train → calibration approval → W4.3 backlog run.
- **2026-07-31 (Cowork, session 31):** W3.7c CLOSED — File-Discovery flow verified in production. Scott's run initially failed at Post_File_Content (404 `No sf_files row` for every gov file; dia fine). Triage via gov API logs found the smoking gun: the gov insert arriving as `GET /rest/v1/sf_files` (no query) — the `GOV_SUPABASE_URL` secret (changed ~2026-07-21) was a redirecting http:// form; the 301 makes Deno fetch convert POST→body-less GET, so gov writes silently no-oped while GET/PATCH (worklist reads, lease stamps) kept working. **Standing hazard note: a 2xx on a redirected POST is invisible to `dbFetch` — any env URL must be exact `https://<ref>.supabase.co`, no trailing slash.** Scott fixed the secret; Cowork verified discover→dedup→file-content→storage end-to-end on gov (test artifacts cleaned), reset 76 gov listing leases, and Scott's re-run succeeded (8 gov OMs stored+queued, batch 04:59:45Z; extraction draining via sf-files-stage-queued-15m). Collateral found: gov staging upserts from the object-sync flow (`?on_conflict=` POSTs) were no-oping for the same reason — gov staging frozen 2026-07-21→07-31; self-heals on next object-sync run (idempotent), verify max(updated_at) advances. All nine GOV_SUPABASE_URL/KEY consumers covered by the one project-wide secret fix; health-check reports gov/dia/ops connected. Docs updated: FLOW_sf_file_discovery.md (production-verified + troubleshooting log), KNOWN_ISSUES.md (gov staging watch item), SALESFORCE_LCC_INGESTION_PLAN.md §8.2 (live status).
- **2026-07-31 (Cowork, session 30):** W3.7c realization: **intake-salesforce-files DEPLOYED to Dialysis_DB (version 21)** via Supabase MCP from the merged #1532 code staged off Scott's machine (index.ts + discovery.ts + _shared/{cors,auth,utils,supabase-client}). Verified live: GET-no-action serves sf-files-2026-07-v7 with discovery-worklist/discover-webhook/file-content (discover/fetch → retired); unauthenticated worklist probe → 401 (PA_WEBHOOK_SECRET set + enforced). Reviewed Scott's PA-flow zip export (LCC — SF File Discovery): defects = wrong auth header `X-lcc-api-key` (and wrong value — the LCC API key, not the PA webhook secret), CDL filter passing the raw id instead of `LinkedEntityId eq '...'`, CV filter referencing `['Id']` instead of `['ContentDocumentId']` + missing IsLatest, the Salesforce-connector HTTP action pointed at the Supabase file-content URL (it can only reach the SF instance — repurposed for VersionData GET), and steps 3–4 absent (DiscoveredFiles array, extension gate, discover-webhook POST, base64 compose, file-content POST). Copy/paste fix list delivered in-chat; spec of record docs/flows/FLOW_sf_file_discovery.md. Manual rebuild wouldn't save → Cowork rebuilt the flow INSIDE Scott's exported package (same flow id/connection refs, import-as-Update) and delivered LCC_SF_File_Discovery_FIXED.zip; one post-import edit required: set the LCC_PA_WEBHOOK_SECRET variable (placeholder REPLACE_WITH_PA_WEBHOOK_SECRET). Package archived at docs/flows/LCC_SF_File_Discovery_import.zip. **Also: the device working-tree ROLLOUT_STATUS.md had reverted to the 2026-07-29 snapshot (PR merge carrying a stale copy) — restored from the Cowork master with this update; sessions 1–29 history re-applied.**
- **2026-07-31 (Cowork, session 29):** W3.7c (PR #1532) + W3.1d recorded — Wave 3 is now FULLY closed pending only the W3.7c realization steps (merge → edge redeploy → PA flow build). PA flow walkthrough issued to Scott; Cowork offered to deploy the edge function via Supabase MCP after merge (no dashboard needed).
- **2026-07-31 (Cowork, session 28):** Scott corrected the SF architecture assumption — no Connected-App rights exist; PA-flow collector is the only SF transport (org doctrine, now a standing rule in the corrections log). W3.7c opened with the PA-collector redesign; SF-creds item struck from the operator list. W3.1d prompt issued (last open Wave 3 build item).
- **2026-07-31 (Cowork, session 27):** County-ingest RUN 3 verified: full success — 91 parcels (all linked) + 76 tax records across 8 counties; W3.1b closed after three operator-caught bugs; W3.1d opened (null entity_name guard). Mailing capture confirmed 0 at runtime → Phase A1b (assessor fetchers) is the next county milestone, post-Wave-4. Standing: W4.1 corpus export ready to run in Cowork on Scott's word; resolver Railway service + SF Connected-App creds still on Scott's list; scheduled check later today covers SAM/mirror/CMS trends.
- **2026-07-30 (Cowork, session 26):** County run 2 logs: env fix confirmed (gov URL) but worklist rollup hit statement timeout 57014 — root-caused to per-row gov_resolve_county_fips over a heavy aggregate; fixed live as W3.1c (same-name matview + daily refresh cron; instant, 1,328 counties). W4.2 recorded (PR #1531). Next: Scott re-triggers county run 3; merges #1531 + creates the resolver Railway service; Cowork runs W4.1 corpus export next session. Scott's manual-run testing has now caught and killed three infra bugs on this one cron — the loop works.
- **2026-07-30 (Cowork, session 25):** W3.7b recorded; **W3.V run — WAVE 3 COMPLETE** (scorecard in row). Corrections note: earlier merge-conflict markers in this file came from Cowork force-commits colliding with PR merges — Code session cleaned them; Cowork now re-stages before every edit (already practice) and Scott should prefer merging tracker-touching PRs promptly. Open operator items: re-trigger county-ingest (env now fixed — verify pull_log after), SF Connected-App creds for the discover sweep (runbook in docs/audits/W3.7b_*.md), spot-check the OM resolver dry-run, keep casting lane verdicts. Watch items for next check: SAM drain pace (5 total — cron consumption suspect), dia llc queue growing (1,174 — sidebar is its only drain). **Wave 4 opens: W4.1 training-data export (Cowork job), W4.2 resolver service (Code).**
- **2026-07-30 (Cowork, session 24):** W3.7 recorded (PR #1529) — pipeline sound, Fort Wayne fixture green, owner_b labels healed 50/50; ceiling is comp→file linkage (4/100) → W3.7b opened (Listing__c attachments never swept into sf_files — explains why Fort Wayne's own OM was missing) + Wave-4 dependency noted. County-ingest smoke-run diagnosed: env vars point at dia not gov — fix steps issued (W3.1b ⛔→ pending Scott). After env fix + re-trigger, Cowork verifies county_ingest_pull_log.
- **2026-07-30 (Cowork, session 23):** W3.6b recorded + verified. Railway county-ingest logs diagnosed: BUILD-ONLY (image pushed clean 18:40) — service healthy, idle until Aug 1 08:00 UTC schedule; optional smoke = blank cron field + redeploy + restore. Scott's 50 owner-reconcile verdicts verified (50 labels + 50 decisions) — QUALITY NOTE: gov-unification label rows have owner_b=null (weakens Wave-4 training pairs; fold into next fix bundle). **Fort Wayne proof case:** read the Northmarq OM, confirmed reliable 8.0% correct, fixed NOI at source with provenance, resolved queue id=11 — W3.7 opened to automate exactly this (OM-in-Salesforce → financials → auto-resolve). Comp queue now: gov 6 open.
- **2026-07-30 (Cowork, session 22):** W3.6 recorded (PR #1526) + data fix Cowork-verified live (9388 NOI corrected, id=1 resolved, implied=reliable). Root-cause pattern → W3.6b opened (54/61 rows). County-ingest Railway service created; smoke-test unconfirmed (pull log 0 rows) — instructions issued. Next: Scott merges #1526 + redeploys → quick lane re-check (names should render) → W3.V with a few days of decidable verdicts.
- **2026-07-30 (Cowork, session 21):** Scott's lane-test screenshots analyzed. WORKING: comp lane end-to-end (his resolve landed live — reopened id=1 with note since he flagged the numbers as suspicious), metadata backfill view ("great — will work side-by-side with the sidebar"), Listing BD chip + grouped view + draft button render. DEFECTS → W3.6 (the recurring theme is display-name resolution: ids render where names should). His live testing has now caught a defect every single round — the testing loop is doing exactly what it should. Railway county-cron walkthrough issued. W3.V holds until W3.6 lands + a few days of decidable lane verdicts.
- **2026-07-30 (Cowork, session 20):** W3.5 recorded (PR #1525) — Wave 3 build work complete; W3.V pending Scott lane-testing + a day of signal accrual. Grounded UI test walkthrough issued to Scott from CURRENT staged ops.js/app.js (route slugs: decisions/research/inbox — all five new surfaces confirmed present in code). Watch item opened: pre-existing review-shared.test.mjs "lane rationalization map" failure on clean main — diagnose in a future small session. Operator items outstanding: merge #1525 + redeploy; Railway county cron (W3.1); 7-row unmerge worklist (Cowork will present rows in-chat).
- **2026-07-30 (Cowork, session 19):** W3.3 + W3.4 recorded, PRs #1523/#357/#1524/#358 merged + redeployed by Scott, numbers Cowork-verified live (orq 17,665 + archived 39,465 = 57,130 exact; unmerge worklist 7; comp queues drainable). The 57k write-only queue that headlined the original audit is now GATED + TRIAGED — largest single queue reduction of the rollout. Scott operator items: (a) work the 7-row gov_owner_merge_unmerge_worklist (evidence + reactivate_sql per row); (b) spend lane time in owner_reconcile + comp_review (labels accrue to entity_match_labels); (c) leave ENABLE_OWNERSHIP_RESEARCH_QUEUE unset; (d) Railway county cron from W3.1 still pending. W3.5 (listing-BD consumer) is the LAST Wave 3 build unit — prompt issued.
- **2026-07-30 (Cowork, session 18):** W3.1 + W3.2 recorded, all four PRs merged + redeployed by Scott, headline claims Cowork-verified live (parcels 11,075/11,100 linked from ZERO; deeds 5,635/5,744; ORE hourly verdicts-only at depth 1,447; disposition columns live; entity_match_labels ready). Plans revised: W3.3 unblocked + scope widened (include ORE candidate-mode merge audit); W3.4 reduced (3 queues already folded). Scott operator step: schedule the Railway county-ingest monthly cron per RUNBOOK_county_ingest_cron.md. Labels now accrue as Scott works the owner_reconcile lane.
- **2026-07-30 (Cowork, session 17):** Cleanup bundle recorded + verified live (dia listing_events leg ok @6 rows/tick; retraction evaluating dia at live_sales=287). W2.3d2 ✅, W1.5c ✅, W0.3i ✅. **Every open engineering item from Waves 0–2 is now closed.** Remaining non-engineering: W0.6 (Supabase support ticket), W0.5b (parked), OpenCorporates decision (~Aug 28). Scott merges Dialysis #7353 + LCC #1520 (files-match-live only, no redeploy needed). Wave 3 kickoff prompts issued (W3.1 county scraper, W3.2 ORE activation).
- **2026-07-30 (Cowork, session 16):** W2.4 (PR #1519) recorded; **Wave 2 verification run and PASSED** — see W2.V row. Highlights: dia mirror fully healed (22.5k/23.7k rows), gov provenance backlog 0/16,860 undrained, retraction live. **CMS backfill SUCCEEDED at the 06:00 cron: latest monthly 2025-03→2025-09, +44,868 rows — Wave 1 now completely closed.** Scott actions: remove PATIENT_MONTH_BACKFILL variable from Railway (marker wrote status=started, skip-guard uncertain); merge #1519 if not already. Open small items: W2.3d2 (dia sales feed view), marker-status tightening, W0.3i (all fold into one next Code session). **WAVE 2 COMPLETE. Wave 3 is next: ownership engine activation.**
- **2026-07-30 (Cowork, session 15):** W2.3 (PR #1517) + W2.5 (PR #1518) recorded and DB-verified (gov mirror parity + advancing watermarks; flush crons live; dia flush drained to 0). W2.3d decision made and EXECUTED in-session: Scott chose scoped definer views; migration applied; anon now reads the two dia mirror-feed views (12,321 rows); dia mirror self-healing via 3-min tick. Scott's remaining steps: merge #1517 + #1518 and redeploy (lets the gov provenance flush drain 16,760→0), then run W2.4 (prompt issued — branch AFTER #1517 merges since it touches the same mirror code). Wave 2 will then be down to W2.V.
- **2026-07-30 (Cowork, session 14):** W2.1 (PR #1515) + W2.2 (PR #1516) recorded and DB-verified live from Cowork (unique index present, 2,964-row dedup ledger, 0 duplicate live keys, decision CHECK carries failed_write+cleared). 1,798 duplicate provenance keys existed — the audit's concurrency finding was real and material. Scott: merge #1515 + #1516 and redeploy (W2.2's JS must ship for the migration's new decisions to be written). Next: W2.3 (watermark mirrors — URGENT, gov at the 18-page ceiling), then W2.4 + W2.5.
- **2026-07-30 (Cowork, session 13):** W1.V run and recorded (row above). **WAVE 1 COMPLETE** (pending only the overnight CMS cron + 07-31 SAM trend check, both automated). ⚠️ New watch item: ingestion_tracker has NO cms runs logged since 07-26 — either the Railway cron stalled 4 days ago or the new code logs differently; tonight's 06:00 UTC run resolves it; if nothing appears by the 07-31 check, the cms-ingestion Railway service needs eyes. Wave 2 kickoff prompts (W2.1 + W2.2) issued to Scott.
- **2026-07-30 (Cowork, session 12):** W1.5b delivered and recorded (Dialysis PR #7352 + LCC PR #1514). With this, EVERY Wave 1 build unit is complete. Remaining before W1.V: Scott merges the two PRs and runs the 2-minute Railway backfill dispatch per the runbook; SAM 48h check fires 2026-07-31 (scheduled). Wave 0 fully closed except the Supabase support ticket (W0.6).
- **2026-07-30 (Cowork, session 11):** CMS dispatch path corrected. Scott flagged the GH Action fails on the free plan; INFRASTRUCTURE.md confirms the 2026-05 consolidation decision: GH Actions = free CI only; compute crons = Railway (`cms-ingestion` 0 6 UTC + `public-record-ingest` 0 7 UTC in handsome-luck; LCC server on tranquil-delight Hobby). W1.5b opened to wire the backfill flag Railway-natively and retire the legacy workflow. Standing rule for all future prompts: any job needing egress or compute runs on Railway, never GH Actions.
- **2026-07-30 (Cowork, session 10):** Screenshot review of Scott's post-#1513 ingestion attempt: **all three L3c fixes verified live** — green "Everything extracted already exists — nothing to apply ✓", "Already on file" panel showing property_id 3811 + lease LFL62270 **from current_record** (the property_id fix works), honest missing-info items (tenant name, lease expiration, cap rate — real gaps in the OCR'd pages). Minor cosmetic leftover: the old "No operations match the current trust filter" line still renders alongside the new 0-ops state — fold into any future LCC session, not worth its own. **W0.3h closed:** discovered the Gov Evidence card is hard-disabled in code (unconditional return ''; FastAPI service undeployed) → live promotion impossible → re-archived lease_lifecycle_events (0 rows) on unit-test + unreachability basis; GOV_EVIDENCE_WORKBENCH added to the feature registry; my walkthrough error logged in Corrections. Wave 0 is now FULLY closed except W0.6 (Supabase support ticket). Wave 1 remaining: CMS dispatch (Scott), SAM 48h check (fires 2026-07-31), then W1.V. W1.4-L3c delivered (PR #1513) and recorded. All three Live Intake defects fixed incl. the ownership-row property_id root cause (gov.js Phase-2 SELECT omitted the column). Wave 1 build work is now COMPLETE except the two operator triggers: (a) CMS backfill workflow dispatch (W1.5), (b) lease-activity promotion via Gov Evidence card (W0.3h) — which after the #1513 deploy will carry property_id in context. SAM 48h delta check fires 2026-07-31 (scheduled). Then W1.V full verification.
- **2026-07-30 (Cowork, session 8):** Analyzed Scott's retry screenshots (extension 1.0.37, redeploy live). Extraction pipeline WORKED (3 OCR pages, high confidence, proposal generated); 0 rows written (verified) and 0 ops was CORRECT — the extracted $5.8M 2021-09-21 sale + prior loan already exist in sales_transactions (costar_export + excel_master rows). Two genuine defects logged into W1.4-L3c: the ownership research row drops property_id (context had ownership_id only; DB row has property_id=3811), and the 0-ops proposal narrative reads as if writes happened, then "Apply Selected" scolds the user. Also noted: Target Record Lookup contained a typo ("4124" vs 4121) so no bound target. W0.3h lease-activity test still pending — requires the Gov Evidence card (BELOW the Proposed Writeback panel on the Ownership view) + a screenshot showing LEASE ACTIVITY (not a sale comp page).
- **2026-07-29 (Cowork, session 7):** Fourth-round responses recorded: W1.5 code-complete (Scott dispatches the backfill workflow), W0.3i ✅, W1.4-L3b ✅ (ext 1.0.37). Diagnosed Scott's Live Intake http_400 on 4121 Southpoint Blvd: property exists (id 3811, USCIS) — the extraction context binds ONLY from (a) the selected research-queue row or (b) the Live Intake "Find"-and-bind target; an open property page does NOT bind. Correct flow: Live Intake → record search box → Find → click the result (Bound-record chip appears) → Extract + Map Changes → Apply Selected. Opened W1.4-L3c for the opaque error + auto-bind UX. Scott's open items: merge `claude/cms-ingest-cleanup-objsdn` (DialysisProject+LCC) and `claude/sidebar-promote-empty-captures-20aevj` (LCC); reload extension 1.0.37; dispatch cms-ingestion-daily.yml with patient_month_backfill=true; retry the lease-activity promotion with a BOUND record; commit docs/audits/ to main.
- **2026-07-29 (Cowork, session 6):** Lane 1 + Lane 3 responses processed and recorded (rows W1.4-L1/L3). ⚠️ PROCESS FINDING: Code sessions clone from GitHub and could NOT see docs/audits/OWNERSHIP_RESEARCH_FREE_FIRST_PLAN.md — the docs/audits/ files exist only in Scott's working tree. ACTION (Scott): commit + push docs/audits/ to main so sessions can ground in the plan docs. Scott's open items: (1) refresh SAM_GOV_API_KEY (free key from api.data.gov → gov project Edge Function secret) — unblocks the entire Lane-1 drain; (2) reload unpacked extension (1.0.36) + one real SOS capture; (3) merge gov #354, LCC #1509, #1510; (4) lease-activity test promotion — the Evidence workbench is on the gov RESEARCH page (not the dashboard): Research → open a research row to bind a record → upload screenshot in Live Intake → Gov Evidence card → Extract Latest Screenshot.
- **2026-07-29 (Cowork, session 5):** dia PG upgrade FAILED (~15 min, contact-support prompt; ticket filed by Scott) — post-failure health check clean: rolled back to 15.8, ACTIVE_HEALTHY, no data impact; W0.6 → ⛔ awaiting support. Lease-activity verification baseline captured (gsa_lease_events: 295,008 rows, latest 2026-07-27 08:25 UTC) — awaiting Scott's test promotion. W1.4 investigation: cross-matched the 2,043-row deferred LLC backlog against every already-ingested free source (<10% coverage), reviewed the full ownership-research history (manual doctrine, sidebar SOS loop, egress blockers, SAM machinery), and produced `docs/audits/OWNERSHIP_RESEARCH_FREE_FIRST_PLAN.md` — five free lanes, OpenCorporates deferred to ~Aug 28. Key finding: 380/885 gov deferred rows are GSA lessors → SAM-resolvable with existing, underfed machinery.

- **2026-07-29 (Cowork, session 4):** All prior branches/PRs merged + redeployed (Scott). W1.2 (PR #1504) + W1.3 (PR #1507) delivered by Code and verified live from Cowork: gov sos_compared_at column present; LCC get_contact_recommendation_weight resolves unified ids; template_sends.replied nullable path confirmed; feedback steady at 3,729. **W0.6 unblocked:** dropped unsupported `pgjwt` from dia (0 DB callers, 0 repo refs — verified before drop) after the upgrade wizard flagged it; leaked-password protection enabled on LCC (Scott). Wave 1 remaining: W1.4 code half (awaiting Scott's OpenCorporates decision), W1.5 (+W0.3i rider), W1.V. Wave 0 remaining: W0.6 upgrade run (Scott, tonight), W0.3h re-archive trigger (needs one live lease-activity promotion), W0.5b (parked).

- **2026-07-29 (Claude Code, W0.3f + W0.7 + W0.8):** Migration-only session across all 3 repos/DBs on `claude/peaceful-meitner-167bsi` (one commit per unit per repo). **W0.3f:** dropped the always-no-op `agency_debt_programs` repoint from `merge_dialysis_dup_property()` + archived the 1.6GB federal table + manifest; ref re-check clean (0 DB refs after; the only code ref is the manual `ingest_agency_debt.py` CLI, flagged for later retirement). **W0.7:** front-end anon-RPC audit (exception lists per DB above) + effective anon lockout on SECURITY DEFINER fns (revoke anon **and** PUBLIC, preserve authenticated/service_role) + `search_path=public` fold-in; verified anon-executable secdef = 0/0/6-exceptions. **W0.8:** dropped 27 dia / 14 gov / 1 LCC redundant non-constraint duplicate indexes (advisor-cross-checked), skipping both-constraint pairs; verified 0 droppable duplicates remain. Grounding corrections logged below. Commits pushed to the feature branch; no PRs opened (per Scott's default).
- **2026-07-29 (Cowork):** Baseline audit + rollout plan produced and committed to docs/audits/. W0.3 executed: archive schema live on dia+gov, 56 tables archived (incl. agency_debt_programs 1.6GB + field_cleanups 838k rows), pending_updates VACUUMed 196MB→2.2MB. Advisors triaged (0 errors; W0.6/W0.7 opened from findings). CORRECTION to plan Part 1: field_cleanups was NOT empty — stale stats; it is a legacy correction ledger (838k rows, dormant since 2025-10), archived not dropped. Decision logged to Cortex memory.
- **2026-07-29 (Claude Code, W0.3v):** Verification-only pass over the already-applied W0.3 moves (no new migrations; dq5_/dq7_ left in public per Scott). Manifest ↔ archive 1:1 on both DBs (dia 35/35, gov 17/17, 0 orphans, 0 public collisions). Observed a concurrent Cowork/MCP session un-archiving 4 tables mid-pass (dia agency_debt_programs + cap_recompute_backup; gov cap_recompute_backup + gov_sale_lease_date_backup) — manifests stayed consistent. **Code-reference audit found 2 archived tables still referenced by runtime code**, contradicting the W0.3 "no code refs" note: dia `field_cleanups` (LOW — `run_field_cleanups()` degrades gracefully but is now dead-lettered) and gov `lease_lifecycle_events` (MEDIUM — `research_artifacts.py`/`write_services.py` lease-activity promotion SELECT+INSERT now error). Details + recommended actions under "W0.3 verification" above. Advisors re-confirmed (0 errors); duplicate-index cleanup + LCC leaked-password toggle flagged as safe do-now.
- **2026-07-29 (Claude Code, W0.3h):** Repointed the gov lease-activity screenshot-observation promotion from deprecated `lease_lifecycle_events` to canonical `gsa_lease_events` (government-lease: `src/research_artifacts.py` + `src/write_services.py`). Field map: `new_state`→`changed_fields`, `event_type`→`latest_action`, `detected_by`+free-text `notes`→`prospect_notes`; `event_date` coalesced to the promotion date and `lease_number` resolved from the property (both NOT NULL on `gsa_lease_events`); skips (no crash, `skipped_no_lease_number`) when `lease_number` is unresolvable. `tests/unit/test_evidence_promotion_gsa_events.py` (2 tests) pass; both modules import clean. **Pending:** verify one live promotion post-deploy, then re-archive `lease_lifecycle_events` (restored to public via w0_3g). dia `field_cleanups` deferred → W0.3i (DialysisProject session).
- **2026-07-29 (Claude Code ×4, Scott-run):** W0.1 (PR #1492), W0.2 (PR #1493), W0.3 verification (PR #1494), W0.4 (PR #1495) all complete — details in the Wave 0 table. PRs pending merge to main.
- **2026-07-29 (Claude Code, W1.1):** Feedback capture wired (audit 3.4.3). Extracted the canonical `writeIntakeFeedback()` writer from `intake-feedback.js:recordFeedback` and reused it in (a) admin.js Decision Center match verdicts via a shared `emitIntakeMatchFeedback` helper (patch-script method — admin.js >200KB; grep-verified 1 def + 5 call sites, `stripNullsLocal` intact) and (b) intake.js `handleIntakePromote` (implicit approve). Machine snapshot always excludes the confidence-1.0 `manual_match` rows so accuracy bands reflect the matcher's real output. Backfill of 3,728 historical promotions applied live (reversible ledger + `source=backfill_w1_1`, idempotent). Wave-1 verification target (1) met early: staged_intake_feedback **1→3,729** and will grow with live intake activity. Cron confirmed active; `v_matcher_accuracy_recent` returns per-band rates. Full before/after in the PR description.
- **2026-07-29 (Cowork, session 2):** W0.3g restore (lease_lifecycle_events), W0.5 retention executed (1.43M rows pruned, daily cron live), W0.5b offload proposal registered, W0.8 opened. Wave 0 remaining: W0.3f, W0.3h, W0.3i, W0.6 (manual), W0.7, W0.8, W0.5b — none block Wave 1. **Wave 1 is clear to start; W1.1 (feedback capture) first.**

# Responses — paste Claude Code replies here

Drop each Claude Code response as `NN-short-slug.response.md`, matching the `NN-short-slug` of the prompt in
`../prompts/` it answers (e.g. prompt `01-cap-rate-reconciliation.md` -> response
`01-cap-rate-reconciliation.response.md`). Paste the full reply verbatim; the next chat will read it, verify,
update the docs, and move the prompt to `../done/`.

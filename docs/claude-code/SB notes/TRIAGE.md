# SB notes — triage ledger

One row per observation. `SBN-n` ids are never reused. Types: `defect` · `design gap` · `automate-me` ·
`data-accuracy` · `question`. Disposition names the backlog row and/or prompt. See `README.md` for the protocol.

| id | source | what Scott saw | type | surface | evidence (measured) | disposition | outcome |
|---|---|---|---|---|---|---|---|
| SBN-1 | `8 of your flow(s) have failed.eml` (Power Automate digest, 2026-09-12) | Eight flows failed in the week: **Get file (LCC Get Artifact) 709**, Outlook Intake to Teams (Hardened) 41, List Folder (SharePoint) 39, SF Listing Activity → LCC engagement 35, Processing Complete → Move Message 7, Flagged Email Intake 6, Http → Switch/Get Account/Contact 6, Outlook Calendar sync 1. | defect | flows | Flow ids in the mail match `docs/os/POWER-AUTOMATE-DEPLOYED-CATALOG.md`. The digest carries counts, not errors — the run-level failure reason is only in the Power Automate run history, which no repo tool can read. | **FLOWS1** prompt (LCC-side tracing) + Scott supplies one failed-run screenshot per flow into this folder. Backlog `FLOWS1`. | ✅ diagnosed; Scott's 7 edits applied and **verified from the exports** 2026-09-16 (F1 chose option B → FLOWS1-artifact); LCC: FLOWS1-artifact / -order prompted, -path open |
| SBN-2 | `LCC app notes - Sept 12.docx` §1 (Home → "Top data gaps to close") | The list is dominated by "Resolve agency drift: property says GSA – NSF, lease says …" rows. These are cleaning topics that should be automated; the list should hold only high-value leads/comps with a **human** next step and say exactly what to ingest next, from where, with the sidebar. | automate-me + design gap | Home | Widget reads `GET /api/admin?_route=next-best-action` over dia+gov `v_next_best_action` (`app.js` ~7498). The agency-drift rows are the ID3a canonicalization class (`app.js:1793` "Resolve agency drift") — a fold the registry already does. 9 of 10 visible rows are that class; the one human row (#7, 350 Rhode Island St → SF Recorder) is the shape Scott wants. | **HOME1** prompt §A. Backlog `HOME1`. | ✅ 2026-09-16 (PR #2513): cleanup classes filtered out of the widget in `api/admin.js`; found ID3a's fold does not cover the drift column → `ID3a-drift` |
| SBN-3 | docx §2 (Home → Daily Briefing "My priorities" / BD lists) | Rows are just owner names / buyer names; buyers are worked by showing listings and are rarely the top next step. Wants the code to recognise cadence + priority and surface **at most three lanes: research, BD, inbox**, significant/urgent on top. | design gap | Home | Briefing block rendered in `app.js` ~7220–7240 from the daily-briefing payload (`domain.government/dialysis`); "My priorities" mirrors the Priority Queue P1 band (same five names as the Priority tab). | **HOME1** prompt §B. Backlog `HOME1`. | spec written (`docs/audits/HOME1_HOME_PAGE_AUDIT_2026-09-16.md`); the duplication is the `_dbFillMyPrioritiesFromQueue` fallback; build = `HOME2` |
| SBN-4 | docx §2 (Home → "Government highlights" / "Dialysis highlights") | Highlights show **deal-tracking activity** (schedule a call, track DD), not market intel — a duplicate of the deal tracker; and **a dialysis deal (DaVita – The Villages, FL) sits under Government highlights**. | defect | Home | `renderBriefingItems(gov.highlights…)` at `app.js:7233`; the payload's per-domain `highlights` arrays are populated by the briefing producer, so the miscategorisation is upstream of the render. | **HOME1** prompt §C. Backlog `HOME1`. | ✅ fixed in `daily-briefing` (`inferDomain` read long-form domain strings; data uses `dia`/`gov`), test with the DaVita fixture — ⚠️ **edge function not yet deployed** (live v25, 2026-05); Highlights measured 100% deal-tracking → rename now, re-source from MB later |
| SBN-5 | docx §3 (Priority tab) | Band names (P1/P2/P3/P4/P5/P8) are not self-explanatory; wants a **dynamic, HITL-only to-do**, ranked, "tip of the spear" across research, BD and pipeline. | design gap | Priority | Bands are the cadence bands (HP1 / cadence dashboard); the tab already carries "Next best touchpoint", "Cadence dashboard", "Top BD actions", "Qualify contacts" as four separate entries — the navigation Scott says is not intuitive. | **PRI1** prompt. Backlog `PRI1`. | ✅ labels live; **PRI2 built** (flag `priority_tab_v2` OFF) — side-by-side gate is Scott's read (`OPERATOR-CHECKLIST` S1) |
| SBN-6 | docx §4 (Dialysis → Overview action items) | Six action items (159 leases expiring, 212 on market, 14 CMS removed, 1,100 NPI signals, 88 clinics review queue, 3,119 lease backfill): are these HITL? which can be automated? Keep the top-of-page action list, but HITL-only, ranked by impact. | automate-me | Dialysis | Rendered in `dialysis.js` (overview). "1,100 NPI signals" and "3,119 lease backfill" are producer-shaped counts, not human queues. | **DIA1** prompt §A. Backlog `DIA1`. | ✅ DIA1b: NPI tile → 81 (raw diff as second line), lease backfill relabelled raw backlog (26 completions ever) |
| SBN-7 | docx §4 | **"Market Economics Exhibit" button does nothing.** | defect | Dialysis | `dialysis.js:2286` button → `_diaMarketEconomicsExhibit()` (`dialysis.js:1744`, async, opens a Northmarq-branded report). Silent no-op means the handler throws or awaits an endpoint that never resolves; nothing is surfaced to the user. | **DIA1** prompt §B. Backlog `DIA1`. | ✅ live (PR #2512): deployed `data-query` edge fn was v41 (July) and 403'd the econ views; redeployed v43; errors now surface via `lccReportError` |
| SBN-8 | docx §4 (Dialysis → summary tiles) | Tile numbers don't match the database; wants freshness/accuracy and **one accurate view per topic that can't be confused**. | data-accuracy | Dialysis | Measured 2026-09-16 on Dialysis_DB: tile **11,804** properties vs live `properties` **11,826**; tile **8,607** with size vs live `building_size IS NOT NULL` **8,629**; tile **45** "distinct operators" vs **21** distinct `operator_id` on properties (the tile counts something else — name strings, or the `operators` table). Tiles are stale by ~22 rows and at least one is a different definition. | **DIA1** prompt §C. Backlog `DIA1`. | ✅ DIA1b: all tiles audited (`docs/audits/DIA1_TILES_2026-09.md`), "as of" stamps live; 45 = raw names, 21 = canonical ids, 878 unresolved → `DIA1b-operators` decision |
| SBN-9 | `Failed flows.docx` (17 run screenshots, 2026-09-16) | One failed run per flow from the FLOWS1 digest, with the red step and its error text. | defect | flows | Every error read and classified (table in the FLOWS1 addendum): Get Artifact = `Response` built from a chunked `body()`; Outlook Intake + Flagged Intake = `NotFound` on a message the Move flow already moved (LCC-caused race); List Folder = OData path with an apostrophe (`Rec'd`) / renamed folder; SF Listing = trigger passes no `id`; Move Message = Flag after Move (stale change key); Switch/Get Account = 504 after 31–57 s of Salesforce lookups; Calendar = OneDrive save conflict. | **FLOWS1** prompt updated in place with the per-flow table and the ours/theirs split (4 LCC, 3 flow-side, 1 benign). | ✅ FLOWS1 round read the same 17 and agreed on causes; disagreements on LCC-side ownership recorded in the backlog |
| SBN-10 | seven `*.zip` flow exports (2026-09-16) | Scott exported each flow after applying F1–F7. | verification | flows | Definitions read (`definition.json`): F2/F3/F4/F5/F7 exactly as specified; F6 has dead-letter + timeout response; **F1 = option B** (metadata+link) — LCC's `fetchSharepointBytes()` still expects `content_base64`. | `FLOWS1-artifact` prompt + F1c addendum; exports moved to the gitignored `private/power-automate/exports-2026-09-16/` (they carry connection references — `.gitignore` says never commit); the verified facts are in this row and the FLOWS1 backlog row. | ✅ read 2026-09-16 |
| SBN-11 | `Http-Getfile(LCCGetArtifact)_20260916163609.zip` (second export, after F1c) | Flow re-tested successfully after the F1c correction. | verification | flows | Definition read: `Get file metadata` first (path from the trigger, site prefix stripped) → Condition `Size < 20000000` → True: content fetch + bytes `Response` (exact expression) / False: bare metadata `Response`. Matches the spec; LCC treats the bare metadata as `too_large`. | F1c closed in `OPERATOR-CHECKLIST.md`; export moved to `private/power-automate/exports-2026-09-16/`. | ✅ verified 2026-09-16 |
| SBN-12 | `Self Clean Triggering.docx` (nine screenshots of the DaVita Banning clinic, 2026-09-17) | The same clinic appears as three different properties; one shows as available after it closed; sales, leases, owners and listings disagree. Wants ingestion of any data to trigger a reconciliation so there is one accurate view; suggests an Ollama local model on a scheduled clean-and-connect task. | data-accuracy / design gap | Dialysis | Measured on Dialysis_DB: `properties` 29894 (`6050-6090 W Ramsey St`, real: 2 sales / 4 leases / 2 ownership / 3 listings), 35786 (`6050 W Ramsey St`, OM intake `e26e414f…` shell, active $4.75M listing, OM/rent-roll/lease-abstract artifacts), 51228 (`6090 W Ramsey St`, CoStar shell, active listing, seller `Genesis Kc Development Llc`); listing 9499 `sold` 2026-06-19 with no sale; sale 15042 (2026-09-14, $4,180,180, `is_northmarq`, broker Scott) with no buyer/seller, no `sf_deal_id`, no ownership row; leases 2013–2018 `is_active=true` vs 2015–2025 inactive; owner `Davita Healthcare Prtnrs` vs `DaVita HealthCare Partners`. Each store consistent alone; none reconciles with the others. | → `RECON1` prompt (trace → fix Banning via ledgered merge → specify `reconcile_property()` rules R1–R7 → blast radius); §P10a first concrete case; Ollama idea recorded as design input, scoped to the fuzzy tail (PL-16). | 🔴 prompted 2026-09-17 |
| SBN-13 | GitHub mail `Run failed: DEPLOY2 — merged-migration application check - main (6656f86)` (2026-09-18 11:41 UTC; 4 annotations) | The merged-migration check went red on `main` after the RECON2-b merge. | false positive | CI | Cowork probed every object the migration window declares against both live databases: all present except `dia_recon1_lease_active_past_expiration_guard()` + `trg_dia_recon1_lease_active_guard`, which `20260917220000_dia_recon2_…` DROPs on purpose (RECON2's rule). No migration is unapplied. | prompt `DEPLOY2-drop-aware`; backlog row of the same name | 🔴 prompted 2026-09-18; the run stays red until the check learns DROP or the window rolls |
| SBN-14 | `SyncSFActivitiestoSupabase_20260918123619.zip` (Power Automate export, 2026-09-18 12:36 UTC) | Scott added `X-PA-Webhook-Secret` to the flow's HTTP step; the log still showed `DENY-WOULD` at 12:01. | defect (config) | flows | Definition read: two HTTP actions (`HTTP` → `/ai-copilot/sync/activities`, `PostDeadLetter` → LCC Opps `lcc_record_flow_failure`), both carry a header whose **key is `X-PA-Webhook-Secret\n`** — trailing newline. Trigger: every 4 h from 2026-03-06 16:00 UTC (why 00:01/04:01/08:01/12:01). | checklist Q1 (retype the key) | 🔴 open — decided by the 16:01 UTC run after the fix |
| SBN-15 | `Re [sbriggssjc_Dialysis] HCRIS-TIMEOUT-8 fix … (PR #7419).eml`, `[sbriggssjc_Dialysis] PR run failed CI … (c063a94).eml`, `[sbriggssjc_Dialysis] Run failed CI - main (e31799d).eml` (GitHub mails, 2026-09-18) | CI red on the Dialysis repo around the HCRIS-TIMEOUT-8 fix. | handoff | Dialysis CI | Belong to the HCRIS-TIMEOUT lane (its own Claude Code thread; STATUS entries `HCRIS-TIMEOUT-7/8` 2026-09-18, PR #2579). Not applied from here (README: handoffs say so and nothing in them is applied from LCC). | filed to done/ for that lane to read; noted on STATUS round 33 | ↗ handoff |
| SBN-16 | `responses/Railway tranquil-delight variables.docx` (Railway dashboard copy, 2026-09-18) | Scott saved the `tranquil-delight` variables page so Cowork can finish the Vercel column-A check. | input | hosting | 46 shared names complete (42 attached, 4 *ADD*); the 79 service variables are only the alphabetical tail (`RESOLVER_URL`…) — the page copy truncated. | checklist Q3: Raw Editor → names-only `.txt` for both services | 🟡 partial |
